import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function SensorOptimization() {
  return (
    <Card data-testid="card-sensor-optimization-placeholder">
      <CardHeader>
        <CardTitle>Sensor Optimization</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">
          Sensor optimization has been consolidated into Equipment Intelligence.
        </p>
      </CardContent>
    </Card>
  );
}
