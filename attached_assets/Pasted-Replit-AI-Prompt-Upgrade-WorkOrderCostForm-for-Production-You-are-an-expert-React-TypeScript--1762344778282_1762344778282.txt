Replit AI Prompt – Upgrade WorkOrderCostForm for Production



You are an expert React + TypeScript engineer. Refactor and enhance the WorkOrderCostForm component according to these exact specs. The goal is to make the form fully controlled, type-safe, scalable, and backend-ready for PdM cost/ROI tracking.

🧩 

Objectives

Upgrade the form to include:

Numeric Input Safety

Replace all parseFloat(...) || 0 with a reusable helper:

const numberChange = (onChange:(v:number)=>void) => (e:React.ChangeEvent<HTMLInputElement>) => {
  const v = e.currentTarget.valueAsNumber;
  onChange(Number.isFinite(v) ? v : NaN);
};


Use it for all numeric fields: hours, costPerHour, quantity, unitCost, amount.

Controlled Select Components

Remove defaultValue and use value={field.value} for all <Select> components.

Ensure form resets maintain consistency (no React warnings).

Batch Submissions

When multiple entries (labor or parts) exist, send a single POST to /api/work-orders/:id/costs/bulk if the endpoint exists; otherwise fallback to Promise.allSettled.

Include currency and optional vendor fields in each object.

Add Currency Selector

Add a top-level dropdown: USD, SGD, BND.

Persist selected currency in local state (useState).

Format all monetary values with Intl.NumberFormat(undefined, { style: 'currency', currency }).

Add Vendor Fields

Labor form: optional vendor (workshop/subcontractor name).

Parts form: optional vendor and poNo (purchase order number).

Include these fields in backend payloads and display them in the entries list.

UX Improvements

Disable Add and Submit buttons during addCostMutation.isPending.

Show toast summary after batch submissions (“3 labor costs submitted successfully”).

Use .toFixed(2) or formatted display everywhere numeric totals appear.

Use useMemo for cost totals to prevent re-renders.

Validation Enhancements

Extend zod schemas:

vendor: z.string().optional(),
poNo: z.string().optional(),


Ensure negative/NaN values trigger inline errors.

Consistency with ROI Engine

After successful cost submission, trigger a background request:

POST /api/cost-savings/recompute?workOrderId=${workOrderId} (ignore failure).

This ensures the PdM ROI dashboard updates live.

Final Layout Adjustments

Add a small “Currency” dropdown next to the section title.

Replace $ literals in UI with {fmt.format(value)}.

Keep data-testid attributes unchanged for tests.

Testing Hooks



Add data-testid for new inputs:

input-labor-vendor, input-part-vendor, input-part-pono, select-currency.

Ensure onCostAdded triggers after batch completes.

✅ 

Acceptance Criteria

No uncontrolled component warnings.

All numeric inputs reject blank/non-numeric gracefully.

Currency and vendor are included in all payloads.

Batch submissions handled atomically with feedback.

UI remains identical except for new fields and currency selector.

Mutation caching keys match existing query pattern.

Successful submission triggers background ROI recompute call.

⚙️ 

Deliverables

Updated file: WorkOrderCostForm.tsx

Optional backend route: /api/work-orders/:id/costs/bulk (POST array of cost entries).

New test: WorkOrderCostForm.test.tsx verifying:

controlled form inputs

currency change updates display

batch submit reduces network calls

💡 Implementation Order

Controlled inputs and zod schema update

Add currency + vendor fields

Batch submit logic and toast feedback

ROI recompute hook

Final numeric formatting and test IDs

Paste this prompt directly into Replit → it will refactor and patch your WorkOrderCostForm end-to-end for production readiness.