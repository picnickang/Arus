You are working inside the main ARUS Replit full-stack project.

Goal:
Integrate the uploaded Crew Management update into the main Replit file tree safely and correctly.

Context:
The uploaded update archive contains a hardened Crew Management module with improvements for:

* Crew roster filtering/sorting/export consistency
* Active vs Former Crew workflow
* Access readiness badges and filters
* Safer offboarding/access revocation
* Onboarding checklist correctness
* Vessel readiness panel
* Safety Alert Log
* Safety alarm resolution notes
* Permission hardening
* Mobile crew cards
* Visual status semantics

Your task is not to create a separate copy of the app. Your task is to update the existing main Replit project file tree so the main app runs with the improved Crew Management implementation.

Important instructions:

1. Inspect the current main project structure first.
2. Inspect the uploaded update archive.
3. Compare the update files against the current files.
4. Apply the update into the existing project tree file-by-file.
5. Do not blindly overwrite unrelated files.
6. Preserve unrelated local improvements in the main project.
7. If both versions changed the same file, merge carefully.
8. Keep the existing architecture, routes, import conventions, API patterns, database schema style, hooks, and design system.
9. Do not introduce a second duplicate Crew Management module.
10. Do not leave old unused duplicate files unless they are intentionally deprecated and removed from imports.
11. Ensure the final app uses the updated Crew Management route/components/hooks/services.

Primary areas to update:

* client/src/pages/CrewManagement.tsx
* client/src/components/UnifiedCrewManagement/**
* client/src/hooks/useCrewManagementData.ts or equivalent crew-management hooks
* client/src/components/crew/**
* client/src/components/safety/**
* server routes related to crew management
* server routes related to safety alarms
* server services/repositories related to crew access, lifecycle, offboarding, and safety alerts
* shared schemas/types related to crew, users, safety alarms, permissions, and readiness
* any tests/manual QA files related to Crew Management

Specific integration requirements:

1. Replace or merge the Crew Management UI with the updated version.
    The main /crew-management route should use the new updated workflow, including:

* Active Roster / Former Crew split
* accurate display count
* accurate export based on visible rows
* former crew sorting
* access readiness badges
* mobile crew cards
* onboarding checklist
* safe offboarding dialog
* vessel readiness panel
* safety tab with Current Alarms and Alert Log

2. Fix import paths after merging.
    After copying/merging files, check all imports for:

* stale paths
* duplicate components
* missing exports
* circular imports
* unused old files
* broken named/default exports

3. Preserve main app routing.
    Do not break:

* authentication
* role-based portal routing
* admin dashboard
* user dashboard
* safety alarm banner
* crew management route
* existing API base paths

4. Preserve database/schema compatibility.
    If the update adds schema changes, merge them into the existing schema/migration style. Do not create conflicting schema definitions.

If safety alarm resolution notes require a new field, implement the smallest compatible backend/schema change needed and update:

* database schema
* repository
* DTO/API response
* clear-alarm handler
* alert log UI

5. Harden Former Crew access behavior.
    Make sure Former Crew does not incorrectly use active-only access readiness data.

Former Crew should either:

* hide normal active access-readiness filters, or
* use a proper former-crew access-risk dataset.

Former Crew access-risk indicators should distinguish:

* linked login
* login actually enabled
* vessel access remaining
* admin/hub access remaining

Do not infer active login from userId alone.

6. Harden onboarding readiness.
    The onboarding checklist must not mark login complete just because an access object exists.

Correct states:

* Login created
* Login intentionally skipped
* Login missing

Deployment readiness must not show Ready if documents/certificates are pending or not assessed.

Use clearer readiness wording if full document readiness is not implemented:

* Profile Ready
* Access Ready
* Documents Pending / Not Assessed
* Deployment Ready: Pending document review

7. Harden offboarding.
    When retiring, cancelling, or marking crew former:

* end duty status
* disable linked login by default
* remove vessel access by default
* remove dashboard/admin/hub access by default
* clear or neutralize high-risk roles where appropriate
* preserve profile/history/documents/alerts
* show an offboarding result summary to the admin

The result summary should show:

* Login disabled
* Vessel access removed
* Dashboard/admin/hub access removed
* Duty ended
* Records preserved
* Any failures or skipped actions

Reinstating crew should not silently restore high-risk access. Require explicit access review before login is re-enabled.

8. Harden safety alert log.
    The Crew Management Safety tab should include:

* Current Alarms
* Alert Log
* Alarm Types/configuration if permitted

Alert Log should show:

* triggered time
* cleared time
* vessel/fleet scope
* severity
* mode: real/drill/test
* status
* triggered by
* cleared by
* acknowledgement count
* outstanding acknowledgements if available
* resolution/clear note
* expandable details

Resolution notes must be persisted and visible in the Alert Log, not only logged server-side.

9. Harden safety permissions.
    Split safety permissions where possible:

* safety_alarms:view
* safety_alarms:trigger
* safety_alarms:clear
* safety_alarms:acknowledge
* safety_alarms:export
* safety_alarm_types:manage

Captains, chief engineers, or fleet managers may operate alarms only if intended, but should not be able to create/edit/delete alarm types unless explicitly permitted.

UI and backend permissions must match.

10. Clean up visual semantics.
    Use consistent status colors:

* Red/destructive: emergency, expired, disabled, missing critical info, failed, unsafe
* Amber/yellow: pending, warning, expiring soon, user action required
* Green: ready, active, verified
* Blue/neutral: on duty, assigned, informational
* Muted/gray: former, inactive, archived, not assessed

On Duty must not use destructive red styling.

11. Run validation.
    After merging, run the appropriate project checks:

* TypeScript check
* lint if available
* unit/integration tests if available
* build if available

Also perform static verification that:

* /crew-management imports resolve
* no duplicate obsolete Crew Management route is still active
* no stale component import points to the old module
* backend crew and safety routes still compile
* shared types align between client and server

12. Add or update tests/manual QA notes.
    Minimum QA scenarios:

* Active roster count/table/export all match
* Former Crew sorting works
* Former Crew export exports only visible former crew
* Former Crew access-risk filters do not rely on active-only access readiness
* Former crew with disabled login is not counted as active-login risk
* Former crew with enabled login is counted as access risk
* Onboarding checklist does not mark no_login as complete
* Explicit skipped login is different from missing login
* Deployment readiness is not Ready when documents are pending/not assessed
* Temporary password issued is distinct from no password set
* Offboarding result summary shows revoked access
* Reinstatement does not silently restore old high-risk access
* Cleared safety alarm displays resolution note in Alert Log
* Captain/fleet manager cannot manage alarm types unless explicitly permitted
* User without safety clear permission cannot clear alarms
* On Duty badge is not destructive/red
* Mobile roster renders as cards

Deliverables:
When finished, provide:

1. Summary of files changed.
2. Summary of files removed or deprecated.
3. Explanation of how the update was merged into the main Replit file tree.
4. Explanation of any backend/API/schema changes.
5. Explanation of readiness/offboarding/safety permission logic.
6. Tests or validation commands run.
7. Any known limitations or unresolved merge conflicts.

Important:
Do not leave the update as a separate archive, duplicate app, or parallel file tree. The main Replit app should be the updated app.