You are an expert full-stack engineer working inside this exact repo (ARUS vessel PdM / operations app with Electron + Node backend + React/Vite frontend, hybrid SQLite/libSQL/Postgres, telemetry simulator, deck & engine log features).



I want to be able to push realistic fake telemetry and events so I can test and demo:

Engine Room Log auto-fill

Deck Log auto-fill



for one or more vessels, without needing real hardware or real StormGeo data.



Please do this in several careful steps and explain what you’re doing as you go.

0. Understand what already exists (NO changes yet)

Scan the codebase and summarise briefly:

Where live telemetry ingestion and simulation live:

e.g. modules/services named like VesselSimulator, MQTT Ingestion, telemetry, Digital Twin, etc.

Where the Engine Room Log logic lives:

Engine log table(s) or models (e.g. engine_room_log_entries, engine_log, etc.)

APIs such as /api/engine-room-log/... or similar.

Where the Deck Log logic lives:

Deck log table(s) / models (e.g. deck_log_entries, bridge_log, voyage_log, etc.)

APIs such as /api/deck-log/... or similar.

Any existing StormGeo integration or placeholders:

e.g. modules named stormgeo, weather, voyage_planning, or settings that mention API keys.

Tell me clearly:

What is the current mechanism for generating telemetry (if any).

Whether there are already any dev/demo scripts that generate fake data for logs.

If engine/deck log auto-fill already have service functions that accept telemetry and produce log entries (e.g. something like generateEngineLogEntriesFromTelemetry(…) or generateDeckLogEntriesFromTelemetry(…)).



Do not modify anything yet; just give me this high-level map.

1. Design a DEV-ONLY fake data pipeline (explain first)



Propose a minimal, safe design (in words) for dev-mode fake data that:

Does not interfere with production or real deployments.

Can be enabled/disabled easily, ideally via:

NODE_ENV and/or a dedicated env var (e.g. ENABLE_FAKE_TELEMETRY=1).

Generates realistic data for:

Main engines / auxiliaries (for Engine Room Log):

ME RPM, load %, key temps/pressures you already use.

DG status & loads.

Running hours, if that’s already modeled.

Navigation / bridge context (for Deck Log):

Position (lat/lon), SOG, COG.

Weather/sea state placeholders (or simulated StormGeo-like data).

Basic voyage events (departure/arrival, anchoring, etc.).

Feeds into the same DB tables and APIs that the real system uses, so that:

Existing Engine Room auto-fill logic sees this data.

Existing Deck Log auto-fill logic sees this data.



Explain how your plan plugs into the current architecture and how you avoid overlapping or breaking anything that already exists.



Only after that explanation should you proceed to implementation.

2. Implement fake telemetry + events generator



Based on the existing structure, implement DEV-ONLY utilities to push fake data.



Concrete goals:

Backend service/module

Create or extend a module (following existing conventions, e.g. in server/):

A function like:

async function seedFakeTelemetryAndEvents(options: {
  orgId: string;
  vesselId: string;
  startTime: Date;
  endTime: Date;
  intervalMinutes: number;
  includeEngineLogTestData?: boolean;
  includeDeckLogTestData?: boolean;
}): Promise<void>


Inside, do all of the following, reusing existing models/ORM:

Insert telemetry rows for:

Main engines (RPM, load, key parameters).

DGs / aux machinery (on/off, load).

Any sensors already modeled as relevant for engine health.

Insert navigation/position rows if there is an existing telemetry type/table for vessel position:

Lat/lon making a plausible route.

Speed over ground, course over ground.

Insert events that logs should see:

A few work orders created/completed in that period.

Some alerts/alarms (e.g. high temp, vibration) that will show up as interesting context.

Make values coherent (e.g. engines ramp up when speed increases, etc.), but they can be random within reasonable ranges.

Deck log-relevant data

If there’s code that uses StormGeo or weather data:

For dev mode, generate a fake weather context object where real StormGeo would normally be read, or:

Populate a separate table that the deck log auto-fill reads from (if that’s how it’s designed).

Clearly separate this fake data from any real StormGeo integration (e.g. guard with ENABLE_FAKE_STORMGEO_DEV).

Engine Room auto-fill integration

Where your engine log auto-fill logic lives (e.g. generateEngineRoomLogEntriesFromTelemetry):

Ensure your fake telemetry is written into exactly the tables/collections it expects.

Provide a simple helper, e.g.:

async function seedFakeEngineRoomLogScenario() {
  const orgId = 'default-org-id';
  const vesselId = 'demo-vessel-id-1'; // or look up / create a demo vessel

  const now = new Date();
  const start = subHours(now, 12);
  const end = now;

  await seedFakeTelemetryAndEvents({
    orgId,
    vesselId,
    startTime: start,
    endTime: end,
    intervalMinutes: 30,
    includeEngineLogTestData: true,
    includeDeckLogTestData: false,
  });

  // Then trigger your existing engine log auto-fill:
  await generateEngineRoomLogsFromTelemetry({
    orgId,
    vesselId,
    from: start,
    to: end,
  });
}


Adapt the function names and parameters to match the actual codebase.

Deck log auto-fill integration

Do the same for deck log:

async function seedFakeDeckLogScenario() {
  const orgId = 'default-org-id';
  const vesselId = 'demo-vessel-id-1';
  const now = new Date();
  const start = subHours(now, 12);
  const end = now;

  await seedFakeTelemetryAndEvents({
    orgId,
    vesselId,
    startTime: start,
    endTime: end,
    intervalMinutes: 30,
    includeEngineLogTestData: false,
    includeDeckLogTestData: true,
  });

  await generateDeckLogsFromTelemetryAndWeather({
    orgId,
    vesselId,
    from: start,
    to: end,
  });
}
Again, map this to whatever functions/entities exist in this repo.

3. Provide safe entrypoints to trigger fake data



Add developer-friendly ways to run these seeds without impacting normal usage:

A Node script under scripts/ (following existing patterns), e.g.:

node scripts/seed-fake-logs.js --engine --deck --hours=24 --vessel=demo-vessel-id-1
Parse CLI args.

Require an explicit ENABLE_FAKE_TELEMETRY=1 env var, otherwise exit with a warning.

Call your seedFakeEngineRoomLogScenario / seedFakeDeckLogScenario.

Optionally, a dev-only API endpoint, e.g.:

POST /api/dev/seed-fake-logs

Guarded by:

NODE_ENV !== 'production' and

maybe a secret dev token env var.

The body can specify:

orgId, vesselId, hoursBack, and flags like seedEngine, seedDeck.



Make sure you clearly comment all dev-only parts so they are easy to disable or remove in production builds.

4. Frontend verification flow



Add a brief instruction (in comments or docs) on how to visually verify:

Steps for a dev:

Start the app as usual (Electron / web dev server).

Run the seeding script or call the dev endpoint.

Navigate to:

Engine Room Log screen for the seeded vessel.

Deck Log screen for the same vessel.

Confirm that:

Log lines exist for the seeded time window.

Values are filled from fake telemetry (RPM, loads, weather, position, etc.).

Sign-off / editing flows still work normally.

If necessary, add a small in-app banner or badge in dev mode (e.g. “DEMO DATA – NOT LIVE TELEMETRY”) when viewing logs for the demo vessel.

5. Tests & documentation

If the project already has tests:

Add a small test (or tests) that confirm:

seedFakeTelemetryAndEvents writes valid rows.

The auto-fill functions can successfully generate log entries from that fake data without crashing.

Add a short DEV.md or section in the main README explaining:

How to enable fake telemetry.

How to run the seeding script.

How to clean up / reset the dev DB if needed.

Constraints:

Do not break any production paths or remove existing simulators.

Reuse existing architecture for telemetry, logs, and org/vessel isolation instead of inventing parallel systems.

Keep everything clearly dev-only and safe.



Start by summarising the existing telemetry + logbook pieces and confirming where the engine and deck log auto-fill hooks live, then implement the fake data pipeline and seeding scripts as described.