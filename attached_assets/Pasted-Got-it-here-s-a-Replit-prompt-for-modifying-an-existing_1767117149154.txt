Got it — here’s a Replit prompt for modifying an existing scheduler codebase, not building a new one. It’s written so Replit AI will inspect your repo first, then implement the missing mobile features from the first screenshot to match the target behavior.

Copy/paste this into Replit AI:

⸻

Replit Prompt (Patch Existing Scheduler)

You are working in an existing codebase for a Crew & Compliance scheduler (mobile + desktop). Do not rebuild the app. Your job is to fill in the missing features in the current mobile schedule screen (the “timeline / gantt” view) so it reaches feature parity with the intended design.

Step 0 — Repo discovery (must do first)
	1.	Identify the framework and structure (React/Next/Vue/etc).
	2.	Locate the schedule/timeline screen used in mobile (search for keywords like: Crew & Compliance, Schedule, Gantt, Timeline, Assignment, Vessel, Roster).
	3.	Identify the data model for assignments, crew, vessels, compliance/constraints.
	4.	List the key components/files you will touch before changing anything.

Goal

Patch the existing mobile scheduler screen to include the missing capabilities that exist in the “ideal” planner UI:
	•	Crew roster access
	•	Assignment details
	•	Constraints/compliance indicators
	•	Draft/apply workflow
	•	Sync state
	•	Export/print

Do this with minimal disruption to existing styles and patterns. Reuse existing state management, UI components, and API calls where possible.

⸻

1) Add View Navigation Tabs (if missing)

In the Crew & Compliance module, add persistent tabs / segmented control:
	•	Crew Roster
	•	Schedule (default)
	•	Hours of Rest
	•	Compliance

If these views already exist elsewhere, route to them. If not, create placeholders but keep navigation working.

Acceptance:
	•	Tabs are visible on mobile and desktop.
	•	Schedule tab shows current timeline.

⸻

2) Add Sync + Status Control (if missing)

In the schedule header, show:
	•	Status chip: Offline / Syncing / Up to date
	•	Manual “Sync” action

Hook into existing sync logic if present; otherwise:
	•	Implement a minimal sync action that refreshes schedule data and updates UI state.

Acceptance:
	•	Status updates correctly on fetch/start/end.
	•	If user edits locally, show a “Draft changes” indicator.

⸻

3) Mobile Crew Roster Access (drawer)

On mobile, add a “Crew” button that opens a drawer (left or bottom sheet) that acts like the desktop left panel.

Drawer must include:
	•	Vessel list
	•	Expand/collapse crew lists per vessel
	•	Search crew
	•	Filter by rank/role (use existing taxonomy)

Selection behavior:
	•	Selecting a crew member highlights their assignments on the timeline.
	•	Selecting a vessel filters the visible rows to that vessel (optional if heavy).

Acceptance:
	•	Drawer is reachable from Schedule view.
	•	Selection affects timeline immediately.

⸻

4) Assignment Details Panel (mobile bottom sheet)

On tap of an assignment bar, open a bottom sheet (or full-screen modal) with tabs:
	•	Details
	•	Constraints
	•	AI Suggestions

Content requirements:
	•	Compliance state: LEGAL / AT RISK / NON-COMPLIANT
	•	Soft vs Hard constraints badges
	•	Fatigue risk indicator (if fatigue exists in data, surface it)
	•	Regulation/Source label (MLC / STCW / Company policy)
	•	Issues list with severity + messages
	•	Suggested fixes list (if no real AI exists, display “suggested actions” from available data)

If desktop already has an assignment details panel, reuse the same component in a responsive mode.

Acceptance:
	•	Sheet opens from timeline tap.
	•	Shows correct data for the selected assignment.

⸻

5) Inline Compliance Indicators on Timeline Bars

Update assignment bar rendering to include non-color signals:
	•	Warning icon if any issues exist
	•	Outline/badge for hard constraints
	•	Small badge for soft constraints
	•	Tooltip (desktop) or long-press preview (mobile) summarizing issues

Acceptance:
	•	User can see risk/violation without opening details.

⸻

6) Edit Mode + Draft Apply/Cancel Workflow

Add an explicit Edit Mode toggle to the schedule.
When Edit Mode is ON:
	•	Allow dragging an assignment horizontally to change dates (snap to day boundaries)
	•	Allow resizing start/end via handles OR a simple edit form in the sheet if drag-resize is too risky
	•	Any change sets “Draft changes = true” and does not persist until Apply

UI:
	•	On mobile, show a bottom action bar with:
	•	Cancel (revert to last committed)
	•	Apply Changes (persist to server)
	•	On desktop, show the same actions near the header.

If the system already supports drafts, connect to it.
If not, implement a local “staged edits” layer (copy state) and only call APIs on Apply.

Acceptance:
	•	Cancel restores prior state
	•	Apply persists and refreshes
	•	Draft badge appears when staged edits exist

⸻

7) Date Range Controls

Ensure the mobile schedule header supports:
	•	Start → End date selection
	•	Presets: 2w / 1m / 3m
	•	Today button that scrolls timeline to today

Reuse existing date utilities.

Acceptance:
	•	Date controls correctly change timeline window and data fetch.

⸻

8) Desktop fixes (only where missing features exist)

Do NOT redesign desktop UI. Only ensure the desktop version has the correct feature support:
	•	Compliance status clearly visible (LEGAL/AT RISK/NON-COMPLIANT)
	•	Override justification capture (if overrides exist)
	•	Minimum manning / coverage warnings (if rules exist)
If these already exist, do nothing.

⸻

9) Export / Print / File Output (Schedule charts)

Add an Export action to Schedule view (desktop + mobile). Must support:
	•	Export PDF (primary, print-ready, landscape)
	•	Export CSV/XLSX (data)
	•	Optional PNG/SVG

PDF must include:
	•	Timeline chart (current view)
	•	Legend
	•	Date range
	•	Footer metadata:
	•	Exported by (current user if available, else placeholder)
	•	Timestamp
	•	Data state: Live / Draft
	•	Compliance engine version (from config or placeholder)
	•	Rule set applied (MLC/STCW/Company; from config or placeholder)

Implementation guidance:
	•	Prefer existing export infrastructure if present.
	•	If none exists, implement client-side export using a library already in repo.
	•	Avoid “screenshot-only” exports if possible; but if that’s the only feasible approach, make it high-resolution and print-safe.

Acceptance:
	•	Export works on both mobile and desktop.
	•	PDF prints cleanly and preserves labels.

⸻

10) Constraints
	•	Keep existing architecture and styling.
	•	Minimal new dependencies. If you must add one, justify it and keep it small.
	•	Write tests only if repo already has a pattern; otherwise add basic unit tests for formatting and export generation.
	•	Update any docs/README if needed.

⸻

Output Requirements (what you must deliver)
	1.	A short summary of what was implemented.
	2.	A list of files changed.
	3.	Any new components added.
	4.	How to manually verify each feature (step-by-step).
	5.	Notes about any assumptions (e.g., if compliance rules aren’t in data yet).

Proceed with implementation now.

⸻

If you paste that into Replit AI, it should do the right thing as a patch.

If you tell me your stack (React Native? Flutter? web responsive?), I can rewrite the same prompt in the exact language Replit’s agent tends to follow best for that framework.