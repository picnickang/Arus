You are a senior full-stack engineer working on an enterprise marine Predictive Maintenance + Digital Twin platform.

Your task is to improve the existing schema and implement all missing full-stack capabilities tied to that schema, based on the current architecture already present in this repository.

This is a FULL-STACK task:
- database schema
- migrations
- backend domain modules
- application services
- ports/adapters
- HTTP routes/controllers
- background jobs / scheduled updates where appropriate
- frontend hooks/pages/components
- reporting / documentation

Do NOT rewrite the system from scratch.
Do a repo-aware upgrade.
Inspect what already exists and extend it cleanly.

==================================================
CONTEXT
==================================================

The repo already contains a large enterprise schema and substantial PdM / Digital Twin functionality.

The schema already includes:
- organizations / users / RBAC
- vessels / equipment / devices
- telemetry, archives, rollups, aggregates
- sensors and mappings
- maintenance, work orders, alerts
- inventory, purchasing, crew, compliance
- ML lifecycle tables
- feature store and fleet baselines
- digital twin tables
- knowledge base / RAG
- costs / ROI / reports / scheduling

Relevant existing areas in schema include:
- shared/schema/telemetry.ts
- shared/schema/ml-analytics-core.ts
- shared/schema/ml-analytics-advanced.ts
- shared/schema/pdm-feature-store.ts
- shared/schema/digital-twin.ts
- shared/schema/equipment.ts
- shared/schema/sensors.ts
- shared/schema/maintenance.ts

The platform already has:
- PdM platform
- feature store
- fleet analytics
- model registry
- inference
- drift monitoring
- digital twin
- residuals
- scenario simulation
- replay UI

You must improve the schema and implement the next-level missing platform pieces from a full-stack perspective.

==================================================
PRIMARY GOALS
==================================================

Implement these major improvements:

1) TRAINING PIPELINE
Build a real end-to-end model training lifecycle tied to the schema:
- training datasets
- model artifacts
- training runs / statuses / metrics
- promotion into model versions / deployments
- frontend visibility into training state

2) CONTINUOUS TWIN UPDATES
Move the digital twin toward scheduled/continuous updates:
- scheduled twin state refresh
- scheduled residual computation
- scheduled feature refresh where appropriate
- frontend visibility into freshness / last update

3) PREDICTION GOVERNANCE
Add governance and lifecycle controls for predictions:
- explicit validity windows
- prediction versioning / feature-set provenance
- approval / review / suppression workflow where appropriate
- frontend visibility into prediction status and provenance

4) SCHEMA IMPROVEMENTS
Improve the schema to support the above safely and cleanly:
- no analytics contamination of operational tables
- append-only analytical history where appropriate
- proper FK/index design
- strong org isolation
- typed enums / status fields where beneficial

==================================================
ARCHITECTURE REQUIREMENTS
==================================================

Maintain modular, hexagonal architecture.

Use this layering wherever possible:

Domain
- entities / types
- ports

Application
- orchestration services
- training pipeline services
- twin update services
- governance services

Infrastructure
- DB adapters
- telemetry adapters
- model artifact adapters
- background job adapters

HTTP
- thin routes/controllers

Frontend
- hooks
- pages
- components

Preferred flow:

routes
→ application service
→ ports
→ adapters

Do NOT put orchestration logic directly in routes.
Do NOT duplicate existing good modules if they already exist.

==================================================
REPO-AWARE REVIEW FIRST
==================================================

Before coding, inspect the repository and determine what already exists vs what is missing.

Review especially:
- shared/schema/*
- server/domains/pdm-platform/*
- server/domains/pdm/*
- current training / model / inference code
- current background jobs / scheduler / health checks
- current digital twin modules
- current frontend pages for PdM / Digital Twin / models
- existing report files in the root

Produce an internal implementation plan before coding:
1) what already exists and is good
2) what exists but is incomplete
3) what schema changes are needed
4) what backend changes are needed
5) what frontend changes are needed

==================================================
IMPLEMENTATION WORK
==================================================

SECTION A — SCHEMA IMPROVEMENTS
Improve the schema in a way that supports the platform cleanly and safely.

A1) Add TRAINING DATASET support
Add a schema module or extend existing ML schema with a table like:

training_datasets
- id
- orgId
- name
- description
- sourceType
- sourceQuery or sourceConfig (jsonb)
- featureColumns (jsonb/text[])
- labelColumn
- targetType
- timeRangeStart
- timeRangeEnd
- rowCount
- splitConfig (jsonb)
- schemaVersion
- status
- createdBy
- createdAt
- updatedAt

Requirements:
- proper FK to organizations
- indexes for orgId, status, createdAt
- support reproducibility

A2) Add MODEL ARTIFACT support
Add a table like:

model_artifacts
- id
- orgId
- modelVersionId
- artifactType
- storageUri
- checksum
- framework
- format
- sizeBytes
- metadata (jsonb)
- createdAt

Requirements:
- FK to model_versions
- indexes for orgId, modelVersionId

A3) Improve TRAINING JOBS / TRAINING RUNS
If training_jobs already exists, review and extend it rather than duplicating.
Make sure the platform can capture:
- dataset used
- artifact produced
- metrics
- status
- startedAt / finishedAt
- error message
- config / hyperparameters
- initiatedBy

Add any missing columns or related tables.

A4) Add PREDICTION GOVERNANCE fields/tables
Review existing failure_predictions / inference / related tables and improve them.

Add or ensure:
- predictionValidUntil
- modelVersionId
- featureSetVersion
- trainingDatasetId (or reference to training basis if appropriate)
- reviewStatus
- reviewedBy
- reviewedAt
- suppressionReason
- governanceMetadata (jsonb)

If a separate table is cleaner, create:
prediction_governance
or
prediction_reviews
or
prediction_suppressions

Keep the design modular and traceable.

A5) Enforce APPEND-ONLY analytical history where appropriate
Review:
- asset_twin_state
- inference_runs
- prediction_explanations
- model_drift_metrics
- equipment_features
- twin_residuals

Ensure they are treated as historical/append-only analytical records.
Do not overwrite analytical history unless there is a compelling reason.
Add indexes / constraints that support time-based history.

A6) Add TELEMETRY STREAM / PROCESSING TRACKING if still missing
The system has telemetry storage already. Review whether a lightweight event/processing tracking layer is missing.

If needed, add a table such as:
telemetry_events or telemetry_processing_runs
to support:
- near-real-time processing state
- scheduled feature/twin refresh bookkeeping
Do NOT overengineer this if existing tables already cover it.

A7) Tighten types, enums, indexes, and org isolation
Across new and modified schema:
- add notNull where appropriate
- add enums/unions/checks for statuses
- ensure orgId FKs everywhere needed
- ensure indexes on orgId + timestamp/status where useful
- do not introduce hardcoded defaults like default-org-id

==================================================
SECTION B — BACKEND IMPLEMENTATION
==================================================

B1) TRAINING PIPELINE MODULE
Implement a full backend module for training lifecycle.

Suggested module shape:
server/domains/pdm-platform/training-pipeline/
  ports.ts
  adapter.ts
  training-pipeline.service.ts
  routes.ts

Responsibilities:
- create/list training datasets
- start a training run
- track training run status
- store metrics
- register produced artifact
- create or promote model version on successful completion
- optionally create deployment or mark candidate for deployment

Keep model training implementation pragmatic:
- If there is no real Python training runtime, create a clean TrainingRunnerPort
- Provide a safe stub or deterministic runner only if necessary
- Structure everything so a real training runtime can be plugged in later

Add endpoints for:
- dataset CRUD/listing as needed
- training run creation
- training run status/history
- artifact/model version linkage

B2) CONTINUOUS TWIN UPDATE MODULE
Implement scheduled / recurring twin updates.

Suggested module shape:
server/domains/pdm-platform/twin-updates/
  ports.ts
  adapter.ts
  twin-update.service.ts
  routes.ts

Responsibilities:
- compute latest twin state for active twins
- compute latest residuals
- optionally refresh feature store first if needed
- update freshness timestamps / statuses
- support on-demand run + scheduled run

Use existing telemetry, feature store, fleet analytics, and digital twin modules.

Integrate with existing background jobs infrastructure.
If background jobs are stubbed or limited, implement a safe scheduler adapter or cron-style job registration.
Do not crash health checks.

Add endpoints for:
- run twin refresh for one twin
- run refresh for all active twins
- get freshness / last updated status

B3) PREDICTION GOVERNANCE MODULE
Implement backend workflow for prediction governance.

Suggested module shape:
server/domains/pdm-platform/prediction-governance/
  ports.ts
  adapter.ts
  prediction-governance.service.ts
  routes.ts

Responsibilities:
- attach validity windows to predictions
- expose provenance (model version, feature set version, training basis)
- support review / approve / suppress / expire actions
- prevent stale predictions from appearing active if validity window has passed
- surface governance status cleanly to UI

Add endpoints for:
- get prediction governance details
- review/update governance state
- list active vs expired predictions

B4) SCHEDULED JOBS / AUTOMATION
Where appropriate, add or improve recurring jobs for:
- feature refresh
- twin state refresh
- residual refresh
- drift refresh
- prediction expiry / governance cleanup

Keep this robust and observable.
Update health endpoints if needed so scheduled functionality does not fail silently.

==================================================
SECTION C — FRONTEND IMPLEMENTATION
==================================================

This must be a FULL-STACK implementation.
Do not stop at schema/backend changes.

C1) TRAINING UI
Add or extend frontend so users can:
- view datasets
- start training runs
- monitor training status
- inspect training metrics
- see linked model versions / artifacts

Suggested location:
- add a Training tab to existing PdM Platform or Model Registry UI
OR
- add a focused ML Training page if that fits the repo better

C2) DIGITAL TWIN FRESHNESS UI
Update Digital Twin frontend to show:
- last twin update time
- freshness / stale badge
- latest residual compute time
- manual refresh controls if appropriate

C3) PREDICTION GOVERNANCE UI
Update PdM / inference / prediction screens to show:
- prediction valid-until
- active / expired / reviewed / suppressed states
- model version and provenance
- review actions if existing UX supports admin/engineer workflows

C4) POLISH / USABILITY
Ensure all new backend improvements are actually visible in UI.
Handle:
- loading states
- empty states
- error states
- timestamps / freshness labels
- clean status badges

Keep styling aligned with the existing app.
Do not redesign the whole UI.

==================================================
SECTION D — OPERATIONAL / ANALYTICAL SEPARATION
==================================================

Preserve the separation between:
1) operational tables
2) observational raw measurement tables
3) analytical derived tables

Do NOT write ML/twin analytical outputs into operational tables such as equipment, vessels, or work orders unless there is a legitimate operational action record.
Keep predictions, twin state, residuals, features, drift metrics, and training outputs in dedicated analytical structures.

If the current code violates this anywhere, refactor it cleanly.

==================================================
SECTION E — VALIDATION
==================================================

Run and verify:
- npm run format:check
- npm run lint
- npm run typecheck

Also verify, where possible:
- server routes register cleanly
- frontend typecheck/build works if such a command exists
- no new hardcoded org IDs
- all new org-scoped paths require org context
- schema compiles cleanly

==================================================
SECTION F — REPORTING (IMPORTANT)
==================================================

Update the existing root text report so all work can be reviewed in one place.

Primary report file to update:
PDM_UPGRADE_REPORT.txt

Also create a second focused schema/implementation report:
FULL_STACK_SCHEMA_IMPROVEMENTS_REPORT.txt

Requirements for PDM_UPGRADE_REPORT.txt:
- update earlier sections if needed so it reflects the current final state
- add a new major section titled exactly:

14. SCHEMA + FULL-STACK PLATFORM IMPROVEMENTS

This section must include:

14.1 What already existed before this work
14.2 What schema gaps were identified
14.3 What schema changes were implemented
14.4 New files added
14.5 Existing files modified
14.6 Backend modules/services/routes added or updated
14.7 Frontend hooks/pages/components added or updated
14.8 Governance / training / twin-refresh features implemented
14.9 What remains stubbed or approximate
14.10 Validation results
14.11 Final end-to-end architecture after this work

Requirements for FULL_STACK_SCHEMA_IMPROVEMENTS_REPORT.txt:
Create a detailed implementation report with these sections:

1) EXECUTIVE SUMMARY
2) REPO REVIEW FINDINGS
3) SCHEMA CHANGES
4) TRAINING PIPELINE IMPLEMENTATION
5) CONTINUOUS TWIN UPDATE IMPLEMENTATION
6) PREDICTION GOVERNANCE IMPLEMENTATION
7) FRONTEND CHANGES
8) BACKEND/API CHANGES
9) DATABASE MIGRATION NOTES
10) REMAINING STUBS / KNOWN LIMITATIONS
11) VALIDATION RESULTS
12) FINAL ARCHITECTURE SUMMARY

Both reports must:
- be written to the project root
- be human-readable
- be accurate
- list files created and modified with full paths
- clearly distinguish reviewed existing functionality vs newly implemented functionality

Do NOT only print summaries to console.
Write the actual files.

==================================================
IMPORTANT CONSTRAINTS
==================================================

- Be repo-aware and inspect before modifying
- Extend existing modules instead of duplicating them
- Keep everything modular and hexagonal wherever possible
- This is a FULL-STACK task, not backend-only
- Do not break existing working endpoints unnecessarily
- Preserve multi-tenant safety
- Revise and polish before finalizing