Begin next production-hardening wave.

Scope:
Only autonomous-safe items:
#9 ML evaluation gate integrity
#10 RAG export memory/DoS protection
#12 KB upload magic-byte verification
#13 Crew scheduler N+1 removal
#21 External HTTP timeout/circuit-breaker protection
#24 Observability fail-open warnings
#27 Strict-target verification script

Do NOT modify:
auth/security policy decisions
tenant-default policy
encryption fail-fast policy
public API semantics unless required for correctness

==================================================
FIX #9 — ML evaluation gate integrity
==================================================

Location:
server/services/ml/model-evaluation-gate.ts

Problem:
Per-sample prediction failures are swallowed; candidate models can pass despite systematic failure on subsets.

Required:
- track failed/total prediction samples
- compute errorRate
- fail promotion if errorRate exceeds threshold
- include categorized failure metrics in evaluation output
- preserve existing promotion flow semantics otherwise

Add tests:
- partial sample failures
- threshold breach rejects promotion
- healthy candidate still passes

==================================================
FIX #10 — RAG export memory/DoS protection
==================================================

Location:
server/routes/rag-routes.ts

Problem:
Large conversations can OOM PDF/export generation.

Required:
- add total byte cap OR streaming export
- paginate/cursor message retrieval if needed
- return/export truncation metadata if cap reached
- preserve existing export UX as much as possible

Add tests for:
- oversized export
- truncation behavior
- bounded memory behavior if test harness supports it

==================================================
FIX #12 — KB upload magic-byte validation
==================================================

Location:
server/routes/kb-routes.ts

Required:
- validate actual file signature/magic bytes
- do not trust mimetype header alone
- reject invalid uploads before persistence
- harden temp/staging directory permissions
- preserve supported file types

Add tests:
- renamed malicious file rejected
- valid PDF/doc accepted

==================================================
FIX #13 — Crew scheduler N+1 removal
==================================================

Location:
server/scheduler/scheduler-controller.ts

Required:
- add bulk getCrewLeave(ids)
- replace Promise.all N+1 pattern
- preserve scheduling semantics

Add perf-oriented test or query-count assertion if possible.

==================================================
FIX #21 — External HTTP timeout + breaker
==================================================

Locations:
server/domains/agent/tools/regulatory-tools.ts
server/sso/oidc.ts
and other raw fetch sites found during audit

Required:
- AbortController timeout defaults
- integrate existing breaker pattern where appropriate
- fail gracefully
- structured logging on timeout/breaker open

No global networking rewrite.

==================================================
FIX #24 — Observability fail-open warnings
==================================================

Locations:
server/otel.ts
server/instrument.ts
client/src/lib/otel.ts

Required:
- warn once in production when DSN missing
- preserve graceful degradation
- avoid noisy repeated logs

==================================================
FIX #27 — check-tsconfig-target.mjs
==================================================

Required:
- implement strict-target verification script referenced by tsconfig.target.json
- compare relevant compilerOptions between configs
- wire into existing guard/check scripts if appropriate
- fail CI if strict-target drifts

==================================================
VALIDATION
==================================================

Run:
- npx tsc --noEmit
- strict checks
- eslint scoped to touched files
- relevant unit/integration tests
- explicit-any remains 0
- cast count remains 1 justified boundary cast

Rules:
- No explicit any
- No as any
- No ts-ignore
- No eslint-disable
- Preserve runtime behavior
- Preserve strict-mode cleanliness

Architect/code review required.

Final report:
- files changed
- runtime risks fixed
- tests added
- performance/security impact
- tsc result
- lint result