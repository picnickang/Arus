You are Replit Agent working INSIDE an EXISTING codebase. The app has slow page loads and slow dev/prod startup. We have confirmed issues:
- Redis connection failures (ECONNREFUSED 127.0.0.1:6379) causing latency before fallback
- Large frontend codebase (5MB+), missing Vite chunking/optimizations, slow /@vite/client
- Backend event loop lag on startup (196ms), many route modules loaded eagerly
- DB creates ~29 indexes on every boot (should be migration-based / once)
- UX improvements desired: route prefetching, skeleton states, API request dedup, asset optimization, optional service worker (prod)

YOUR TASK: Implement a performance overhaul that improves:
1) backend startup + request latency
2) frontend initial load + navigation speed
3) perceived performance (skeletons)
while preserving existing architecture, offline/online hybrid behavior, and functionality.

ABSOLUTE RULES
- Do NOT start a new project.
- Preserve existing framework/languages, and reuse existing services/repositories.
- Do NOT remove features or change behavior.
- No breaking DB changes. Any schema/index changes must be additive and safe.
- Any caching must not cause stale scheduling data to display incorrectly.
- Provide a before/after PERF REPORT with measurements.

==========================================================
PHASE 0 — BASELINE MEASUREMENTS (DO FIRST)
==========================================================
1) Add lightweight timing logs (or use existing) to measure:
  - Server boot time milestones:
    - start
    - env loaded
    - DB connected
    - routes registered
    - server listening
  - Event loop lag (existing) and peak during boot
2) Record backend request timings for key endpoints (top 5 slowest).
3) Record Vite dev timings:
  - time to first dev server ready
  - GET /@vite/client time (first request after start)
4) Record frontend bundle sizes:
  - build output size
  - largest chunks
Use a simple PERF_BASELINE.md (or console output) to capture these numbers.

==========================================================
PART A — REDIS FAST-FAIL + CIRCUIT BREAKER (HIGH IMPACT)
==========================================================
A1) Find Redis client initialization and all modules that depend on it:
  - rate limiter
  - inventory cache
  - analytics cache
A2) Implement ONE shared Redis client wrapper with:
  - connect timeout: 150–300ms
  - no infinite retries in dev
  - exponential backoff max 1–2 retries in prod (or configurable)
A3) Add a circuit breaker:
  - on first ECONNREFUSED/connect failure, mark Redis unavailable for 60s
  - during that window, do not attempt reconnect on every request
  - fall back immediately to in-memory cache/rate limit path
A4) Add env toggles:
  - REDIS_ENABLED=false disables Redis entirely (dev default if no URL)
  - REDIS_URL controls remote redis
  - REDIS_CONNECT_TIMEOUT_MS
Ensure logs are not spammy: log once per circuit-break window.

ACCEPTANCE:
- ECONNREFUSED no longer repeats on every request
- Requests no longer incur redis connection latency before fallback

==========================================================
PART B — STOP CREATING INDEXES ON EVERY BOOT (HIGH IMPACT)
==========================================================
B1) Locate where “29 indexes created” happens (startup code).
B2) Move index creation to proper migrations:
  - If using an ORM migration system, create a migration file(s) for indexes.
  - If using raw SQL, implement a one-time migration runner that:
    - checks a schema_version table (or migration ledger)
    - runs index creation only once
    - is idempotent
B3) For local/offline DB (if used), ensure migrations run on first open only, not every app start.

ACCEPTANCE:
- Index creation is not executed on normal boots
- Boot is faster and event loop lag reduced

==========================================================
PART C — REDUCE BACKEND BOOT / EVENT LOOP LAG (HIGH IMPACT)
==========================================================
C1) Identify heavy synchronous imports / route module registration:
  - find “61+ route modules loading”
C2) Defer non-critical initialization:
  - analytics collectors
  - optional caches
  - background schedulers/cron
Use:
  - lazy init on first use OR
  - init after server starts listening (setImmediate)
C3) Avoid doing CPU-heavy work at import time:
  - move expensive schema validation, large JSON parsing, file reads into runtime functions
C4) Add route registration timing logs to find the slowest modules and optimize them.

ACCEPTANCE:
- Event loop lag during boot reduced under 100ms (or significantly improved)
- First response time improved

==========================================================
PART D — VITE OPTIMIZATION + CODE SPLITTING (HIGH IMPACT)
==========================================================
D1) Update vite.config.ts with production build optimizations:
- Add build.rollupOptions.output.manualChunks to split:
  - vendor-react (react, react-dom, router)
  - vendor-ui (radix/shadcn UI deps)
  - vendor-utils (date libs, lodash-like libs)
  - features/crew (crew pages/components)
  - features/schedule (schedule planner)
  - features/compliance (compliance)
- Set chunkSizeWarningLimit appropriately
- Ensure minify enabled (esbuild) for production
D2) Improve dev optimizeDeps:
- Add optimizeDeps.include for hot deps
- If necessary, exclude deps that cause slow scanning
- Ensure cacheDir stable
D3) Add a bundle visualizer script (dev-only) to inspect chunk composition.

ACCEPTANCE:
- Smaller initial JS chunk
- faster page navigation
- /@vite/client time reduced after warm start

==========================================================
PART E — ROUTE-LEVEL LAZY LOADING + PREFETCH (MEDIUM)
==========================================================
E1) Ensure main routes are lazy-loaded (React.lazy / dynamic import) for heavy pages:
- Schedule
- Compliance
- Analytics
- Settings (if heavy)
E2) Add route prefetch hints for desktop:
- On nav hover/focus: prefetch the route chunk
- On idle after login: prefetch next-likely pages (Schedule, Crew)
Respect network conditions:
- skip prefetch if save-data enabled or slow connection

ACCEPTANCE:
- Navigation between primary pages feels instant after hover/idle prefetch
- No prefetch stampede (cap concurrency)

==========================================================
PART F — SKELETON STATES (PERCEIVED PERFORMANCE)
==========================================================
F1) Replace generic PageLoader spinner with page-specific skeletons:
- Crew roster skeleton list rows
- Schedule planner skeleton grid rows + date header
- Compliance card skeletons
F2) Ensure skeletons match final layout to reduce layout shift.

ACCEPTANCE:
- Users see immediate structure, not blank/spinner
- Reduced CLS / jank

==========================================================
PART G — API REQUEST DEDUP + STALE TIME (MEDIUM)
==========================================================
G1) Audit TanStack Query queryKeys and duplicate fetches:
- vessels
- crew
- assignments
Standardize keys across app.
G2) Set reasonable default staleTime/cacheTime:
- vessels/crew: staleTime 5–30 minutes
- assignments: staleTime 10–60 seconds, plus manual invalidation on writes/sync completion
G3) Ensure queries are not re-fetched on every mount unnecessarily.

ACCEPTANCE:
- Fewer redundant API calls
- Faster page transitions

==========================================================
PART H — ASSET OPTIMIZATION (LOW/MEDIUM)
==========================================================
H1) Audit images/assets:
- ensure proper sizing
- convert to WebP where applicable
- lazy-load below fold
H2) Remove duplicate icon packs if present and unify.

ACCEPTANCE:
- Reduced asset payload
- Faster LCP where images exist

==========================================================
PART I — SERVICE WORKER (PRODUCTION-ONLY, OPTIONAL)
==========================================================
I1) Add a service worker using a standard Vite PWA approach ONLY for production:
- Cache hashed static assets (cache-first)
- For API requests: network-first with short fallback TTL, or skip caching for scheduling endpoints to avoid stale data
- Provide an update UX (new version available)
This must be behind a toggle: PWA_ENABLED=true.

ACCEPTANCE:
- Repeat visits faster
- No stale schedule data issues

==========================================================
TESTING + VALIDATION (MANDATORY)
==========================================================
1) Run full test suite.
2) Add/extend minimal performance smoke tests if framework supports:
- server boot does not attempt redis repeatedly
- migration/index creation not executed on every boot
3) Verify schedule planner correctness: no stale caches causing wrong assignments.

==========================================================
DELIVERABLE: PERF REPORT (MANDATORY)
==========================================================
Provide a PERF_REPORT.md with before/after metrics:
- server boot time milestones
- peak event loop lag during boot
- top 5 slow endpoints before/after
- Vite dev: /@vite/client timing
- production build: chunk sizes (top 10)
- any tradeoffs noted

NOW START
1) Produce PERF_BASELINE (measurements)
2) Implement Part A → D first (highest impact)
3) Implement E → G (UX + dedup)
4) Optional H/I if time
5) Run tests
6) Output PERF_REPORT with file paths changed and measurements