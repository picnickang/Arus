CONTEXT
You are editing a monorepo named `marine-pdm-node` with:
- Fastify API (Node.js, TF.js, ONNX) under `src/service/`
- Next.js 14 dashboard under `dashboard/`
- Vessel profile JSON + training under `src/`
Do NOT assume commercial-grade features exist. The repo may contain more features than discussed; you must audit first and avoid breaking anything.

PRIMARY GOALS
1) Non-destructive audit & migration plan.
2) Implement a professional Vessel Performance System (VPS) with four core charts and anomaly overlays.
3) Add a Context Layer Engine (CLE) that enriches telemetry and overlays with operational context.
4) Add AIS/GPS and Metocean adapters (graceful fallbacks and fixtures).
5) Add sorting, customizability, organization, contextualization hooks.
6) Provide endpoints, pages, tests, docs, and a runbook.

A) AUDIT (MANDATORY BEFORE CHANGES)
- Scan codebase:
  • Backend routes/endpoints under `src/service/**`
  • Frontend pages/components under `dashboard/**`
- Identify overlaps or conflicts with features we’re about to create.
- Produce `docs/ui_vps_migration.md` with a table:
  | Existing Item | Keep/Augment/Replace | New Location/Route | Notes/Conflicts |
- Do not overwrite conflicting routes/components. If names collide, propose a new name and document it.

B) INFORMATION ARCHITECTURE (create or augment)
Establish routes (app router):
- `/fleet`                          → Fleet Overview (map/list stub + vessel KPI cards)
- `/vessel/[id]/performance`        → VPS charts (+ anomaly overlays)
- `/vessel/[id]/alerts`             → Anomaly/Condition list w/ top contributors & actions
- `/vessel/[id]/maintenance`        → Work orders (stub ok)
- `/reports`                        → Export stubs (CSV/PDF)

Keep dark theme and existing Tailwind/Recharts usage.

C) BACKEND ENDPOINTS (augment without breaking)
In Fastify (modularize under `src/service/routes/*` if cleaner), add or extend:

1) GET `/fleet/summary`
Returns array of vessel cards:
[{ id, name, type, region, anomaly_score, threshold, efficiency, open_alerts, engine }] // engine:'tfjs'|'onnx' if known
Use real data if available; otherwise placeholders.

2) GET `/vessels/:id/performance`
Return time-aligned arrays:
{
  ts: (string[]|number[]),
  load_pct: number[],
  sfoc_g_per_kwh: number[],
  fuel_lph: number[],
  power_kw: number[],
  stw_kn: number[] | [],            // estimate if missing
  load_hist: { bin:number, hours:number }[],
  meta: { context_available: boolean },
  segments: [{ start:number, end:number, mode?:string, sea_state?:number }] // for background shading
}

3) GET `/vessels/:id/baselines`
Comparatives for vessel baseline & fleet average:
{
  baseline: {
    load_vs_sfoc: { x:number, y:number }[],
    power_vs_stw: { x:number, y:number }[],
    load_hist: { bin:number, hours:number }[]
  },
  fleet_avg?: { same shape }
}
Synthesize when real arrays absent.

4) GET `/vessels/:id/alerts`
{
  items: [{
    id, timestamp,
    severity:'low'|'med'|'high',
    anomaly_score:number, threshold:number,
    top_contributors:string[],
    recommendation?:string,
    acknowledged?:boolean
  }],
  next_cursor?: string
}

5) GET `/vessels/:id/overlays`
Overlay markers & optional bands to draw on charts (must be enriched via CLE):
{
  points: [{
    idx?:number, ts?:number,
    anomaly_score:number, threshold:number,
    label?:string, contributors?:string[],
    context?: { mode?:string, sea_state?:number, region?:string, mission?:string, wind_speed?:number },
    confidence?: number
  }],
  bands?: [{ chart:'load_vs_sfoc'|'fuel_vs_time'|'power_vs_stw', yMin?:number, yMax?:number, label?:string }]
}

D) CONTEXT LAYER ENGINE (CLE)
Create:

src/context/
  index.js                 // enrich(), align(), derive()
  cache.js                 // simple LRU/memo for recent windows
  schema.ts                // TS interfaces or JSDoc typedefs
  sources/
    opslog.js              // reads /data/company/<vesselId>/opslog.json if present
    maintenance.js         // reads /data/company/<vesselId>/maintenance.json
    route.js               // stub region/leg mapping; extend later
    weather.js             // stub metocean CSV; extend later
    ais.js                 // AIS adapter (file+API)
    metocean.js            // Metocean adapter (file+API)
  heuristics/
    inferMode.js           // DP/Transit/Standby from thruster load, rpm var, stw
    inferSeaState.js       // estimate from vibration/accel if no API

Implement:
- align(tsPrimary, tsSecondary, toleranceMs=15000) → index map
- derive.loadPctFromTorque({ torqueNm[], maxTorqueNm }) → number[]
- inferMode(), inferSeaState() with thresholds from profile JSON or defaults
- enrich({ vesselId, ts[], series, overlays? }) → { context: ContextPoint[], overlays: OverlayPoint[] }
  Steps: gather sources (opslog, maintenance, ais, metocean), infer where missing, align, merge, add confidence (authoritative=1.0; inferred≈0.6), cache.

E) AIS & METOCEAN ADAPTERS
- `src/context/sources/ais.js`
  • `getAisTrack(vesselId, tStart, tEnd)` → [{ ts, lat, lon, sog_kn, cog_deg?, heading?, nav_status? }]
  • `deriveRouteContext(track)` → ContextPoints (mode inference via sog + nav_status)
  Data sources: files under `/data/ais/<vesselId>/*.json`; or if `AIS_API_URL` + `AIS_API_KEY` set, fetch JSON API; else synthesize transit/DP legs.

- `src/context/sources/metocean.js`
  • `getMetoceanContext(vesselId, tStart, tEnd)` → [{ ts, sea_state, wind_speed, wave_height?, current_kn?, current_dir? }]
  Sources: `/data/metocean/<region>.csv` or API if `METOCEAN_API_URL` + `METOCEAN_API_KEY`; else infer from motion variance.

Add to `.env.example`:
AIS_API_URL= 
AIS_API_KEY= 
METOCEAN_API_URL=
METOCEAN_API_KEY=

F) FRONTEND (Next.js) — PAGES & COMPONENTS
Use Recharts; responsive; dark theme.

1) `/fleet`
- Filters (chips): type (tug/psv/pilot/workboat), region, mission.
- Sorting: `?sort=anomaly_score|efficiency|alerts&order=asc|desc` (persist in URL).
- Vessel cards: name/type/region, RAG badge computed from anomaly vs threshold (green ≤0.8×, amber 0.8–1.0×, red >1.0×), KPIs (efficiency, open alerts), engine (‘tfjs’/‘onnx’). Click → `/vessel/[id]/performance`.

2) `/vessel/[id]/performance`
- Four charts:
  • Load ↔ SFOC (scatter/line)
  • Fuel ↔ Time (line; + moving average)
  • Power ↔ STW (scatter/line)
  • Load Distribution Histogram (bars)
- Comparative lines: dashed baseline, dotted fleet avg (toggleable).
- Anomaly overlays: marker glyphs on all applicable charts; tooltip shows anomaly score, threshold, contributors, and context (mode, sea_state, wind).
- Background segment shading by `segments[]` (e.g., DP bands).
- Time presets: 2h / 24h / 7d / voyage; brush zoom.
- Context & legend:
  • `dashboard/app/vessel/[id]/_context/ContextChips.tsx` (mode/sea state/mission chips; show confidence via opacity)
  • `dashboard/app/vessel/[id]/_context/OverlayLegend.tsx` (toggle markers/bands/baseline/fleet avg; persist in URL)
- Customizability: `SettingsDrawer` to hide/show/reorder charts; threshold overrides; persist to localStorage and URL param `charts=sfoc,fuel,power,hist`.

3) `/vessel/[id]/alerts`
- Table/cards with sorting (severity, timestamp, ack). Tooltips with context.
- Buttons: Acknowledge, Create Work Order (stub ok).
- Right drawer: mini-trends for top contributors (last 30/90 days).

4) `/vessel/[id]/maintenance`
- List (open/overdue/closed), create form stub, timeline/calendar. Link from alerts.

5) `/reports`
- Buttons for CSV/PDF exports (stub endpoints).

G) SCHEMAS (TS or JSDoc typedefs)
`src/context/schemas/types.ts` (or .d.ts)
- ContextPoint, OverlayPoint, Event, AisTrackPoint, MetoceanPoint, PerformanceSegment, OverlaysResponse, ContextResponse.

H) PROFILES EXTENSION (for heuristics & UI prefs)
Extend `src/profiles/*.json` with:
{
  "ui": { "default_visible_charts": ["sfoc","fuel","power_stw","hist"] },
  "context": {
    "mode_rules": { "dp_thruster_load_min": 0.55, "dp_stw_max_kn": 0.8, "transit_stw_min_kn": 5.0 },
    "sea_state_rules": { "vibz_var_bands": [0.002, 0.006, 0.012, 0.020] }
  }
}

I) FIXTURES & SCRIPTS
Add `scripts/generate_context_fixtures.js`:
- Emit mock `/data/company/V001/opslog.json`, `/data/company/V001/maintenance.json`.
- Emit mock `/data/ais/V001/*.json` and `/data/metocean/region.csv`.
- Scatter a few anomaly overlay points.
Add npm scripts:
- `"fixtures:context": "node scripts/generate_context_fixtures.js"`

J) TESTS (basic)
Add `scripts/test_context.js` to assert:
- `/fleet/summary` returns ≥1 item.
- `/vessels/V001/performance` returns arrays + `segments.length ≥ 1`.
- `/vessels/V001/overlays` returns `points[]` and each point has `context` or `confidence`.
- `/vessels/V001/context` returns `{ context, events }`.

K) FEATURE FLAGS & LEGACY
- Add `NEXT_PUBLIC_UI_V2=true` to gate new nav. If older pages exist, keep under `/legacy/*` with a deprecation note. Do not break existing routes.

L) SORTING, CUSTOMIZABILITY, ORGANIZATION, CONTEXTUALIZATION (MANDATORY)
- Sorting: on `/fleet` and `/alerts` tables/cards with URL params.
- Customizability: `SettingsDrawer` to toggle/reorder charts; unit prefs; threshold overrides; persist in localStorage and `/user/preferences` (stub).
- Organization: Breadcrumbs (Fleet > Vessel > Performance), collapsible sections (Fuel/Propulsion/Powertrain).
- Contextualization: chart annotations for ops/maintenance events; tooltips show mode/sea_state/wind and contributors; narrative summary card per vessel (generated from current vs baseline vs fleet avg).

M) DOCS & DIAGRAMS
Create:
- `docs/ui_vps_summary.md` — routes, endpoints, component tree, how to run.
- `docs/ui_vps_migration.md` — audit results, conflicts, renames, deprecations.
- `docs/sequence_performance.md` — Mermaid sequence diagram for `/vessels/:id/performance` (include rendering instructions).
(If PlantUML available, also add `docs/sequence_performance.puml` and minimal npm scripts to render.)

N) ACCEPTANCE CRITERIA
- `/fleet`: cards with RAG status, filters, sort works (URL-driven), persists.
- `/vessel/[id]/performance`: FOUR charts render; baseline and fleet avg toggle; anomaly markers display with context tooltips; background segment shading by mode/sea_state; settings persist; shareable URL reflecting filters and visible charts.
- `/vessel/[id]/alerts`: sortable list; acknowledge + create work order stubs; drawer with mini-trends.
- `/vessel/[id]/maintenance`: basic list & timeline; links from alerts.
- Endpoints respond without breaking any existing routes; migration docs present.
- Running `npm run fixtures:context` yields visible context and overlays without real feeds.

O) RUNBOOK (print at end)
- `npm run fixtures:context`
- `npm run dev` (API)
- `cd dashboard && npm run dev` (UI)
- Open: `/fleet` then `/vessel/V001/performance?range=24h&charts=sfoc,fuel,power,hist&sort=anomaly_score&order=desc`

OUTPUT
At completion, print:
1) Summary table of added/updated files.
2) List of new/augmented endpoints.
3) Any conflicts avoided and how they were resolved.
4) Exact commands to run API and UI locally.