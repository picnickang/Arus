You are a senior Electron + electron-builder engineer helping me harden my desktop build config in THIS Replit.

GOAL
- Review and repair my electron-builder configuration so that:
- The app bundles cleanly for macOS, Windows, and Linux.
- `server` and `shared` are NOT duplicated across `files`, `asarUnpack`, and `extraResources`.
- `asar` usage is explicit and correct.
- We don’t ship unnecessary dev/test files or huge node_modules bloat.
- Runtime paths for the server and shared code remain correct for my existing Electron main process.

CONTEXT ABOUT THIS PROJECT
- This is a full-stack Electron app for a marine PdM system.
- The existing electron-builder config looks roughly like this (or very similar), either in `electron-builder.json`, `electron-builder.yml`, or under `"build"` in `package.json`:

{
"appId": "com.arus.marine",
"productName": "ARUS Marine",
"directories": {
"output": "release",
"buildResources": "build"
},
"files": [
"dist/**/*",
"dist-electron/**/*",
"server/**/*",
"shared/**/*",
"scripts/**/*",
"node_modules/**/*",
"package.json"
],
"asarUnpack": ["server/**/*", "shared/**/*"],
"extraResources": [
{ "from": "server", "to": "server" },
{ "from": "shared", "to": "shared" },
{ "from": "scripts", "to": "scripts" }
],
"mac": {
"category": "public.app-category.business",
"target": [
{ "target": "dmg", "arch": ["x64", "arm64"] },
{ "target": "zip", "arch": ["x64", "arm64"] }
],
"icon": "build/icon.icns",
"darkModeSupport": true,
"hardenedRuntime": true,
"gatekeeperAssess": false,
"entitlements": "build/entitlements.mac.plist",
"entitlementsInherit": "build/entitlements.mac.plist"
},
"dmg": { ... },
"linux": {
"target": ["AppImage", "deb"],
"category": "Office"
},
"win": {
"target": ["nsis", "portable"],
"icon": "build/icon.ico"
},
"nsis": {
"oneClick": false,
"allowToChangeInstallationDirectory": true,
"createDesktopShortcut": true,
"createStartMenuShortcut": true
}
}

- I want to keep the same high-level targets (mac dmg/zip, win nsis/portable, linux AppImage/deb), but I want the config cleaned up and production-ready.

YOUR TASKS
1. **Locate the electron-builder config**
- Find where the build config lives:
- either `electron-builder.json` / `electron-builder.yml` at repo root,
- OR in `package.json` under the `"build"` key.
- Work directly on that real config file (do NOT create a new unused file).

2. **Remove duplication of `server` and `shared`**
- Currently `server` and `shared` are:
- included in `"files"`,
- unpacked via `"asarUnpack"`,
- AND copied via `"extraResources"`.
- Adopt this strategy:
- Keep `server/**/*` and `shared/**/*` listed in `"files"`.
- Keep them in `"asarUnpack"` so they are accessible outside of the ASAR.
- REMOVE them from `"extraResources"` to avoid double packaging.
- Keep `scripts/**/*` in `files` OR `extraResources` (pick one consistent approach). Prefer:
- Keep `scripts/**/*` in `"files"`.
- Keep `"extraResources"` ONLY for scripts if needed, not for server/shared.

3. **Make `asar` usage explicit and safe**
- Add `"asar": true` at the top level if it’s not already present.
- Ensure `asarUnpack` includes any paths that:
- need to be executed as separate Node processes, or
- rely on dynamic `require` or native modules.
- Keep at least:
- `"asarUnpack": ["server/**/*", "shared/**/*"]`.

4. **Slim down `files` to avoid unnecessary bloat**
- Adjust the `"files"` section so it:
- Includes:
- `dist/**/*`
- `dist-electron/**/*`
- `server/**/*`
- `shared/**/*`
- `scripts/**/*`
- `package.json`
- Excludes:
- source maps
- tests
- logs
- any clearly dev-only directories
- A good baseline:

"files": [
"dist/**/*",
"dist-electron/**/*",
"server/**/*",
"shared/**/*",
"scripts/**/*",
"package.json",
"!**/*.map",
"!**/__tests__/**",
"!**/test/**",
"!**/*.log"
]

- Let electron-builder resolve needed `node_modules` automatically based on the main process entry points; do NOT explicitly include `"node_modules/**/*"` unless absolutely required. If the project currently depends on that, tighten it as much as possible (e.g. remove test fixtures, docs, etc.).

5. **Keep and lightly refine target config**
- Preserve the existing OS targets and icons.
- You MAY:
- Change Linux `category` from `"Office"` to something more appropriate like `"Utility"` or `"Engineering"` if that’s straightforward.
- Leave `mac.hardenedRuntime: true` and `gatekeeperAssess: false` (that’s okay for dev; don’t make signing assumptions).
- Do not remove or break existing targets; only refine.

6. **Ensure runtime path expectations still work**
- Find the Electron main process file (`main.ts`, `main.js`, `electron-main.ts`, etc.), typically under something like:
- `dist-electron/` (built)
- or `src/electron/` (source)
- Look for how the server is started, for example:
- `path.join(process.resourcesPath, 'server', 'index.js')`
- or similar.
- Because we’re keeping `server/**/*` in `files` and `asarUnpack`, adjust any path logic if needed so that at runtime it correctly resolves the unpacked server location:
- For example, for asar unpacked content, a common pattern is:

const path = require("path");
const serverPath = app.isPackaged
? path.join(process.resourcesPath, "app.asar.unpacked", "server", "index.js")
: path.join(__dirname, "..", "server", "index.js");

- If my project already uses similar logic, just make sure it’s consistent with the new packaging layout and doesn’t assume `extraResources/server` anymore.

7. **Run the existing build script and verify**
- Use whatever build script is already defined in `package.json` (for example: `"build": "electron-builder"`).
- Run it inside Replit.
- Confirm the config compiles and that electron-builder runs without errors.
- If there are new errors, FIX THEM in the config or minimal supporting code instead of working around them.

8. **Report back in a short summary comment at the top of the config file**
- Add a short block comment at the top of the config file (or above the `"build"` key in `package.json`) summarizing:
- What you changed
- How `server` and `shared` are now packaged
- Any assumptions about runtime paths for the server
- Keep this comment 6–10 lines max.

ACCEPTANCE CRITERIA
- There is exactly one source of truth for `server` and `shared` packaging:
- They are in `"files"` and `"asarUnpack"`.
- They are NOT duplicated as `extraResources`.
- `asar: true` is explicitly set.
- `files` excludes obvious noise (maps, tests, logs).
- The Electron app builds successfully using the existing build script.
- The server and shared code can still be resolved correctly at runtime using `process.resourcesPath` and the `app.asar.unpacked` layout.
- The updated config is in-place and actively used by the build, with a brief summary comment explaining the design.

Now implement these steps directly in this Replit project and update the relevant config + any necessary main-process path code to match.