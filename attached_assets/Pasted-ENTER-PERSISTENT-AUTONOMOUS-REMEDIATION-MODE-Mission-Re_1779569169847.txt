ENTER PERSISTENT AUTONOMOUS REMEDIATION MODE.

Mission:
Reduce repository-wide @typescript-eslint/no-explicit-any count to EXACTLY 0.

Current baseline:
646

Source of truth:
node scripts/check-explicit-any-burndown.mjs

Inventory:
scripts/type-debt/explicit-any-inventory.json

You are NOT operating in single-task mode.
You are operating as a CONTINUOUS MAINTENANCE AGENT.

==================================================
CORE DIRECTIVE
==================================================

Continue executing lanes automatically WITHOUT stopping,
WITHOUT waiting for user confirmation,
and WITHOUT summarizing after each lane unless required.

After each successful lane:
IMMEDIATELY continue to the next lane.

Do not stop because:
- a lane completed
- a commit succeeded
- the baseline improved
- a merge occurred

Only stop for the explicit stop conditions below.

==================================================
STOP CONDITIONS
==================================================

You may stop ONLY if:

1. Global explicit-any count == 0
2. TypeScript cannot be restored after 3 repair attempts
3. A lane requires dangerous cross-cutting architectural changes
4. A shared/core runtime contract cannot be safely narrowed
5. Validation repeatedly fails
6. Human approval is required for protected areas

If stopping:
- explain the exact blocker
- identify affected files/directories
- explain why autonomous repair became unsafe
- propose the next manual action

==================================================
PROTECTED / HIGH-RISK AREAS
==================================================

DO NOT autonomously modify without extreme caution:
- auth/security
- websocket core
- realtime orchestration
- migration frameworks
- runtime loaders
- production infra bootstrap
- core shared generics
- cross-domain contracts used by many consumers

For these:
- use minimal diff strategy
- preserve public signatures whenever possible
- prefer wrapper narrowing over signature rewrites

==================================================
EXECUTION LOOP
==================================================

REPEAT UNTIL BASELINE = 0:

1. Read:
scripts/type-debt/explicit-any-inventory.json

2. Choose next lane using:
- highest reduction potential
- lowest blast radius
- clustered violations
- self-contained boundaries
- domain isolation

3. Remove ALL explicit-any violations in that lane.

4. Preferred strategies:
- unknown
- typed DTOs
- Record<string, unknown>
- discriminated unions
- Zod schema parsing
- narrowing helpers
- typed adapters
- generic constraints

NEVER:
- add new explicit-any
- use as any
- suppress lint rules
- disable TypeScript safety

5. Run validation:
- npx tsc --noEmit
- scoped eslint
- node scripts/check-explicit-any-burndown.mjs

6. If validation fails:
- repair automatically
- retry up to 3 times

7. If validation succeeds:
- commit lane
- continue immediately

==================================================
AUTONOMOUS CONTINUATION REQUIREMENT
==================================================

After every successful lane, explicitly perform:
“continue to next lane automatically”

Do NOT enter idle/reporting state.

==================================================
OUTPUT RULES
==================================================

Keep progress logs compact during execution.

Only provide a comprehensive summary:
- when baseline reaches 0
OR
- when a stop condition occurs

Final summary must include:
- initial baseline
- final baseline
- total reductions
- remaining blockers
- highest-risk unresolved areas
- architectural safety assessment
- recommendations for future autonomous maintenance