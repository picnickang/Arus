You are working on the ARUS/RecipeRealm codebase that runs as:

- A desktop Electron app (embedded “VESSEL” mode with local SQLite)
- A cloud/server deployment (Postgres/Turso/libSQL) accessed by browsers (desktop + mobile)

Goal: **Harden multi-device & multi-environment behavior** so the app can safely be used from multiple desktops and mobile devices (web) across multiple vessels and tenants, with clean separation between “vessel / embedded” and “cloud / server” modes.

I want you to:

1) DISCOVER WHAT’S IN PLACE

Search the repo and build a quick inventory (no edits yet):

- Find the **environment / deployment mode detection**:
- `Deployment Mode: VESSEL (Offline-First)` logs
- Any `deploymentMode`, `isEmbedded`, `isVesselMode`, `ENVIRONMENT_CONFIG` or similar.
- Where the local SQLite (`vessel-local.db`) is created and initialized.
- Where the cloud / libSQL / Turso client is created (look for `libsql`, `@libsql/client`, `drizzle-orm`, etc.).
- Find the **tenant + vessel isolation logic**:
- Middleware for `x-org-id` and possibly `x-vessel-id`.
- Any `TENANT_ISOLATION_SUCCESS` logs, `requireOrgId` or similar functions.
- Find the **dual-storage abstraction**:
- Repositories / services that can read/write both local SQLite and cloud DB.
- Any “dual write” or “dual read” logs (`[DualWrite:equipment]` etc.).
- Find **sync / background jobs**:
- The “Sync Manager” (Turso sync, libSQL client).
- Job queue / schedulers (update scheduler, telemetry pruning, connection pool health check).
- Look for functions like `checkConnectionPoolHealth`, `UpdateScheduler`, `Telemetry Pruning`, etc.
- Find **Electron-specific startup & API wiring**:
- Electron main process (e.g. `dist-electron/main.cjs` source config, or its TS source).
- Where Electron tells the renderer / server what mode it’s in and what backend URL to use.
- Find the **React / frontend bootstrapping**:
- Where the frontend sets base API URL(s).
- Where it injects or omits `x-org-id`, `x-vessel-id`, auth token, etc. on requests.

OUTPUT (for me as a summary comment in code or in the Replit console):
- A short, concrete map of:
- Which files control deployment mode & DB setup,
- Which files implement tenant & vessel isolation,
- Which files implement dual-storage & sync,
- Which parts run ONLY in cloud vs ONLY in vessel vs both.

2) HARDEN DEPLOYMENT MODES (VESSEL vs CLOUD)

I need a **clear, centralized config** that decides what can run in which mode.

Implement / enforce all of this:

- Create or refine a single module (if it doesn’t exist, create something like `server/config/runtimeEnv.ts`):
- Export: `isVesselMode`, `isCloudMode`, `deploymentMode`, and any `canUseCloudDb`, `canUseEmbeddedDb` flags.
- Determine this from environment variables (e.g. `DEPLOYMENT_MODE=VESSEL | CLOUD`, presence of `DATABASE_URL` vs local file, etc.).
- Anywhere we currently create a cloud/libSQL client or call `db.execute`:
- Guard those calls with checks from this config.
- In VESSEL mode with no cloud DB, **never** try to create a libSQL connection or run `db.execute`.
- Replace those with:
- A no-op stub (for things like connection pool health checks).
- Or a log that clearly says “cloud-only feature skipped in vessel mode”.
- Ensure **background jobs** that require the cloud DB ONLY run in cloud mode:
- Connection pool health checks (`checkConnectionPoolHealth` – this is currently throwing `TypeError: db.execute is not a function` in embedded mode).
- Update scheduler (`update_settings` table missing in local SQLite).
- Any libSQL / Turso sync jobs.
- For **embedded/vessel mode**, make sure:
- Only the local SQLite (`vessel-local.db`) is used for write-heavy operations.
- Cloud-only tables like `update_settings` are not required and not queried locally.

3) TENANT & VESSEL ISOLATION – MAKE IT CONSISTENT ACROSS DEVICES

We want multiple *vessels* and multiple *tenants* to use this safely, from multiple devices.

Do the following:

- In the **backend**:
- Ensure middleware (e.g. `requireOrgId`) is applied consistently across all routes.
- If not already present, add a **`requireVesselId`** middleware for vessel-bound resources (equipment, telemetry, alerts, schedules, etc.) and wire it in consistently.
- For each major route group (`/api/equipment`, `/api/equipment/health`, `/api/alerts/*`, `/api/maintenance-schedules*`, `/api/sync/*`):
- Confirm that queries to DB are always scoped by `orgId` and, where applicable, `vesselId`.
- In the **frontend (React)**:
- Implement a **single source of truth** for:
- `x-org-id`
- `x-vessel-id` (when in vessel context)
- auth token (if existing)
- That should be injected via:
- A `fetch` wrapper or
- Axios instance or
- React Query default `queryFn`.
- Ensure both the Electron renderer and web/mobile browsers use the same mechanism so all devices send the right headers.
- For **mobile and browser** usage:
- Use environment-aware config for the API base URL.
- For example:
- In Electron: base URL is `http://localhost:<embedded-port>` (server bundled with the app).
- In browser/mobile: base URL is the cloud deployment URL, e.g. `https://<arus-cloud-domain>/api`.
- Make sure there is no hard-coded `localhost:5000` in production browser builds.

4) DUAL STORAGE & SYNC – MAKE BEHAVIOR EXPLICIT

Treat this system as having **two layers**:

- Cloud layer (Postgres/libSQL, global truth across all devices).
- Vessel/local layer (SQLite, offline-first on each vessel device).

I want you to:

- Identify all repositories/services that:
- Read/write equipment, maintenance schedules, alerts, telemetry, etc.
- For each one, enforce this pattern:
- **Cloud-only data** (e.g. org-level settings, update scheduler, cross-vessel analytics) goes only to the cloud DB.
- **Vessel-bound data**:
- In VESSEL mode: write to local SQLite, and enqueue for sync if a mechanism exists (MQTT/reliable queue/libSQL sync).
- In CLOUD mode: operate directly on the cloud DB.
- If a “dual write” abstraction already exists (logs show `[DualWrite:equipment]`), refactor it to:
- Use stable primary keys (UUIDs) so multiple devices never create conflicting records for the same logical entity.
- Prefer idempotent “upsert”-style operations where possible.
- For **sync jobs**:
- Ensure they respect `deploymentMode`:
- On vessel: only push local changes upstream if/when cloud connectivity is present.
- On cloud: accept merges from vessels and mark conflicts with a clear strategy (e.g. last-write-wins + an audit entry).

Remove or guard anything that currently causes noise/errors in vessel mode, such as:

- LibSQL / Turso health checks when no cloud DB is configured.
- Update scheduler queries against tables that don’t exist in the local SQLite schema (`update_settings`).

5) MULTI-DEVICE BEHAVIOR: DESKTOP + MOBILE

We want a clear story for how **multiple devices** can use this safely:

- Vessel Mac mini running Electron + server in VESSEL mode.
- Laptops / tablets on board (via Electron or browser pointing at the vessel server or cloud).
- Shore / office devices and phones hitting the cloud deployment.

Please:

- Document (in a markdown file at the repo root, e.g. `MULTI_DEVICE_ARCHITECTURE.md`) and, where necessary, adjust code so that:
- **Electron desktop app**:
- Always runs server in VESSEL mode with local SQLite.
- Knows its `orgId` and `vesselId` via env/config or onboarding flow.
- **Web/mobile clients**:
- Use the cloud API.
- Always send `x-org-id` + auth, and optionally `x-vessel-id` when in a vessel-specific view.
- Add comments in code (at the environment configuration and DB initialization points) explicitly explaining:
- Which modules are safe in VESSEL mode.
- Which modules are cloud-only.
- How a new route should decide whether to hit local DB, cloud DB, or both.

6) SAFETY & REGRESSION CHECKS

After you refactor:

- Make sure the app still:
- Starts correctly in VESSEL mode via Electron on macOS (embedded server + local SQLite).
- Serves the React app without MIME type errors (CSS and JS loaded correctly).
- Add quick sanity checks:
- A small endpoint like `/api/health/deployment` that returns:
- `deploymentMode`
- `orgId` (if any)
- `vesselId` (if any)
- Whether `cloudDbAvailable` and `localDbAvailable` are true.
- Ensure no more `db.execute is not a function` errors appear in vessel logs when cloud DB is not configured.
- Ensure `MISSING_ORG_ID` 401s only occur when they should (e.g. in cloud routes that truly require tenant identity), and not for internal embedded-only traffic.

IMPORTANT:
- Do NOT break existing offline vessel behavior.
- Prefer small, well-scoped changes with clear environment checks.
- Where behavior is ambiguous, add TODO comments and log warnings instead of silently failing.

Once done, summarize the changes in a short comment at the top of the new `MULTI_DEVICE_ARCHITECTURE.md` and in any core config file you created/updated.