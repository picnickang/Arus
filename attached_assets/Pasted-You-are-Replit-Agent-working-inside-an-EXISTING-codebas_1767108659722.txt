You are Replit Agent working inside an EXISTING codebase. The current Schedule UI is a dead grid (vessels x dates) with “No assignments” and no interaction. It must be refactored to MATCH the reference planner UX shown by the user: a SmartPAL-style schedule planner with assignment blocks, selection, a right-side drawer/bottom sheet, constraint badges, suggestions, and commit actions.

ABSOLUTE RULES
- Do NOT start a new project.
- Reuse existing stack, DB language(s), hybrid offline/online sync system, and existing email notification pipeline.
- No breaking migrations. Additive only if required.
- Mobile-first interactions required; desktop drag/resize can be optional phase 2.
- Do not invent new design system: match existing UI styling but implement the same layout and affordances as the reference.

PHASE 0 — COMPARE AGAINST REFERENCE (MANDATORY)
1) Open current Schedule page files and identify:
- where vessels are loaded
- where assignments should load (but aren’t)
- why blocks aren’t rendering (missing query? wrong date filtering? mapping bug?)
2) Create a “Reference Gap Checklist” with the following items and mark current status:
- Assignment blocks spanning dates
- Empty-cell create flow
- Block selection flow
- Drawer/Sheet with tabs (Details/Constraints/Suggestions)
- Inline constraint badges (soft/hard)
- Apply Changes commit
- Sync status indicator tied to pending ops
Stop only after this checklist is output, then implement.

IMPLEMENTATION REQUIREMENTS (MUST MATCH REFERENCE)

A) LAYOUT MUST MATCH REFERENCE (within your theme)
- Left column: vessel list (sticky)
- Center: timeline grid with day headers
- Right panel on desktop OR bottom sheet on mobile:
  - Assignment Drawer with tabs: Details | Constraints | AI Suggestions
- Top: date range + quick range buttons (2w/1m/3m) + status pill (Up to date / Syncing / Offline)

B) ASSIGNMENT BLOCKS (MOST IMPORTANT)
- Render assignments as colored blocks spanning from startDate to endDate across the grid.
- Each block shows:
  - crew name
  - role/rank
  - warning icon count (if any)
- Blocks must be clickable/tappable.
- If there are no assignments, show an empty-state message only at the row level (not “No assignments” across the entire grid).

C) INTERACTIONS (MOBILE FIRST)
1) Tap EMPTY cell -> open CreateAssignmentSheet
   Prefill: vesselId + startDate; choose crew + role; choose endDate.
2) Tap BLOCK -> open AssignmentDrawer/Sheet
   Tabs:
   - Details: crew/role/vessel/dates/status
   - Constraints: list results (HARD red / SOFT amber)
   - AI Suggestions: top 5 candidates with tags + “why”
3) Footer actions:
   - Cancel
   - Apply Changes (commits locally + queues sync)

D) WRITE PATH + OFFLINE BEHAVIOR
- On Apply Changes:
  - call existing SchedulingService/Repository mutation (create/update/delete)
  - update local DB immediately (optimistic)
  - enqueue sync operation (existing queue)
  - update status pill:
    - Offline if no network
    - Syncing if pending ops > 0
    - Up to date if pending ops == 0
- DO NOT send emails on Apply Changes (unless your app already defines auto-email on publish).
- If a publish action exists, it must validate no HARD constraints and then send emails via existing pipeline.

E) CONSTRAINTS + BADGES
- Every create/edit should run constraint check:
  - show inline badge on blocks
  - show full list in Constraints tab
- HARD violations should prevent publishing; SOFT allow but warn.

F) AI SUGGESTIONS (ASSISTIVE)
- Use deterministic filter+scoring for suggestions.
- If there is an existing AI system in repo, reuse it only for “explanations”, not core validation.
- Suggestions must be shown in UI with action “Apply”.

TESTING (MUST RUN)
- Add tests to confirm:
  1) assignments render as spanning blocks in the timeline
  2) tapping empty cell opens create sheet with correct vessel/date
  3) save creates local assignment + pending op
  4) tapping a block opens drawer and shows details
  5) constraints render and badge appears when warnings exist
- Run test suite and report commands + pass status.

DELIVERABLE
- The schedule planner must visually and functionally resemble the reference:
  - blocks, selection drawer, constraints, suggestions, apply changes, sync status.
- Provide a short summary of changed files and what each change did.