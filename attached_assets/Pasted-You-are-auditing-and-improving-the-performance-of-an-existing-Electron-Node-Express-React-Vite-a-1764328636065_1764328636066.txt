You are auditing and improving the performance of an existing Electron + Node/Express + React/Vite app for ARUS vessel telemetry and predictive maintenance.



High-level architecture (do NOT change this, only optimize within it):

Desktop app using Electron (Chromium renderer + Node main process).

Embedded Node/Express server bundled from server/index.ts → server/index.js.

Frontend built with React + Vite, output copied into dist/ and served by the embedded server.

SQLite local DB in WAL mode for vessel/embedded deployments (data/vessel-local.db).

Optional libSQL/Turso/Postgres cloud sync layer (dual write/read paths).

MQTT Reliable Sync and a VesselTelemetry simulator/service already exist.

Live data is exposed over HTTP + WebSocket and visualised with various chart components in the frontend.



The goal is to perform a telemetry performance audit and hardening pass so the app can safely handle live data streams without UI lag or crashes.

0. Safety & Non-Overlap Check (MANDATORY FIRST STEP)

Scan the codebase for:

Telemetry ingestion (MQTT / simulator / any “VesselTelemetry” or “Digital Twin” services).

APIs for telemetry & equipment health (REST + websocket).

Frontend telemetry visualisation (charts, dashboards, live health/alerts views).

For each improvement you propose, FIRST answer in comments:

Does a similar mechanism already exist (throttling, batching, downsampling, backpressure)?

If yes, reuse or extend it instead of adding a new one.

If you must introduce new code, explain why the existing mechanism is insufficient.

Do not introduce a second, conflicting or parallel system if there is already:

An existing rate limiter,

An existing telemetry buffering / aggregation path,

An existing WebSocket data adapter,

An existing telemetry simulator that can be reused for performance tests.



Keep behaviour the same for normal users unless explicitly improved, and avoid breaking any existing APIs.

1. Backend Telemetry Pipeline Audit (Node/Express)



Tasks:

Locate:

Telemetry ingestion entry points (MQTT handler or simulator).

Any middleware / services that process telemetry (e.g. digital twin, ML, metrics).

Routes used by the frontend for live data:

e.g. /api/equipment/health, /api/alerts/*, /api/telemetry/*, websocket channels.

For each telemetry path, answer (in code comments and/or a short MD file):

How often data can arrive (assume high frequency: >10 Hz).

Whether each sample is processed synchronously on the main event loop.

Whether each sample is written directly to the DB or batched.

Whether any ML / heavy calculations run inline per message.

Implement safe performance improvements only if compatible with the current architecture:

Add batching/buffering for DB writes if they are currently per-sample.

Move any heavy per-sample processing (ML, complex transforms) into:

Batches, OR

Worker threads / child processes (if already supported), OR

A lightweight, approximate online metric for live display while heavy analysis is deferred.

Ensure existing rate limiting and tenant isolation (x-org-id headers, TENANT_ISOLATION_SUCCESS) remain intact.

Add minimal performance instrumentation in the backend, such as:

Logging average response time for key telemetry APIs.

Logging simple counters for incoming telemetry messages per second (per vessel/org).

Optionally, event loop delay checks if a helper exists.



Make all backend changes incremental, with clear comments describing the performance intent.

2. Database Load & Access Patterns (SQLite + libSQL/Turso/Postgres)



Tasks:

Identify all schemas and queries related to:

Telemetry storage

Equipment health and alerts

Any historical metrics views or dashboards

For DB calls in telemetry and live dashboards:

Confirm whether they use SQLite locally, libSQL/Turso, Postgres, or a dual-write/dual-read abstraction.

Ensure we are not calling a cloud DB synchronously on hot telemetry paths in vessel/offline mode.

Implement improvements only if compatible with the defined architecture:

Add or confirm indexes on fields used to filter or sort live data (e.g. timestamp, vesselId, equipmentId).

Avoid long-running queries on frequently updated tables in high-frequency endpoints.

If appropriate, introduce read models / materialized views / summary tables for dashboards rather than hitting raw telemetry tables directly.

Fix any obvious issues surfaced by the logs or code:

Adapter mismatches (e.g. using db.execute on an object that doesn’t implement it).

Missing tables like update_settings, but only if clearly part of the existing migration design.



Do not redesign database technology choices. Optimize schemas and queries for performance and stability.

3. Frontend Performance (React + Vite + Charts)



Tasks:

Identify components and hooks that:

Subscribe to live telemetry (websocket or polling).

Render charts, health dashboards, or alert grids.

Maintain in-memory histories of telemetry data.

For each major telemetry view:

Check if every message triggers a React state update.

Check if charts are re-rendering on every single data point.

Determine the maximum number of data points being stored per chart.

Implement safe UI performance improvements that do NOT change business logic:

Downsampling / windowing:

Keep a fixed-size sliding window (e.g. last N points or last T minutes).

Avoid storing unbounded telemetry history in component state.

Throttled rendering:

Use a throttling / debouncing mechanism so charts update at a fixed cadence

(e.g. every 250–500 ms) instead of per message.

Lightweight data structures:

Avoid unnecessary deep copies; use efficient updates for time series.

Error boundaries:

Ensure a rendering error in one telemetry panel cannot blank the entire app.

If any charting library usage is obviously inefficient (e.g. full re-instantiation on each render), refactor it to:

Reuse chart instances where possible.

Minimize prop changes to only what actually changed.



Always ensure the displayed metrics remain correct; we are reducing frequency and volume of updates, not lying about the data.

4. Load Simulation & Regression Guards



Tasks:

If a telemetry simulator already exists (it seems to in the logs), reuse it:

Create a high-load test mode that:

Streams telemetry at a configurable rate.

Drives the main dashboards used by operators.

Add a simple, non-flaky test or script to:

Start the server in a local/test mode.

Start the telemetry simulator at a reasonable stress level.

Hit key dashboards or APIs to ensure:

No unhandled exceptions in server logs.

No React runtime errors in the console (at least for basic flows).

Do not introduce a complex test framework; stick to pragmatic, code-local checks and comments that guide future developers.

5. Documentation & Developer Notes



Add or update a short Markdown file (e.g. docs/performance-telemetry.md) summarising:

Where telemetry enters the system.

Where it’s stored.

How it’s streamed to the frontend.

What throttling, batching, and downsampling mechanisms are now in place.

Any limits / expectations (e.g. “designed for X messages/sec per vessel on a typical laptop”).



This document should help another developer quickly understand how to push, debug, and optimize telemetry performance without breaking the architecture.

Constraints & Style

Do not change the fundamental app architecture (Electron + Node/Express + React + SQLite/libSQL).

Prefer small, well-commented improvements over massive rewrites.

Before introducing any new abstraction, check for an existing one and reuse/extend it.

Keep behaviour backwards-compatible for existing features as much as possible.



When you’re done, summarise:

What you found (bottlenecks, risks).

What you changed (with file paths).

How to run the simulator / tests to validate performance locally.

Use this prompt as your complete spec.