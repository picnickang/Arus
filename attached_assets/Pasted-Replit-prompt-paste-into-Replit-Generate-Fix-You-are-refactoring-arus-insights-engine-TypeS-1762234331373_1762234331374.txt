Replit prompt (paste into Replit → Generate/Fix):



You are refactoring arus-insights-engine (TypeScript/Node). Apply ALL changes below to the file just provided (and any tiny helper edits if needed). Do not add new external deps. Preserve public exports and function signatures.



Objectives

Multi-tenant correctness: pass orgId to all storage reads; fix parameter order to (orgId, ...) where applicable.

Stable vessel grouping: group by vesselId, display by vesselName (avoid name-as-key).

Safer time handling: never mutate shared arrays; ensure ISO strings are from valid Dates.

Telemetry triggers: handle missing thresholds gracefully; avoid NaN; clearer deviation text.

Prediction fields: tolerate daysUntilFailure vs remainingDays.

Priority sorting: robust comparator with fallback bucket.

KPI counts: dedupe signalsMapped; remove overcounts from comma strings.

Structured logging: concise JSON logs for key operations.

Remove unused imports/vars: keep bundle clean.

Edits (make these exact changes)



1) Clean imports

Remove unused: mean, standardDeviation, getSystemTypeInfo, formatFailureProbability.

Keep formatConfidence.



2) Scope storage reads by 

orgId

 and fix parameter order



In computeInsights(...), replace the initial fetch block with:

const [devices, equipment, alerts, telemetryReadings, sensorMappings, allVessels] = await Promise.all([
  storage.getDevices(orgId),
  storage.getEquipmentRegistry(orgId),
  storage.getAlertNotifications(orgId, { since: since7d }),
  storage.getLatestTelemetryReadings(undefined, orgId, { limit: 1000 }),
  (storage.getSensorMappings?.(orgId)) || [],
  storage.getVessels(orgId),
]);
Remove the later filter(v => v.orgId === orgId) since reads are now scoped.



3) Stable vessel grouping by 

ID

 (no name-as-key)



Replace the “Group telemetry by vessel/equipment” and “Analyze each vessel” logic with:

const perVessel: FleetKPI["perVessel"] = {};
const latestGapVessels: string[] = [];

const eqIndex = new Map(equipment.map(e => [e.id, { vesselId: e.vesselId ?? 'unassigned', vesselName: e.vesselName ?? 'Unassigned' }]));

type VesselBucket = { name: string; points: typeof telemetryReadings };
const vesselTelemetry = new Map<string, VesselBucket>();

for (const t of telemetryReadings) {
  const meta = eqIndex.get(t.equipmentId) ?? { vesselId: 'unassigned', vesselName: 'Unassigned' };
  if (!vesselTelemetry.has(meta.vesselId)) vesselTelemetry.set(meta.vesselId, { name: meta.vesselName, points: [] as any });
  vesselTelemetry.get(meta.vesselId)!.points.push(t);
}

vesselTelemetry.forEach(({ name, points }, vId) => {
  if (!vId || vId === 'unassigned') return;

  // find latest without mutating
  const latestReading = points.reduce((a, b) => (new Date(a.ts) > new Date(b.ts) ? a : b), points[0]);
  const lastTsISO = latestReading ? new Date(latestReading.ts).toISOString() : null;
  const stale = lastTsISO ? new Date(lastTsISO) < staleCutoff : true;

  if (stale) latestGapVessels.push(name);

  const vesselAlerts = recentAlerts.filter(a => {
    const alertEq = equipment.find(e => e.id === a.equipmentId);
    return alertEq?.vesselId === vId;
  });

  const vesselEquipment = equipment.filter(e => e.vesselId === vId);
  const totalSignals = vesselEquipment.reduce((sum, eq) => {
    const eqReadings = points.filter(r => r.equipmentId === eq.id);
    return sum + new Set(eqReadings.map(r => r.sensorType)).size;
  }, 0);

  perVessel[name] = { lastTs: lastTsISO, dq7d: vesselAlerts.length, totalSignals, stale };
});
4) KPI: safer 

signalsMapped

 counting



Replace your signalsMapped calc with:

const signalsMapped = sensorMappings.length
  ? sensorMappings.length
  : devices.reduce((sum, d) => {
      const set = new Set((d.sensors ?? '')
        .split(',')
        .map(s => s.trim())
        .filter(Boolean));
      return sum + set.size;
    }, 0);
5) Anomalies: Date safety



Replace anomalies mapping with:

const anomalies = criticalAlerts.slice(0, 20).map(alert => {
  const created = new Date(alert.createdAt);
  const ack = alert.acknowledgedAt ? new Date(alert.acknowledgedAt) : now;
  const alertEquipment = equipment.find(e => e.id === alert.equipmentId);
  return {
    vesselId: alertEquipment?.vesselId || 'unassigned',
    src: alert.equipmentId,
    sig: alert.sensorType,
    kind: 'threshold_breach',
    severity: alert.alertType,
    tStart: isNaN(created.getTime()) ? now.toISOString() : created.toISOString(),
    tEnd: isNaN(ack.getTime()) ? now.toISOString() : ack.toISOString()
  };
});
6) Recommendations & compliance: keep as-is (already fine).



7) Structured success log for compute



At end of computeInsights (just before return), add:

console.log(JSON.stringify({
  msg: 'insights_compute_done',
  orgId,
  vessels,
  dq7d,
  staleCount: latestGapVessels.length,
  signalsMapped,
  signalsDiscovered,
  t_ms: Date.now() - now.getTime()
}));
8) 

persistSnapshot

 error log is good; keep.



9) 

getLatestSnapshot

 unchanged.



10) 

generateTechnicianInsight

: fix storage order, telemetry fetch, triggers, prediction fields

Equipment fetch (orgId first):

const equipment = await storage.getEquipment(orgId, equipmentId);
if (!equipment || equipment.orgId !== orgId) return null;
Telemetry fetch: adapt to your signature (scoped + limit):

const telemetry = await storage.getLatestTelemetryReadings(equipmentId, orgId, { limit: 100 });
Prediction service call stays the same if your facade is (equipmentId, orgId). If your actual call is (storage, equipmentId, orgId) keep it; otherwise don’t change.

Triggers: safer threshold & deviation wording:

const triggers: Array<{ sensorType: string; value: number; threshold: number; deviation: string; }> = [];

for (const reading of telemetry) {
  const val = Number(reading.value);
  if (!Number.isFinite(val)) continue;
  if (reading.status === 'warning' || reading.status === 'critical') {
    const thr = Number.isFinite(reading.threshold) ? Number(reading.threshold) : null;
    const deviation = (thr != null && thr !== 0)
      ? `${Math.abs(((val - thr) / thr) * 100).toFixed(0)}% ${val > thr ? 'above' : 'below'} normal`
      : 'deviation detected';
    triggers.push({ sensorType: reading.sensorType, value: val, threshold: thr ?? 0, deviation });
  }
}
Prediction days field harmonization:

const daysUntilFailure =
  (prediction as any)?.daysUntilFailure ??
  (prediction as any)?.remainingDays ??
  null;
Use harmonized value in mlPrediction:

mlPrediction: {
  failureProbability: prediction?.failureProbability || 0,
  confidence: prediction?.confidence || 0,
  daysUntilFailure,
  triggers,
},
Structured success log at end (before return):

console.log(JSON.stringify({
  msg: 'technician_insight_done',
  orgId,
  equipmentId,
  vesselId: equipment.vesselId ?? null,
  statusLevel,
  failureProb: prediction?.failureProbability ?? 0,
  confidence: prediction?.confidence ?? 0
}));
11) 

generateFleetTechnicianInsights

: robust priority sort



Replace the sort block with:

const priorityOrder = { immediate: 0, urgent: 1, scheduled: 2, monitor: 3, routine: 4 } as const;

insights: insights.sort((a, b) => {
  const pa = priorityOrder[a.priority as keyof typeof priorityOrder] ?? 99;
  const pb = priorityOrder[b.priority as keyof typeof priorityOrder] ?? 99;
  return pa - pb;
}),
Add a structured log at the end (before return):

console.log(JSON.stringify({
  msg: 'fleet_insights_done',
  orgId,
  vesselFilter: vesselId ?? null,
  groups: result.length,
  t_ms: Date.now() - startTime
}));
12) Remove unused 

since48h

 and any other now-unused variables.

Acceptance criteria

All storage reads in this file are scoped by orgId; parameter order fixed to (orgId, ...) where your storage expects it.

Vessel grouping uses vesselId internally; perVessel keys remain human-readable (vesselName).

Anomalies and triggers produce valid ISO timestamps and no NaN deviations.

daysUntilFailure tolerantly reads from remainingDays if necessary.

Priority sorting never throws; unknown priorities sink to bottom.

signalsMapped is deduped; no inflated counts from commas/empties.

New structured logs appear for compute, technician insight, and fleet insights.



When done, show a short summary of the files changed and the key diffs applied.