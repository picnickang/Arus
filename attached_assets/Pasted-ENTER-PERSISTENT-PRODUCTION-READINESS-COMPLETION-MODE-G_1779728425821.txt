ENTER PERSISTENT PRODUCTION-READINESS COMPLETION MODE.

Goal:
Continue autonomously until all production-readiness tasks are either:
1. fully implemented, validated, reviewed, and committed
OR
2. explicitly blocked by a required product/security decision that cannot be safely assumed.

Do NOT stop after one task, one wave, one merge, or one report.

Current completed state:
- TypeScript strict hardening complete
- explicit-any = 0
- unsafe casts eliminated
- default tsc clean
- major P0/P1/P2 autonomous-safe hardening waves completed

Remaining decision-based items:
#2 setup-token remote policy
#3 WebSocket tenant strict-mode default
#14 production encryption-key fail-fast
#16 FK cascade strategy
#17 global vs tenant-scoped tables
#18 natural-key unique indexes
#25 API response validation strictness
#31 production TODO/stub decisions

Execution rules:
- For each decision item, choose the safest production default for ARUS.
- If the safe production default is obvious, implement it.
- If the choice would change product semantics or risk data loss, pause only that item and continue with the next safe item.
- Do not stop the whole run unless every remaining item is either done or explicitly blocked.
- Continue automatically across all safe tasks.

Recommended production defaults:
#2 Setup token:
Require localhost AND valid token. Use timingSafeEqual. Audit every attempt. Rotate/disable after first successful setup if supported.

#3 WebSocket strict tenant mode:
Enable strict tenant mode by default in production. Keep development override.

#14 Encryption keys:
Fail fast in production if ENCRYPTION_KEY or SESSION_SECRET is missing. Keep dev fallback only outside production.

#16 FK cascades:
Only add cascade for clear exclusive child-owned rows. Use restrict/no-action for cross-aggregate or audit-sensitive relationships.

#17 Tenant scoping:
Treat vessel_3d_models and tenant settings as tenant-scoped. Treat industry_benchmarks/software_patches as global unless code proves tenant-specific use.

#18 Unique natural keys:
Add unique indexes for clear tenant-scoped natural keys:
- parts(org_id, part_number) where part_number is not null
- crew(org_id, passport_number) where passport_number is not null
- maintenance_templates(org_id, name, equipment_type)
Use partial indexes where nullable.

#25 API response validation:
Keep tolerant in production but emit metrics/log warnings. Make CI/tests assert zero schema violations where feasible.

#31 Production TODO/stubs:
Classify each TODO/stub as:
- safe degraded mode with explicit warning/metric
- GA blocker needing implementation
- backlog non-critical
Implement warnings/metrics for degraded modes. Do not fake missing integrations.

Validation after every task:
- npx tsc --noEmit
- strict checks
- scoped eslint
- relevant tests
- migration validation if migrations are added
- explicit-any remains 0
- cast count remains at documented baseline
- architect/code review

Hard rules:
- No explicit any
- No as any
- No ts-ignore
- No eslint-disable
- No unsafe casts
- No destructive migrations without reversible migration path
- Preserve tenant isolation
- Preserve strict-mode cleanliness

Continuation directive:
After each completed task:
- commit/merge if validation passes
- immediately continue to the next task
- do not enter idle/reporting state

Stop only when:
1. all items are complete
OR
2. every remaining item is explicitly blocked by a real product/security/data decision.

Final report only when stopped:
- completed items
- blocked items and exact decisions needed
- migrations added
- tests added
- tsc/lint/test status
- remaining production risks
- recommended launch readiness assessment