You are refactoring an Electron main process (TypeScript) and its Vite build config for a macOS desktop app called ARUS Marine. The current main file spawns a Node server on a fixed port (5000) and loads http://localhost:5000. It works in dev but is brittle in production. The Vite config currently emits ESM and doesn’t force a single-file bundle.



Goals (apply to both files)

Production-safe embedded server (don’t rely on a system node on PATH).

Dynamic port with health checks (no hardcoded 5000 collisions).

No orphan processes; clean shutdown of the server process tree.

Single-instance app; second launch focuses the first.

Resilient window loading (retry on did-fail-load) and user feedback if the server dies.

Secure renderer defaults kept: nodeIntegration: false, contextIsolation: true, sandbox: true.

Main bundle should be single-file CommonJS targeting Node (Electron’s Node), with correct externals.

What to change in the Electron main process (TypeScript)



Implement all of the following:

Server spawn (dev vs prod):

Dev: spawn npx tsx <repo-root>/server/index.ts.

Prod: spawn Electron itself as Node: cmd = process.execPath with ELECTRON_RUN_AS_NODE=1 and run the unpacked process.resourcesPath/server/index.js.

Set env:

EMBEDDED_MODE=true, LOCAL_MODE=true, ENABLE_BACKGROUND_JOBS=false, ENABLE_SCHEDULERS=false, PORT=<dynamicPort>, and appropriate NODE_ENV.

Log the resolved server entry and command.

Dynamic Port + Health Check:

Use get-port to pick from [5000, 5001, 0].

Health poll GET /api/dashboard with header x-org-id: default-org-id every 500–1000 ms up to 30 attempts. Proceed only on HTTP 200. Build SERVER_URL from the chosen port.

Lifecycle & Process Tree Kill:

requestSingleInstanceLock(); on second instance, focus the first window.

On before-quit, window-all-closed, SIGINT, SIGTERM, kill the entire server process tree:

POSIX: send signal to -pid (group), fallback to tree-kill.

Windows: standard .kill('SIGTERM') and/or tree-kill.

If the server exits unexpectedly, show a dialog telling the user to relaunch.

Window robustness:

Add a did-fail-load listener to retry loadURL(SERVER_URL) once after ~1s.

Keep nodeIntegration: false, contextIsolation: true, sandbox: true, add safeDialogs: true. Only add a preload if needed.

Logging & errors:

Stream stdout/stderr to console with tags.

On startup failure, show an error dialog and quit.



Deliverable (1): Output a single, final TypeScript file (the main process) that fully implements the above. Use imports for get-port and tree-kill (or a minimal inline fallback if deps are disallowed). Name stays the same (e.g., electron/main.ts) unless stated otherwise.

What to change in the Vite config for the main process



Refactor the Vite config so the main build is Node-targeted, single-file, and CommonJS:

formats: ['cjs'], output filename main.cjs.

inlineDynamicImports: true to force a single-file bundle.

target: 'node20' (adjust if your Electron ships a different Node).

esbuild.platform = 'node', ssr.target = 'node'.

Keep only Electron and Node built-ins external (e.g., electron, child_process, fs, path, http, os). Bundle 3rd-party deps like get-port / tree-kill.

Use sourcemap: 'inline' in dev; false in prod.

emptyOutDir: false if other builds share the folder.

Keep your aliases for @ and @shared.



Deliverable (2): Output the final Vite config file (e.g., vite.config.electron.ts or your existing vite.config.ts if it is dedicated to main). If you share one Vite config across renderer/preload, clearly scope the build to the main process build (you can keep it dedicated for main).

Packaging (electron-builder) requirements



Ensure the server runtime exists at runtime in production:

Unpack and ship the server directory with the app:

{
  "asarUnpack": ["server/**"],
  "extraResources": [{ "from": "server", "to": "server" }]
}


Ensure server/index.ts is transpiled to server/index.js for production packaging (your main will launch server/index.js from process.resourcesPath/server/index.js).



Deliverable (3): Output a short electron-builder JSON snippet including the above. If you use electron-forge, provide the equivalent config shape.

NPM additions



Provide exact install commands:

With npm:

npm i get-port tree-kill
npm i -D tsx


With pnpm:

pnpm add get-port tree-kill
pnpm add -D tsx




(If you choose to avoid tree-kill, include a short internal helper using process.kill(-pid) on POSIX with a Windows fallback.)



Deliverable (4): List the install commands exactly as above.

Acceptance criteria (must pass)

Dev: launches via npx tsx server/index.ts, picks a free port, health check passes, window loads; did-fail-load retry in place.

Prod: launches via process.execPath + ELECTRON_RUN_AS_NODE=1, executes process.resourcesPath/server/index.js (unpacked), no dependency on system node.

Shutdown: quitting the app kills the entire server process tree (no lingering children).

Single instance: second launch brings the first to foreground.

Security: nodeIntegration: false, contextIsolation: true, sandbox: true remain.

Build: main bundle is a single CJS file targeting Node; only Node/Electron are externalized; third-party deps are bundled.

Now:

Analyze my current electron/main.ts and produce the updated TypeScript file.

Analyze my current Vite config and produce the updated config.

Provide the electron-builder snippet.

Provide the exact install commands.



Return only these deliverables, clearly labeled.