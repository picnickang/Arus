Fix follow-up #194 — BottomNav override leak.

Context:
UI Align Phase 4 completed successfully. User portal now renders the simplified preview-aligned dashboard. Architect filed a pre-existing BottomNav override leak as follow-up #194.

Goal:
Prevent stale/localStorage navigation data from leaking admin modules into the user portal.

Scope:
Frontend only.
No backend changes.
No UI redesign creep.

Requirements:
- Inspect BottomNav.tsx, PortalLogin, SwitchPortalButton, and navigation storage usage.
- Centralize duplicated storage keys:
  - arus-user-role
  - arus-bottom-nav-items
  into a typed shared frontend constant/module.
- Treat localStorage nav items as cache only, never authority.
- The application navigation policy must remain the source of truth.
- If cached nav items conflict with the current role/portal policy, discard/rebuild from policy.
- User portal roles must only see:
  - Dashboard
  - Feedback / Flags
- Admin roles must still see:
  - Maintenance
  - System Admin
  - Crew Management
  - Logistics
  - AI Analytics
- SwitchPortalButton must clear all relevant portal/nav storage.
- PortalLogin must write storage only through the shared helper if storage is still needed.

Add regression tests:
- stale admin nav cache does not leak into user portal
- user role sees only Dashboard + Feedback / Flags
- admin role sees the five admin categories
- Switch Portal clears role/nav storage keys
- policy remains the source of truth

Architecture rules:
- keep role/nav policy in application/navigation
- storage adapter/helper may live under infrastructure or application as appropriate
- React components should not implement role-policy logic
- no duplicated role/category logic in components

Validation:
- npx tsc --noEmit
- strict checks
- scoped eslint
- relevant unit tests
- Playwright spec discovery if affected
- explicit-any remains 0
- cast count does not increase
- architect review

Rules:
- no explicit any
- no as any
- no ts-ignore
- no eslint-disable
- no unsafe casts
- no backend changes
- no removed functionality

Final report:
- leak cause
- files changed
- storage helper/module added
- tests added
- validation status