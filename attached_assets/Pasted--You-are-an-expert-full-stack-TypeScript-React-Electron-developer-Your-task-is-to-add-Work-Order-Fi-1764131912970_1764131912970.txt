
You are an expert full-stack TypeScript/React/Electron developer.
Your task is to add Work-Order Filtering by Vessel and by Engineer into this ARUS app.

Before touching code, you must FIRST scan the entire repo and confirm:
Whether Work Orders already have vessel_id and engineer_id fields
Whether filtering endpoints or UI components already exist
Whether any filtering logic exists in GET /api/work-orders or related routes
Whether a Work Order search or sorting component already exists in the React frontend

Do NOTHING until this compatibility check is completed and you produce a short summary of the current state.

After confirming compatibility, implement the following—ONLY IF missing:
✅ 

BACKEND REQUIREMENTS



1. Update Work Order API



Add optional query parameters:

GET /api/work-orders?vesselId=<id>&engineerId=<id>
Rules:

If vesselId is provided → filter by vessel

If engineerId is provided → filter by assigned engineer

If both provided → filter by both

If none provided → return all work orders



Implement server-side filtering using the existing DB/ORM layer (SQLite + Postgres/Turso hybrid).

Ensure compatibility with org-based tenant isolation (x-org-id).



2. Validate DB Schema



Check if the table has:

vessel_id

assigned_engineer_id



If missing → propose minimally invasive schema migration but DO NOT apply without confirmation.

✅ 

FRONTEND REQUIREMENTS



1. Add Filter Controls to Work Orders Page



Inside the Work Orders list view (React):

Add a “Vessel” dropdown populated from /api/vessels

Add an “Engineer” dropdown populated from /api/crew or /api/users

Selecting either filter automatically updates the list



2. Update Work Order Fetch Logic



Modify the React hook or data-fetching call:

/api/work-orders?vesselId=xxx&engineerId=yyy
Ensure:

State is preserved when switching pages

Filters combine correctly

Filters persist when returning to the page

✅ 

UI / UX REQUIREMENTS

Simple two dropdowns at the top of the Work Orders list

Show active filters as removable pills:

“Filtered by: Tagoban – Vessel 3”

“Engineer: Samir”

Add a “Clear All Filters” button

Ensure table updates instantly

✅ 

REPLIT EXECUTION RULES

Do not modify code until you finish the compatibility scan.

Output the scan results as a Markdown summary.

Then implement backend → frontend → UI in clean incremental commits.

Ensure no duplication, no breaking of existing components, and UI stays aligned with the current design system.

🔥 

BEGIN NOW with the compatibility scan



(Start by analyzing the repo. Do NOT edit anything yet.)

