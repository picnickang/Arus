You are working inside the main ARUS Replit full-stack React/TypeScript maritime operations app.

Goal:
Build comprehensive CRUD and workflow tests for the recent Crew Management, Role Dashboard, Former Crew Access Risk, Offboarding, Safety Alert Log, and Permission changes.

The app was recently updated to support:
- role-specific dashboards
- super admin role/dashboard configuration
- role-grouped user/crew roster
- Active Roster / Former Crew split
- active crew access readiness
- former crew access-risk review
- onboarding checklist
- safer offboarding and reinstatement
- offboarding revocation result summary
- vessel readiness panel
- mobile crew cards
- Safety tab with Current Alarms and Alert Log
- safety alarm resolution notes
- split safety permissions
- permission registry updates
- UserAccessEditor partial-save handling
- improved visual semantics

Your task:
Create a reliable CRUD/workflow test suite that proves these changes work end-to-end.

Do not only test happy paths. Include negative permission tests, edge cases, and data consistency tests.

Important constraints:
1. Do not use production data.
2. Use the project’s existing test framework if one exists.
3. If the project already uses Vitest/Jest/Supertest/Playwright, extend that setup.
4. If no test framework exists, add the smallest reasonable test setup.
5. Avoid large new dependencies unless clearly necessary.
6. Keep TypeScript strictness clean.
7. Use isolated seeded test data.
8. Clean up test data after each test.
9. Do not rely on dev auth bypass unless the test harness explicitly controls it.
10. Backend permission checks must be tested directly through API calls, not only through UI behavior.

Test coverage required:

────────────────────────────────
A. Test data setup
────────────────────────────────

Create a reusable test seed/helper layer for:

1. Organization / tenant
2. Vessels:
   - Vessel A
   - Vessel B
3. Users:
   - super_admin
   - system_admin
   - company_admin
   - fleet_manager
   - captain
   - chief_engineer
   - logistics_user
   - procurement_user
   - maintenance_planner
   - viewer
   - crew_user
   - view_only_user
4. Roles and permissions:
   - crew_members:view
   - crew_members:create
   - crew_members:edit
   - crew_members:duty
   - crew_members:lifecycle
   - crew_members:delete
   - crew_access:manage
   - safety_alarms:view
   - safety_alarms:trigger
   - safety_alarms:clear
   - safety_alarms:acknowledge
   - safety_alarms:export
   - safety_alarm_types:view
   - safety_alarm_types:manage
5. Crew members:
   - active crew with no login
   - active crew with login enabled
   - active crew with login disabled
   - active crew with temporary password issued
   - active crew with no vessel scope
   - active crew with fleet-wide scope
   - former crew with disabled login
   - former crew with enabled login
   - former crew with vessel access remaining
   - former crew with hub/admin access remaining
6. Safety alarm types:
   - real emergency type
   - drill/test type
7. Safety alarms:
   - active real alarm
   - cleared real alarm with resolution note
   - drill alarm
   - test alarm

Acceptance:
- Seed helpers can create deterministic test records.
- Tests can run repeatedly without duplicate-key failures.
- Tests are tenant-scoped.
- Tests clean up after themselves.

────────────────────────────────
B. Crew member CRUD tests
────────────────────────────────

Test backend API and, where practical, UI behavior.

Required tests:

1. CREATE active crew member
- create crew with name, rank, vessel, department, emergency contact
- verify crew appears in Active Roster
- verify count increases
- verify export includes the new crew member

2. READ crew member
- fetch roster
- fetch individual crew profile
- verify profile fields match created data
- verify tenant isolation

3. UPDATE crew member
- update rank
- update vessel
- update department
- update emergency contact
- verify changes persist
- verify roster and profile agree

4. DUTY update
- set crew on duty
- verify duty state updates
- verify On Duty badge is not destructive/red
- set crew off duty
- verify state updates

5. DELETE / archive behavior
- verify hard delete requires proper permission
- verify view-only user cannot delete
- prefer former/archive lifecycle over destructive delete where applicable

Acceptance:
- CRUD works for permitted users.
- Unauthorized users receive 403/permission failure.
- UI does not expose forbidden actions to users without permission.
- Backend blocks forbidden actions even if API is called directly.

────────────────────────────────
C. Active Roster filtering/sorting/export tests
────────────────────────────────

Test that the same final data array drives:
- displayed rows
- result count
- empty state
- export CSV

Required tests:

1. Active Roster shows only active crew.
2. Former Crew shows only former/inactive crew.
3. Active Roster filters do not accidentally include former crew.
4. Former Crew filters do not accidentally include active crew.
5. Sorting by name works in Active Roster.
6. Sorting by rank works in Active Roster.
7. Sorting by name works in Former Crew.
8. Export from Active Roster exports only visible active rows.
9. Export from Former Crew exports only visible former rows.
10. Count equals displayed rows.
11. Empty state appears only when displayed rows are actually empty.

Acceptance:
- No mismatch between count/table/export.
- Former Crew sorting works.
- Former Crew export does not export active crew.

────────────────────────────────
D. Active access readiness tests
────────────────────────────────

Test active crew access-readiness states.

Required states:
- ready
- no_login
- login_disabled
- no_password_set
- temporary_password_issued
- password_change_required
- no_vessel_scope
- no_dashboard_or_role
- fleet_scope_review

Required tests:

1. Active crew with no linked login returns no_login.
2. Active crew with disabled login returns login_disabled.
3. Active crew with login but no password returns no_password_set.
4. Active crew with temporary password returns temporary_password_issued.
5. Active crew with mustChangePassword returns password_change_required or user-pending state.
6. Active crew with no vessel scope returns no_vessel_scope.
7. Active crew with no dashboard/role returns no_dashboard_or_role.
8. Active crew with fleet-wide scope returns fleet_scope_review.
9. Fully configured active crew returns ready.

Acceptance:
- Temporary password is not treated the same as no password.
- User-pending states are distinct from admin-blocking states.
- Readiness badges match backend readiness data.

────────────────────────────────
E. Former Crew access-risk CRUD/workflow tests
────────────────────────────────

This is critical.

Former Crew must not use active-only access-readiness records.

Required tests:

1. Former crew with linked user but disabled login is not counted as active-login risk.
2. Former crew with linked user and enabled login is counted as active-login risk.
3. Former crew with vessel assignment/scope remaining is counted as vessel-access risk.
4. Former crew with fleet-wide access remaining is counted as fleet-access risk.
5. Former crew with hub/admin access remaining is counted as admin-access risk.
6. Former crew with additional roles remaining is counted as role-risk.
7. Former crew with no remaining access is marked safe/no active access risk.
8. Former Crew risk filters work:
   - linked login
   - login still enabled
   - vessel access remaining
   - hub/admin access remaining
   - additional roles remaining
9. Former Crew does not disappear because active access readiness is missing.
10. API method equivalent to `listFormerCrewAccessRisks` works and is tenant-scoped.

Acceptance:
- Do not infer active login from `userId`.
- Active login must be based on actual account enabled/login state.
- Former Crew risk review is separate from Active Roster readiness.

────────────────────────────────
F. Onboarding checklist tests
────────────────────────────────

Required tests:

1. New crew with no login shows login missing, not complete.
2. New crew with explicit “skip login for now” shows login intentionally skipped.
3. Skip login is not assumed automatically.
4. New crew with login enabled but no role/dashboard is not access ready.
5. New crew with login enabled but no vessel scope is not access ready.
6. New crew with temporary password issued shows admin setup complete / user pending.
7. New crew with missing emergency contact is not profile ready.
8. New crew with documents pending/not assessed is not deployment ready.
9. Final readiness distinguishes:
   - Profile Ready
   - Access Ready
   - Documents Pending / Not Assessed
   - Deployment Ready / Pending document review

Acceptance:
- `no_login` is never marked complete unless explicitly skipped.
- Deployment readiness is not overstated when documents are pending.
- Checklist tells admin what remains incomplete.

────────────────────────────────
G. Offboarding and reinstatement tests
────────────────────────────────

Required tests:

1. Retire active crew.
2. Cancel contract for active crew.
3. Mark crew former.
4. Offboarding defaults:
   - end duty status
   - disable login
   - remove vessel access
   - remove fleet-wide access
   - remove hub/admin access
   - clear additional roles where appropriate
   - downgrade high-risk primary role where appropriate
   - preserve profile/history/documents/alerts
5. Offboarding result summary is returned by backend or visible in UI.
6. Result summary includes:
   - login disabled
   - vessel access removed
   - fleet access removed
   - hub/admin access removed
   - additional roles cleared
   - primary role downgraded
   - duty ended
   - records preserved
   - failures/skipped actions
7. Partial offboarding failure is visible.
8. Former crew cannot log in after offboarding.
9. Former crew no longer appears in Active Roster.
10. Former crew appears in Former Crew.
11. Reinstating crew does not silently restore old vessel/admin/hub access.
12. Reinstatement requires explicit access review before login is re-enabled.

Acceptance:
- Former crew cannot accidentally retain live access.
- Reinstatement does not revive old privileges.
- Offboarding is auditable.

────────────────────────────────
H. UserAccessEditor CRUD/partial-save tests
────────────────────────────────

Required tests:

1. Create login for crew member.
2. Update username.
3. Set temporary password.
4. Enable login.
5. Disable login.
6. Assign primary role.
7. Add secondary role.
8. Remove secondary role.
9. Assign vessel scope.
10. Remove vessel scope.
11. Grant hub/admin access when permitted.
12. Deny hub/admin grant when current admin lacks permission.
13. Simulate partial save failure:
   - role save succeeds
   - vessel scope save fails
   - password save succeeds
14. Verify dialog stays open after partial failure.
15. Verify result panel shows exactly what succeeded and failed.
16. Verify parent `onSaved` or close callback is not called after partial failure.

Acceptance:
- Partial failures are not hidden.
- Admin can see failed access-save steps.
- Backend remains source of truth.

────────────────────────────────
I. Vessel readiness panel tests
────────────────────────────────

Required tests:

1. Select Vessel A.
2. Verify total assigned crew count.
3. Verify on-duty crew count.
4. Verify missing-login count.
5. Verify login-disabled count.
6. Verify no-vessel-scope count.
7. Verify fleet-scope-review count.
8. Verify documents pending/not assessed count.
9. Verify unresolved alert count.
10. Verify former crew with active login risk count uses actual login state.
11. Verify former crew with disabled login is not counted as active-login risk.
12. Verify blocking issues list includes crew names and reasons.
13. Verify clicking issue opens relevant crew profile/action.

Acceptance:
- Vessel readiness answers: “Is this vessel crew-ready?”
- Counts are accurate and not based on userId shortcuts.
- Document readiness is honest if not fully implemented.

────────────────────────────────
J. Safety alarm type CRUD tests
────────────────────────────────

Test alarm type configuration separately from alarm operation.

Required tests:

1. User with `safety_alarm_types:manage` can create alarm type.
2. User with `safety_alarm_types:manage` can edit alarm type.
3. User with `safety_alarm_types:manage` can delete/deactivate alarm type.
4. User without `safety_alarm_types:manage` cannot create alarm type.
5. Captain/fleet_manager/chief_engineer cannot manage alarm types unless explicitly granted.
6. Alarm type list/read requires appropriate view/manage permission.
7. UI hides create/edit/delete alarm type controls for users without permission.
8. Backend blocks direct API calls without permission.

Acceptance:
- Alarm configuration is not controlled by broad role gate.
- Alarm operation and alarm type management are separate.

────────────────────────────────
K. Safety alarm CRUD/workflow tests
────────────────────────────────

Required tests:

1. User with `safety_alarms:trigger` can trigger real alarm.
2. User without `safety_alarms:trigger` cannot trigger alarm.
3. User with `safety_alarms:view` can list current alarms.
4. User without `safety_alarms:view` cannot list alarms.
5. User with `safety_alarms:acknowledge` can acknowledge alarm.
6. User without acknowledge permission cannot acknowledge alarm.
7. User with `safety_alarms:clear` can clear alarm.
8. User without clear permission cannot clear alarm.
9. Clearing serious real alarm requires resolution note.
10. Clearing drill/test alarm behaves according to intended policy.
11. Clear alarm persists:
    - cleared time
    - cleared by
    - resolution note
12. Alert Log displays:
    - triggered time
    - cleared time
    - vessel/fleet scope
    - severity
    - mode
    - status
    - triggered by
    - cleared by
    - acknowledgement count
    - outstanding acknowledgements if available
    - resolution note
13. Alert Log filters work:
    - date range
    - vessel/scope
    - severity
    - mode
    - status
    - triggered by / cleared by where available
14. Export alert log requires `safety_alarms:export`.
15. User without export permission cannot export.

Acceptance:
- Resolution note is stored and visible, not just logged.
- Backend and UI permissions match.
- Real/drill/test alarms are visually distinct.

────────────────────────────────
L. Permission registry and role template tests
────────────────────────────────

Required tests:

1. Central permission registry includes:
   - safety_alarms:view
   - safety_alarms:trigger
   - safety_alarms:clear
   - safety_alarms:acknowledge
   - safety_alarms:export
   - safety_alarm_types:view
   - safety_alarm_types:manage
2. Role editor can display and modify these permissions.
3. Role template grants expected permissions to:
   - super_admin
   - system_admin
   - company_admin
   - admin
   - fleet_manager
   - captain
   - chief_engineer
   - logistics_user
   - procurement_user
   - maintenance_planner
   - viewer
4. Super admin can grant/revoke hub/module access.
5. Admin users only see hubs/modules granted by super admin.
6. User without permission cannot access hidden module through direct API call.
7. Backend permission enforcement matches UI visibility.

Acceptance:
- No safety permission exists only in UI.
- No permission exists only in backend but cannot be managed.
- Super admin is not blocked by stale hardcoded role arrays.

────────────────────────────────
M. Role dashboard CRUD tests
────────────────────────────────

Test dashboard configuration as CRUD.

Required tests:

1. Create dashboard config for a role.
2. Read dashboard config for a role.
3. Update visible widgets.
4. Update widget order.
5. Update task sources.
6. Update high-impact questions.
7. Update dashboard label/description.
8. Delete/reset dashboard config to default if supported.
9. Verify fallback dashboard when config missing.
10. Verify every user has a dashboard.
11. Verify different roles receive different dashboards:
    - logistics_user sees logistics-related widgets
    - crew_user sees crew-specific widgets
    - maintenance_planner sees maintenance widgets
    - admin sees permitted admin widgets
12. Verify user with multiple roles uses correct effective role/dashboard resolution.
13. Verify dashboard config persists after reload.
14. Verify unauthorized user cannot modify role dashboard config.
15. Verify super_admin can modify role dashboard config.

Acceptance:
- Role dashboards are configurable and persistent.
- Dashboard rendering uses effective role/dashboard config.
- Role dashboard configuration is permission-gated.

────────────────────────────────
N. Role-grouped user/crew roster tests
────────────────────────────────

Required tests:

1. Admin roster groups users by primary role.
2. Users with secondary roles are shown clearly.
3. User appears in correct role group.
4. User with multiple roles is handled according to intended policy:
   - primary group only plus secondary role chips, or
   - appears in multiple groups with clear labeling
5. Roster shows:
   - username/name
   - primary role
   - secondary roles
   - vessel scope
   - department
   - supervisor
   - login active/disabled
   - last login
   - password change required
   - dashboard profile
6. Changing a user’s role moves them to the correct group.
7. Disabling login updates the roster state.
8. Former crew access risks are not mixed into active user readiness incorrectly.

Acceptance:
- User/crew profiles are sorted into role-based tables.
- Role grouping stays correct after CRUD updates.

────────────────────────────────
O. Frontend route and UI smoke tests
────────────────────────────────

Use Playwright or the existing frontend test setup if available.

Required smoke tests:

1. `/crew-management` loads for permitted admin.
2. `/crew-management` blocked for user without `crew_members:view`.
3. Active Roster tab loads.
4. Former Crew tab loads.
5. Crew profile opens.
6. Access editor opens.
7. Onboarding checklist opens after creating crew.
8. Offboarding dialog opens.
9. Offboarding result summary appears after action.
10. Vessel readiness panel renders.
11. Safety tab renders for user with safety permissions.
12. Safety tab hidden or blocked for user without safety permissions.
13. Alarm Types controls hidden from user without `safety_alarm_types:manage`.
14. Home dashboard loads for multiple roles.
15. Role Dashboard admin tab loads for super admin.
16. Role-grouped user roster renders.
17. Mobile viewport renders crew cards instead of unusable wide table.

Acceptance:
- Key routes do not crash.
- Permission-gated UI appears/disappears correctly.
- Mobile layout remains usable.

────────────────────────────────
P. Visual semantics tests
────────────────────────────────

Where possible, test rendered class names, variants, labels, or snapshots.

Required checks:

1. On Duty badge does not use destructive/red variant.
2. Emergency/real critical alarm uses destructive/red.
3. Drill/test alarm is visually distinct from real alarm.
4. Former/inactive crew uses muted styling.
5. Documents pending/not assessed does not use green/ready styling.
6. Temporary password issued uses pending/user-action styling, not failed/missing styling.
7. Ready state uses positive styling.
8. Disabled/login blocked state uses warning or destructive styling as appropriate.

Acceptance:
- Status colors are consistent and not misleading.

────────────────────────────────
Q. Migration/schema tests
────────────────────────────────

Required tests:

1. Safety alarm resolution note migration applies cleanly.
2. Migration is idempotent or safe under project migration conventions.
3. `resolution_note` / `resolutionNote` is written on clear alarm.
4. Existing alarms without resolution notes still read correctly.
5. DTO transforms snake_case/camelCase correctly.
6. No migration number conflict exists.
7. Database schema and TypeScript types agree.

Acceptance:
- App starts after migration.
- Alert Log can read old and new alarm records.

────────────────────────────────
R. API permission negative tests
────────────────────────────────

For each protected endpoint, test direct API calls with under-permissioned users.

Required endpoint groups:
- crew create
- crew edit
- crew duty
- crew lifecycle/offboarding
- crew delete
- crew access manage
- former crew access risks
- safety alarm view
- safety alarm trigger
- safety alarm acknowledge
- safety alarm clear
- safety alarm export
- safety alarm type manage
- role dashboard manage
- permission registry/role template manage

Acceptance:
- UI hiding is not the only protection.
- Backend returns correct unauthorized/forbidden response.

────────────────────────────────
S. Deliverables
────────────────────────────────

When finished, provide:

1. List of test files added/changed.
2. Test framework used.
3. Seed/test data strategy.
4. Validation commands run.
5. Passing/failing test summary.
6. Any tests skipped and why.
7. Bugs found while writing tests.
8. Any fixes applied to make tests pass.
9. Manual QA checklist if some UI tests cannot be automated.
10. Recommended next tests.

Preferred test file organization:
Use the existing project convention. If none exists, use something like:

- tests/helpers/testSeed.ts
- tests/helpers/testAuth.ts
- tests/api/crew.crud.test.ts
- tests/api/crewAccessReadiness.test.ts
- tests/api/formerCrewAccessRisks.test.ts
- tests/api/offboarding.test.ts
- tests/api/safetyAlarms.test.ts
- tests/api/safetyAlarmTypes.permissions.test.ts
- tests/api/roleDashboards.crud.test.ts
- tests/api/permissions.registry.test.ts
- tests/ui/crewManagement.spec.ts
- tests/ui/roleDashboard.spec.ts
- tests/ui/safetyAlertLog.spec.ts
- tests/ui/mobileCrewCards.spec.ts

Validation commands:
Use actual commands available in package.json. Try:
- npm run check
- npm run build
- npm test
- npm run test
- npm run test:unit
- npm run test:e2e
- npm run lint

Final acceptance:
The test suite should prove that the hardened Crew Management, Role Dashboard, Former Crew Access Risk, Offboarding, Safety Alert Log, and Permission changes work correctly across create/read/update/delete, workflow behavior, permissions, and UI visibility.