You are editing an existing TypeScript / Node / React monorepo for a marine predictive maintenance app (ARUS). I want you to IMPLEMENT and WIRE UP full database-backed sensor templates, including support for oil debris templates, and make sure everything compiles and works end-to-end.

Follow these instructions carefully and update all relevant files. Do NOT invent new folder structures; reuse existing patterns in this repo.

---

## GOAL

Implement a **multi-tenant, DB-backed sensor template system** that:

- Stores sensor templates in Postgres via Drizzle.
- Is org-scoped (multi-tenant) and secure.
- Supports an **oil debris** sensor type and other types.
- Exposes CRUD API endpoints under `/api/sensor-templates`.
- Integrates with the existing Equipment Registry’s **sensor configuration dialog** so users can apply templates.
- Integrates with the Sensor Management UI (if present) for managing templates.
- Protects system default templates from deletion and improper modification.
- Passes TypeScript, lint, and runtime checks.

Before you make changes, **scan the repo** so you understand how `@shared/schema`, `server/storage.ts`, orgId validation, and existing sensor config APIs work.

---

## 1. UPDATE DATABASE SCHEMA (`shared/schema.ts`)

1. Locate the main Postgres schema definition file (likely `shared/schema.ts` or equivalent where all tables like `equipment`, `sensor_config`, etc. are declared).

2. Add a new `sensorTemplates` table using Drizzle, following existing style:

   - Columns:
     - `id`: `varchar("id").primaryKey().default(sql\`gen_random_uuid()\`)`
     - `orgId`: `varchar("org_id").notNull().references(() => organizations.id)`
     - `templateId`: `text("template_id").notNull()` (e.g. `"OIL-DEBRIS-GEARBOX"`)
     - `name`: `text("name").notNull()`
     - `kind`: `text("kind").notNull()` (e.g. `oil_debris`, `vibration`, `pressure`, `temperature`, etc.)
     - `unit`: `text("unit").notNull()`
     - `equipmentTypes`: `text("equipment_types").array()` (e.g. `["engine", "gearbox", "thruster"]`)
     - `fields`: `jsonb("fields").notNull()` — this will include thresholds, sample rates, etc.
     - `notes`: `text("notes")`
     - `isSystemDefault`: `boolean("is_system_default").default(false)`
     - `createdBy`: `text("created_by")`
     - `createdAt`: `timestamp("created_at", { withTimezone: true }).defaultNow()`
     - `updatedAt`: `timestamp("updated_at", { withTimezone: true }).defaultNow()`

3. Add a **unique index** so templates are unique within an org:

   ```ts
   export const sensorTemplates = pgTable("sensor_templates", {
     // columns...
   }, (table) => {
     return {
       orgTemplateUnique: uniqueIndex("sensor_templates_org_template_uidx").on(
         table.orgId,
         table.templateId
       ),
     };
   });
Add Zod schemas & types consistent with the rest of the repo:

insertSensorTemplateSchema

selectSensorTemplateSchema (if you have that pattern)

Types:

export type InsertSensorTemplate = z.infer<typeof insertSensorTemplateSchema>;

export type SensorTemplate = typeof sensorTemplates.$inferSelect;

Use a flexible type for fields, e.g.:

fields: z.record(z.string(), z.unknown())
or a more structured type if consistent with existing sensor config fields.

2. UPDATE STORAGE LAYER (

server/storage.ts

 or equivalent)

Open server/storage.ts (or wherever IStorage, MemStorage, PgStorage live).

Extend the IStorage interface with sensor template methods:

getSensorTemplates(orgId: string, equipmentType?: string): Promise<SensorTemplate[]>;
getSensorTemplateById(id: string, orgId: string): Promise<SensorTemplate | null>;
createSensorTemplate(template: InsertSensorTemplate): Promise<SensorTemplate>;
updateSensorTemplate(
  id: string,
  orgId: string,
  data: Partial<InsertSensorTemplate>
): Promise<SensorTemplate>;
deleteSensorTemplate(id: string, orgId: string): Promise<void>;


Implement these methods in MemStorage:

Store templates in an in-memory array/map.

Always filter by orgId.

If equipmentType is provided, return templates whose equipmentTypes is empty OR includes that type.

Implement these methods in PgStorage using Drizzle:

Always scope by orgId to enforce multi-tenancy.

For getSensorTemplates:

Filter by orgId.

If equipmentType is passed, filter where that equipmentType is in equipmentTypes (and/or equipmentTypes is empty/null meaning “generic”).

For updateSensorTemplate and deleteSensorTemplate:

Load the existing record for that id + orgId.

Enforce system default rules (see next section).

Return the updated/created record according to repo conventions.

Enforce system default safety in storage methods:

deleteSensorTemplate:

If isSystemDefault is true, throw an error (e.g. forbidden).

updateSensorTemplate:

Optionally disallow updates to system default templates or restrict which fields may change (at minimum, do not allow switching isSystemDefault from true to false via API).

3. NEW API ROUTES (

server/routes.ts

 or a dedicated router)

Find where your main Express routes are registered (server/routes.ts or similar).

Add a new router section for sensor templates (or a dedicated router file imported into routes.ts) with endpoints:

GET /api/sensor-templates

Reads orgId from your existing org middleware (e.g. req.orgId or header validated by validateOrgIdHeader / requireOrgId).

Optional query param equipmentType for filtering.

Uses storage.getSensorTemplates(orgId, equipmentType).

GET /api/sensor-templates/:id

Uses storage.getSensorTemplateById(id, orgId).

Returns 404 if not found for that org.

POST /api/sensor-templates

Validates body with insertSensorTemplateSchema.

Ignores/overrides any orgId in body; always uses orgId from middleware.

For normal users, ensure isSystemDefault is either false or ignored (only system/seed should create isSystemDefault: true).

Returns created template.

PUT /api/sensor-templates/:id

Uses a partial schema for updates or merges with existing template.

Rejects updates to system defaults if that’s your policy, or restricts what can be changed.

Always enforces org scoping.

DELETE /api/sensor-templates/:id

Checks template belongs to org.

Rejects deletion if isSystemDefault is true.

Use your enhanced error handler and existing security middleware (requireAuthentication, requireOrgId, etc.) for these routes, consistent with other /api endpoints.

If there’s an older server/sensor-routes.ts handling YAML/JSON templates:

Either:

Redirect old template endpoints to the new DB-backed ones (thin wrapper), or

Return a deprecation message if they are no longer used.

Keep unknown signal / J1939 / J1587 features intact.

4. SEED SCRIPT FOR DEFAULT TEMPLATES (

server/seed-sensor-templates.ts

)

Create a new script file, e.g. server/seed-sensor-templates.ts.

This script should:

Initialize DB connection (reuse existing DB/Drizzle helper).

Read existing JSON config file if present (likely something like server/config/sensor_templates.json).

Map each JSON entry into an InsertSensorTemplate object.

Assign:

orgId to a default org (e.g. "default-org-id") or a system org as used elsewhere.

isSystemDefault = true

createdBy = "system-seed" (or similar)

UPSERT by (orgId, templateId) so the script is idempotent:

If a template with same (orgId, templateId) exists, update it.

Otherwise insert.

Include one or more oil debris sensor templates in the JSON / seed:

Examples:

templateId: "OIL-DEBRIS-GEARBOX" with:

kind: "oil_debris"

unit: "ppm" or "mg/kg"

equipmentTypes: ["gearbox", "thruster"]

fields populated with appropriate warn/crit thresholds, sample rates, etc.

templateId: "OIL-DEBRIS-ENGINE" for main engines.

Expose a way to run this seed script via npm/yarn script (e.g. "seed:sensor-templates"), consistent with your project conventions.

5. FRONTEND: QUERY KEYS (

client/src/utils/queryKeys.ts

 or similar)

Find where you centralize React Query keys.

Add:

export const sensorTemplateKeys = {
  all: () => ['/api/sensor-templates'] as const,
  lists: () => [...sensorTemplateKeys.all(), 'list'] as const,
  list: (filters: string) => [...sensorTemplateKeys.lists(), { filters }] as const,
  details: () => [...sensorTemplateKeys.all(), 'detail'] as const,
  detail: (id: string) => [...sensorTemplateKeys.details(), id] as const,
};


Use these keys in all query/mutation hooks for sensor templates (don’t hardcode raw strings elsewhere).

6. FRONTEND: INTEGRATE TEMPLATES INTO EQUIPMENT REGISTRY SENSOR DIALOG

Open the main Equipment Registry page (you already have a massive EquipmentRegistry React component with sensor config dialogs).

In the sensor configuration dialog (sensorForm):

Add a useQuery to fetch templates for the selected equipment type and current org, e.g.:

const { data: sensorTemplates = [] } = useQuery<SensorTemplate[]>({
  queryKey: sensorTemplateKeys.list(selectedEquipment?.type ?? 'all'),
  queryFn: () =>
    apiRequest(
      "GET",
      `/api/sensor-templates${selectedEquipment?.type ? `?equipmentType=${selectedEquipment.type}` : ""}`
    ),
  enabled: !!selectedEquipment && isSensorDialogOpen,
});


Add a template select control in the dialog UI, e.g.:

A Select or Combobox labeled “Apply Template”.

Options: sensorTemplates.map(t => <SelectItem key={t.id} value={t.id}>{t.name}</SelectItem>).

When a template is selected, auto-fill sensor form fields:

Define a helper:

const applyTemplateToSensorForm = (template: SensorTemplate) => {
  const fields = template.fields as any; // or a typed interface if you defined one

  sensorForm.reset({
    ...sensorForm.getValues(),
    sensorType: template.kind,
    targetUnit: template.unit,
    sampleRateHz: fields.sampleRateHz ?? 1.0,
    gain: fields.gain ?? 1.0,
    offset: fields.offset ?? 0.0,
    deadband: fields.deadband ?? 0.0,
    minValid: fields.minValid ?? null,
    maxValid: fields.maxValid ?? null,
    warnLo: fields.warnLo ?? null,
    warnHi: fields.warnHi ?? null,
    critLo: fields.critLo ?? null,
    critHi: fields.critHi ?? null,
    hysteresis: fields.hysteresis ?? 0.0,
    emaAlpha: fields.emaAlpha ?? 0.1,
    notes: fields.notes ?? "",
    enabled: fields.enabled ?? true,
  });
};


Attach this to the template selection handler.

Add a small badge/label indicating when a sensor config was created from a template (if you store templateId in the config, or at least show the applied name while editing).

Ensure form validation still uses insertSensorConfigSchema and that numeric fields are handled correctly (current code already deals with null vs empty strings — keep that working).

7. FRONTEND: SENSOR TEMPLATES MANAGEMENT UI

Find client/src/components/SensorTemplates.tsx or the closest equivalent. If not present, create a reusable SensorTemplates component that:

Uses useQuery to fetch templates from /api/sensor-templates.

Displays:

Template name

kind

unit

equipment type compatibility list

isSystemDefault badge

Offers:

Create template dialog (using insertSensorTemplateSchema).

Edit template dialog (for custom templates).

Delete button (disabled for isSystemDefault).

Wire it into the Sensor Management page (e.g. client/src/pages/sensor-management.tsx):

Add a section/tab “Templates”.

Render the SensorTemplates component there.

Keep existing “unknown signals” / J1939/J1587 features intact.

For mutations:

Use your existing useCreateMutation, useUpdateMutation, and useDeleteMutation hooks with the new /api/sensor-templates endpoints.

Invalidate sensorTemplateKeys.all() or the appropriate list/detail keys after mutations.

8. HARDENING, IO VALIDATION, AND TESTING



Before finishing:

Type & lint checks

Run the TypeScript compiler and lint scripts for the repo.

Fix:

Any TS type errors from new schemas/types.

Any missing/incorrect imports.

Any unused variables introduced by the new code.

Runtime / I/O validation

Ensure all new routes:

Validate request bodies with Zod before hitting storage.

Never trust orgId from the body; always use the org context (header/middleware).

Return consistent JSON response shapes and HTTP status codes matching the rest of the API.

Manual happy paths to verify

Implement and ensure the following flows work in a running dev server:

From the Sensor Management page:

Create a new oil debris sensor template suitable for gearboxes.

Edit it (if custom).

Delete it.

From Equipment Registry:

Open an equipment item of type gearbox or engine.

Open the sensor configuration dialog.

Select the oil debris template.

Confirm that fields (gain, thresholds, sample rate, unit) are auto-populated.

Save the sensor config and verify it appears correctly and can receive telemetry.

Confirm system default templates:

Are visible and usable.

Cannot be deleted via UI.

Are protected by backend checks (try to delete them and ensure you get a 403/meaningful error).

No regressions

Ensure existing sensor configuration flows still work for non-templated configs.

Ensure no existing API routes for sensors or equipment are broken.

After you finish all changes, summarize:

Files modified.

Any new files created (e.g. seed script).

How to run the seed script.

How to exercise the oil debris template flow end-to-end.