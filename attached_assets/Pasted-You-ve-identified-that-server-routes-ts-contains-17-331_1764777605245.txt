You’ve identified that:

server/routes.ts contains 17,331 lines with ~446 inline route handlers.

server/storage.ts contains 24,164 lines of mixed storage operations.

Target size for each is ~3,000 lines.

~35,000 lines still require extraction into modular domain files.



Before extracting anything else, complete the following:

PHASE 1 — Generate a Complete Modularization Plan



Produce a full domain-based mapping of all remaining routes and storage functions.

Return a structured plan showing:

Route name

HTTP method

Current location in routes.ts

Target domain module (e.g., telemetry, crew, inventory, logs, compliance, alerts, work orders, etc.)

Dependencies required

Cross-domain calls that need decoupling



Do NOT move or rewrite code yet — just map everything.

PHASE 2 — Dependency & Impact Analysis



For both routes.ts and storage.ts:

Build a dependency graph:

Which functions call which?

What storage methods are shared between domains?

Where are common utilities located?

Identify:

Cyclic dependencies

Areas requiring interface extraction

Functions that must be split (e.g., generic vs domain-specific)

PHASE 3 — Safety & Regression Protection



Before extraction:

Create domain-level TypeScript interfaces

Add missing zod validators

Add central error-handling abstractions

Add at least minimal integration test scaffolding

Ensure no extraction breaks existing imports

PHASE 4 — Begin Controlled Extraction



Follow this order to reduce breakage:

Extract low-risk domains first

utilities

simple CRUD handlers

inventory

crew

Then extract medium-risk domains

work orders

compliance

scheduler

Then extract high-risk/complex domains

telemetry ingestion

J1939/J1587/CANbus decoders

equipment health

alerts engine

sync



Each extraction should:

Create a new domain folder

Move only one subdomain at a time

Replace imports using a migration shim

Run typecheck & compile after each domain

Goal



At the end, routes.ts and storage.ts should only contain:

imports

top-level wiring

zero domain logic



Total size target: <2,000 lines each

Please begin by producing the complete modularization plan and the dependency graph before moving any code.