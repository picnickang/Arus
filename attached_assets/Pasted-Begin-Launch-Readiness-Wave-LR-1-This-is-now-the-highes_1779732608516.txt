Begin Launch Readiness Wave LR-1.

This is now the highest-priority production-hardening wave.

Scope:
1. Migration reversibility + schema drift protection
2. Cross-tenant leakage verification
3. Role/permission denial verification
4. ML promotion approval hardening

Do not stop after one subtask.
Continue until all LR-1 items are complete or a true blocker occurs.

==================================================
LR-1A — Migration safety
==================================================

Goals:
- eliminate irreversible migration risk
- eliminate schema drift risk
- make rollback verification automatic

Required:
1. Backfill reversible .down.sql migrations for 0015–0021.
2. Replace drizzle-kit push --force usage in scripts/post-merge.sh with explicit migration execution flow.
3. Add scripts/check-migrations-reversible.sh:
   - create ephemeral DB
   - apply up migrations
   - apply down migrations
   - re-apply up migrations
   - verify schema consistency
4. Add CI wiring for migration reversibility checks.
5. Add schema snapshot-vs-live drift check if feasible.

Rules:
- no destructive migration shortcuts
- migrations must remain reversible
- preserve current schema semantics

==================================================
LR-1B — Cross-tenant leakage verification
==================================================

Goals:
- prove tenant isolation beyond equipment routes

Required:
Add parameterized 2-org integration tests covering:
- work-orders
- KB
- ml-models
- telemetry
- schedules
- 3d-models
- alerts

Also add:
scripts/check-routes-require-orgid.ts

The audit script should:
- inspect registered routes
- flag missing requireOrgId on tenant-scoped paths
- fail CI on violations

==================================================
LR-1C — Role/permission verification
==================================================

Goals:
- prove role gates reject unauthorized access

Required:
Add parameterized integration tests:
wrong-role → 403

Cover:
- 3D upload
- ML promote
- system admin routes
- maintenance admin flows
- any route using requireRole(...)

Enumerate routes dynamically where feasible.

==================================================
LR-1D — ML promotion approval hardening
==================================================

Goals:
- prevent unilateral unsafe model promotion

Required:
- add second-approver gate for model promotion
OR
- require explicit approval token/workflow consistent with existing safety-service patterns

Also add tests:
- strict-tier auto-approve defaults OFF
- unauthorized promotion rejected
- second approval required
- approved promotion succeeds

Preserve existing ML lineage tracking.

==================================================
VALIDATION
==================================================

After every cluster:
- npx tsc --noEmit
- strict checks
- scoped eslint
- relevant integration/E2E tests
- migration reversibility checks
- explicit-any remains 0
- cast count remains documented baseline

Rules:
- No explicit any
- No as any
- No ts-ignore
- No eslint-disable
- Preserve strict-mode cleanliness
- Preserve runtime semantics

Architect/code review required.

==================================================
CONTINUATION
==================================================

After successful validation:
- commit/merge
- immediately continue to the next LR-1 cluster
- do not enter idle/reporting state

==================================================
FINAL REPORT
==================================================

Provide:
- LR-1 items completed
- migrations added
- tests added
- CI changes
- tenant-isolation coverage added
- role-gate coverage added
- ML approval hardening summary
- remaining launch risks
- tsc/lint/test status