You are an expert full-stack engineer working on a maritime predictive-maintenance app called ARUS, running inside an Electron desktop app with a Node/Express backend and a React/TypeScript frontend.



The app already has:

A telemetry ingestion pipeline (for engine / generator / sensor data)

A vessel track / location tracking system

Engine and deck logbooks with auto-fill from telemetry

Hybrid storage: SQLite local (vessel / embedded) plus optional PostgreSQL / libSQL / Turso for cloud sync

A lot of existing code for:

/api/logbook/engine/autofill

telemetry simulator

equipment / work orders / analytics

vessel track log or “position” tables



We now have an Aquametro FMCC fuel monitoring unit onboard that can provide:

Fuel consumption / flow metrics

Engine-related telemetry

Location / speed information



We want to integrate FMCC telemetry without breaking or duplicating existing architecture, and we also want FMCC location data to feed into the existing vessel track log, not a new parallel system.

0. First: Understand and Check Architecture (VERY IMPORTANT)

Scan the entire codebase and identify:

Existing telemetry ingestion modules/services

Existing DB schemas for:

engine / generator telemetry

vessel position / track log

anything referencing AIS/GPS/location

Existing APIs related to telemetry and track, e.g.:

/api/telemetry/**

/api/vessels/**

/api/logbook/**

any “track”, “position”, or “route” endpoints

Document (in comments or a short markdown summary in the repo):

What tables currently store position / track points

(e.g. vessel_track_points, telemetry_position, etc.)

How telemetry is used by:

engine logbook autofill (/api/logbook/engine/autofill)

any dashboards or charts

How location is currently sourced:

GPS, AIS, manual entries, anything else

Before making ANY changes, confirm these design rules in code comments near the new FMCC integration:

FMCC must plug into the existing telemetry & track schema, not create a parallel universe.

If FMCC is disabled, the system must behave exactly as it does today (no regressions).

Track log and telemetry consumers should not need to care if data came from GPS vs FMCC; they just see unified telemetry/track tables with a source field.



If you find that there is already some notion of a “unified ingestion abstraction”, extend that; do not bypass it.

1. Add FMCC Telemetry Integration (Fuel + Engine + Nav)



Goal: Add an FMCC ingestion module that:

Reads FMCC data (assume Modbus TCP or similar, but design the module so that the low-level driver can be swapped)

Normalizes FMCC output into the existing telemetry schema

Is fully controlled by configuration (e.g. ENV flags) and easy to disable



Tasks:

Create or update a backend module like:

server/integrations/fmcc/adapter.ts

or similar location consistent with existing integrations

The adapter should:

Poll FMCC at a configurable interval (e.g. env var FMCC_POLL_INTERVAL_MS)

Map FMCC registers/fields into a normalized snapshot type, for example:

interface FmccSnapshot {
  vesselId: string;
  timestamp: string; // ISO
  source: "fmcc";
  fuel: {
    totalFlow?: number;
    portEngineFlow?: number;
    stbdEngineFlow?: number;
    density?: number;
    temperature?: number;
  };
  engine?: {
    rpm?: number;
    loadPercent?: number;
  };
  navigation?: {
    latDeg?: number;
    lonDeg?: number;
    speedOverGround?: number;
  };
  raw?: Record<string, any>;
}


Use a clear mapping table from FMCC registers → these fields.

Handle unknown/missing values gracefully.

Make FMCC integration optional & safe:

Guard everything behind configuration:

FMCC_ENABLED=true|false

FMCC_HOST, FMCC_PORT, etc.

If disabled or misconfigured:

Log a clear warning

Do not crash the server

The rest of the app must behave exactly as before.

Connect FMCC snapshot into the existing telemetry ingestion service, not bypassing it:

If there is a central TelemetryService.ingest() or similar, call that.

If not, create a small abstraction that both FMCC and existing sources use.

2. Integrate FMCC Location into the Vessel Track Log



Goal: Use FMCC’s navigation block (latDeg, lonDeg, speedOverGround) to feed into the existing vessel track / position tables so that:

The current vessel track log UI continues to work, but now sees FMCC points as well.

There is no duplication of track logic.



Tasks:

Find the existing vessel track schema:

E.g. a table like vessel_track_points / telemetry_position with fields such as:

id

vesselId

timestamp

lat

lon

speed

source

If there is no source column yet, consider adding one in a migration (with a safe default, like 'gps' or 'unknown' for historical data).

Implement an FMCC → track storage step that:

On each FMCC snapshot, if navigation has lat/lon:

Insert a row into the existing track table only (no FMCC-specific track table), e.g.:

{
  vesselId,
  timestamp,
  lat: navigation.latDeg,
  lon: navigation.lonDeg,
  speedOverGround: navigation.speedOverGround ?? null,
  source: "fmcc"
}


Apply basic de-duplication / filtering:

Avoid spamming the DB with unchanged points (e.g., only insert if time delta or position delta > small threshold).

Respect the same retention / pruning strategy as existing track points.

Ensure compatibility with existing track consumers:

Any APIs like /api/vessels/:id/track should not need to change much.

They should just return all track points for that vessel in a time range, possibly ordered by timestamp, regardless of source.

Optionally: allow a source filter query param (e.g. ?source=fmcc), but don’t break existing clients that don’t pass it.

Add a small, clearly labelled code comment near the track ingestion logic explaining:

FMCC points are unified with existing GPS/AIS track points via the source field.

The track log is the single source of truth; we do not maintain separate FMCC-only tracks.

3. Ensure Compatibility with Existing Telemetry & Logbook Auto-Fill



Goal: FMCC integration should augment existing behavior, not replace or break it.

Update or confirm the logic in:

/api/logbook/engine/autofill

Any other telemetry→logbook or telemetry→analytics functions

so that they:

Continue to read from the same unified telemetry tables.

Can use FMCC data if available, but gracefully fall back to existing sources if not.

If there is any logic that currently assumes:

source is only 'simulator' or 'mqtt', etc.

then update it to allow 'fmcc' without special hacks.

Add at least one automated test (unit or integration) that:

Inserts sample FMCC telemetry (including navigation data).

Confirms:

Track query endpoints see the FMCC points.

Engine log auto-fill can still work.

No runtime crash occurs when FMCC is disabled.

4. Performance, Safety, and Config

Make sure FMCC polling is:

On a sensible interval (e.g., 5–30 seconds), configurable via env.

Implemented with proper error handling and reconnection (no unbounded retry loops that crash the process).

Ensure that FMCC integration respects:

Existing SQLite + PostgreSQL / Turso dual-storage choices.

Any sync logic that decides what telemetry/track data is pushed to cloud.

Add health / diagnostics:

Log basic FMCC health:

last successful poll timestamp

last error

Optionally expose a light diagnostic in /livez or a similar endpoint, but keep it cheap.

5. Clean, Documented, and Non-Overlapping Implementation



When implementing all of the above:

Do NOT create duplicate track tables just for FMCC.

Do NOT bypass the main telemetry ingestion/track logic.

Do NOT change existing behavior when FMCC_ENABLED=false.



Instead:

Extend existing abstractions.

Use source: 'fmcc' as the distinguishing attribute.

Keep the integration self-contained but well-wired into the existing architecture.



Finally, once you’re done, please:

Summarize the changes in a short markdown file or comment block:

Where FMCC integration lives

How position data flows from FMCC → telemetry → track log

How to enable/disable it with environment variables

Fix any type errors, lints, or failing tests introduced by these changes.