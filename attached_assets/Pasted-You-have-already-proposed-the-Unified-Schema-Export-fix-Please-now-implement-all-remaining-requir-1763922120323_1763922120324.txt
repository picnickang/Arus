You have already proposed the “Unified Schema Export” fix. Please now implement all remaining required fixes below.

Apply changes automatically across the codebase. Do not ask follow-up questions.

1. Finalize the Unified Schema Runtime System



You already proposed shared/schema-runtime.ts.

Now apply the change fully:



a. Create (or verify):

shared/schema-runtime.ts
With:

import { isLocalMode } from "../server/db-config";

export * from (isLocalMode
  ? "../server/schema-sqlite-vessel"
  : "../server/schema");
b. Update every server file that imports the schema



Replace all imports like:

import * as schema from "./schema";
import { errorLogs } from "./schema";
With:

import * as schema from "@shared/schema-runtime";
Do this for all 92 files.

2. Replace all database-generated UUIDs with explicit UUIDs



SQLite does NOT support:

gen_random_uuid()

uuid_generate_v4()

Postgres-only default UUID functions



Fix:



Replace all inserts that rely on DB UUIDs with:

import { randomUUID } from "crypto";

await db.insert(schema.tableName).values({
  id: randomUUID(),
  ...otherFields
});
Do NOT leave any DEFAULT gen_random_uuid() or similar in the schema.

3. Fix Connection Pool Health Check Errors



The error:

db.execute is not a function
Means SQLite client is being passed into Postgres-style pool checking logic.



Fix:



In health-check logic (checkConnectionPoolHealth):

If in SQLite/local mode (isLocalMode === true):

Skip Postgres connection pool checks entirely.

Replace with:

return { healthy: true, mode: "sqlite" };
4. Implement Missing SQLite Tables for Local Mode



The log shows:

SQLITE_ERROR: no such table: update_settings
Fix:



Ensure every table present in the Postgres schema also exists in:

server/schema-sqlite-vessel.ts
Add missing definitions for:

update_settings

Any others referenced during runtime



Use matching schema parity but compatible with SQLite types.

5. Apply Static File Fixes in Electron Server



Electron still sometimes returns CSS/JS with text/html.



Fix:



In the Express static configuration:

Ensure absolute path resolution using:

const staticRoot = path.resolve(process.cwd(), "dist");
Ensure:

app.use(express.static(staticRoot, { extensions: ["html"] }));
Ensure MIME detection by adding:

import mime from "mime";
mime.define({ "text/css": ["css"] }, true);
6. Fix Rate-Limit Flood in Embedded Mode



You are seeing hundreds of:

429 Too many requests
This is caused by the frontend polling intervals being tuned for cloud mode.



Fix:



When isLocalMode === true:



Reduce all polling intervals (alerts, equipment, health, notifications, conflicts) by 80%, or disable polling and rely on WebSockets.

7. Ensure x-org-id is Automatically Injected



The logs show:

401 MISSING_ORG_ID
Fix:



When local/embedded mode:

req.headers["x-org-id"] = "default-org-id";
Before tenant isolation middleware runs.

8. Disable All Cloud Sync Logic When Local



Errors:

Sync Manager: libSQL client not initialized
Fix:



If running SQLite:

Never start the sync manager, update scheduler, or pruning scheduler.

Replace with stub implementations that log “disabled in local mode”.

9. Remove Duplicate libvips (sharp) Binaries



The logs show:

Class GNotificationCenterDelegate is implemented in both sharp libraries
Fix:

Remove old sharp version at:

node_modules/@xenova/transformers/node_modules/sharp

Or explicitly alias to @img/sharp-libvips-darwin-x64



Only one libvips copy should remain.

✔ After applying all changes:

The Electron app should load reliably.

No more 404 CSS/JS.

No more blue screen.

No more PostgreSQL-only behavior in SQLite.

No more schema mismatches.

No more rate-limit storms.

No more db.execute is not a function.

No more missing tables in embedded mode.

This is the full required repair patch. Please apply all changes automatically.

