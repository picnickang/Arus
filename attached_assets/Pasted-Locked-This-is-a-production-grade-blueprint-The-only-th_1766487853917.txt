Locked. This is a production-grade blueprint.

The only things I’d adjust (minor, but worth it) are:

4 tiny tweaks to make it bulletproof

1) Fix ListSources RPC type

Right now you have:
	•	rpc ListSources(Empty) returns (stream SourceConfig);

That works, but it’s awkward to consume and hard to “complete” as a list. Prefer:

message SourceList { repeated SourceConfig sources = 1; }

rpc ListSources(Empty) returns (SourceList);

Keep SubscribeStats as streaming (that’s correct).

2) Add an explicit “frame envelope version”

This prevents future protocol envelope changes from breaking decoders.

In raw_frames add optional column:
	•	payload_format_version INTEGER NOT NULL DEFAULT 1

Or bake it into protocol like J1939_V1, but a column is cleaner.

3) Make retention safe with “processed cursor”

Don’t delete frames newer than cursor (rare but important if time jumps):
	•	delete by id threshold and time threshold:

Example: keep 30 days OR keep everything after (cursor.last_id - safetyWindow).

4) Ensure the bridge never “double-enqueues” if flush fails

Your current loop decodes and queues, then flush fails, then it retries and will decode/queue the same frames again unless you guard it.

Two clean options:

Option A (recommended): Make BridgeProcessor.process(frames) pure and write directly via TelemetryBatchWriter.writeBatch(readings) (if you have it).
If you only have queue(), then:

Option B: Add TelemetryBatchWriter.queueMany(readings, batchKey) that dedupes by batchKey until committed.

If you want the simplest change today: Option A—add a method that writes a provided list in one flush cycle and resolves only after commit.

⸻

Everything else is solid
	•	WAL + busy_timeout: ✅
	•	Cursor only advances after commit: ✅
	•	Heartbeat fallback: ✅
	•	Decoder registry: ✅
	•	flushOnce() waiters: ✅
	•	Backpressure via getQueueDepth(): ✅
	•	Tests list is exactly right: ✅
	•	UI panel polling /api/agent/status: ✅
	•	Installer + recovery actions: ✅

⸻

Pasteable “Build Prompt” to generate everything exactly as your plan

Use this in Replit to implement Tasks 1–14 without duplication:

⸻

BUILD PROMPT

You are implementing the “ARUS Hybrid Telemetry Architecture” exactly as specified below. Follow the plan precisely, keep modules small, avoid duplicating existing infrastructure, and ensure production readiness.

Repo facts (already exist)
	•	TelemetryBatchWriter is in server/telemetry-batch-writer.ts and already implements batching, backpressure, retries, and metrics.
	•	PostgreSQL pool is in server/db-config.ts (near line ~268) and exported as pool.
	•	TelemetryReading interface exists in server/telemetry-batch-writer.ts.

Goal
Add a thin C# Windows Service (“Telemetry Agent”) that writes raw frames to local SQLite (raw_frames) and exposes gRPC + heartbeat; add a Node SQLite→Postgres bridge using cursor + decoder registry feeding the existing TelemetryBatchWriter.

HARD RULES
	1.	Do NOT re-implement Postgres writes: reuse TelemetryBatchWriter.
	2.	Do NOT put decoding logic in C# agent: agent only does hardware I/O + SQLite.
	3.	Cursor advances ONLY after confirmed Postgres commit (flushOnce() commit barrier).
	4.	SQLite connections MUST use WAL + busy_timeout=5000 on every connection.
	5.	Bridge must avoid double-enqueue on flush failures (implement a safe mechanism).
	6.	Add tests for cursor safety, J1939 parsing, PGN handler, offline retry, validation.

⸻

Implement Tasks 1–14

Task 1–5 (C# Agent)

Create apps/agent/Arus.TelemetryAgent/ with:
	•	Program.cs, Worker.cs, appsettings.json
	•	Protos/telemetry_agent.proto
	•	Services/TelemetryAgentGrpcService.cs
	•	Storage/SqliteFrameWriter.cs
	•	Sources/IIngestSource.cs, SerialSource.cs (stub), CanJ1939Source.cs (stub)

Must include:
	•	env vars: ARUS_DB_PATH, ARUS_HEARTBEAT_PATH, ARUS_GRPC_PORT
	•	SQLite schema with WAL, synchronous=NORMAL, busy_timeout=5000, temp_store=MEMORY
	•	batch flush: 500 records every 50ms
	•	30-day retention every 6 hours + incremental_vacuum
	•	heartbeat JSON every 1 second with atomic tmp→move

Task 2 (Proto)

Use the proto from the plan, but modify ListSources to return a list, not a stream:
	•	rpc ListSources(Empty) returns (SourceList);
	•	message SourceList { repeated SourceConfig sources = 1; }

Task 6–7 (Node agent client + heartbeat)

Create:
	•	server/services/agent-client.ts
	•	server/services/agent-heartbeat.ts
Implement gRPC client + heartbeat fallback logic exactly as described.

Task 8 (Decoder module)

Create server/telemetry/decode/:
	•	index.ts, types.ts, registry.ts, j1939.ts, j1587.ts, validation.ts, util.ts
J1939 payload envelope: [canId:u32le][dlc:u8][data:0..8]
Implement PGN extraction (PDU1/PDU2 rule) and registerPgn registry pattern.
Add validation: equipmentId non-empty, finite value, timestamp sanity.

Task 9 (SQLite Bridge)

Create server/services/sqlite-bridge/:
	•	index.ts, cursorStore.ts, sqliteRawFrameSource.ts, bridgeProcessor.ts, config.ts
Cursor table schema includes last_id, last_ts, updated_at.
Implement backpressure check using TelemetryBatchWriter.getQueueDepth().
Implement loop: fetch batch → decode → enqueue/write → flushOnce() → advance cursor.
IMPORTANT: Ensure no duplicate enqueue when flush fails (design required).

Task 10 (TelemetryBatchWriter additions)

In server/telemetry-batch-writer.ts, add:
	•	getQueueDepth()
	•	flushOnce() with flushWaiters resolved only after commit, rejected on rollback.
Add any minimal hook needed to prevent duplicate enqueue if flush fails.

Task 11 (Idempotency constraint)

Add migration or SQL note to ensure unique constraint exists on idempotency_key.
Use idempotency key format: raw:${source}:${protocol}:${rawFrameId}.

Task 12 (Tests)

Create server/__tests__/sqlite-bridge/ with:
	•	cursor.test.ts, j1939-decoder.test.ts, pgn-handler.test.ts, bridge-retry.test.ts, validation.test.ts
Use mocks to simulate Postgres offline and ensure cursor doesn’t advance.

Task 13 (Agent Health UI)

Create client/src/components/agent-health-panel.tsx and an API route:
	•	GET /api/agent/status with gRPC-first then heartbeat fallback
Show status, frames, last frame age, queue depth, disk usage, source controls.

Task 14 (Installer + Docs)

Add README.md and placeholders for WiX MSI config.
Include commands for console run, service start, and health validation (grpcurl, heartbeat file).

⸻

Output
	1.	Print a file tree of what you created/modified.
	2.	Provide the exact commands to run:
	•	C# agent console mode
	•	Node bridge service
	•	UI status endpoint
	3.	Call out any repo-specific path adjustments you had to make.

Proceed now.

⸻

If you want, I can also rewrite Task 9’s “no duplicate enqueue” as a concrete implementation that matches your TelemetryBatchWriter internals once you paste the relevant parts of server/telemetry-batch-writer.ts (the flush loop + queue structures).