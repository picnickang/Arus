Good — briefing page handles the empty state well with a “Generate Now” button. Now I have the full picture of all issues. Let me summarize:​​​​​​​​​​​​​​​​

I have the complete list. Here are all the similar issues — UX flows that look right but behave wrong:
Issue 1 (already fixed): Role selection immediately redirects away from home page. User picks a role, never sees the customized home page. Fixed in the tar I just gave you.
Issue 2: Quick action URLs are ignored by 3 of 4 target pages.
The Chief Engineer’s quick actions include "Log Engine Entry" → /engine-logbook?action=new and the Deck Officer has "New Deck Entry" → /deck-logbook?action=new and "Record Rest Hours" → /hours-of-rest?action=record. The work-orders page correctly parses ?action=create from the URL and opens the create dialog. The engine-logbook, deck-logbook, and hours-of-rest pages do NOT read the URL parameter — the user lands on the page and has to find and click the “Add” button manually. The quick action saves zero taps.
Issue 3: BottomNav has its own hardcoded ROLE_DEFAULTS instead of importing from config/roles.ts.
The BottomNav defines chief_engineer: ["operations", "maintenance", "fleet", "records"] while config/roles.ts defines pinnedGroups: ["maintenance", "operations", "fleet"]. These are intentionally different concepts (4-item bottom bar vs 3-group home page), but they share role IDs and will drift independently. If a role is renamed or a new role is added, you have to update both files — and the briefing-redirect ROLE_LANDING map, and the SuggestionCards MAINTENANCE_ROLES list.
Issue 4: ConnectivityBanner never shows sync status. The component accepts pendingSyncCount as a prop (defaults to 0) and shows “syncing” state when count > 0. But in App.tsx it’s rendered as <ConnectivityBanner /> with no props — so pending sync count is always 0, and the user never sees sync status. On a vessel with intermittent connectivity, this means WOs created via QuickWorkOrderSheet could be pending upload with no visible indicator.
Issue 5: QuickWorkOrderSheet has no vessel context. It’s rendered as <QuickWorkOrderSheet open={quickWoOpen} onClose={...} /> without a vesselId prop. The sheet fetches the equipment list from /api/equipment without a vessel filter, so the engineer on Green Belait sees equipment from all vessels in the fleet, not just their vessel. On a fleet with 20 vessels and 200+ pieces of equipment each, that’s a 4,000-item dropdown.
Issues 2, 3, 4, and 5 are the same class of problem as the role picker: the pieces exist and render without errors, but the wiring between them is incomplete, so the user experience degrades silently.
Want me to fix all five?​​​​​​​​​​​​​​​​