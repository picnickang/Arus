You are refactoring a Node.js ML prediction service for predictive maintenance. Apply the following changes across the codebase (TypeScript where applicable). Maintain current public function signatures and the “null-on-error” contract. Avoid introducing heavy deps; use what’s already in the project.



Goals (implement all):

Deterministic time bucketing & ordering



Replace ad-hoc Map<string, ...> timestamp grouping with a utility that buckets telemetry into fixed steps (default 1000ms), sorts by time, and merges sensors into a single feature row per bucket.

Reuse this utility in LSTM, XGBoost, RF, hybrid, and ensemble paths.



Sequence handling



For LSTM/sequence models, if there are ≥ sequenceLength rows overall, use the last sequenceLength rows instead of returning null. If still insufficient, return null.



Train-infer preprocessing parity



Persist preprocessing params (means/stds, Kalman settings, etc.) alongside the trained model as <modelPath>.preproc.json.

At inference, load and apply the same params. If missing, proceed without normalization.



Tensor lifecycle & memory



Wrap TF ops in tf.tidy; dispose intermediate tensors and predictions after use.

Add a small inference semaphore (max 2 in-flight) to avoid memory spikes on small instances.



LRU model cache



Replace the unbounded modelCache with an LRU capped at 6 models; evict least-recently-used.



Remove duplicate heavy imports



Keep lazy/dynamic imports only (e.g., getBestModel, TF, XGBoost). Remove any top-level imports that load native bindings at startup.



RF/XGB feature correctness



Replace placeholder fields (operatingHours, maintenanceAge, failureHistory, etc.) with values from storage if available; otherwise omit them (do not force zeros that bias trees).



Configurable lookback window



Replace hardcoded 30 days with equipment.lookbackDays ?? defaultLookbackDays from a config (fallback 30).



NaN/units sanitation



Extend sanitizeTelemetry to drop rows with non-finite value and log a single warning with counts (not per row).



Guard types and circuit-breaker accounting



Create a type guard isPrediction(x): x is MLPredictionResult and use it in withProtection to count successes/failures properly.



Explainability: reuse data + timeout



Reuse already-fetched telemetry for SHAP. Add a 1.5s timeout wrapper; if exceeded, return the prediction without explanation. No double-fetching.

Explainability failure must not flip CB to failure.



Structured logging



Replace free-form logs with structured JSON logs: {method, equipmentId, orgId, latencyMs, status, details...}.



(Optional) ONNX runtime path



If an ONNX checkpoint is present for LSTM, use ONNX Runtime for inference (keep TF path as fallback). If not present, continue as before.