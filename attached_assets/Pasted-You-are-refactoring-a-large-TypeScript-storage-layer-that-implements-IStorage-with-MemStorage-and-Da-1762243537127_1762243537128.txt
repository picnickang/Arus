You are refactoring a large TypeScript storage layer that implements IStorage with MemStorage and DatabaseStorage for a maritime PdM/CMMS app. Make minimal, safe changes that compile, enforce tenancy, and remove runtime 500s—without changing external behavior unless specified.



0) Scope and style

Target the file I’ve provided. Keep ESM import syntax. Use Drizzle queries already in the codebase.

Only import symbols actually used from @shared/schema; remove unused type/value imports.



1) Remove duplicates & stabilize telemetry

Ensure DatabaseStorage.getTelemetryHistory exists only once. Keep the overload behavior supporting:

(equipmentId, sensorType, hours?) (default hours=24)

(orgId, equipmentId, sensorType, startTime, endTime)

Keep its chronological ordering and condition assembly exactly as implemented; just dedupe.

Create a tiny wrapper getTelemetryByEquipmentAndDateRange(equipmentId, start, end, orgId?) that calls the same internal builder (already present).

(Reference sections show duplicate implementations—unify them into one.)   



2) Enforce multi-tenant isolation everywhere

Keep private validateOrgId(orgId, op) and call it at the start of every org-scoped read/write across devices, telemetry, alerts, inventory, knowledge base, optimization, crew, vessels, etc.

If a method truly supports system-wide use, allow orgId to remain optional; otherwise require it at the method boundary or add a guarded fallback that returns an empty, typed result (never throws in hot paths).

(You already call it in some device methods; extend consistently.) 



3) System Metrics & Health: make non-throwing and usable

Replace current stubs that throw or return empty arrays with minimal, safe logic:

getSystemPerformanceMetrics, getLatestMetricsByCategory, getMetricTrends should query existing tables if present; if not, return a typed empty array without throwing.

getSystemHealthChecks, getSystemHealthCheck, createSystemHealthCheck, updateSystemHealthCheck, deleteSystemHealthCheck, updateHealthCheckStatus, getFailingHealthChecks, and getSystemHealth should either use a simple in-memory map (for MemStorage) or tables (for DatabaseStorage).

Never throw “not found” from polling/overview endpoints; return a typed “unknown/unset” state.

(Replace throw new Error("System health check not found") paths with safe returns.)   



4) Latest-reading & fleet overview hygiene

In getLatestTelemetryReadings and fleet overview, ensure org filtering is applied consistently (either by join on equipment.orgId or by equipmentTelemetry.orgId), and use narrow selects instead of select() to reduce row size. Preserve existing behavior. 

Keep deterministic ordering (orderBy(desc(equipmentTelemetry.ts))) and respect limit.



5) Inventory & parts correctness (atomicity + feature flag)

For addPartToWorkOrder*, reservePartsForWorkOrder, releasePartsFromWorkOrder, and bulk actions:

Wrap in a transaction.

Perform stock checks and reservations atomically.

If insufficient stock, rollback and return a typed error object instead of partially committing.

Introduce an env flag INVENTORY_ENHANCED (default false). For any formerly “temporarily removed” enhanced methods, guard behind this flag; when disabled, return a typed { enabled: false } style result (no throw).

(Applies across the big parts/stock surface area.) 



6) Sensor configuration/state: parameter consistency

Keep getSensorConfigurations(orgId?, equipmentId?, sensorType?) behavior; ensure conditions array is built once and passed via and(...).

Add a small assertExistsEquipment(orgId, equipmentId) helper used by createTelemetryReading and sensor state upserts to avoid orphan rows.

(Your pattern building is fine; standardize it.) 



7) Imports & types clean-up

Trim the enormous @shared/schema import list to only used values/types in this file.

If a referenced type isn’t exported, create a minimal local type only for signatures (do not broaden the public API).



8) Tests & smoke script (lightweight)

Add unit tests covering:

getTelemetryHistory overload resolution and time-window defaults.

validateOrgId rejecting missing/blank orgs on org-scoped methods.

addPartToWorkOrderWithValidation insufficient stock path rolls back and returns a clear typed error.

Add a scripts/smoke:storage script that:

Inserts an org, equipment, and sensor config.

Inserts telemetry; reads both history signatures.

Creates a work order; attempts an over-reservation of a part and expects a safe error result (no partial commits).

Calls getSystemHealth() and confirms non-throwing healthy payload.



9) Acceptance criteria

pnpm build + pnpm test pass with no TS errors.

No duplicate methods; no unguarded throws in metrics/health paths.

All org-scoped methods call validateOrgId before data access.

Inventory operations are atomic and feature-flagged.

Imports are minimal; Drizzle queries are narrow and ordered.

If you want, I can also produce a compact CHANGELOG snippet once Ghostwriter finishes the refactor.