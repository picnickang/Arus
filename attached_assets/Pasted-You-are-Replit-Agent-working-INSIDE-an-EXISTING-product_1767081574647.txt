You are Replit Agent working INSIDE an EXISTING production codebase.

THIS IS A REFACTOR + OVERHAUL PROMPT.
DO NOT start a new project.
DO NOT replace the stack.
DO NOT replace the hybrid offline/online database.
DO NOT replace the existing email alert system.
DO NOT introduce a new AI system without first evaluating the existing one.

Your job is to ASSESS, UNIFY, and OVERHAUL ALL scheduling logic while preserving the existing Crew & Compliance UI structure (Crew Roster, Schedule, Hours of Rest, Compliance tabs already exist).

════════════════════════════════════
ABSOLUTE NON-NEGOTIABLE RULES
════════════════════════════════════
- Reuse existing frameworks, database language(s), ORM/query patterns, sync engine, migrations, auth, UI kit, routing, and folder structure.
- Preserve the hybrid offline-first architecture (local store + queued writes + sync + conflict handling).
- Use ADDITIVE schema changes only. No breaking migrations.
- Reuse the EXISTING email alert/notification system (templates, queues, providers, retries, preferences).
- There is an EXISTING AI system in this repo. You MUST evaluate it before building anything new.
- Implement a compatibility layer so legacy scheduling continues to work during migration.
- All changes must be incremental, feature-flagged, and reversible.
- NO CODE until assessment phases are complete.

NEW REQUIREMENT: MODULARITY + FULL END-TO-END TESTING
- The refactor must result in clean modular boundaries (domain/repository/adapters/UI) so scheduling logic is not duplicated.
- At the end of the implementation, you MUST run and pass the full test suite and also add new automated tests to cover all scheduling functionality, including offline-first and email notifications.
- Do not declare “done” until tests have been executed and pass in the Replit environment.

════════════════════════════════════
PRIMARY OBJECTIVE
════════════════════════════════════
Replace all existing scheduling implementations with a SINGLE canonical Scheduling Domain that delivers:

• SmartPAL-style graphical roster planner (inside existing Schedule tab)
• VoyageX-style assistive scheduling (constraints + ranked suggestions)
• Unified logic shared across:
  - Schedule tab
  - Crew Roster tab
  - Hours of Rest tab
  - Compliance tab
• Offline-first behavior
• Individual crew + admin notifications via EXISTING email system
• Correct modularity and end-to-end verified behavior

════════════════════════════════════
PHASE 0 — MANDATORY SCHEDULING ASSESSMENT
════════════════════════════════════
Before writing ANY code, scan the repository and produce:

1) SCHEDULING INVENTORY
- All scheduling-related routes, pages, components, services, jobs
- Existing Schedule tab implementation
- Crew Roster internals
- Hours of Rest internals
- Compliance logic tied to scheduling
- Any duplicated or parallel scheduling logic
- Exact files/modules and responsibilities

2) DATA + HYBRID SYNC MAP
- Offline DB technology and schemas used
- Online DB technology and schemas
- Sync mechanism (queued ops, conflict strategy, metadata)
- Scheduling-related entities and how they sync
- Known sync pain points

3) BUSINESS RULES EXTRACTION
- What rules are currently enforced vs only displayed:
  - overlaps
  - availability
  - vessel assignment
  - rest hours
  - compliance
- Where each rule lives today (frontend, backend, cron, sync)

4) EMAIL ALERT SYSTEM DISCOVERY
- Locate existing email system:
  - sender service
  - queues/jobs
  - templates
  - retries/failure handling
  - user preferences / opt-out
- Document how scheduling events will hook into it WITHOUT bypassing it

5) GAP ANALYSIS
- Current Schedule UX vs target planner UX
- Performance issues
- Technical debt and duplication

6) RISK REGISTER
- Top 10 refactor risks
- Mitigation for each

DELIVERABLE (STOP AFTER THIS):
A) Scheduling Overhaul Assessment  
B) Refactor & Cutover Plan  
C) Decision Log (what will NOT change)

════════════════════════════════════
PHASE 0.5 — EXISTING AI SYSTEM DISCOVERY (MANDATORY)
════════════════════════════════════
There is an AI system already in this repo. You MUST find it and assess reuse BEFORE implementing any scheduling AI.

MANDATORY DISCOVERY
Search for:
- /ai, /ml, /llm, /agents, /prompts, /tools, /rag
- openai / anthropic / llama / gemini / ollama
- ranking, recommendation, explanation, policy engines
- /api/ai*, /api/assistant*, inference endpoints
- prompt routing, tool calling, vector DBs, caching

AI REUSE ASSESSMENT (OUTPUT REQUIRED)
Produce an “AI Reuse Assessment” covering:
1) What AI components exist (files + responsibilities)
2) How they are invoked (API, jobs, UI)
3) Models/providers and configuration
4) Structured output capability (JSON, tools, scoring)
5) What can be reused for:
   - explanation (“why this crew?”)
   - summarization (“what changed?”)
   - ranking/tie-breaking
6) What MUST remain deterministic (constraints, compliance)
7) Risks (hallucination, latency, offline) + mitigations
8) Clear decision:
   - Reuse as-is
   - Extend existing AI
   - Use AI only for explanations, not decisions

CRITICAL RULE:
Scheduling correctness MUST remain deterministic.
AI is assistive only — never the authority on compliance.

STOP AFTER PHASE 0 + 0.5.
NO CODE YET.

════════════════════════════════════
MODULAR ARCHITECTURE REQUIREMENTS (MUST IMPLEMENT)
════════════════════════════════════
Refactor scheduling into clear modules with explicit boundaries:

1) /domain/scheduling
- Pure business logic: entities, constraints, scoring, suggestion ranking
- No framework imports (no DB, no HTTP, no UI)
- Fully unit tested

2) /data/scheduling
- SchedulingRepository interface
- Implementations:
  - Offline adapter (existing local DB tech)
  - Online adapter (existing API/DB)
  - Legacy compatibility adapter mapping old calls → new repository
- All persistence and mapping lives here

3) /services/scheduling
- Orchestration layer used by UI and API:
  - applyAssignmentChange()
  - checkConstraints()
  - getSuggestions()
  - publishSchedule()
  - emitNotifications()
- Contains transactional boundaries consistent with existing stack

4) /ui/crew-compliance (or existing UI folder)
- Schedule tab planner UI
- Crew roster UI (refactored to use services/repository)
- Hours of Rest UI (refactored to use domain outputs)
- No direct DB writes; all through services/repository

5) /notifications
- SchedulingNotificationService that reuses existing email system
- Recipient resolver (crew/admin/both)
- Audit logging of notification status

You MUST remove duplicated scheduling logic from scattered areas by the end. Only the domain module should contain the rule logic.

════════════════════════════════════
TARGET END STATE (POST-REFACTOR)
════════════════════════════════════

A) CANONICAL SCHEDULING DOMAIN
Entities:
- CrewMember, Vessel, Position/Rank, Qualification, Competency/Endorsement
- Availability, Assignment, Ruleset/Rule, RestLog (reuse existing)
- Notification, Acknowledgement, AuditLog

Constraint Engine:
- HARD: overlaps, missing certs, invalid docs, outside availability
- SOFT: fatigue risk, upcoming expiry, fairness, churn

Suggestion Engine:
- Deterministic filter + scoring
- Top 5 candidates
- “Why” explanations
- Optional AI-generated natural language using EXISTING AI system (assistive only)

B) SCHEDULE TAB — SMARTPAL-STYLE PLANNER
Inside EXISTING Schedule tab:
- Vessel × date timeline
- Drag/drop assignment blocks
- Crew pool filters
- Assignment drawer:
  - details
  - constraints
  - suggestions
- Inline warnings
- Offline/sync status
- Conflict/Sync Issues view if relevant

C) CREW ROSTER TAB (REFACTORED)
- Current + next assignment, vessel status
- Reads from new services/domain

D) HOURS OF REST TAB (REFACTORED)
- Preserve Rest Hours Grid UI
- Link rest logs to assignments
- Surface violations to planner + compliance

E) EMAIL NOTIFICATIONS (MANDATORY)
Use EXISTING email system ONLY.
Events:
- Assignment Created/Updated/Cancelled
- Schedule Published
- Compliance Warning
- Rest Hours Violation
Recipients:
- crew/admin/both with preferences respected
Audit:
- create AuditLog for each send (queued/sent/failed)

F) HYBRID OFFLINE-FIRST GUARANTEE
- Planner works offline
- Writes queued locally
- Sync via existing mechanism
- Conflicts surfaced, strategy unchanged

════════════════════════════════════
REFACTOR STRATEGY (IMPLEMENT IN ORDER)
════════════════════════════════════
PHASE 1 — Safety net
- Add golden tests around existing scheduling flows
- Add telemetry/logging around scheduling actions
- Introduce feature flag NEW_SCHEDULER_ENABLED

PHASE 2 — Domain layer (parallel run)
- Build domain constraints + scoring
- Compare vs legacy behavior and log diffs

PHASE 3 — Additive schema evolution
- Add missing fields/tables only if needed
- Ensure sync metadata compatibility

PHASE 4 — Write-path migration
- All scheduling mutations go through services + repository
- Legacy mapped via compatibility adapter

PHASE 5 — UI cutover
- New planner behind feature flag
- Gradual rollout

PHASE 6 — Cleanup
- Remove dead legacy scheduling logic after stability confirmed

════════════════════════════════════
TESTING REQUIREMENTS (MUST DO AND MUST RUN)
════════════════════════════════════
You MUST implement and run automated tests to verify all functionality.

1) UNIT TESTS (domain)
- overlap detection
- cert validity checks
- rest/fatigue rule checks
- suggestion ranking & explanations
- scoring determinism

2) INTEGRATION TESTS (services + data)
- create/update/move/delete assignment
- publish schedule (bulk)
- offline queue creation + replay on reconnect
- conflict resolution behavior consistent with existing strategy
- notification recipient resolution (crew/admin/both)
- ensure email sender invoked via existing pipeline (mock provider)

3) UI TESTS (if framework exists)
- planner renders roster timeline
- drag/drop triggers correct mutation calls
- constraint warnings display
- suggestions load and apply
- sync status indicator

4) END-TO-END / SMOKE TEST
- Create a schedule change → verify:
  - local/offline state updates
  - sync queue entry created
  - on “online”, server updated
  - notification event generated
  - audit logged
- Run the full test suite:
  - “npm test” / “pnpm test” / “pytest” or whatever exists in the repo
  - DO NOT claim completion until tests pass

DELIVERABLE:
- A final “TEST REPORT” section listing commands run and pass status.

════════════════════════════════════
ACCEPTANCE CRITERIA
════════════════════════════════════
- Offline scheduling works
- No double booking
- Cert + rest rules enforced
- Suggestions return in <1s typical
- Emails sent via existing system to crew/admin/both as configured
- Audit logs created for scheduling + notifications
- No regressions in existing workflows
- Tests executed and passing

════════════════════════════════════
NOW START
════════════════════════════════════
1) Scan the repo
2) Produce:
   - Scheduling Overhaul Assessment
   - Refactor & Cutover Plan
   - AI Reuse Assessment
3) STOP — do not write code yet
4) After approval in subsequent steps, implement phases 1–6 and finish with full test execution + TEST REPORT