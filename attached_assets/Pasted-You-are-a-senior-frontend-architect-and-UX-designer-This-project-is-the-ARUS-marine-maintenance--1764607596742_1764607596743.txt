You are a senior frontend architect and UX designer.

This project is the ARUS marine maintenance + fleet management app built with:

React + TypeScript + shadcn/ui + Tailwind

React Query for data layer

Electron shell for desktop

Node/Express backend (don’t change APIs unless absolutely required)



The current problem:

There are too many pages/tabs and a lot of overlap between views (Fleet Overview, Bridge View, Equipment Health, etc.). The UX feels fragmented and cluttered.



Your job:

👉 Analyze the existing UI, consolidate overlapping pages, and refactor into a smaller, clearer, more consistent set of screens – WITHOUT breaking existing functionality.

PHASE 1 – DISCOVERY & SAFETY CHECK (NO CODE CHANGES YET)

Scan the codebase and identify ALL top-level pages / routes / tabs, for example (names may vary):

Fleet Overview

Bridge View

Equipment Health

Telemetry / Live Data

Alerts / Notifications

Work Orders

Parts/Inventory

Crew Management

Crew Scheduler

Hours of Rest

Deck Log

Engine Log

Compliance / Reports

Settings

Analytics / Insights

Any duplicates like “Overview”, “Dashboard”, etc.

For each of these screens, answer:

What is its primary purpose?

What data does it show? (vessels, equipment, crew, logs, alerts, KPIs, etc.)

Which other screens show similar or overlapping data?

Produce a short UX audit summary (as markdown in a single file, e.g. docs/ux-audit.md) that includes:

A table of all pages

Their purpose

Overlaps with other pages

Candidate consolidations (e.g. “Fleet Overview + Bridge View + Equipment Health → single ‘Fleet Dashboard’ with tabs/sections”)



⚠️ In this phase: do not remove or rename any routes yet. Just analyze and write the report.

PHASE 2 – PROPOSE A CONSOLIDATED UX STRUCTURE



Based on the audit, design a simpler navigation with fewer, clearer “hubs”. For example (adjust to match reality):

Fleet Dashboard

Combines: Fleet Overview, Bridge View, Equipment Health, Alerts

Sections or sub-tabs:

Vessel List (status, health, location, alerts)

Selected Vessel Detail (map + KPIs + current alerts)

Equipment health summary

Maintenance

Combines: Work Orders, Equipment detail, Parts link

Tabs:

Active Work Orders

Completed Work Orders

By Vessel / By Equipment

Inventory

Parts, stock levels, locations, low-stock alerts

Crew & Compliance

Combines: Crew Management, Scheduler, Hours of Rest, Compliance alerts

Views:

Crew roster per vessel

Scheduler timeline

Hours-of-rest violations list

Compliance status summary

Logs

Deck Log

Engine Log

Log exports (PDF/Excel)

Analytics & Reports

Predictive maintenance insights

Trend charts

KPI summaries

Settings

Org / Vessel config

Integrations (StormGeo, FMCC, email alerts)

Notification rules

Update management (Electron)



Create this proposed IA in docs/ux-structure.md.

✅ Confirm in that doc that:

All existing functionality has a “home” in the new structure.

No critical feature disappears.

Nothing important is duplicated across hubs.

PHASE 3 – COMPONENT CONSOLIDATION (SHARED UI LIB)



Now create / enhance shared components so we don’t have 5 slightly different versions of the same thing:

In the UI layer, identify duplicate patterns and refactor into reusable components, such as:

<VesselHeader />

<VesselStatusCard />

<HealthKpiGrid />

<TelemetryWidget />

<AlertList />

<EquipmentTable />

<WorkOrderTable />

<InventoryTable />

<CrewScheduleTimeline />

<LogbookToolbar />

Replace duplicated JSX in Fleet/Bridge/Equipment screens with these shared components only if:

Props are compatible with existing data structures, OR

You provide non-breaking adapters that map old data → new props.

Ensure:

Consistent typography & spacing (Tailwind + shadcn)

Consistent icon usage (lucide-react)

Consistent filters/search bars

Sticky headers/tables where useful

Responsive layout for laptop screens (primary target)

PHASE 4 – ROUTE / PAGE CONSOLIDATION (NON-BREAKING)

Refactor pages so that:

Fleet Overview, Bridge View, and Equipment Health become one primary “Fleet Dashboard” screen with integrated sections or tabs.

Any other obviously redundant views are either:

Nested under a shared parent (e.g. “Logs” tab), or

Converted into sub-sections instead of separate top-level nav.

Preserve existing URLs/route names where possible. If you must change or merge routes:

Add a thin compatibility layer or redirects (if applicable).

Keep existing links/menu items working, but have them now point to the consolidated screen.

Make sure the main navigation clearly reflects the new structure:

No more than ~5–7 top-level tabs.

Everything else is reachable from within those hubs.

PHASE 5 – UX POLISH & REDUNDANCY CLEANUP

Remove:

Dead pages that are truly unused (only after confirming via search).

Duplicate filter components or ad-hoc variants of common views.

Add:

Consistent loading skeletons (shadcn Skeleton) for main views.

Clear empty states (“No work orders yet”, “No active alerts”, etc.).

Breadcrumbs or contextual headers where deep navigation exists.

Improve legibility:

Align tables and cards properly.

Don’t show too many columns by default; use column priority or grouping.

Make vessel selection and “current vessel context” obvious.

PHASE 6 – REGRESSION CHECKS



Before finalizing:

Confirm that from the new navigation, users can still:

See all vessels and their statuses

Open a single vessel and view details (health, location, telemetry)

View and manage work orders

View and manage inventory

View crew, scheduler, and hours-of-rest

Access deck and engine logbooks

Access compliance/alerts

Access settings

Run existing tests, and if missing, add a few basic UI tests:

Each new top-level hub renders without crashing.

Basic navigation between hubs works.

Update any in-app labels/tooltips to match the new structure.

OUTPUT EXPECTATIONS

docs/ux-audit.md – current pages + overlap analysis

docs/ux-structure.md – proposed consolidated IA + mapping from old pages → new hubs

Refactored React components:

Shared UI components for repeated patterns

New consolidated “Fleet Dashboard” and other hubs

Updated navigation/sidebar/top-nav reflecting the new, simpler structure

Removal or demotion of redundant/overlapping views

Zero loss of critical functionality; only consolidation and UX improvement



Do all of this incrementally and safely, preserving working features and only consolidating where you can prove compatibility with the current architecture and data models.

