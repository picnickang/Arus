You are Replit AI working on the ARUS / RecipeRealm project.

This app is complex: it’s a predictive-maintenance / fleet-management system with:

- Electron desktop app (embedded “VESSEL” mode, offline-first, local SQLite like `vessel-local.db`)
- Node/Express backend (`server/index.ts` → `server/index.js`)
- React + Vite frontend (built into `dist/public` then copied to `dist`)
- Dual-storage & sync (local SQLite + remote libSQL/Turso/Postgres via `@libsql/client` / Drizzle)
- MQTT-based reliable sync and telemetry ingestion
- Multi-tenant isolation via `x-org-id` (and possibly `x-vessel-id`)
- Background jobs (update scheduler, connection pool health checks, telemetry pruning)
- Offline/embedded vessel mode vs cloud/server deployment

My goal: **treat this as a full codebase review and hardening pass** so the app is “production-complete” for both desktop and cloud/web. I want you to *actively audit and fix* issues. Don’t only tweak a single file: reason across the whole project.

Please structure your work in stages:

====================================
STAGE 1 – ORIENTATION & INVENTORY
====================================

1. Scan the repo and identify, with file paths:

   - **Framework & tooling:**
     - Package manager and scripts (`package.json`).
     - Build configs (`vite.config.*`, `electron.vite.config.*`, any `tsconfig.*`, `esbuild` scripts).
     - Electron entry / main process sources (the TS/JS that generates `dist-electron/main.cjs`).

   - **Backend:**
     - Express server entry (`server/index.ts`, bundled `server/index.js`).
     - Route registration (where all `/api/**` routes are attached).
     - Middlewares for logging, error handling, CORS, rate limiting, tenant isolation (`requireOrgId`, `requireVesselId`, etc.).
     - Any sync / job / scheduler modules (connection pool health checks, update scheduler, telemetry pruning, sync manager).

   - **Databases & storage:**
     - Local SQLite / embedded DB initialization (e.g. `vessel-local.db` in a `data` folder).
     - Cloud DB / libSQL / Turso client configuration (look for `@libsql/client`, drizzle `libsql`, `DATABASE_URL`, `TURSO_*`, etc.).
     - Schema / migrations (SQL or Drizzle definitions for tables like `equipment`, `maintenance_schedules`, `update_settings`, etc.).
     - Any dual-write / dual-read abstraction (e.g. logs like `[DualWrite:equipment]`).

   - **Frontend:**
     - React app root (main entry, router, layout).
     - API client(s) (fetch/axios wrappers, React Query config).
     - Where base API URL is set (for Electron vs cloud).
     - Any service-worker / PWA pieces.

   - **Electron integration:**
     - Code that starts the Node backend when Electron launches (the dev command that runs `server/index-wrapper.js` or similar).
     - Logic that chooses which URL Electron loads (e.g. `http://localhost:5000` vs a `file://` URL in production).
     - Any preload scripts and Electron security settings.

2. Summarize to me (in comments or console log) a short inventory:
   - Where deployment mode and environment are determined.
   - Where DBs are initialized.
   - Where tenant/vessel isolation is enforced.
   - Where sync and background jobs are configured.
   - How the frontend finds the backend URL in dev vs prod, and in Electron vs browser.

====================================
STAGE 2 – DEPLOYMENT MODES & ENVIRONMENT CONFIG
====================================

Goal: **No more mode confusion.** The app must behave cleanly in:

- VESSEL / embedded (Electron + local SQLite, often offline, no Turso/libSQL).
- CLOUD / server (cloud DB, multiple tenants, accessed by browsers/mobile).

1. If not already present, create a single config module (e.g. `server/config/runtimeEnv.ts` or equivalent). It should:

   - Export clear flags:
     - `deploymentMode` (e.g. `"VESSEL"` | `"CLOUD"`).
     - `isVesselMode`, `isCloudMode`.
     - `localDbAvailable`, `cloudDbAvailable`.
   - Derive these from:
     - Env vars (`DEPLOYMENT_MODE`, `DATABASE_URL`, `TURSO_*`, etc.).
     - Presence of the local SQLite file.
   - This should be the **only** place where we interpret env vars and decide mode.

2. Refactor backend code to *always* consult this config:

   - Any libSQL / Turso / Postgres usage (`db.execute`, `LibSQL client`, Drizzle libsql sessions) must be guarded with `isCloudMode && cloudDbAvailable`.
   - In VESSEL mode, functions like `checkConnectionPoolHealth` must not call `db.execute` on a cloud client that doesn’t exist; instead:
     - Either do a no-op or skip with a clear log like `[Health] Skipping cloud DB check in vessel mode`.
   - Background jobs:
     - **Update scheduler** that queries `update_settings`:
       - Only start in cloud mode.
       - In vessel mode, do not query tables that don’t exist locally.
     - **Connection pool health checks**:
       - Only run against cloud DB when available.
     - **Sync manager with Turso/libSQL**:
       - Start only when the relevant env vars are present and cloud DB is configured.

3. Verify: after refactoring, there should be **no errors like**:
   - `TypeError: db.execute is not a function` in vessel mode.
   - `SQLITE_ERROR: no such table: update_settings` when running purely local.

====================================
STAGE 3 – DATABASES, SCHEMA & DUAL STORAGE
====================================

Goal: DB layer is consistent, safe, and ready for multi-device usage.

1. Inspect all schema definitions and migrations:

   - Ensure both local SQLite and cloud DB define:
     - Core domain tables: equipment, telemetry, alerts, maintenance schedules, crew, orgs, vessels, update_settings (if needed), etc.
   - Confirm that each table has:
     - Primary keys (prefer UUIDs or clear integer IDs).
     - Tenant scope columns (`orgId`) and, where appropriate, `vesselId`.
   - If a table is intended to exist in both local and cloud DB, but only exists in one place:
     - Either:
       - Add the missing table via migration; or
       - Explicitly mark it as “cloud-only” or “local-only” in comments and ensure code respects that.

2. Review all repository / data-access abstractions:

   - Identify which repos:
     - Are local-only (e.g. vessel telemetry simulator tables).
     - Are cloud-only (global settings, update scheduler).
     - Are “dual” (equipment, maintenance schedules, alerts, etc.).
   - For dual ones, enforce a clear pattern:
     - When in VESSEL mode:
       - Writes go to local SQLite.
       - Optionally queue changes for sync via MQTT / reliable sync.
     - When in CLOUD mode:
       - Writes go to cloud DB.
   - If you see ad-hoc direct DB queries scattered across routes:
     - Centralize them into repositories/services where feasible.
     - At minimum, ensure every query includes `orgId` and `vesselId` when relevant.

3. Fix any schema/DB bugs surfaced in logs or code, including:
   - Missing tables referenced in queries (`update_settings`, etc.).
   - Incorrect column names or types.
   - Queries that assume cloud DB in environments where only SQLite is present.

====================================
STAGE 4 – TENANT & VESSEL ISOLATION, AUTH & RATE LIMITING
====================================

1. Tenant/vessel isolation:

   - Locate the `requireOrgId` middleware (and any `requireVesselId`).
   - Confirm that **all sensitive API routes** use them:
     - `/api/equipment`, `/api/equipment/health`
     - `/api/alerts/*`
     - `/api/maintenance-schedules*`
     - `/api/inventory*`, `/api/finance*`, etc.
   - Ensure that every DB query relating to shared resources uses `orgId` (and `vesselId` when needed) in where clauses.

2. Ensure consistent header behavior:

   - Decide the canonical headers:
     - `x-org-id` (required in cloud mode).
     - `x-vessel-id` (required for vessel-specific data).
   - In the backend:
     - If in vessel embedded mode and we *know* the orgId/vesselId from config, allow those to be inferred to avoid constant 401s.
     - In cloud mode, 401 with `MISSING_ORG_ID` is valid **only when** it’s truly needed (external clients).
   - In the frontend API client:
     - Implement a centralized HTTP client that always attaches:
       - `x-org-id` (from user/org context).
       - `x-vessel-id` when in vessel-specific views.
     - Ensure Electron renderer and web/mobile use the same abstraction.

3. Rate limiting:

   - Find where rate limiting is implemented (the logs show 429 with `RATE_LIMIT_GENERAL`).
   - Review:
     - Default limits.
     - Which routes are limited.
   - Make sure we are not rate limiting critical internal polling too aggressively (e.g. alerts, equipment health in dashboards).
   - Avoid spamming logs with repeated 429s; consider:
     - Lower polling frequency on the frontend.
     - Adjusting rate limits and/or adding backoff.

====================================
STAGE 5 – BACKEND ROBUSTNESS & ERROR HANDLING
====================================

1. Global error handling:

   - Ensure there is an Express error handler registered **after** all routes and static serving.
   - It should:
     - Log errors with enough context.
     - Send clean JSON responses `{ code, message, error }`.
   - Make sure unhandled promise rejections or thrown errors from async routes are properly caught (e.g. via `express-async-errors` or explicit try/catch wrappers).

2. Health endpoints:

   - Implement or verify existence of endpoints like:
     - `/livez` – basic liveness, used by Electron health check.
     - `/readyz` – readiness (checks DB availability + any essential services).
     - `/api/health/deployment` – return an object with:
       - `deploymentMode`
       - `orgId` (if applicable)
       - `vesselId` (if applicable)
       - `localDbAvailable`
       - `cloudDbAvailable`
   - Make sure these work correctly in both VESSEL and CLOUD modes.

3. Background jobs:

   - For each scheduled job (telemetry pruning, update scheduler, connection pool checks, sync jobs):
     - Ensure it’s only started in appropriate mode.
     - Wrap logic in try/catch and log meaningful errors.
     - Avoid crashing the process if optional services fail.

4. MQTT & reliable sync:

   - Verify:
     - MQTT connection handling (handles offline broker, queues messages).
     - Queue directory configuration and error handling.
   - Ensure logs such as “[MQTT Reliable Sync] Reconnecting…” don’t spam indefinitely without any backoff or limit.

====================================
STAGE 6 – FRONTEND (REACT + VITE) COMPLETENESS
====================================

1. Build configuration:

   - Verify Vite config:
     - Correct `base` path for production.
     - Output to `dist/public`, then `post-build` script copies to `dist` (as already seen).
   - Confirm that final `dist/index.html` references assets with correct paths that match what Express serves.

2. Static serving:

   - Ensure Express static middleware is configured in the right order:
     - `express.static(staticRoot)` where `staticRoot` resolves to `/dist` (the directory with `index.html` and `assets/`).
     - SPA fallback (`app.get('*', ...)`) returning `index.html`.
     - Error handler AFTER the SPA fallback.
   - Confirm that requesting:
     - `/` returns `index.html` with status 200 and `Content-Type: text/html`.
     - `/assets/<css>` returns real CSS with `Content-Type: text/css` (no more `text/html` for CSS).

3. API client & error UX:

   - Implement a single HTTP client (or React Query `queryFn`) that:
     - Handles base URL per environment:
       - Electron dev: `http://localhost:<port>`.
       - Browser dev: `http://localhost:<dev-port>` or via proxy.
       - Production cloud: `https://<cloud-domain>`.
     - Automatically includes tenant/vessel headers.
   - Make sure the main dashboard:
     - Handles 429s and 401s gracefully (shows user-friendly message or banners, not infinite spinners).
     - Handles offline mode with clear indicators.

4. UI completeness:

   - For all main navigation sections (Fleet Overview, Maintenance, Equipment Registry, Crew, AI Insights, etc.):
     - Ensure there are no React errors in console.
     - All key routes render without crashing even with empty data (e.g. returning `[]` from `/api/equipment` is handled).
     - Add loading and empty states where missing.

====================================
STAGE 7 – ELECTRON APP: DEV & BUILD
====================================

1. Dev mode:

   - Electron should:
     - Start the embedded server (Node process).
     - Poll `/livez` until healthy.
     - Load `http://localhost:<port>` in the BrowserWindow.
   - Ensure:
     - No repeated “Cannot GET /” errors.
     - No blank blue screen due to CSS/JS failing to load.
     - Console in the Electron renderer has **no 404/500s for static assets**.

2. Production build:

   - Verify / configure packaging (e.g. `npm run electron:build` or electron-builder config).
   - In production Electron:
     - Decide whether to:
       - Serve local `file://` URL (bundled `index.html`).
       - Or run embedded server and still use `http://localhost:<port>`.
     - Whichever approach is chosen, ensure static paths line up so assets load correctly.

3. Security:

   - Check Electron BrowserWindow options and preload scripts:
     - `nodeIntegration` should be `false` in renderer.
     - `contextIsolation` should be `true`.
     - CSP in `index.html` should be reasonably restrictive (no unrestricted `unsafe-eval` unless absolutely necessary for dev).
   - Ensure any use of `ipcRenderer` / `ipcMain` is from preload, with a safe, typed API.

====================================
STAGE 8 – MULTI-DEVICE & MULTI-ENV STORY
====================================

1. Confirm the final behavior for:

   - Vessel / embedded (Mac Mini or similar):
     - Electron + server in VESSEL mode, local SQLite.
     - Optionally configured for upstream sync to cloud when network exists.
   - Shore / office:
     - Web app hitting cloud API (CLOUD mode).
   - Mobile:
     - Browser on mobile hitting the same cloud API.

2. Ensure base URLs, headers, and mode detection all align so that:

   - The same React bundle can talk to different backends depending on config.
   - Multi-device access to the same org/vessel:
     - Uses stable IDs.
     - Doesn’t create duplicate records.
     - Handles eventual consistency where sync is involved.

3. Create or update a root-level doc like `MULTI_DEVICE_ARCHITECTURE.md` summarizing:

   - Deployment modes.
   - How local vs cloud DB are used.
   - How tenants/vessels are separated.
   - What a new engineer must know to add a feature that works correctly across devices.

====================================
STAGE 9 – QUALITY GATES & FINAL CHECKS
====================================

1. Lint & format:

   - Run lint and format (ESLint, Prettier, or whatever the project uses).
   - Fix any violations that point to real bugs or dangerous patterns.

2. Tests / smoke checks:

   - If tests exist (`npm test`, etc.), run them and fix any failing ones.
   - If no tests, consider adding a **minimal smoke test suite** that:
     - Spins up the backend (in a test mode).
     - Calls a few key endpoints (`/livez`, `/api/equipment`, `/api/maintenance-schedules`, etc.).
     - Asserts correct status codes and basic shape of responses.

3. Manual sanity checklist (automate where possible, but at least ensure code supports this):

   - App installs (`npm install`) cleanly.
   - `npm run build:renderer` + `node scripts/post-build.js` works.
   - `npm run build:server` works.
   - `npm run build:electron-main` works.
   - `npx electron .` launches:
     - No fatal backend errors.
     - Frontend loads with no MIME or 404 errors for static assets.
   - Key flows:
     - Load Fleet Overview.
     - View equipment list & details (even if empty).
     - View maintenance schedules & upcoming list.
     - Verify alerts panel loads (even if empty).
   - Logs have **no repeating errors** about DB, sync, or missing tables in the normal happy path.

Throughout this process, do NOT just make superficial edits. Reason carefully, make coherent refactors, and keep behavior consistent between desktop/vessel and cloud/web deployments. Where behavior is ambiguous, prefer explicit guards, clear logs, and TODO comments rather than silent failure.

At the end, summarize the key fixes and remaining TODOs (if any) in a short bullet list.