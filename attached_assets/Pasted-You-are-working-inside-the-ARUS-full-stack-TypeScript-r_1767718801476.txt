You are working inside the ARUS full-stack TypeScript repo (PostgreSQL) that currently provisions DB indexes at application startup via server/db-indexes.ts using `CREATE INDEX IF NOT EXISTS ...`. 

Your task: implement Option A (gold standard) — **Migrations own index creation/changes**, and **boot-time only verifies** required indexes (and may auto-create ONLY in non-production).

HARD CONSTRAINTS
1) Do NOT change application behavior or data model.
2) Do NOT change existing query logic; only change how indexes are managed.
3) No breaking changes to build/run commands.
4) Production boot MUST NOT perform heavy DDL work. In prod it should verify + warn only.
5) Keep the existing self-healing spirit for dev/new DBs.

PHASE 0 — DISCOVERY
- Determine ORM/query layer (Drizzle/Prisma/Kysely/etc) and whether a migrations system already exists.
- Find existing DB bootstrap scripts and any schema migration tooling.
- Identify current index definitions (the 7 indexes listed in server/db-indexes.ts).

PHASE 1 — INTRODUCE/USE A MIGRATIONS SYSTEM
If a migrations system already exists:
- Use it.

If none exists:
- Add a minimal migrations setup consistent with the repo:
  - Prefer Drizzle migrations if Drizzle is used.
  - Prefer Prisma migrate if Prisma is used.
  - Otherwise implement a lightweight SQL migration runner:
    - migrations stored in `server/migrations/*.sql` (timestamped)
    - a `schema_migrations` table tracks applied migrations
    - a CLI script `server/scripts/migrate.ts` applies pending migrations safely

Add package.json scripts at the appropriate workspace root:
- "db:migrate": run migrations
- "db:migrate:status": show applied/pending
- "db:migrate:deploy": non-interactive apply (for CI/CD)

PHASE 2 — MOVE INDEX CREATION INTO MIGRATIONS
Create a new migration that creates the current indexes (7 total) using safe Postgres practices:
- In production, use `CREATE INDEX CONCURRENTLY` for large/hot tables where appropriate.
- Because `CONCURRENTLY` cannot run inside a transaction, ensure the migration runner supports “no-transaction” migrations OR split migrations:
  - One transactional migration for small tables/non-concurrent indexes
  - One non-transactional migration for concurrent indexes

Include IF NOT EXISTS only if compatible with your Postgres version and chosen syntax; otherwise implement “check then create” using `pg_indexes`/`to_regclass`.

Indexes to include (existing):
- idx_equipment_vessel_created on equipment(vessel_id, created_at DESC)
- idx_maintenance_records_equipment_date on maintenance_records(equipment_id, actual_start_time DESC)
- idx_maintenance_records_org_id on maintenance_records(org_id)
- idx_raw_telemetry_equipment_ts on raw_telemetry(src, ts DESC)
- idx_ml_models_org_status on ml_models(org_id, status)
- idx_pdm_alerts_asset_time on pdm_alerts(asset_id, at DESC)
- idx_pdm_alerts_vessel on pdm_alerts(vessel_name, at DESC)

PHASE 3 — CONVERT BOOT-TIME SCRIPT INTO A VERIFIER (NO DDL IN PROD)
Refactor server/db-indexes.ts into server/db-indexes-verify.ts:
- On boot, it checks that required indexes exist:
  - Use `SELECT to_regclass('public.idx_name')` or query `pg_class/pg_indexes`.
- If missing:
  - In production (NODE_ENV=production): log an ERROR with clear remediation:
    - “Run db:migrate” and identify missing index names
  - In non-production: it may optionally create missing indexes (DEV_SELF_HEAL=true), but by default it should still prefer migrations.

Add clear output:
- Verified indexes: list
- Missing indexes: list
- Status: OK/WARN/ERROR

Do NOT crash the app by default, but expose a single boolean return value so the caller can decide to fail fast if desired.

PHASE 4 — ADD AN OPERATOR-FRIENDLY HEALTH SIGNAL
Add a lightweight endpoint or log marker:
- `/health/db-indexes` (or equivalent) returns:
  - ok: true/false
  - missing: [idx...]
  - lastCheckedAt

If the backend already has a health endpoint, extend it.

PHASE 5 — UPDATE DOCS + RELEASE FLOW
Add docs:
- docs/db-indexing.md
Include:
- “Indexes are managed via migrations”
- How to run migrations locally
- How prod deploy should run db:migrate:deploy before app rollout
- How boot-time verification works and what errors mean

PHASE 6 — SAFETY CHECKS
- Ensure migration runner is idempotent and safe to re-run.
- Ensure concurrent index migrations are not executed inside a transaction.
- Ensure app startup is fast and does not perform DDL in production.
- Run: typecheck, build, and existing tests.

DELIVERABLES
1) A working migrations system (or integrated existing one) with scripts.
2) A migration that creates the 7 current indexes safely.
3) Boot-time verification script that checks indexes and warns in production.
4) Health signal + docs.
5) Confirm in code comments why this is “Option A” and why prod avoids DDL on boot.

BEGIN NOW. Execute Phase 0 discovery, then implement phases 1–6 end-to-end.