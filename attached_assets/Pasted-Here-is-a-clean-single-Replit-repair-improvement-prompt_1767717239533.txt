Here is a clean, single Replit repair / improvement prompt tailored exactly to the quality review you shared. It is structured so Replit assesses first, then fixes, and it aligns with your dual PostgreSQL/SQLite architecture and existing good practices.

You can paste this directly into Replit Agent.

⸻

🔧 Replit Quality Hardening & Refactor Prompt (Assessment-First)

You are a senior TypeScript + database architect reviewing a production maritime operations platform with:
	•	Node.js backend (TypeScript)
	•	PostgreSQL (cloud) + SQLite (vessel/offline) dual-mode storage
	•	React frontend with polling hooks
	•	Redis caching, Prometheus metrics, circuit breakers already in place

A quality review has identified specific, known issues. Your task is to assess, confirm, and then fix them in priority order, without breaking runtime behavior.

⸻

PHASE 1 — ASSESSMENT (DO NOT CHANGE CODE YET)

Before making changes:
	1.	Confirm findings
	•	Locate and analyze db-storage.ts
	•	Count adapters
	•	Identify domain boundaries (logbook, crew, inventory, work orders, ML, etc.)
	•	Review TypeScript errors in the storage layer
	•	Confirm errors are caused by PostgreSQL/SQLite type divergence
	•	Identify tables missing constraints:
	•	work_order_parts
	•	maintenance_costs
	•	ml_model_accuracy_history
	•	Identify frontend polling hooks using 30-second intervals
	•	Classify which data is slow-changing vs fast-changing
	2.	Produce a brief internal checklist (comments only) confirming:
	•	What is safe to refactor immediately
	•	What must be staged to avoid migration risk
	•	What should remain untouched for now

Only proceed once assessment confirms alignment with the findings.

⸻

PHASE 2 — FIXES (IN STRICT PRIORITY ORDER)

✅ Priority 1 — Reduce Frontend Polling Load (LOW RISK, HIGH IMPACT)
	1.	Locate polling hooks using 30s intervals.
	2.	Reclassify:
	•	Slow-changing data → 60–120s polling
	•	Inventory summaries
	•	Compliance dashboards
	•	Crew certificates
	•	ML accuracy metrics
	•	Near-real-time data → leave unchanged
	•	Live telemetry
	•	Active alerts
	3.	Where possible:
	•	Add conditional polling (pause when tab not visible)
	•	Use cache-first strategies where Redis already exists

⚠️ Do not change hook signatures or API responses.

⸻

✅ Priority 2 — Add Database Indexes (SAFE, NON-BREAKING)

Add non-breaking indexes only (no schema rewrites yet):
	1.	Identify high-frequency query patterns (ORDER BY, WHERE).
	2.	Add composite indexes such as:
	•	(vessel_id, created_at)
	•	(work_order_id, part_id)
	•	(equipment_id, timestamp)
	3.	Ensure:
	•	PostgreSQL migrations are additive
	•	SQLite schema mirrors indexes where supported

⚠️ Do not add foreign keys yet in this step.

⸻

✅ Priority 3 — Route Input Validation Consistency
	1.	Audit API routes.
	2.	Ensure every mutating route uses:
	•	Zod schema validation for request body + params
	3.	If validation exists but is inconsistent:
	•	Normalize structure
	•	Centralize shared schemas where appropriate

⚠️ Do not change API shapes.

⸻

✅ Priority 4 — Begin Storage Layer Modularization (STAGED)

The db-storage.ts file is known to be too large.

Do NOT fully rewrite it yet.

Instead:
	1.	Create domain adapter modules:
	•	storage/work-orders.adapter.ts
	•	storage/inventory.adapter.ts
	•	storage/logbook.adapter.ts
	•	storage/crew.adapter.ts
	•	storage/ml.adapter.ts
	2.	Move logic incrementally:
	•	Start with 1–2 low-risk domains
	3.	Re-export adapters through db-storage.ts to preserve API compatibility.

⚠️ Runtime behavior must remain identical.

⸻

PHASE 3 — TYPE SAFETY IMPROVEMENTS (NO RUNTIME CHANGES)

Address TypeScript noise caused by dual-DB mode:
	1.	Introduce explicit adapter interfaces:
	•	PostgresAdapter<T>
	•	SqliteAdapter<T>
	2.	Use:
	•	Narrowed generics
	•	Explicit return types
	3.	Reduce TypeScript errors without using any.

Goal: Developer confidence, not refactoring for elegance.

⸻

PHASE 4 — DEFERRED (DOCUMENT ONLY, DO NOT IMPLEMENT YET)

Document (but do not yet enforce):
	•	Foreign key additions for:
	•	work_order_parts
	•	maintenance_costs
	•	ml_model_accuracy_history
	•	Migration strategy:
	•	Backfill orphaned rows
	•	Add constraints safely in a future release

Add notes to a docs/db-hardening-plan.md.

⸻

SUCCESS CRITERIA

You are done when:
	•	Polling load is reduced without breaking UI
	•	Database query performance improves
	•	Route validation is consistent
	•	Storage layer begins modularization safely
	•	TypeScript errors are meaningfully reduced
	•	No runtime regressions occur
	•	A short summary is provided of:
	•	What was changed
	•	What was intentionally deferred
	•	What should be tackled next

⸻

⚠️ Constraints
	•	Do not remove Redis, Prometheus, or circuit breakers
	•	Do not rewrite schemas wholesale
	•	Do not introduce breaking migrations
	•	Prefer incremental, production-safe improvements

⸻

If you want, next I can:
	•	Turn this into a 2–3 week phased execution plan
	•	Estimate dev hours per fix
	•	Prepare a CTO-level quality report
	•	Convert this into a task list for a contractor team

Just tell me.