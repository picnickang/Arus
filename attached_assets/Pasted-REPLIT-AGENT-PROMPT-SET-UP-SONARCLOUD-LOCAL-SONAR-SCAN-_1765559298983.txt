REPLIT AGENT PROMPT — SET UP SONARCLOUD + LOCAL SONAR SCAN (REPLIT FRIENDLY)

You are working in the ARUS repo (Electron + Node + React + TypeScript). I want Sonar integrated so I can run scans from Replit and use SonarCloud to track issues and guide fixes.

Constraints
	•	Do not assume repo structure; discover it (client/server/electron/shared etc.)
	•	Do not add Docker-only dependencies.
	•	Keep changes minimal and reversible.
	•	Make it work with “New Code” focus (don’t block on legacy).

⸻

1) Discover project layout & tooling
	1.	Inspect package.json (and workspace configs if any) to detect:
	•	frontend root(s)
	•	backend root(s)
	•	electron root(s)
	•	test scripts, coverage tooling (vitest/jest/etc.)
	2.	Identify TS configs used (tsconfig.json, tsconfig.*.json).

⸻

2) Add Sonar scanner tooling

Add a dev dependency that allows running Sonar scans from the repo:
	•	Prefer sonarqube-scanner npm package (works without global installs)

Add npm scripts to the root package.json:
	•	"sonar": "node ./scripts/sonar-scan.js"
	•	"sonar:local": "SONAR_HOST_URL=http://localhost:9000 node ./scripts/sonar-scan.js" (optional fallback)
Do not break existing scripts.

Create scripts/sonar-scan.js that:
	•	reads env vars: SONAR_TOKEN, SONAR_ORG, SONAR_PROJECT_KEY
	•	validates they exist and prints a friendly error if missing
	•	runs the scanner with appropriate options

⸻

3) Create sonar-project configuration

Create sonar-project.properties at repo root.

It must:
	•	Set sonar.projectKey from env SONAR_PROJECT_KEY
	•	Set sonar.organization from env SONAR_ORG
	•	Set sonar.sources by auto-detecting actual source roots present (client/server/electron/shared) and including only those that exist
	•	Exclude generated/build folders:
	•	**/dist/**, **/build/**, **/out/**, **/.vite/**, **/.next/**, **/coverage/**, **/node_modules/**
	•	Exclude migrations/seeds if they cause noise (only if necessary)
	•	Include TS/TSX/JS/JSX
	•	If coverage exists (vitest/jest), configure coverage report path(s) if present:
	•	sonar.javascript.lcov.reportPaths=... (set only if file exists)

Important: Use “New Code” focus by documenting in a README note how to set it in SonarCloud UI (do not attempt API calls).

⸻

4) Make scans stable on Replit
	•	Ensure npm run sonar works even if tests aren’t run (coverage optional)
	•	If coverage is present, keep it; if not, don’t fail the scan
	•	Keep logs concise

⸻

5) Verification

After changes:
	1.	Run typecheck and lint (existing scripts)
	2.	Run npm run sonar (it will fail gracefully if env vars aren’t set)
	3.	Provide a short checklist to the user:
	•	which env vars to set in Replit secrets
	•	the exact command to run

⸻

Implement now.

⸻

After Replit finishes

Run:
	•	npm run sonar

If it says missing env vars, add them in Replit Secrets:
	•	SONAR_TOKEN
	•	SONAR_ORG
	•	SONAR_PROJECT_KEY

Then rerun.

⸻

How you’ll use Sonar to “debug” on Replit

Once it’s wired:
	•	Run npm run sonar after big refactors/modularization
	•	Fix issues in priority order:
	1.	Bugs / Vulnerabilities
	2.	Security Hotspots (especially Electron preload/IPC)
	3.	Code Smells with high impact (S3776/S2004)
	•	Keep the Quality Gate set to New Code so progress is steady