You are working on the ARUS Electron + Node/Express + React + React Query + Tailwind + TypeScript + SQLite/libSQL/Postgres + Drizzle app. The codebase is heavily modularized; do not assume file paths—discover them first. Keep each file under 500 lines.

The current UI has:
	•	A “Request Parts” dialog that only supports one part per request with free-text description
	•	A “Request External Service” dialog that is minimal (provider + scope + hours + quote)

Upgrade both to be production-grade.

HARD RULES
	•	Preserve existing architecture patterns, routing, and UI style (shadcn components).
	•	Do not break offline-first behavior.
	•	Do not remove existing functionality; extend it.
	•	Keep new code modular (<500 lines per file).
	•	After changes: run lint/typecheck/build and confirm UI flows work.

⸻

1) DISCOVERY PHASE
	1.	Locate the existing components/dialogs for:
	•	“Request Parts”
	•	“Request External Service”
	2.	Locate backend endpoints and DB tables used by these dialogs.
	3.	Locate the Parts Inventory module and APIs (search endpoint/list endpoint).
	4.	Identify how suppliers are linked to inventory items (if present), but do not change supplier logic in this task unless required for form submission.

⸻

2) PURCHASE REQUEST FORM UPGRADE (MULTI-LINE “CART” + INVENTORY DROPDOWN)

2.1 Data model

Implement a normalized line-item model:
	•	Create new table (or Drizzle schema) purchase_request_items (name can vary by conventions):
	•	id
	•	purchase_request_id (FK)
	•	inventory_item_id (nullable FK to parts/inventory table)
	•	description (string fallback; required if inventory_item_id is null)
	•	quantity (number)
	•	notes (nullable)
	•	created_at, updated_at

Update purchase request creation to accept multiple items.

2.2 API

Implement/extend backend endpoint to create PR with items in one request:
	•	POST /api/purchase-requests
Body should support:
	•	request metadata (vessel/department/etc if needed)
	•	items: Array<{ inventoryItemId?: string; description?: string; quantity: number; notes?: string }>
Validate:
	•	At least 1 item
	•	quantity > 0
	•	either inventoryItemId OR description must exist per item

2.3 UI

Refactor “Request Parts” dialog into:
	•	A line items editor:
	•	Add Item button
	•	Table/list of items with per-row controls:
	•	Inventory item selector (searchable dropdown/combobox) pulling from inventory API
	•	Auto-display part fields (part number, unit, current stock) read-only when selected
	•	Quantity input
	•	Notes input
	•	Remove row button
	•	Allow “Custom item” row where inventory selection is empty and description is typed manually
	•	Footer:
	•	Cancel
	•	Create Purchase Request (creates ONE PR with multiple items)

Ensure the dialog supports adding multiple parts in one request.

⸻

3) SERVICE REQUEST FORM UPGRADE (SMART + STRUCTURED INPUT)

3.1 Requirements

Upgrade “Request External Service” into a richer Service Request creation flow that captures:
	•	Service provider (required)
	•	Equipment selection (required: choose one or more equipment from Equipment module)
	•	Severity (critical/safety/general)
	•	Assistance required tags (multi-select):
	•	Servicing, Calibration, Pressure Test, Replacement, Certificate Renewal, Repair
	•	Diagnostics fields:
	•	symptom/issue description (required)
	•	probable cause (optional)
	•	action taken so far (optional)
	•	recurring defect yes/no
	•	Scheduling inputs:
	•	requested start date/time (optional)
	•	requested end date/time (optional)
	•	Estimated hours (optional numeric)
	•	Quoted amount (optional numeric)
	•	Notes (optional)
	•	MOC required yes/no + MOC number conditional
	•	If “Certificate Renewal” selected: allow adding one or more certificate items (name + expiry + remarks)

3.2 Data model & API

Implement SR → SO workflow:
	•	Ensure Service Requests can be stored as a structured entity (either:
A) a service_requests table + join table to equipment, OR
B) a Work Order subtype presented as SR in UI, with SR fields stored on WO)
Choose the approach that best matches existing code, but keep UX as “Service Request”.

Then implement creation of a Service Order from a Service Request:
	•	On SR submit, create a Service Order record linked to the SR/WO.
	•	Service Order must support cancellation and service time later (already requested).

3.3 UI

Refactor the dialog to be either:
	•	a 2–3 step wizard (Recommended), OR
	•	a single structured form with sections

At minimum include:
	•	Equipment selector (searchable)
	•	Severity badges
	•	Diagnostics panel
	•	Assistance tags
	•	Provider selector
	•	Scheduling fields
	•	Estimate fields (hours/quote)

The “Create Service Order” button should only be enabled when required fields are satisfied.

⸻

4) VALIDATION & UX REQUIREMENTS
	•	Inline validation messages on required fields
	•	Disable submit if invalid
	•	Toast notifications on success/fail
	•	Preserve existing Tailwind/shadcn design
	•	Reuse existing form patterns (react-hook-form + zod if used)

⸻

5) VERIFICATION

After implementation:
	1.	Create a Purchase Request with 3 items:
	•	2 from inventory dropdown
	•	1 custom item
Confirm one PR is created and items persist correctly.
	2.	Create a Service Request:
	•	select provider + equipment + severity + diagnostics
Confirm a Service Order is created and visible where service orders are listed.
	3.	Confirm no regression to existing PO/SO workflows.

Proceed to implement now, modularly, following repo conventions.
