You are a senior staff engineer completing the FINAL convergence pass on ARUS.

The system is already architecturally strong and guarded. Your job is to REMOVE the last remaining legacy and enforce a single clean system.

This is NOT a safe cleanup. This is a controlled, aggressive convergence.

-----------------------------------
CRITICAL RULES
-----------------------------------

1. Prefer deletion over adaptation.
2. Do NOT add new compatibility layers.
3. Do NOT keep "just in case" code.
4. Do NOT leave parallel implementations.
5. Preserve functionality, not legacy structure.
6. Every feature must have ONE path only.
7. After changes, run all guardrail checks and fix issues.

-----------------------------------
GOALS (ALL MUST BE COMPLETED)
-----------------------------------

### 1. STORAGE FACADE — FULL REMOVAL

- Locate all remaining imports of server/storage.ts
- Migrate every remaining usage to:
  - domain services
  - repositories/ports

- Then:
  EITHER
    - delete server/storage.ts
  OR
    - move it to server/legacy/legacyStorage.ts

- Ensure:
  - zero production imports remain
  - guardrails fail if reintroduced

-----------------------------------

### 2. ROUTE SYSTEM — SINGLE PATTERN

- Enforce domain-router-registry as the ONLY route pattern
- Remove:
  - legacy router wrappers
  - inline route registration (except bootstrap-only)

- Refactor remaining routes into domain structure

- Final state:
  - one clear route registration path
  - no ambiguity for developers

-----------------------------------

### 3. PDM DOMAIN — FULL CONVERGENCE (HIGHEST PRIORITY)

- Audit all PdM files:
  - pages
  - hooks
  - routes
  - services

- Identify duplicates and parallel flows

- Keep:
  - the most complete and correct path

- DELETE:
  - all legacy PdM pages
  - duplicate hooks
  - deprecated routes

- Ensure:
  - one PdM workflow per action
  - one API path
  - no fallback logic

-----------------------------------

### 4. REDIRECTS — NEAR ZERO

- Open client/src/routes/legacy-redirects.ts

- For each redirect:
  - check if still needed
  - if not → DELETE

- Update all internal navigation to use canonical routes directly

- Target:
  - reduce redirects to near-zero (only critical ones remain)

-----------------------------------

### 5. DRIFT BASELINE — REDUCE IT

- Analyze "allowed drift" entries

- Categorize:
  - quick fixes → fix immediately
  - medium → fix if safe
  - hard → leave but document

- Reduce total drift count significantly

- Add rule:
  - drift must NEVER increase

-----------------------------------

### 6. ENFORCEMENT HARDENING

Strengthen guardrails:

- storage imports → must fail if used
- schema imports → must fail outside boundaries
- domain boundaries → enforce strictly
- route pattern → detect non-registry routes

Update scripts if needed:
- npm run check:guards must catch all violations

-----------------------------------

### 7. CLEANUP + CLARITY

- Remove dead files
- Remove deprecated comments that are no longer true
- Update documentation:
  - architecture rules
  - approved patterns
  - domain ownership

-----------------------------------

WORKFLOW

1. Analyze current state
2. Execute changes domain-by-domain
3. After each change:
   - run checks
   - fix issues
4. Do not stop at partial fixes
5. Finish ALL items

-----------------------------------

FINAL OUTPUT (REQUIRED)

Provide:

1. Files DELETED (list)
2. Files MODIFIED (list + purpose)
3. What legacy systems were fully removed
4. Remaining intentional exceptions (if any)
5. Guardrail results after changes
6. Confirmation:
   - single path per feature
   - no fallback logic
   - no legacy duplication

-----------------------------------

SUCCESS CRITERIA

- Storage facade removed or isolated with zero usage
- Domain router registry is the ONLY route pattern
- PdM domain has ONE workflow per action
- Redirect count near-zero
- Drift baseline reduced
- All guardrails pass
- No regressions

-----------------------------------

Do not stop at analysis. Execute the changes.