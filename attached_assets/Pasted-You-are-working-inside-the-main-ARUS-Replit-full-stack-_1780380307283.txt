You are working inside the main ARUS Replit full-stack project.

Goal:
Update the main Replit project with the hardened Crew Management / Role Dashboard / Safety Permission gap-closure patch.

Use the hardened update archive as the source of truth:

- arus-replit-gap-closure-hardened.tar.gz

Also review the included hardening report if available:

- crew-management-gap-closure-hardening.md

Your task:
Merge the hardened update into the existing main Replit file tree safely. Do not create a parallel app, duplicate source tree, or disconnected patch folder. The main app should run using the hardened implementation after this update.

Important:
Do not blindly overwrite the whole project. Inspect, compare, and merge file-by-file. Preserve unrelated improvements already present in the main Replit tree.

Core issues this update is intended to close:
1. Missing `listFormerCrewAccessRisks` backend method.
2. Incorrect former-crew active-login detection based only on `userId`.
3. Former Crew view incorrectly using active-roster access-readiness filters.
4. Onboarding checklist marking login complete when status is actually `no_login`.
5. Deployment readiness being overstated while documents/certificates are pending or not assessed.
6. Temporary password state being treated as a failed/missing password state.
7. Offboarding not clearly surfacing what access was revoked.
8. Offboarding leaving high-risk access/roles that could revive later.
9. UserAccessEditor closing even after partial access-save failure.
10. Safety alarm resolution notes not being persisted/displayed.
11. Safety backend permissions being too broad and not aligned with UI permissions.
12. Missing safety permissions in the central permission registry.
13. On Duty using destructive/red visual styling.
14. Role/dashboard/crew roster gaps remaining after the previous update.

Merge requirements:

1. Inspect the current main project

Before applying anything:
- list the current relevant files
- identify existing Crew Management, role dashboard, safety, permission, and migration files
- identify whether prior update code already exists
- check for conflicting versions of the same component/service/route

Relevant areas include:
- client/src/pages/CrewManagement.tsx
- client/src/components/UnifiedCrewManagement/**
- client/src/components/crew/**
- client/src/components/safety/**
- client/src/hooks/useCrewManagementData.ts
- client/src/pages/home.tsx
- client/src/components/admin/**
- client/src/config/**
- shared/**
- server/routes/**
- server/services/**
- server/repositories/**
- server/middleware/**
- server/auth/**
- server/db/**
- server/migrations/**
- permission registry / role templates / role dashboard config files

2. Apply the hardened Crew Management changes

The final `/crew-management` route must support:
- Active Roster
- Former Crew
- accurate filtering
- accurate sorting
- accurate result count
- export matching the visible rows
- access readiness badges for active crew
- former crew access-risk review
- onboarding checklist
- safe offboarding
- offboarding result summary
- vessel readiness panel
- mobile crew cards
- Safety tab with Current Alarms and Alert Log
- alert resolution notes
- permission-gated safety alarm type management

Do not leave obsolete duplicate Crew Management components imported anywhere.

3. Implement former crew access-risk support

Make sure the backend has a working method equivalent to:

- `listFormerCrewAccessRisks`

This must not infer active login from `userId`.

Correct behavior:
- A former crew member with a linked user account but disabled login is not an active-login risk.
- A former crew member is only an active-login risk if the linked account is actually login-enabled/active.
- Former crew access-risk data should include, where possible:
  - linked login exists
  - login enabled
  - vessel access remaining
  - fleet-wide access remaining
  - hub/admin access remaining
  - additional role assignments remaining
  - primary role still privileged
  - recommended action

Add the smallest backend/repository changes needed to support this.

4. Separate Active Roster readiness from Former Crew risk review

Active Roster:
- may use normal access-readiness states:
  - ready
  - no login
  - login disabled
  - no password set
  - temporary password issued
  - password change required
  - no vessel scope
  - no dashboard/role
  - fleet-scope review

Former Crew:
- must not use active-only access-readiness data.
- should use former-specific risk filters:
  - Former with linked login
  - Former with login still enabled
  - Former with vessel access remaining
  - Former with hub/admin access remaining
  - Former with additional roles remaining

Acceptance:
- Former Crew rows do not disappear because active access-readiness records are missing.
- Former Crew filters operate on former crew risk data only.

5. Fix onboarding checklist logic

The onboarding checklist must separate:
- Login created
- Login intentionally skipped
- Login missing

Do not mark login complete just because an access object exists.

Correct behavior:
- `no_login` means login is missing unless the admin explicitly skipped login.
- “Skipped” should only appear if there is an explicit skip state or admin action.
- If login is enabled, checklist should evaluate:
  - role/dashboard present
  - vessel scope present unless intentionally fleet-wide
  - temporary password issued or valid password state
  - login not disabled

Acceptance:
- A crew member with `no_login` does not show the login checklist item as complete.
- The checklist tells the admin what remains incomplete.

6. Fix deployment/document readiness wording

Do not claim full Deployment Ready unless documents/certifications have actually been checked.

If a real document readiness engine exists, use it:
- ready
- missing
- expired
- expiring soon
- pending
- not assessed

If real document readiness is not implemented yet, use honest wording:
- Profile Ready
- Access Ready
- Documents Pending / Not Assessed
- Deployment Ready: Pending document review

Acceptance:
- The UI does not show “Deployment Ready” while documents/certs are pending or not assessed.
- Readiness badges do not falsely imply maritime compliance.

7. Clarify password/access readiness states

Split password states into clear operational states:
- No password set
- Temporary password issued
- Password change required by user
- Login disabled
- No login
- Ready

Operational meaning:
- “No password set” is an admin blocker.
- “Temporary password issued” means admin setup is complete but user action may be pending.
- “Password change required” is user-pending, not the same as no password.

Acceptance:
- New crew with a temporary password do not look like setup failed.
- Access editor clearly explains the next action.

8. Harden offboarding

When crew is retired, cancelled, or marked former:
- end duty status
- disable linked login by default
- remove vessel access by default
- remove fleet-wide scope by default where applicable
- remove dashboard/admin/hub access by default
- clear additional roles where appropriate
- downgrade primary role to the lowest safe role where appropriate
- preserve profile, history, documents, alerts, and audit records
- do not hard-delete unless explicitly confirmed

After offboarding, show a clear result summary:
- Login disabled: yes/no/not applicable
- Vessel access removed: yes/no/not applicable
- Fleet access removed: yes/no/not applicable
- Dashboard/admin/hub access removed: yes/no/not applicable
- Additional roles cleared: yes/no/not applicable
- Primary role downgraded: yes/no/not applicable
- Duty ended: yes/no
- Records preserved: yes
- Any failures/skipped actions

Reinstatement:
- Do not silently restore old high-risk access.
- Require explicit access review before login or admin/hub access is re-enabled.

Acceptance:
- Former crew cannot accidentally retain live access.
- Reinstating someone does not revive old access without admin action.
- Admin can see what offboarding actually changed.

9. Fix UserAccessEditor partial-save behavior

If access save involves multiple API calls, do not close the dialog after partial failure.

Required:
- Show which steps succeeded and failed.
- Keep the dialog open if any step failed.
- Only call parent `onSaved` / close behavior after full success or explicit admin close.
- Make partial failure obvious with toast/result panel.

Acceptance:
- Admin does not lose visibility into failed role/vessel/password/hub access saves.

10. Persist and display safety alarm resolution notes

Add/merge support for a `resolution_note` / `resolutionNote` field.

Required:
- database schema/migration
- repository update
- clear-alarm route update
- DTO/API response update
- Alert Log UI update

Use the existing migration style. The hardened archive may include:

- server/migrations/027-safety-alarm-resolution-note.sql

Merge this migration safely. If the main project already has migration 027, renumber consistently.

Alert Log must show:
- triggered time
- cleared time
- vessel/fleet scope
- severity
- mode: real/drill/test
- status
- triggered by
- cleared by
- acknowledgement count
- outstanding acknowledgements if available
- resolution/clear note
- expandable details

Acceptance:
- A cleared alarm’s resolution note is visible in the Alert Log.
- Resolution note is not only written to server logs.

11. Split safety permissions

Backend and UI must align around these permissions:
- safety_alarms:view
- safety_alarms:trigger
- safety_alarms:clear
- safety_alarms:acknowledge
- safety_alarms:export
- safety_alarm_types:view
- safety_alarm_types:manage

Do not use one broad role gate for everything.

Expected policy:
- Captains, chief engineers, and fleet managers may view/trigger/acknowledge/clear alarms only if the role template grants those permissions.
- They must not create/edit/delete alarm types unless explicitly granted `safety_alarm_types:manage`.
- Super admin/system/company/admin roles may manage alarm types only through the correct permission.
- UI must hide/disable controls the current user lacks.
- Backend must enforce the same permission checks.

Acceptance:
- User without `safety_alarm_types:manage` cannot create/edit/delete alarm types through API.
- User without `safety_alarms:clear` cannot clear alarms.
- User with `safety_alarms:trigger` can trigger alarms if intended, even if not system admin.
- Super admin is not blocked by stale hardcoded role arrays.

12. Update central permission registry and role templates

Add the safety permissions to the central permission registry/config, not only UI checks.

Ensure role templates, dashboard config, navigation policy, and backend authorization agree.

Review and align role vocabulary across:
- shared role dashboard config
- client role dashboard config
- role templates
- permission registry
- role-auth middleware
- navigation/sidebar visibility
- admin role editor
- home dashboard role resolution
- crew access editor

Important roles include:
- super_admin
- system_admin
- company_admin
- admin
- fleet_manager
- captain
- chief_engineer
- vessel_master if still used
- logistics_user
- procurement_user
- maintenance_planner
- viewer
- crew/user roles currently present in the app

Acceptance:
- No role appears in dashboard config but is impossible to assign.
- No role appears in backend templates but is missing from UI selectors.
- Super admin can configure role dashboards and permissions.
- Admin users only see hubs/modules granted by super admin.

13. Preserve and integrate role dashboard updates

The main dashboard should support role-specific dashboard layouts.

Ensure:
- every user has a dashboard
- different roles see different widgets/data
- logistics roles see logistics-related information
- crew roles see crew-specific information
- maintenance roles see maintenance-related information
- admin roles see permitted admin modules
- hubs are accessible to admin users only if super admin grants access
- super admin can configure what each role sees/has access to

The role-dashboard admin UI should support:
- visible widgets
- widget order
- task sources
- high-impact questions
- labels/descriptions
- role-based dashboard templates

Acceptance:
- Home/dashboard rendering uses effective role/dashboard config.
- Role dashboard configuration persists through the intended API/storage mechanism.
- Missing dashboard config falls back safely.

14. Preserve role-grouped user/crew roster

The admin user/crew roster should group user profiles into tables by assigned role.

Ensure it shows:
- username/name
- primary role
- secondary roles
- vessel scope
- department
- supervisor
- login active/disabled state
- last login
- password change required
- dashboard profile
- access/readiness risk where relevant

Acceptance:
- Users are grouped by role.
- Users with multiple roles are handled clearly.
- Former crew access risks are visible separately from active user readiness.

15. Clean up visual semantics

Use consistent status styling:
- Red/destructive: emergency, expired, disabled, missing critical info, failed, unsafe
- Amber/yellow: pending, warning, expiring soon, user action required
- Green: ready, active, verified
- Blue/neutral: on duty, assigned, informational
- Muted/gray: former, inactive, archived, not assessed

Acceptance:
- On Duty is not destructive/red.
- Drill/test safety alarms are visually distinct from real alarms.
- Blocking readiness states are visually stronger than normal states.

16. Validate imports and remove duplicate old code

After merging:
- fix stale imports
- remove unused duplicate Crew Management components where safe
- ensure no route points to the old implementation
- ensure shared client/server types match
- ensure server routes compile
- ensure migrations do not conflict
- ensure no duplicate permission definitions conflict

17. Run validation

Run the best available validation commands.

Try:
- npm run check
- npm run build
- npm test, if available
- lint command, if available

If dependencies are missing in Replit, install or use the project’s intended install command first.

Also perform manual/static checks:
- `/crew-management` route resolves
- home dashboard resolves
- admin role dashboard tab resolves
- safety alarm API routes compile
- former access-risk endpoint works
- no duplicate obsolete route is active

18. Add or update QA notes

Minimum QA scenarios:
1. Active roster count/table/export all match.
2. Former Crew sorting works.
3. Former Crew export exports only visible former crew.
4. Former Crew does not use active-only access-readiness filters.
5. Former Crew access-risk filters work.
6. Former crew with disabled login is not counted as active-login risk.
7. Former crew with enabled login is counted as access risk.
8. Onboarding checklist does not mark `no_login` as complete.
9. Explicit skipped login is different from missing login.
10. Deployment readiness is not Ready when documents are pending/not assessed.
11. Temporary password issued is distinct from no password set.
12. Offboarding result summary shows revoked access.
13. Reinstatement does not silently restore old admin/vessel access.
14. Access editor stays open after partial save failure.
15. Cleared safety alarm displays resolution note in Alert Log.
16. Captain/fleet manager cannot manage alarm types unless explicitly permitted.
17. User without safety clear permission cannot clear alarms.
18. User with safety trigger permission can trigger alarms if intended.
19. Super admin can access/configure safety and role dashboards according to permissions.
20. On Duty badge is not destructive/red.
21. Mobile roster renders as cards.
22. Role dashboards differ by role.
23. Admin users only see hubs/modules granted by super admin.
24. Users are grouped by role in the admin roster.

Deliverables:
When complete, provide:
1. Summary of changed files.
2. Summary of removed/deprecated duplicate files.
3. Explanation of how the hardened archive was merged into the main Replit file tree.
4. Explanation of backend/API/schema changes.
5. Explanation of former crew access-risk logic.
6. Explanation of onboarding/readiness logic.
7. Explanation of offboarding/reinstatement behavior.
8. Explanation of safety permission changes.
9. Explanation of role dashboard/user roster changes.
10. Validation commands run and results.
11. QA checklist results.
12. Known limitations or follow-up recommendations.

Priority order:
1. Compile/blocker fixes.
2. Former crew access-risk correctness.
3. Former Crew filter correctness.
4. Onboarding checklist correctness.
5. Deployment/document readiness honesty.
6. Offboarding revocation and result summary.
7. Safety resolution notes.
8. Safety permission split.
9. Permission registry/role template alignment.
10. Role dashboard/user roster integration.
11. Access editor partial-save behavior.
12. Visual semantics/mobile polish.
13. Tests/QA notes.

Final requirement:
The main Replit app must use the hardened Crew Management, Role Dashboard, Former Crew Access Risk, and Safety Alert Log implementation. Do not leave the update as a separate archive or unused file tree.