You are a senior systems architect + data/platform engineer. You are working inside an existing Node.js/TypeScript full-stack app for ARUS (vessel/equipment telemetry + PdM). Your job is to REVIEW the current telemetry ingestion architecture in this repo and recommend improvements that are realistically compatible with the current codebase.

GOALS
1) Precisely map the current telemetry ingestion pipeline end-to-end:
   - entrypoints (HTTP/MQTT/websocket/etc.)
   - auth and device identity model
   - payload formats and schemas
   - parsing/decoding + validation
   - dedup/idempotency
   - buffering/backpressure
   - storage tables (raw vs decoded signals) and indexes
   - any background workers/queues
   - observability (logs/metrics) and error handling
2) Compare current pipeline with recommended “ideal” ingestion:
   - edge buffering / store-and-forward
   - ingestion gateway separation
   - raw immutable archive vs hot time-series store
   - streaming backbone (Kafka/Redpanda) OPTIONAL, not required
   - schema versioning / registry approach
   - data quality checks + DLQ
3) Determine which improvements will WORK with the existing architecture and will HELP, ranked by ROI and implementation effort.
4) Produce a concrete implementation plan with:
   - quick wins (1–3 days)
   - medium changes (1–2 weeks)
   - longer-term scale changes (1–2 months)
5) Generate patch-ready code changes ONLY after analysis:
   - propose exact file edits (paths, functions)
   - add any missing DB tables / indexes (idempotent CREATE IF NOT EXISTS style like the repo uses)
   - add tests: telemetry ingestion integration test + data integrity checks
   - add minimal observability: metrics counters + structured logs

CONSTRAINTS
- Do NOT rewrite the whole system.
- Prefer incremental changes compatible with current DB (likely Postgres/Timescale) and current Node/TS stack.
- Keep ingestion path fast: avoid heavy synchronous decoding in request handlers unless already present.
- Keep changes safe and reversible behind feature flags when possible.
- Respect the repo’s existing patterns (index provisioning at startup, current logging style, folder structure).

REPO DISCOVERY TASKS (DO THESE FIRST)
A) Search for telemetry-related code by scanning for:
   - "telemetry", "raw_telemetry", "ingest", "ingestion", "sensor", "can", "j1939", "j1587", "mqtt", "webhook"
   - API routes/controllers that accept telemetry
   - DB schema files: tables raw_telemetry, decoded_signals, equipment, vessels, pdm_alerts
   - job queues/workers/cron tasks
B) Identify the precise DB schema for telemetry tables and any retention policies.
C) Identify expected throughput assumptions if present (batch size, rate limits).

DELIVERABLES
1) Architecture map (bullet list + diagram in text) of current telemetry ingestion.
2) Gap analysis table: Current vs Ideal, Impact, Effort, Risk.
3) Recommended upgrades (ranked):
   - MUST DO now
   - SHOULD DO soon
   - NICE TO HAVE later
4) Concrete patch plan:
   - New/modified DB tables/indexes (idempotent SQL)
   - Code changes with file paths and pseudo-diffs
   - Test plan: ingestion integration test + persistence verification + dedup test
   - Observability additions

IMPORTANT DESIGN GUIDELINES TO APPLY
- Introduce idempotency: prefer device_sequence + src + ts + signal_id unique constraint OR a computed event_id hash.
- Split raw vs decoded: raw payload stored first; decoding can be async in worker.
- Add DLQ for bad payloads: store the raw payload + error.
- Add “last seen per device/equipment” tracking for freshness monitoring.
- Add backpressure: accept batches, enqueue, and respond quickly.
- Ensure time correctness: handle timezone + clock skew; store ts in UTC.

NOW EXECUTE:
Start by listing the key files you found and summarizing the current pipeline. Then produce the deliverables above.

When you propose changes, keep them minimal, incremental, and aligned to the existing codebase patterns.