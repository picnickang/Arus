You are an expert full-stack engineer (Node.js, TypeScript, React, Electron, SQLite/PostgreSQL, MQTT/Modbus/CAN, telemetry pipelines) working on a maritime predictive-maintenance / vessel-management app called ARUS.



Your job now is to scan this entire Replit project for weak points that could cause crashes, freezes, or performance problems when full telemetry is streaming, and then propose and implement a safe hardening plan, with minimal disruption to the current architecture.

0. High-level goals



This app supports:

Live telemetry ingestion (MQTT/Modbus/CAN/J1939 etc.)

Local SQLite (vessel / embedded mode) + optional PostgreSQL/libSQL

Electron desktop app (renderer + backend server)

Predictive maintenance + digital twin + logbooks (deck / engine)

Auto-fill of logs (engine room, deck, fuel/emissions, etc.)



I need you to:

Find all weak points in telemetry handling and related features that might cause:

UI freezes

Memory bloat

Crashes under heavy load

Verify your changes align with the existing architecture (embedded, offline-first, dual DB, etc.) and don’t overlap features that already exist.

Implement hardening fixes around:

Batching

Backpressure & queue limits

Downsampling / aggregation

Renderer vs server responsibility

Stress testing



Work in small, clearly-explained commits / steps and keep the code consistent with the existing style.

1. Discover the telemetry architecture and weak points

Scan the codebase and identify:

where telemetry is ingested (MQTT, Modbus, CAN, FMCC, simulators, etc.)

where telemetry is written to the database

where telemetry is used to drive UI (dashboards, live charts, log auto-fill, alerts, digital twin, engine logbook, deck logbook).

Specifically locate and review:

MQTT / Modbus / J1939 ingestion services

Any vessel telemetry simulator modules

Any telemetry queue or “reliable sync” implementations

Any scheduled telemetry jobs (pruning, syncing, digital twin updates, ML pipelines, etc.)

Any renderer-side telemetry processing or heavy analytics logic.

For each relevant module, note:

Does it write per-packet directly to the DB?

Does it batch writes?

Is there any backpressure or max queue size?

Are there any “unbounded” arrays, maps, or in-memory caches?

Does any heavy telemetry computation run inside the Electron renderer process?

Output a short summary of the telemetry pipeline:

Ingestion sources

Queues / buffers

Storage layer (tables, schemas)

UI consumption

Any current controls for load / rate limiting



Do NOT change code yet. First, produce a concise textual summary of what you found.

2. Design a minimal, compatible hardening plan



Based on the scan, design a concrete plan that does NOT break the existing ARUS architecture:

Keep:

Embedded / vessel mode

SQLite WAL mode and performance tuning

Optional PostgreSQL/libSQL / cloud sync paths

Existing logbook auto-fill behavior

Existing domain concepts (equipment, sensors, digital twin, ML, etc.)



Your plan MUST include:

Telemetry batching

Introduce a small, in-process buffer for incoming telemetry.

Write to DB every X ms (e.g. 250–1000ms) instead of per packet.

Make X configurable via env/settings.

Backpressure and queue limits

Define a max queue size for telemetry buffers (e.g. N records).

If queue exceeds this:

Drop oldest entries (ring buffer) OR

Coalesce them into aggregated snapshots (min/avg/max).

Downsampling & aggregation

For high-frequency signals, store:

A sampled or aggregated point every N seconds,

Or compute summarised windows (avg / min / max).

Ensure this remains compatible with:

Engine logbook autofill endpoint (/api/logbook/engine/autofill if present)

Deck logbook autofill

Digital twin calculations

Renderer vs server separation

Identify and move any heavy telemetry/analytics computations out of the Electron renderer and into:

The Node server process or

A dedicated service/module.

The renderer should only:

Subscribe to pre-processed, filtered, or aggregated telemetry

Render charts / tables / alerts.

Health monitoring & safety

Add logging around:

Telemetry buffer size

Dropped/aggregated packets count

Time per batch write

Add safeguards to avoid infinite retries or tight loops.



Propose this plan in plain language, referencing actual file paths and functions you’ve found. Wait to implement until after you’ve shown the plan.

3. Implement batching & backpressure (without breaking compatibility)



Once the plan is approved in your own reasoning, implement it:

Introduce or update a telemetry buffer manager module:

Single responsibility: buffer, batch, flush to DB.

Configurable:

BATCH_INTERVAL_MS

MAX_BUFFER_SIZE

Extensible for multiple streams (engine, generators, fuel, emissions, location, FMCC, etc.)

Replace any per-packet DB writes in telemetry ingestion with calls to this buffer manager.

Implement backpressure:

If buffer size exceeds MAX_BUFFER_SIZE:

Use a ring buffer or

Aggregate older values (per signal, per time window) and retain summaries.

Ensure:

No busy loops

No blocking on flush

No long-running operations on the main thread



Add comments explaining:

The trade-off between precision vs performance

How to tune buffer / batch settings.

4. Implement downsampling / aggregation for high-freq signals



Identify telemetry signals with high frequency (e.g. engine RPM, load, temperatures):

Implement an aggregation layer that:

Groups measurements by signal + time window (e.g. 1s, 5s)

Stores:

avg

min

max

count

Exposes aggregated data to:

Auto-fill endpoints

Dashboards

Digital twin

Ensure this does not break:

Engine logbook auto-fill logic (e.g. /api/logbook/engine/autofill)

Daily summary logic (e.g. computing running hours, averages)

Compliance or reporting endpoints.



If there is already an aggregation layer or simulator logic, reuse and extend it instead of duplicating.

5. Move heavy calculations off the Electron renderer



Scan the renderer/React/Electron UI code for:

Telemetry transformations

Chart aggregation logic

Any loops over large telemetry arrays

Any ML / anomaly detection / digital twin computation



For any expensive logic:

Move it into:

Node server controllers, or

A shared Node module imported from the server side.

Expose lightweight APIs to the renderer:

/api/telemetry/latest

/api/telemetry/summary

/api/telemetry/chart-data

Update React components to:

Only fetch processed data from APIs

Not maintain large in-memory telemetry arrays

Rely on pagination/windowing for charts.

6. Create telemetry stress tests / simulators



Add or enhance a telemetry stress test harness that:

Simulates:

High-rate telemetry (e.g. 100–1000 messages/sec)

Multi-sensor, multi-vessel data

Sends data through the same ingestion path as real telemetry:

No special shortcuts that bypass the real pipeline.



Include tests or scripts to:

Measure:

CPU usage

Memory usage

DB write throughput

Detect:

Buffer overflow

Slow DB writes

Event loop blocking



If there is already a “VesselSimulator” or similar, extend it instead of recreating it.

7. Guard against crashes, log health, and document configuration



Add:

Health endpoints / logs:

Telemetry buffer length

Dropped messages count

Average batch flush time

Any DB errors or retries

Config documentation:

Where and how to tune:

BATCH_INTERVAL_MS

MAX_BUFFER_SIZE

Downsampling window sizes

Recommended values for:

Low-end vessel hardware

High-end office hardware

Comments + short README section specifically about telemetry resiliency:

What you’ve implemented

How it prevents crashes under full load

How to extend it for new telemetry sources (e.g. FMCC fuel meter, GPS, etc.)

8. Safety checks and final review



Before you finish:

Ensure:

All existing tests pass (if present).

No TypeScript/JS errors.

No imports are broken.

Confirm:

Offline/embedded mode still works.

SQLite WAL + performance settings untouched.

Optional PostgreSQL/libSQL integration still compiles.

Summarize:

What weak points you found.

What you changed.

How to revert/tune if performance issues still arise.



Output:

A short human-readable summary of:

Detected bottlenecks

Implemented fixes

Files/modules modified

How to run the stress tests and interpret their output.



Only then, stop.