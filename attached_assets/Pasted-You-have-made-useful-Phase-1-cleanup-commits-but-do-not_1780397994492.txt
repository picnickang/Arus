You have made useful Phase 1 cleanup commits, but do not continue deleting or consolidating more UI until the current behavior is fully verified.

The screenshots show:
- Equipment Intelligence is now the canonical risk/health view.
- PdM duplicate routes were removed or redirected.
- PdM lifecycle mutations now use requirePermission("predictive_maintenance", "manage_config").
- Some dead/misleading buttons and KPI cards were corrected.
- However, runtime navigation was not fully verified because the login gate prevented deeper nav testing.
- Existing seeded orgs do not auto-reseed role templates, meaning existing admins may lose predictive_maintenance manage_config access unless permissions are manually re-granted.

Your task now is to perform a verification and stabilization pass before any further UI simplification.

PHASE 1 — VERIFY AUTHENTICATED RUNTIME NAVIGATION

Create or use test accounts for these personas:

1. Super Admin
2. Admin with all hubs granted
3. Admin with only Maintenance granted
4. Admin with only Crew Management granted
5. Admin with only Logistics granted
6. Admin with no hubs granted
7. Maintenance user
8. Crew user
9. Logistics user
10. Viewer / Auditor
11. Normal user

For each persona, verify:

- Login succeeds.
- The landing page sends the user to the correct dashboard.
- The sidebar/top navigation only shows allowed hubs.
- Hidden hubs cannot be reached directly by URL.
- Admin users only see hubs granted by Super Admin.
- Normal users only see dashboard, assigned tasks, profile, and feedback/flags.
- Super Admin can see System Admin and role/permission controls.
- Super Admin can grant and revoke hub access.
- Revoked hub access immediately removes the hub from navigation.
- Revoked hub access also blocks direct URL access.
- Backend authorization matches frontend hiding.

Document results in:

docs/authenticated-navigation-verification.md

PHASE 2 — VERIFY LEGACY ROUTES AND REDIRECTS

Test these removed or migrated routes:

- /pdm-dashboard
- /health-monitor
- /pdm-pack
- /pdm/schedule
- /pdm-platform?tab=diagnostics
- /pdm-platform?tab=schedule
- /equipment-intelligence

Verify:

- Old routes do not 404.
- Old routes redirect to the correct canonical target.
- Query strings are preserved.
- Deep links still open the intended tab.
- Unauthorized users cannot use legacy URLs to bypass permissions.
- Browser back/forward behavior works.
- Navigation breadcrumbs do not point to deleted pages.

Add redirect tests if no tests exist.

PHASE 3 — FIX EXISTING ADMIN PERMISSION BACKFILL

The current deployment note says existing seeded orgs do not auto-reseed templates.

Do not leave this as a manual-only deployment note.

Implement a safe, idempotent backfill or admin repair script that ensures existing Super Admin/Admin role templates receive the correct predictive maintenance permissions where appropriate.

Required permissions to verify:

- predictive_maintenance:view
- predictive_maintenance:manage_config
- predictive_maintenance:override where appropriate

Rules:

- Super Admin should receive all predictive maintenance permissions.
- Admin should receive permissions only if the intended default template allows it.
- Company Admin and Chief Engineer should keep existing permissions.
- Do not grant excessive permissions to normal users.
- The script must be idempotent.
- It must not duplicate permission rows.
- It must be safe to run more than once.
- It must be documented.

Create one of the following, depending on project conventions:

- migration
- seed repair script
- admin maintenance script

Also update:

docs/permission-backfill-notes.md

PHASE 4 — VERIFY BUTTON/ACTION TRUST

For the files touched in the recent trust cleanup, verify the actual behavior:

Files mentioned:
- client/src/.../equipment-hub.tsx
- client/src/.../ai-health-dashboard.tsx
- client/src/.../analytics-hub.tsx
- client/src/.../logistics-hub.tsx

Verify:

- Removed Acknowledge/Assign buttons had no real handlers and no important backend route was orphaned.
- Create Work Order still works.
- AI Health dashboard correctly fetches /api/ml/models.
- Models Active count is accurate.
- Last Training handles empty, loading, error, and populated states.
- Analytics Key Findings shows the correct state for:
  - real findings
  - no findings
  - fetch failure
  - partial data failure
- Logistics currency formatting distinguishes:
  - N/A
  - $0
  - positive values
  - failed fetch

Add or update tests where practical.

PHASE 5 — VERIFY AUDIT COMPLETENESS

Confirm that the original requested audit exists and is complete:

Required files:
- docs/ui-usefulness-density-audit.md
- docs/ui-usefulness-density-audit.json

Check that the audit actually covers the full application, not only PdM/Equipment Intelligence.

It must include:

- Maintenance
- System Admin
- Crew Management
- Logistics
- AI Analytics
- User Dashboard
- Feedback/Flags
- Navigation items
- Dashboard widgets
- Buttons
- Forms
- Modals
- API routes
- Role visibility
- Dead routes
- Duplicate routes
- Placeholder/demo/fake features
- Broken/no-op/misleading buttons

If the audit is incomplete, expand it before implementing more changes.

PHASE 6 — OUTPUT

Do not perform broad UI deletion in this pass.

You may modify code only to:
- fix permission backfill
- add verification tests
- fix obviously broken redirects
- fix authentication/permission mismatches
- update documentation

Final response must include:

1. What was verified
2. What failed
3. What was fixed
4. Which personas were tested
5. Whether existing admins retain required access
6. Whether legacy redirects work
7. Whether hidden pages are blocked by backend authorization
8. Whether docs/ui-usefulness-density-audit.md is complete
9. Remaining risks
10. Recommended next implementation prompt for Phase 2 navigation simplification

Acceptance criteria:

- npx tsc --noEmit passes
- app boots
- authenticated navigation is verified for all personas
- existing admin permission issue is fixed or clearly blocked with reason
- no hidden page is reachable by direct URL without permission
- old PdM routes redirect correctly
- no misleading success/normal state remains in the touched files
- no broad UI deletion is done before verification is complete