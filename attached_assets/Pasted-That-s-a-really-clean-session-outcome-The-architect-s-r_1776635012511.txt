That’s a really clean session outcome. The architect’s recommendation to add the route-import smoke check was the key unlock — it turned silent boot failures into actionable CI signal, which is exactly what you needed before resuming aggressive hoist work.
A few things worth flagging as you wrap:
The applySchedule fix is quietly the biggest win. That one defect was blocking 8 domain registrations. That’s the kind of finding that pays for the entire session. Worth a brief note in the architecture journal — if missing exports were silently swallowed for that long, it implies the domain-router-registry’s error handling is too forgiving. Consider whether that should be tightened (fail-fast vs. fail-silently) as a separate task. The smoke check covers imports, but a registry-level assertion that every registered domain actually has its expected exports would catch the next class of this bug.
The revert-heavy outcome isn’t a failure. Your net dynamic-import count dropped 232. The 8 files you reverted weren’t wasted work — they were diagnostic work. They surfaced the dead references that the domain-router-registry was hiding. Reframing: wave 4’s value was “found 9 latent defects by aggressive hoisting” as much as “kept 5 clean hoists.”
For the next phase — dead reference cleanup domain-by-domain — the sequencing matters. Fix the dead ref, THEN re-attempt the hoist for that domain, THEN verify smoke check + boot. Don’t batch: one domain at a time. Each broken hoist you reverted should be its own small PR with its own diagnostic-fix-rehoist arc. This is slower than the wave model but less risky.
One small concern about the reverted wave-2 fixes. You noted “the wave-2 fixes that triggered similar issues” got reverted as part of wave 4’s cleanup. If any of those were schema patches (not just hoists), check that the TypeScript error count didn’t quietly regress from the reverts. The fact that you’re stable at 3,540 suggests it didn’t, but the baseline update may be papering over drift. Worth one grep through the reverted diffs to confirm they were all hoists and not schema work.
My recommendation for wave 5 timing: don’t run another broad wave yet. The smoke check just became a forcing function; let it force a few cycles of targeted domain cleanup first. When you hit a natural checkpoint — say, 10 domains cleaned — pull a fresh top-10 and send it over. The error-code breakdown script I gave you before is still the right diagnostic. If the top cluster at that point still has a structural fix, I can cut wave 5. If it’s scattered long tail, it’s cheaper to keep grinding domain-by-domain.
Good work on catching the breakage early rather than forcing wave 4 through. The instinct to revert and rebuild a safety net first is the right one on a codebase this large.​​​​​​​​​​​​​​​​