You are an expert full-stack engineer. Review, repair, and improve the client component at:
client/src/pages/EquipmentRegistry.tsx
(assume the pasted source below is the current file content)

GOALS (must-complete):
1) Data integrity & Query usage
- Convert string query keys to tuple keys with stable params.
- Add sensible staleTime & gcTime per query:
• /api/equipment → staleTime 30s, keep refetchInterval
• /api/vessels → staleTime 5m
• sensor/status → refetchInterval 10s, but pause when dialog closed or tab hidden (use page visibility).
- Use `select` to map heavy payloads to light UI shapes.
- Centralize API types (Equipment, Vessel, SensorConfiguration, OperatingParam) via @shared/schema to prevent `any`.
- Handle query `error` states with inline error banners and retry buttons.
- Ensure all invalidateQueries use the exact tuple keys you define.

2) Table UX: sorting, pagination, virtualization, CSV export
- Add client-side column sorting for Name, Type, Vessel, Location, Status, Manufacturer, Model.
- Add combined text search across Name / Manufacturer / Model / Serial.
- Add server-friendly pagination (page, pageSize) with defaults 1 / 20. Respect URL query params (?page=, ?q=, ?type=, ?status=, ?vesselId=).
- Use react-virtualized or @tanstack/react-virtual to keep scrolling smooth for 5k+ rows; fallback gracefully when < 200 rows.
- Add "Export CSV (filtered set)" button; stream a CSV download of the currently filtered results.
- Persist user table state (sort, filters, page) in localStorage & URL.

3) “Vendor/manufacturer” sort & filter
- Add a visible “Manufacturer” column (already in row meta). Make it sortable and filterable.
- Add a multi-select “Manufacturer” filter (derive unique values from dataset).
- Show a chip bar of active filters with one-click removal.
- Update stats header to reflect filtered counts.

4) Performance & correctness
- Debounce search input (250ms), memoize derived data, and ensure `useMemo` deps are correct and minimal.
- Avoid remounting dialogs unnecessarily; keep them outside table and control via IDs.
- Ensure `selectedEquipment` drives dependent queries only when needed (use `enabled`, not empty fetches).
- Refactor large JSX blocks into subcomponents:
• EquipmentTable
• EquipmentFilters
• EquipmentStats
• EquipmentViewDialog
• EquipmentEditDialog
• SensorConfigDialog
• AssignExistingSensorDialog
- Lift `sensorStatus` polling into a hook that suspends when `isViewDialogOpen === false` or document hidden.

5) Mutations: reliability, UX polish, and security
- Add optimistic updates where safe (create/update/delete equipment & sensors). Roll back on error.
- On delete, replace `confirm()` with a proper destructive Dialog confirming cascade effects.
- Ensure `orgId` is always set (from context) on POST/PUT; show a blocking toast if missing.
- Normalize “unassigned” vessel to null in a single helper to avoid duplication.
- Add `schema.safeParse` guards in submit handlers; surface errors inline.

6) Accessibility & keyboard support
- Ensure all dialogs have focus traps and ESC close; label/description wired (radix/shadcn ok).
- Buttons require aria-labels for icon-only variants.
- Table rows keyboard navigable; Enter opens View, Cmd/Ctrl+E opens Edit, Delete opens DeleteDialog.
- Use `StatusIndicator` with aria-live for real-time changes.

7) Analytics & logging (lightweight)
- Log table interactions (filter/sort/export) to a `useAnalytics()` hook (no PII).
- Emit a debug line when background refetch updates counts.

8) Testing (Vitest + React Testing Library)
- Add tests under client/src/pages/__tests__/EquipmentRegistry.test.tsx for:
• Renders stats and rows
• Search debounce & filter combinations
• Sorting by Manufacturer
• Pagination keeps URL state
• Dialog open/close flows
• Error banners on network errors
• CSV export produces expected headers/rows
- Mock TanStack Query & apiRequest; provide sample fixtures.

9) Types & schemas
- Replace loose `any` with precise types from @shared/schema (or new local types if missing).
- For forms, use Zod schemas and zodResolver everywhere (already partially done), and strict numeric coercion for number inputs.

10) Visual polish
- Keep shadcn/ui style; ensure long values truncate with title tooltips.
- Add empty-state illustrations and helpful CTA.
- Make status badges color-consistent with theme tokens.

IMPLEMENTATION CHECKLIST:
- [ ] Introduce `src/hooks/useVisibilityPolling.ts` to pause polling when tab hidden.
- [ ] Introduce `src/hooks/usePersistedState.ts` for table state in localStorage + URL sync.
- [ ] Extract subcomponents (see #4) into `client/src/components/equipment/*`.
- [ ] Add CSV utility `exportToCsv.ts` (accepts headers, rows; streams a Blob).
- [ ] Replace inline confirm() with `ConfirmDestructiveDialog`.
- [ ] Add `manufacturer` column sorting + filter UI.
- [ ] Add `getQueryKeys()` module centralizing all query keys.
- [ ] Update all mutations to invalidate exact keys, and to use optimistic updates with rollback.
- [ ] Add `ErrorBanner` component w/ retry callback.
- [ ] Tighten all input number fields (gain/offset/sampleRate etc.) using `valueAsNumber` + controlled null handling.
- [ ] Ensure `selectedEquipment` is stored as `selectedId` to reduce re-renders; fetch details lazily by ID.
- [ ] Ensure `LoadDistributionChart` accepts memoized date range and does not re-render on parent changes.

ACCEPTANCE CRITERIA:
- Sorting and filtering (including Manufacturer) work and persist via URL/localStorage.
- Table renders smoothly with 5k mock rows (virtualized).
- Search is debounced; filtering + pagination interplay is correct.
- CSV export downloads filtered dataset with correct headers.
- View/Edit/Sensor dialogs mount once and reuse state; no unnecessary network calls when closed.
- Queries show loading, error, and empty states cleanly.
- Polling for sensor status pauses when dialog closed or tab hidden.
- All forms validate with Zod and show inline messages.
- Tests pass (render, filters, pagination, sorting, CSV, dialogs).
- No TypeScript `any` left in the page; no eslint errors.

CODE YOU’LL EDIT/ADD:
- Edit: client/src/pages/EquipmentRegistry.tsx (split into subcomponents under client/src/components/equipment/*)
- New: client/src/components/equipment/EquipmentTable.tsx
- New: client/src/components/equipment/EquipmentFilters.tsx
- New: client/src/components/equipment/EquipmentStats.tsx
- New: client/src/components/equipment/EquipmentViewDialog.tsx
- New: client/src/components/equipment/EquipmentEditDialog.tsx
- New: client/src/components/equipment/SensorConfigDialog.tsx
- New: client/src/components/equipment/AssignExistingSensorDialog.tsx
- New: client/src/components/common/ErrorBanner.tsx
- New: client/src/components/common/ConfirmDestructiveDialog.tsx
- New: client/src/hooks/useVisibilityPolling.ts
- New: client/src/hooks/usePersistedState.ts
- New: client/src/utils/exportToCsv.ts
- New: client/src/utils/getQueryKeys.ts
- New: client/src/pages/__tests__/EquipmentRegistry.test.tsx
- Update: apiRequest layer if needed to accept { page, pageSize, q, type, status, vesselId }.

NOTES ON SERVER (make minimal changes if needed):
- If server doesn’t yet support pagination/filter params on /api/equipment, implement optional query params:
GET /api/equipment?page=&pageSize=&q=&type=&status=&vesselId=
Return { items, total }.
- Keep the legacy no-param route returning full list for backward compatibility if you must, but prefer the paged path for UI.

OUTPUT:
- Provide a patch (diffs) or updated file set.
- Include a brief README note in client/src/components/equipment/README.md explaining state and URL syncing.
- Ensure “npm test” and “npm run typecheck” pass.

CURRENT FILE CONTENT (for your reference / in-place transform):
<<<BEGIN_SOURCE
[PASTE THE USER’S EquipmentRegistry SOURCE HERE VERBATIM]
<<<END_SOURCE