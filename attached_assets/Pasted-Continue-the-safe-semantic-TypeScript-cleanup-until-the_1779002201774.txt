Continue the safe semantic TypeScript cleanup until the target is 0 TypeScript errors.

Current state:
- TypeScript errors reduced from 1797 to 1739.
- TS2307 and TS2308 are cleared.
- npm run check:guards passes.
- Architecture must remain intact.

Goal:
- `npm run typecheck` must end with 0 errors.
- `npm run check:guards` must pass.
- Runtime must remain healthy.

Hard rules:
- No `@ts-nocheck`.
- No tsconfig weakening.
- No disabling strict mode.
- No mass `any`.
- No broad casts just to silence errors.
- Do not revert LLM gateway work.
- Do not bypass hexagonal ports/adapters.
- Do not import repositories directly where boundaries forbid it.
- Do not change security defaults to permissive behavior.

Work loop:
1. Run `npm run typecheck`.
2. Group errors by root cause, not just file.
3. Fix one semantic cluster at a time.
4. Rerun `npm run typecheck`.
5. Continue until 0 errors.
6. Run `npm run check:guards`.
7. Run relevant tests for touched areas.
8. Regenerate the TypeScript burndown baseline only after verified reduction.

Priority order:
1. TS2339 property drift
   - Fix by aligning DTOs, view models, and mappers.
   - Do not add fake properties to domain entities unless they truly belong there.

2. TS2322 assignment mismatches
   - Fix null vs undefined properly.
   - Normalize API/domain values before they enter forms.
   - Normalize form values before sending API payloads.

3. TS2345 argument mismatches
   - Fix function signatures or call sites based on the real intended contract.
   - Prefer typed request/response DTOs over casts.

4. React Hook Form issues
   - Create explicit `FormValues` types.
   - Use one consistent `useForm<FormValues>()` generic per form.
   - Controlled inputs must receive `value ?? ""`, not null.

5. Analytics/reporting drift
   - Create feature-level view models.
   - Map backend DTOs into UI display shapes.
   - Do not pollute shared domain schemas for dashboard-only fields.

6. Server/Drizzle typing
   - Align insert/select payloads with schema.
   - Use canonical repositories and existing ports.
   - Do not bypass boundaries.

7. Final cleanup
   - Remove dead compatibility shims only if no longer used.
   - Keep backwards-compatible overloads if legacy callers still exist.

Expected final output:
- Starting error count.
- Ending error count: must be 0.
- Files changed.
- Major clusters fixed.
- Confirmation that `npm run typecheck` passes.
- Confirmation that `npm run check:guards` passes.
- Any remaining non-TypeScript warnings clearly separated from TypeScript errors.

Important:
If reaching 0 errors requires a risky architectural change, do not make that change. Instead, create the smallest semantic adapter, DTO, mapper, or overload that preserves the existing architecture and gets TypeScript to 0.