Replit Prompt:



You are modifying an existing TypeScript/React/Express ARUS marine predictive maintenance application that already supports:

Equipment registry & sensor configurations (/api/equipment, /api/sensor-configs, etc.)

Telemetry ingestion and health index computation

ML/AI endpoints for vibration and other sensors

A rich React UI with TanStack Query, forms, and sensor management (including EquipmentRegistry with sensor config dialogs)



I want you to add first-class support for “oil debris” sensors, and to thoroughly review and respect existing I/O patterns, schemas, and observability while doing it.

1. Understand the existing architecture 

before

 editing

Search the repo for these key areas and skim them first (do NOT change yet):

Telemetry & sensor handling:

server/routes or similar API route files, especially anything involving:

/api/telemetry

/api/sensor-configs

/api/operating-parameters

/api/vibration/… and other ML endpoints

Schema & types:

@shared/schema (especially:

SensorConfiguration

insertSensorConfigSchema

Telemetry / sensor readings tables or types

Operating parameter tables/types for equipment

Frontend:

The EquipmentRegistry component (already provided) where sensors are created/edited/assigned.

Any “Operating Parameters” page or component.

AI/ML:

Any code that:

Calculates health indices

Generates AI/LLM-based maintenance reports

Uses sensor types for predictive maintenance (e.g., vibration/temperature/pressure)

Observability & errors:

Logging & metrics utilities used by other sensor/ML endpoints.

Infer and document the existing I/O patterns in comments or a short markdown file (e.g. docs/oil-debris-plan.md):

Typical POST body used for telemetry ingestion (fields like sensorType, equipmentId, value, timestamp).

Typical structure of endpoints like /api/vibration/bearing-fault-detection:

Request shape

Response shape

Error handling pattern

How SensorConfiguration is used end-to-end:

Created/updated via /api/sensor-configs

Bound to equipmentId

Fields like sensorType, minValid, maxValid, warnLo, warnHi, critLo, critHi, etc.



Do this first so that all oil debris changes follow the same patterns.

2. Add “oil_debris” support to schemas & templates

SensorConfiguration / schema changes

In @shared/schema (or equivalent shared models):

Ensure sensorType allows a value of "oil_debris".

If an enum is used, add "oil_debris" there.

Ensure any Zod schema (insertSensorConfigSchema) and TypeScript types accept "oil_debris".

Operating parameters

If you have operating parameter tables for equipment (e.g. operatingParameters, equipmentOperatingParameters):

Add a way to define default parameter sets for oil_debris sensors, such as:

parameterType: "oil_debris"

parameterName: "Oil Debris Level"

unit: "ppm" or unit: "particles/mL" (choose one and use consistently).

Example ranges for testing purposes:

optimalMin: 0

optimalMax: 150

criticalMin: null

criticalMax: 300

lifeImpactDescription: short text about how rising debris impacts component life.

recommendedAction: generic action suggestion (e.g., “Schedule oil analysis and inspect filters/gear teeth.”)

Insert a small seed or migration so at least one standard operating parameter for oil_debris exists and will show up in the UI (especially the “Operating Condition Status” section in EquipmentRegistry’s view dialog).

Sensor templates (if you have them)

If you maintain sensor templates per equipment type (e.g. MARINE_EQUIPMENT_SENSOR_TEMPLATES or similar), add entries like:

{
  equipmentType: "engine", // and/or "gearbox", "thruster" etc.
  sensorType: "oil_debris",
  defaultConfig: {
    sampleRateHz: 0.0007, // e.g. ~1 sample per 24 minutes, just as a placeholder
    gain: 1.0,
    offset: 0.0,
    deadband: 0.0,
    minValid: 0,
    maxValid: 500,
    warnLo: null,
    warnHi: 150,
    critLo: null,
    critHi: 300,
    hysteresis: 0.0,
    emaAlpha: 0.2,
    targetUnit: "ppm",
    notes: "Oil debris sensor template for predictive wear monitoring."
  }
}


Ensure template insertion respects existing orgId / security patterns.

3. Telemetry ingestion & storage for oil debris

Extend telemetry ingestion

In the API that handles telemetry ingestion (e.g., /api/telemetry):

Confirm it already accepts sensorType.

Ensure sensorType === "oil_debris" is treated like other numeric sensors:

Validate value as numeric.

Store it in the same table as other scalar sensor readings.

Apply min/max validity filtering using SensorConfiguration (if that’s already done for others).

Add robust validation & logging:

If sensorType === "oil_debris" but configuration is missing or disabled:

Log a warning.

Either drop the point or mark it with low dataQuality.

Health index / anomaly integration

Where health indices are computed (e.g., “health index” per equipment or per pack):

Plug oil debris into the logic:

Compute an “oil_debris_score” based on where the latest value sits relative to optimal and critical ranges.

Incorporate this into any composite health index, using similar weighting as other indicators (vibration/temperature), but keep it tunable.

Ensure that any time-series ML or heuristics that look at risk/condition can also look at "oil_debris" data:

For now, simple heuristics are OK: value > warnHi → degraded; > critHi → at-risk.

I/O examples

Add or update inline JSDoc comments to show example POST bodies, e.g.:

POST /api/telemetry
{
  "equipmentId": "main-engine-001",
  "sensorType": "oil_debris",
  "timestamp": "2025-01-10T12:34:56Z",
  "value": 180.5,
  "unit": "ppm"
}


And example readings in responses where relevant.

4. New AI/ML endpoint: 

/api/oil-debris/analyze



Create a new Express route handler based on the style of existing vibration/acoustic ML endpoints. For example:

Route: POST /api/oil-debris/analyze

Request body shape:

interface OilDebrisAnalyzeRequest {
  equipmentId?: string;
  dataPoints: Array<{
    timestamp: string;
    value: number;
  }>;
  unit?: string; // default "ppm"
}


Response shape:

interface OilDebrisAnalyzeResponse {
  summary: string;
  status: "normal" | "warning" | "critical" | "insufficient_data";
  currentValue?: number;
  trendSlope?: number;
  trendClassification?: "improving" | "stable" | "worsening";
  riskScore?: number; // 0–1
  recommendedActions: string[];
}


Implementation behavior:

Use simple stats for now:

Compute average, latest value.

Compute linear trend (least squares or simple delta between first/last).

Classify trend: improving / stable / worsening.

Use configured thresholds (from SensorConfiguration and/or operating parameters) to classify:

status = "critical" if current > critHi.

status = "warning" if between warnHi and critHi.

status = "normal" if within optimal.

status = "insufficient_data" if too few points.

Add logging & input validation:

Return 400 if dataPoints is empty/malformed.

Add tests or at least manual cURL examples in comments.

LLM integration (if existing):

If you have a generic AI summary endpoint that ingests sensor context, extend it to mention oil debris if:

oil debris is present, and

status is warning or critical.

Keep the prompt consistent with existing style but add language like:

“Oil debris levels are elevated, indicating possible wear of bearings/gear teeth or contamination. Recommend oil lab analysis and inspection.”
5. Frontend: enable oil debris in the UI

EquipmentRegistry sensor configuration dialog

The EquipmentRegistry component already has a full sensor configuration dialog and logic. Make sure:

The sensorType field can accept "oil_debris":

If there’s a dropdown, add "oil_debris" as an option, labeled “Oil Debris”.

If it’s free text, optionally provide hint text: temperature, pressure, rpm, oil_debris.

When editing/creating oil debris sensors:

Default targetUnit to "ppm".

Pre-fill sensible defaults for:

minValid, maxValid, warnHi, critHi, etc. (as per the schema additions).

In the “Sensor Configurations” list for an equipment:

Show sensorType === "oil_debris" with a nice label (e.g., “Oil Debris”).

Show units (ppm) and last value if available.

Operating Condition Status section

This section already pulls operatingParams and equipmentTelemetry.

Confirm that oil debris parameters and telemetry values appear correctly:

Parameter name: “Oil Debris Level”

Unit: ppm

Status badges (NORMAL/WARNING/CRITICAL) work with the newly added ranges.

Make sure parameterType used for oil debris matches whatever you used in the backend (e.g., parameterType: "oil_debris" vs "oil_debris_ppm").

Tables & charts

If you have any charts (e.g., LoadDistributionChart or other analytics components) that are sensor-type-specific, add handling for sensorType === "oil_debris" where appropriate:

For now, it’s okay to keep oil debris mostly numeric-trend-based (line chart).

Ensure no component assumes only a fixed set of sensor types (e.g. temperature|pressure|rpm).

6. I/O & logic consistency checks



Before finishing, do a mini “integration audit” for oil debris:

Schema-level checks

Typescript: SensorConfiguration["sensorType"] includes "oil_debris".

Zod: insertSensorConfigSchema accepts "oil_debris" and validates numeric fields appropriately.

DB: migrations are applied (or schema updated) with any needed defaults.

API-level checks

POST /api/sensor-configs: can create oil debris sensors without validation errors.

GET /api/sensor-config?equipmentId=…: includes oil debris config.

GET /api/sensor-configs/status?equipmentId=…: shows oil debris sensors with status.

POST /api/telemetry with sensorType: "oil_debris" stores data correctly.

POST /api/oil-debris/analyze: returns the expected response shape for:

Normal data

Warning-level data

Critical data

Too few points.

UI-level checks

In EquipmentRegistry:

Create a new equipment.

Add an oil debris sensor.

See it appear in the list with correct status and units.

Ensure forms submit and validate without crashing.

In the View dialog:

Confirm “Operating Condition Status” shows oil debris when telemetry and parameters exist.

Logic interaction checks

If your ML/health index logic aggregates multiple indicators, confirm oil debris is:

Included in that aggregation.

Not overweighted to the point of breaking existing good behavior.

Ensure that if there is no oil debris sensor, the system behaves as before.

7. Final polish

Add or update comments above the new oil debris endpoint and any schema additions to clearly state:

The purpose (oil wear and debris monitoring for predictive maintenance).

Expected units and ranges.

Limitations (e.g., heuristics vs full ML model for now).

Make sure all new code:

Passes TypeScript type checks.

Follows existing error-handling and logging patterns.

Uses the same observability hooks/metrics where appropriate.



When you’re done, summarize the changes in a short markdown note (docs/oil-debris-support.md) so future maintainers can understand the design.

Use the above instructions to implement full oil debris sensor support end-to-end, keeping I/O, schemas, and existing patterns consistent and hardened.