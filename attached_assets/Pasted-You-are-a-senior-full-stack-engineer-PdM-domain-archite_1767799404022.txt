You are a senior full-stack engineer + PdM domain architect. You are working inside an existing Node.js/TypeScript app (ARUS). The PdM “Schedule” tab already exists in the Maintenance > Predictive Maintenance section (mobile view shows: tabs Risk Queue / Schedule, KPI tiles “Scheduled This Week” and “Unassigned High-Risk”, and a vessel x day grid called “Maintenance Schedule”). However, it currently shows 0 scheduled tasks and the grid is empty.

Your task: make THIS EXISTING Schedule tab functional by wiring it to real PdM-derived data (alerts → tasks → schedule), and propose either integration or deletion of any legacy/duplicate scheduling logic.

Do NOT redesign UI. Identify what’s missing and implement the minimum backend + frontend changes to populate the KPI tiles and grid.

====================================================
1) DISCOVERY (MANDATORY FIRST)
====================================================
A) Locate the existing PdM Schedule tab code:
- Find the route/page that renders “Maintenance > Predictive Maintenance”
- Find the “Risk Queue” tab component and the “Schedule” tab component
- Find the grid component that renders the vessel x date columns
- Find the KPI tile components (“Scheduled This Week”, “Unassigned High-Risk”)
Output file paths.

B) Identify what the Schedule tab currently uses for data:
- Does it call an endpoint? Which?
- Is it using mock/placeholder data?
- Is it expecting a data shape that is not being returned?
Output the current hook/client calls and response expectations.

C) Backend discovery:
Search for endpoints and services containing: pdm, schedule, maintenanceSchedule, riskQueue, alerts, rul, workOrders.
List:
- Existing PdM endpoints (dashboard, risk queue, alert detail)
- Any schedule endpoints
- Work order endpoints
- Relevant DB tables (pdm_alerts, work_orders, maintenance_records, vessels/assets)

====================================================
2) ROOT CAUSE: WHY THE GRID IS EMPTY
====================================================
Determine which of these is true:
- No schedule endpoint exists
- Schedule endpoint exists but returns empty because no task derivation occurs
- Schedule endpoint exists but date range mismatch
- Frontend is not calling/refetching correctly
- Keys mismatch (vessel_id/asset_id)
- Data exists but UI mapping is wrong

Provide a short diagnosis.

====================================================
3) DEFINE THE DATA CONTRACT REQUIRED BY THIS UI
====================================================
From the existing grid + tiles, infer and document the minimum data it needs:

A) KPI summary (for the two tiles):
- scheduled_this_week_count
- scheduled_this_week_range_label (e.g. “Jan 4 – Jan 10”)
- unassigned_high_risk_count
- optional: list of unassigned high-risk items (for drilldown)

B) Grid data:
- dates[] (the column headers displayed)
- vessels[] (rows)
- per vessel, per date: array of scheduled task “chips/cards”
Each task chip needs:
- task_id (should map to pdm_alert_id)
- asset_name (short)
- failure_mode_label (short)
- severity
- duration_hours (optional)
- status (scheduled/draft/etc)

C) Task detail (if clicking a chip exists or should exist):
- RUL range (P10/P50/P90 or min/max)
- confidence
- evidence chips
- recommended actions
- link to alert/work order

====================================================
4) IMPLEMENT THE MISSING PdM → TASK → SCHEDULE PIPELINE
====================================================
You must implement these steps using existing data first:

A) PdM alerts → tasks
- Use existing risk queue/alerts source (pdm_alerts table or endpoint)
- For each alert, derive a schedulable Task object:
  - task_id = alert_id
  - vessel_id, asset_id
  - failure_mode_label
  - severity
  - rul estimate (range if available)
  - confidence (nullable)
  - last_seen_at (nullable)
  - estimated_duration_hours (lookup default by failure_mode/equipment_type; fallback = 2h)

B) Compute schedule windows from RUL
- Convert RUL days into actual calendar windows:
  - earliest_start_date = today + prep_days
  - preferred_date = today + rul_p50 - buffer_days
  - latest_safe_finish_date = today + rul_p10 - buffer_days
- If earliest_start_date > latest_safe_finish_date -> mark BLOCKED and include block_reason

C) Place tasks into grid (Phase 1 scheduler)
- Implement a simple greedy scheduler:
  - Sort tasks: severity desc, latest_safe_finish asc, confidence desc
  - Capacity model (config defaults):
    - per_vessel_max_downtime_hours_per_day (default 6)
    - optional global crew hours cap
  - Place tasks into earliest feasible date between earliest and latest
- Anything not placeable becomes UNASSIGNED with block_reason="capacity"

====================================================
5) BACKEND: PROVIDE DATA TO THE EXISTING UI
====================================================
Prefer reusing/extending existing API.
If no schedule endpoint exists, add:

GET /api/pdm/schedule?from&to&vesselId&equipmentType&q
Response shaped to match the current UI needs:
{
  "summary": {
    "scheduled_this_week_count": number,
    "scheduled_range_label": string,
    "unassigned_high_risk_count": number
  },
  "grid": {
    "dates": ["YYYY-MM-DD", ...],
    "rows": [
      {
        "vessel_id": "...",
        "vessel_name": "...",
        "cells": {
          "YYYY-MM-DD": [
            { "task_id": "...", "asset_name": "...", "failure_mode_label": "...", "severity": "...", "duration_hours": 2, "status": "scheduled" }
          ]
        }
      }
    ]
  },
  "unassigned": [
    { "task_id": "...", "vessel_name": "...", "asset_name": "...", "severity": "...", "latest_safe_finish_date": "...", "block_reason": "capacity" }
  ]
}

====================================================
6) FRONTEND: WIRE THE EXISTING SCHEDULE TAB
====================================================
- Replace placeholder data / empty arrays with response from /api/pdm/schedule
- Ensure filters (the dropdowns shown in UI) pass through to the schedule endpoint
- KPI tiles render summary counts and date range label
- Grid renders vessel rows and task chips inside date columns
- Add empty state messaging:
  - No tasks in range
  - Tasks exist but all blocked/unassigned (show unassigned list)

Export CSV:
- Wire “Export CSV” to export the flat list of tasks (scheduled + unassigned)
- Include block_reason, scheduled_start/end, latest_safe_finish

====================================================
7) WORK ORDER INTEGRATION (IF AVAILABLE)
====================================================
If work orders exist:
- Add a “Create Work Order Draft” action from a scheduled task
- Link WO to alert_id/task_id
- Update alert/task status accordingly

If not available:
- Leave action disabled and document missing dependency.

====================================================
8) LEGACY FEATURE REVIEW (INTEGRATE OR DELETE)
====================================================
After wiring is complete:
- Search repo for any other scheduling logic/screens/endpoints
- Determine if duplicate/legacy schedule exists
- Propose integration (reuse) or deletion (deprecate then remove) with evidence (imports, route usage)

====================================================
9) TESTS (MINIMUM)
====================================================
Add:
- unit tests for window computation and greedy placement
- integration test for /api/pdm/schedule with seeded alerts

====================================================
DELIVERABLES
====================================================
1) Diagnosis: why schedule tab is empty
2) Implemented schedule endpoint + task derivation + greedy scheduler
3) Wired existing Schedule tab to display real scheduled tasks
4) Export CSV works
5) Minimal tests
6) Recommendation: integrate or delete any legacy scheduling features

START NOW:
Do discovery and identify what the Schedule tab currently calls, then implement the schedule pipeline and wire the existing UI.