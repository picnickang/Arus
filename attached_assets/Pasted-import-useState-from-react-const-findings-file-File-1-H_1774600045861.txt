import { useState } from “react”;

const findings = [
{
file: “File 1 — HMAC Key Rotation”,
path: “server/services/hmac-key-rotation.ts”,
grade: “B+”,
issues: [
{
severity: “bug”,
title: “timingSafeEqual crashes on mismatched lengths”,
detail: “Line 123: `crypto.timingSafeEqual(Buffer.from(signature, 'hex'), Buffer.from(expected, 'hex'))` will throw if signature and expected have different byte lengths (e.g., if the client sends a truncated or malformed hex string). timingSafeEqual requires both buffers to be the same length.”,
fix: “Add a length check before the comparison:\n`ts\nconst sigBuf = Buffer.from(signature, 'hex');\nconst expBuf = Buffer.from(expected, 'hex');\nif (sigBuf.length !== expBuf.length) continue;\nif (crypto.timingSafeEqual(sigBuf, expBuf)) { ... }\n`”,
},
{
severity: “design”,
title: “State is in-memory only — lost on restart”,
detail: “After a key rotation, the previous key mapping exists only in memory. If the server restarts before the grace period ends, the previous key is lost and devices using it will fail silently. The env var fallback partially covers this, but the keyId→secret mapping and grace period end time aren’t persisted.”,
fix: “This is acceptable for an MVP but should be noted. For production, persist active keys to the database or a file alongside .env.”,
},
],
positives: [“Grace period logic is correct”, “Timing-safe comparison is the right approach”, “Status endpoint enables monitoring”, “ENV var loading is a reasonable bootstrap”],
},
{
file: “File 2 — Replay Protection”,
path: “server/middleware/replay-protection.ts”,
grade: “A-”,
issues: [
{
severity: “minor”,
title: “Nonce store unbounded in edge case”,
detail: “If a flood of unique nonces arrives (DDoS), the Map grows without limit before the 60-second cleanup runs. The cleanup only removes expired entries — new entries added during a burst are kept for the full 5-minute window.”,
fix: “Add a max size check: `if (nonceStore.size > 100000) { /* reject or evict oldest */ }`. This is low-risk in practice with rate limiting in front.”,
},
],
positives: [“Backward compatible (skip if headers absent)”, “Correct 409 for duplicate nonce”, “Timestamp validation with drift tolerance is right”, “Clean separation of concerns as middleware factory”],
},
{
file: “File 3 — RAG Document Sanitizer”,
path: “server/services/rag/document-sanitizer.ts”,
grade: “A-”,
issues: [
{
severity: “minor”,
title: “Regex for `.call/.apply/.bind` is too broad”,
detail: “The pattern `/\\.(?:call|apply|bind)\\s*\\(/gi` will match legitimate technical documentation that discusses JavaScript or engine calibration (e.g., ‘apply torque to the bolt’). Maritime technical manuals could have the word ‘apply’ followed by a parenthetical.”,
fix: “This is a false-positive risk. Consider making the data exfiltration patterns configurable or applying them only to HTML/web content, not to plaintext extracted from PDFs.”,
},
],
positives: [“PDF safety validation checks the right PDF actions (JavaScript, OpenAction, SubmitForm, Launch)”, “Sanitization result is richly typed — callers can log what was stripped”, “Prompt injection detection patterns are comprehensive”, “Separates ‘dangerous’ (blocked) from ‘messy’ (stripped) correctly”],
},
{
file: “File 4 — DB Degradation Layer”,
path: “server/services/db-degradation.ts”,
grade: “B+”,
issues: [
{
severity: “bug”,
title: “replayQueuedWrites double-inserts failed writes”,
detail: “Lines 625-633: When failed >= 3, the code does `this.writeQueue.unshift(...failedWrites, ...this.writeQueue)`. But failedWrites have already been pushed to failedWrites array earlier (line 621), and then on line 633 they’re unshifted again. If write #1 fails and write #2 fails and write #3 fails (triggers break), failedWrites = [w1, w2, w3]. Line 627 unshifts [w1,w2,w3, …remaining] correctly. But line 633 then unshifts failedWrites AGAIN, duplicating them.”,
fix: “Remove line 633 (`this.writeQueue.unshift(...failedWrites);`) — the early break on line 627 already handles re-queuing. Or restructure: only do one unshift at the end.”,
},
{
severity: “design”,
title: “Cache eviction is O(n) sort on every insert”,
detail: “Line 537: When cache exceeds max entries, it sorts the entire Map by cachedAt to find the oldest entry. This is O(n log n) on every successful DB read when the cache is full. With 1000 entries and high read throughput, this adds measurable latency.”,
fix: “Use a simple LRU approach: keep a separate array of keys in insertion order, or switch to an LRU cache library. Or just delete the first entry from the Map iterator (Maps preserve insertion order):\n`ts\nconst firstKey = this.readCache.keys().next().value;\nif (firstKey) this.readCache.delete(firstKey);\n`”,
},
],
positives: [“cachedRead generic typing is well done”, “Failure threshold before marking unhealthy prevents flapping”, “Write queue with cap prevents unbounded growth”, “markHealthy() enables explicit recovery signaling”],
},
{
file: “File 5 — WebSocket Message Buffer”,
path: “server/services/ws-message-buffer.ts”,
grade: “B+”,
issues: [
{
severity: “design”,
title: “recordBroadcast iterates all client buffers on every message”,
detail: “Every broadcast pushes a message to every client buffer. With 500 connected clients and 100 messages/second (realistic for telemetry alerts), that’s 50,000 Map operations per second. The trimming logic (lines 730-734) also runs per-client per-message when buffers are full.”,
fix: “Instead of pushing to individual client buffers, maintain a single global ring buffer. getMissedMessages reads from the global buffer using the client’s lastSeenId as a pointer. This reduces recordBroadcast from O(clients) to O(1).”,
},
{
severity: “minor”,
title: “Message ID collision is theoretically possible”,
detail: “`msg-${Date.now()}-${Math.random().toString(36).slice(2, 6)}` has 4 random chars (≈1.7M combinations). Under high-frequency broadcasting, Date.now() could repeat within the same millisecond, relying on 4 chars of randomness.”,
fix: “Use `crypto.randomUUID()` or at least 8 random chars. Low practical risk but easy to fix.”,
},
],
positives: [“Critical event priority is exactly right — alerts and patches should survive trimming”, “Disconnect handling correctly keeps the buffer for reconnection”, “TTL-based cleanup prevents memory leaks from abandoned clients”, “Acknowledge correctly prunes non-critical messages”],
},
{
file: “File 6 — LLM Statistical Fallback”,
path: “server/services/llm-statistical-fallback.ts”,
grade: “B”,
issues: [
{
severity: “bug”,
title: “Median calculation is wrong”,
detail: “Line 927: `sorted[Math.floor(sorted.length / 0.5)]` divides by 0.5, which doubles the length — this will always be out of bounds. Should be `sorted[Math.floor(sorted.length / 2)]` for the median, or `sorted[Math.floor(sorted.length * 0.5)]`.”,
fix: “Change to `Math.floor(sorted.length / 2)` or `Math.floor(sorted.length * 0.5)`.”,
},
{
severity: “minor”,
title: “medianHealth is computed but never used”,
detail: “The variable is assigned on line 927 but never referenced anywhere in the output. Dead code.”,
fix: “Either include medianHealth in the summary output or remove the variable.”,
},
{
severity: “minor”,
title: “isFallback flag should be on the FallbackAnalysis type”,
detail: “The `isFallback: true` literal type in the interface is good, but the function signature doesn’t indicate to callers that they should check this flag before presenting the data.”,
fix: “Acceptable as-is. The `isFallback` and `fallbackReason` fields are clear enough.”,
},
],
positives: [“Data-driven output is dramatically better than the boilerplate it replaces”, “Risk matrix showing worst 10 equipment gives actionable focus”, “Cost estimation provides business context even without AI”, “Summary paragraph is readable as natural language”, “Handles empty input gracefully”],
},
{
file: “File 7 — Storage Circuit Breaker”,
path: “server/services/storage-circuit-breaker.ts”,
grade: “A”,
issues: [
{
severity: “minor”,
title: “Timeout timer isn’t cleaned up on success”,
detail: “The Promise.race on line 1054 creates a setTimeout that fires even after the operation succeeds. The rejection handler runs against an already-resolved promise (harmless), but the timer object stays alive until it fires. Under sustained load, this creates thousands of pending timers.”,
fix: “Use AbortController or track the timer and clear it on success:\n`ts\nlet timer: NodeJS.Timeout;\nconst timeout = new Promise<never>((_, reject) => {\n  timer = setTimeout(() => reject(...), this.timeoutMs);\n});\ntry { const result = await Promise.race([operation(), timeout]); clearTimeout(timer!); ... }\n`”,
},
],
positives: [“Classic three-state circuit breaker (CLOSED → OPEN → HALF_OPEN → CLOSED) is correctly implemented”, “Recovery requires multiple consecutive successes — prevents flapping”, “Operation name in error messages enables debugging”, “Reset method enables testing and manual recovery”],
},
{
file: “File 8 — Idempotency Middleware”,
path: “server/middleware/idempotency.ts”,
grade: “B+”,
issues: [
{
severity: “design”,
title: “Monkey-patching res.json has edge cases”,
detail: “Lines 1200-1209: The patched res.json captures the response body. But if the handler calls res.status(500).json() after an error, the idempotency cache stores the error response. Subsequent retries with the same key return the cached 500 error instead of re-executing. This is actually correct idempotency behavior, but it means a transient error becomes permanent for 24 hours.”,
fix: “Only cache successful responses (2xx status codes):\n`ts\nres.json = function(body) {\n  if (res.statusCode >= 200 && res.statusCode < 300) {\n    processedKeys.set(fullKey, { statusCode: res.statusCode, body, processedAt: Date.now() });\n  }\n  return originalJson(body);\n};\n`”,
},
{
severity: “minor”,
title: “res.send and res.end bypass the cache”,
detail: “Only res.json is patched. If a handler calls res.status(201).send(body) instead of res.json(body), the response isn’t cached. This is fine for JSON APIs but worth documenting.”,
fix: “Document in the JSDoc that only res.json responses are cached.”,
},
],
positives: [“Org-scoped key namespacing prevents cross-tenant collisions”, “24-hour TTL with periodic cleanup is appropriate”, “Backward-compatible when header is absent”, “Clean middleware factory pattern”],
},
{
file: “File 9 — Telemetry Partitioning SQL”,
path: “server/migrations/003-telemetry-partitioning.sql”,
grade: “B+”,
issues: [
{
severity: “design”,
title: “Option B runs destructive DDL without transaction”,
detail: “The ALTER TABLE RENAME + CREATE TABLE + INSERT is not wrapped in a transaction. If the INSERT fails (e.g., disk full), you’ve renamed the old table and created a new empty one. The data exists in telemetry_aggregated_old but the app is writing to an empty telemetry_aggregated.”,
fix: “Wrap in BEGIN/COMMIT, or at minimum document that this should be run during a maintenance window with a backup.”,
},
{
severity: “minor”,
title: “SERIAL doesn’t work as expected in partitioned tables”,
detail: “PostgreSQL partitioned tables can’t have SERIAL (or any sequence-backed default) with a UNIQUE constraint across partitions. The `id SERIAL` column will work for insertion but the sequence may produce duplicates across partitions if they’re ever split differently.”,
fix: “Use `gen_random_uuid()` as the primary key instead of SERIAL, consistent with other tables in the schema (like class_surveys). Or simply drop the id column and make the unique constraint the primary key.”,
},
],
positives: [“Provides both TimescaleDB and native PostgreSQL options — smart given unknown deployment”, “TimescaleDB section includes compression and retention policies”, “Partition-per-bucket-size is the correct strategy for different retention periods”, “Indexes are correctly scoped to each partition”],
},
{
file: “File 10 — Equipment ID Validator”,
path: “server/services/telemetry-equipment-validator.ts”,
grade: “A”,
issues: [
{
severity: “minor”,
title: “Warning suppression timeout accumulates closures”,
detail: “Line 1369: Each suppressed warning creates a new setTimeout closure that holds a reference to the warningKey string. With thousands of unknown equipment IDs (misconfigured fleet), this creates thousands of pending timers. The memory impact is negligible per-timer, but it’s technically unbounded.”,
fix: “Use a Map<string, number> of suppression expiry timestamps instead of setTimeout, and clean up in the refresh cycle.”,
},
],
positives: [“5-minute cache refresh is appropriate — not too aggressive, not too stale”, “Log suppression prevents flooding from a single misconfigured device”, “Passes storage as parameter rather than importing — testable”, “getStats enables monitoring dashboard”],
},
{
file: “File 11 — Dashboard Pre-compute”,
path: “server/services/dashboard-precompute.ts”,
grade: “A-”,
issues: [
{
severity: “design”,
title: “Sequential computation across orgs blocks each other”,
detail: “Line 1512: `runAll()` awaits each org’s computation sequentially. If one org has a slow query (10 seconds), all subsequent orgs wait. With 20 orgs on a 60-second interval, the computation might not finish before the next interval fires.”,
fix: “Use `Promise.allSettled` for parallel computation, or at least add a guard to skip if the previous cycle is still running:\n`ts\nprivate computing = false;\nprivate async runAll() {\n  if (this.computing) return;\n  this.computing = true;\n  try { await Promise.allSettled(this.orgIds.map(id => this.computeForOrg(id))); }\n  finally { this.computing = false; }\n}\n`”,
},
],
positives: [“Never blocks the API — always serves from cache”, “addOrg for dynamic org discovery is well thought out”, “Keeps stale data on failure instead of clearing cache”, “Clean start/stop lifecycle”],
},
{
file: “File 12 — Equipment Health Pagination [PATCH]”,
path: “server/domains/equipment/routes.ts”,
grade: “B+”,
issues: [
{
severity: “design”,
title: “Paginated response caches the sliced page, not the full result”,
detail: “Line 1574: `setCache(cacheKey, health)` caches only the paginated slice. Next request with different page/pageSize for the same equipment gets a cache miss, re-fetches the full dataset, and slices again. The cache key should include page params or cache the full result.”,
fix: “Either: (a) cache the full result and slice on read, or (b) include page+pageSize in the cache key:\n`ts\nconst cacheKey = `health:${orgId}:${vesselId}:${equipmentId}:${page}:${pageSize}`;\n`”,
},
],
positives: [“Backward compatible — no params returns the full array as before”, “Pagination response shape matches industry conventions”, “totalCount and totalPages enable frontend pagination controls”],
},
{
file: “File 13 — Async Compliance Check”,
path: “server/services/async-compliance-check.ts”,
grade: “A-”,
issues: [
{
severity: “minor”,
title: “No error notification to the user if compliance check fails”,
detail: “Line 1694: If the compliance check throws, it’s logged but the WebSocket notification isn’t sent. The user who triggered the save has no feedback that the check failed — it silently doesn’t complete.”,
fix: “Send a failure notification via WebSocket in the catch block:\n`ts\ncatch (error) {\n  logger.error(...);\n  if (this.wsServer) {\n    this.wsServer.broadcast?.({ type: 'compliance_check_failed', orgId, vesselId, error: error.message, ... });\n  }\n}\n`”,
},
],
positives: [“Debouncing is exactly right for logbook save scenarios”, “Key structure (org:vessel:date:type) correctly deduplicates”, “Clean integration with existing compliance rules engine”],
},
{
file: “File 14 — Migration Runner”,
path: “server/services/migration-runner.ts”,
grade: “B”,
issues: [
{
severity: “bug”,
title: “SQL injection via filename in INSERT”,
detail: “Line 1788: `VALUES ('${file}', '${checksum}')` interpolates the filename directly into SQL. If a migration file is named `'; DROP TABLE schema_migrations; --`, this executes arbitrary SQL. While filenames come from the local filesystem (not user input), this is a bad pattern.”,
fix: “Use parameterized queries:\n`ts\nawait this.db.execute(\n  sql.raw('INSERT INTO schema_migrations (filename, checksum) VALUES ($1, $2)'),\n  [file, checksum]\n);\n`”,
},
{
severity: “design”,
title: “No rollback support”,
detail: “If a migration partially succeeds (multi-statement SQL where statement 3 of 5 fails), the database is in an inconsistent state with no way to roll back. The migration is not recorded as applied, so re-running will fail on the already-applied statements.”,
fix: “Wrap each migration in a transaction:\n`ts\nawait this.db.execute(sql.raw('BEGIN'));\ntry {\n  await this.db.execute(sql.raw(sqlContent));\n  await this.db.execute(sql.raw('INSERT INTO ...'));\n  await this.db.execute(sql.raw('COMMIT'));\n} catch (e) {\n  await this.db.execute(sql.raw('ROLLBACK'));\n  throw e;\n}\n`”,
},
],
positives: [“Checksum tracking enables drift detection”, “Stop-on-first-error prevents cascading migration failures”, “Alphabetical sort for chronological ordering is the convention”, “Clean reporting of applied/skipped/errors”],
},
{
file: “File 15 — Structured Error Codes”,
path: “server/lib/api-error.ts”,
grade: “A”,
issues: [
{
severity: “minor”,
title: “mapErrorToApiResponse relies on string matching”,
detail: “Matching ‘not found’ or ‘timeout’ in error messages is fragile — a message like ‘Configuration not found to be valid’ would incorrectly return 404.”,
fix: “Acceptable for a fallback mapper. Better approach: throw typed errors (class NotFoundError extends Error) and match on instanceof. But the string matcher is fine as a safety net for untyped errors.”,
},
],
positives: [“Comprehensive error code enum covering all hardening scenarios”, “Details field only exposed in development — good security practice”, “requestId integration enables correlation with the middleware from File 17”, “Consistent metadata shape across all error responses”],
},
{
file: “File 16 — API Versioning”,
path: “server/middleware/api-versioning.ts”,
grade: “C+”,
issues: [
{
severity: “bug”,
title: “URL rewriting creates infinite recursion risk”,
detail: “Line 1972-1976: The middleware sets `req.url = /api${req.url}` and then calls `app.handle(req, res, next)`. But app.handle will re-enter the middleware stack, which includes this same middleware. If the rewritten URL matches `/api/v1/*` again (it shouldn’t because /api/v1 is stripped by Express mount), there could be edge cases with nested paths.”,
fix: “Add a guard header to prevent re-entrance:\n`ts\napp.use('/api/v1', (req, res, next) => {\n  if (req.headers['x-version-rewritten']) return next();\n  req.headers['x-version-rewritten'] = 'true';\n  req.url = `/api${req.url}`;\n  app.handle(req, res, next);\n});\n`”,
},
{
severity: “design”,
title: “Unused v1Router is dead code”,
detail: “Lines 1963-1968: A v1Router is created with a wildcard handler but never mounted on the app. The actual routing is done by the app.use(’/api/v1’, …) below it. The Router is dead code.”,
fix: “Remove the unused v1Router block.”,
},
{
severity: “design”,
title: “Version header middleware runs on ALL requests, not just API”,
detail: “Line 1979: `app.use(...)` adds the X-API-Version header to every response including static files, health checks, etc. Minor but noisy.”,
fix: “Scope it: `app.use('/api', (req, res, next) => { res.setHeader('X-API-Version', 'v1'); next(); });`”,
},
],
positives: [“The approach of aliasing /api/v1 to /api is the right migration strategy”, “Version header in responses helps clients detect version”],
},
{
file: “File 17 — Correlation ID”,
path: “server/middleware/correlation-id.ts”,
grade: “A”,
issues: [],
positives: [“Preserves client-provided X-Request-Id for distributed tracing”, “Global type augmentation for Express.Request is the correct TypeScript pattern”, “Uses crypto.randomUUID for proper UUID format”, “getRequestId helper enables service-layer access”, “Simple, focused, no bugs”],
},
{
file: “File 18 — Smoke Tests”,
path: “server/tests/pdm-gap-fill-smoke.ts”,
grade: “A-”,
issues: [
{
severity: “minor”,
title: “Uses global fetch without polyfill check”,
detail: “The tests use `fetch()` which requires Node.js 18+ or a polyfill. If running on an older Node version, all tests will fail with ReferenceError.”,
fix: “Add a note that Node 18+ is required, or import node-fetch as fallback.”,
},
],
positives: [“All tests are non-destructive reads or validation-error triggers”, “Clean test runner pattern with timing per test”, “Suite-level summary enables automated pass/fail gating”, “Tests correct response shapes, not just status codes (e.g., checking for totalGroups field)”],
},
{
file: “File 19 — Vessel Timezone”,
path: “server/services/vessel-timezone.ts”,
grade: “B+”,
issues: [
{
severity: “bug”,
title: “toUTC conversion logic is inverted”,
detail: “Lines 2229-2233: The offset calculation computes `utcStr - localStr` and adds it to localDate. But this is backwards. If the timezone is UTC+8, utcStr (treated as UTC) is 8 hours behind localStr. The offset would be negative, and adding it to localDate moves it further from UTC instead of toward it.”,
fix: “The correct conversion: given a local time in timezone X, to get UTC, subtract the UTC offset:\n`ts\nexport function toUTC(localDate: Date, timezone: string): Date {\n  // Create a date string that will be parsed as UTC\n  const localStr = localDate.toLocaleString('en-US', { timeZone: timezone });\n  const utcStr = localDate.toLocaleString('en-US', { timeZone: 'UTC' });\n  const offsetMs = new Date(localStr).getTime() - new Date(utcStr).getTime();\n  return new Date(localDate.getTime() - offsetMs);\n}\n`\nNote: swap the subtraction order AND subtract from localDate.”,
},
{
severity: “design”,
title: “Timezone cache never expires”,
detail: “Once a vessel’s timezone is cached, it stays until clearTimezoneCache is explicitly called. If a vessel updates its timezone (e.g., crossing time zones on a voyage), the cache serves stale data indefinitely.”,
fix: “Add a TTL (e.g., 30 minutes) or clear on vessel update via an event.”,
},
],
positives: [“en-CA locale for YYYY-MM-DD formatting is clever and correct”, “getVesselLocalDate is exactly what logbook entries need”, “clearTimezoneCache supports both single-vessel and fleet-wide invalidation”, “Graceful fallback to UTC on invalid timezone”],
},
{
file: “File 20 — Bandwidth-Aware Middleware”,
path: “server/middleware/bandwidth-aware.ts”,
grade: “A-”,
issues: [
{
severity: “minor”,
title: “compactResponse strips fields that may be needed by mobile clients”,
detail: “The verbose field list (description, notes, details, metadata) is hardcoded. Some clients may need the description field even in low-bandwidth mode (e.g., displaying work order titles). The truncation to 50 chars for string values in these fields may cut off important information.”,
fix: “Consider a tiered approach: ‘low’ mode truncates to 100 chars, ‘satellite’ mode strips completely. Or let the client specify which fields to include via a query parameter.”,
},
],
positives: [“X-Response-Mode header lets clients verify compact mode is active”, “Recursive compaction handles nested objects correctly”, “Null stripping alone can reduce response size by 20-30% for sparse data”, “pageSize override to 10 is a smart default for satellite connections”],
},
{
file: “File 21 — Running Hour Accumulator”,
path: “server/services/running-hour-accumulator.ts”,
grade: “B+”,
issues: [
{
severity: “design”,
title: “Running hours are in-memory only — lost on restart”,
detail: “totalHours accumulates in memory and resets to 0 on server restart. A vessel that has been running for 3,999 hours will appear to have 0 hours after a restart and the 4,000-hour threshold won’t fire. This is the most critical flaw in this file.”,
fix: “On each threshold check or periodically (every 5 minutes), persist the running hour state to the database. On startup, load the last known state:\n`ts\nasync initialize(orgId: string) {\n  const states = await this.storage.getRunningHourStates(orgId);\n  for (const state of states) {\n    this.states.set(`${orgId}:${state.equipmentId}`, state);\n  }\n}\n`”,
},
{
severity: “minor”,
title: “1-hour gap threshold may be too aggressive”,
detail: “Line 2442: `timeDeltaMs > 60 * 60 * 1000` skips any reading with a gap > 1 hour. During a long port stay (engine off for days), the first reading after restart will be discarded. This is correct behavior for preventing false accumulation, but the engine might have been running during the gap (telemetry connectivity loss).”,
fix: “Log the discarded gap so operators can investigate:\n`ts\nlogger.warn(LOG_CTX, `Gap > 1h for ${equipmentId}: ${timeDeltaMs / 3600000}h — reading discarded`);\n`”,
},
],
positives: [“MIN_RPM_FOR_RUNNING = 50 is a reasonable threshold for marine engines”, “Threshold crossing detection (previousHours < threshold && currentHours >= threshold) is correct”, “Priority scaling by threshold value (10000h → priority 1) aligns with maritime overhaul schedules”, “Work order metadata includes trigger details for audit trail”],
},
{
file: “File 22 — Class Survey Tracking”,
path: “server/domains/surveys/routes.ts”,
grade: “B”,
issues: [
{
severity: “bug”,
title: “SQL injection in daysAhead parameter”,
detail: “Line 2744: `INTERVAL '${daysAhead} days'` interpolates the user-provided daysAhead value directly into SQL. While it’s parsed through Number() on line 2733, a NaN value would produce `INTERVAL 'NaN days'` which is a SQL error, not an injection. But the pattern is unsafe — if the Number() parse were removed, it would be exploitable.”,
fix: “Use parameterized interval or compute the date in JS:\n`ts\nconst cutoffDate = new Date();\ncutoffDate.setDate(cutoffDate.getDate() + daysAhead);\n// Then: AND cs.due_date <= $2\n`”,
},
{
severity: “bug”,
title: “PATCH paramIdx is off-by-one”,
detail: “Lines 2685-2705: paramIdx starts at 1 and increments for each SET clause. Then `req.params.id` is pushed as the next param and `orgId` as the one after. But the WHERE clause references `$${paramIdx++}` which has already been incremented past the SET clauses. If status, completedDate, and surveyorName are all provided, paramIdx = 4. The WHERE references $4 and $5. But the params array has [status, completedDate, surveyorName, id, orgId] — so $4 = id and $5 = orgId. This is actually correct! But only by coincidence — the paramIdx++ in the WHERE clause produces the right values. It’s fragile and confusing.”,
fix: “Compute the WHERE param indices explicitly:\n`ts\nconst idIdx = paramIdx++;\nconst orgIdx = paramIdx++;\nparams.push(req.params.id, orgId);\n// WHERE id = $${idIdx} AND org_id = $${orgIdx}\n`”,
},
{
severity: “design”,
title: “Route ordering: /api/surveys/summary/upcoming shadows /api/surveys/:id”,
detail: “The `GET /api/surveys/summary/upcoming` route is registered AFTER `GET /api/surveys/:id`. Express will match ‘summary’ as an :id parameter and try to find a survey with id=‘summary’. The summary endpoint will never be reached.”,
fix: “Move the summary route BEFORE the /:id route, or use a different path pattern like `/api/surveys-summary/upcoming`.”,
},
],
positives: [“Zod schemas for survey types and class societies are comprehensive and correct”, “Finding severity enum (observation, non_conformity, major_non_conformity) matches ISM/Class conventions”, “Overdue calculation in the summary endpoint is straightforward and correct”, “All queries are org-scoped”],
},
];

const severityColors = {
bug: { bg: “#fef2f2”, border: “#fca5a5”, text: “#dc2626”, label: “Bug” },
design: { bg: “#fefce8”, border: “#fcd34d”, text: “#a16207”, label: “Design” },
minor: { bg: “#eff6ff”, border: “#93c5fd”, text: “#2563eb”, label: “Minor” },
};

const gradeColors = {
“A”: “#15803d”, “A-”: “#16a34a”,
“B+”: “#1d4ed8”, “B”: “#2563eb”, “B-”: “#3b82f6”,
“C+”: “#ca8a04”, “C”: “#a16207”,
};

export default function App() {
const [expanded, setExpanded] = useState(null);

const totalBugs = findings.reduce((s, f) => s + f.issues.filter(i => i.severity === “bug”).length, 0);
const totalDesign = findings.reduce((s, f) => s + f.issues.filter(i => i.severity === “design”).length, 0);
const totalMinor = findings.reduce((s, f) => s + f.issues.filter(i => i.severity === “minor”).length, 0);
const cleanFiles = findings.filter(f => f.issues.length === 0).length;

return (
<div style={{ fontFamily: “‘DM Sans’, system-ui, sans-serif”, maxWidth: 900, margin: “0 auto”, padding: “20px 16px”, color: “var(–text-color, #0f172a)” }}>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=DM+Mono:wght@400&display=swap" rel="stylesheet" />

```
  <h1 style={{ fontSize: 22, fontWeight: 700, margin: "0 0 2px" }}>Hardening Files — Quality Review</h1>
  <p style={{ fontSize: 13, color: "#64748b", margin: "0 0 16px" }}>
    Line-by-line review of all 22 hardening implementations for bugs, design issues, and correctness
  </p>

  {/* Summary stats */}
  <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 8, marginBottom: 16 }}>
    {[
      { label: "Bugs Found", value: totalBugs, color: "#dc2626" },
      { label: "Design Issues", value: totalDesign, color: "#ca8a04" },
      { label: "Minor Issues", value: totalMinor, color: "#2563eb" },
      { label: "Clean Files", value: cleanFiles, color: "#15803d" },
    ].map(s => (
      <div key={s.label} style={{ textAlign: "center", padding: 12, borderRadius: 10, border: `1px solid ${s.color}30`, background: `${s.color}08` }}>
        <div style={{ fontSize: 26, fontWeight: 800, color: s.color }}>{s.value}</div>
        <div style={{ fontSize: 11, color: "#64748b" }}>{s.label}</div>
      </div>
    ))}
  </div>

  {/* Critical fixes summary */}
  <div style={{ background: "#fef2f2", border: "1px solid #fca5a5", borderRadius: 10, padding: 14, marginBottom: 16 }}>
    <div style={{ fontSize: 14, fontWeight: 700, color: "#dc2626", marginBottom: 6 }}>Must-fix before deploying:</div>
    <div style={{ fontSize: 13, lineHeight: 1.7, color: "#334155" }}>
      <strong>File 1:</strong> timingSafeEqual crash on mismatched signature length.
      <strong> File 4:</strong> Double-insert of failed writes in replay queue.
      <strong> File 6:</strong> Median calculated as length/0.5 (always out of bounds).
      <strong> File 14:</strong> SQL injection via unparameterized filename INSERT.
      <strong> File 19:</strong> toUTC conversion offset is inverted.
      <strong> File 22:</strong> Route ordering causes /summary/upcoming to be unreachable.
    </div>
  </div>

  {/* Per-file review */}
  {findings.map((f, i) => {
    const isOpen = expanded === i;
    const gc = gradeColors[f.grade] || "#64748b";
    return (
      <div key={i} style={{ marginBottom: 6, border: "1px solid var(--border-color, #e2e8f0)", borderRadius: 8, overflow: "hidden" }}>
        <div onClick={() => setExpanded(isOpen ? null : i)}
          style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "10px 14px", cursor: "pointer", background: isOpen ? "var(--bg-secondary, #f8fafc)" : "transparent" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
            <span style={{ fontSize: 10, color: "#94a3b8", transform: isOpen ? "rotate(90deg)" : "rotate(0)", transition: "0.15s" }}>▶</span>
            <span style={{ fontSize: 16, fontWeight: 800, color: gc, minWidth: 28 }}>{f.grade}</span>
            <span style={{ fontSize: 13, fontWeight: 600 }}>{f.file}</span>
          </div>
          <div style={{ display: "flex", gap: 6 }}>
            {f.issues.filter(x => x.severity === "bug").length > 0 && (
              <span style={{ fontSize: 11, padding: "1px 6px", borderRadius: 6, background: "#fef2f2", color: "#dc2626", fontWeight: 600 }}>
                {f.issues.filter(x => x.severity === "bug").length} bug{f.issues.filter(x => x.severity === "bug").length > 1 ? "s" : ""}
              </span>
            )}
            {f.issues.filter(x => x.severity === "design").length > 0 && (
              <span style={{ fontSize: 11, padding: "1px 6px", borderRadius: 6, background: "#fefce8", color: "#a16207", fontWeight: 600 }}>
                {f.issues.filter(x => x.severity === "design").length} design
              </span>
            )}
            {f.issues.length === 0 && (
              <span style={{ fontSize: 11, padding: "1px 6px", borderRadius: 6, background: "#f0fdf4", color: "#15803d", fontWeight: 600 }}>clean</span>
            )}
          </div>
        </div>
        {isOpen && (
          <div style={{ borderTop: "1px solid var(--border-color, #e2e8f0)", padding: 14 }}>
            <div style={{ fontSize: 11, color: "#94a3b8", marginBottom: 8, fontFamily: "'DM Mono', monospace" }}>{f.path}</div>

            {f.issues.length > 0 && (
              <div style={{ marginBottom: 12 }}>
                {f.issues.map((issue, j) => {
                  const sc = severityColors[issue.severity];
                  return (
                    <div key={j} style={{ marginBottom: 10, padding: 10, borderRadius: 8, border: `1px solid ${sc.border}`, background: sc.bg }}>
                      <div style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 4 }}>
                        <span style={{ fontSize: 11, fontWeight: 700, color: sc.text, padding: "0 6px", borderRadius: 4, background: `${sc.text}15` }}>{sc.label}</span>
                        <span style={{ fontSize: 13, fontWeight: 600, color: "var(--text-color, #0f172a)" }}>{issue.title}</span>
                      </div>
                      <div style={{ fontSize: 12, lineHeight: 1.6, color: "var(--text-color, #334155)", marginBottom: 6 }}>{issue.detail}</div>
                      <div style={{ fontSize: 12, lineHeight: 1.6, color: "#15803d", background: "#f0fdf4", padding: 8, borderRadius: 6, border: "1px solid #86efac" }}>
                        <strong>Fix:</strong> {issue.fix.split("```")[0]}
                        {issue.fix.includes("```") && (
                          <pre style={{ margin: "6px 0 0", fontSize: 11, fontFamily: "'DM Mono', monospace", whiteSpace: "pre-wrap", color: "#334155", background: "#fff", padding: 8, borderRadius: 4, border: "1px solid #e2e8f0" }}>
                            {issue.fix.split("```ts\n")[1]?.split("\n```")[0] || issue.fix.split("```ts")[1]?.split("```")[0] || ""}
                          </pre>
                        )}
                      </div>
                    </div>
                  );
                })}
              </div>
            )}

            {f.positives.length > 0 && (
              <div>
                <div style={{ fontSize: 12, fontWeight: 600, color: "#15803d", marginBottom: 4 }}>What's good:</div>
                <div style={{ display: "flex", flexWrap: "wrap", gap: 4 }}>
                  {f.positives.map((p, k) => (
                    <span key={k} style={{ fontSize: 11, padding: "2px 8px", borderRadius: 6, background: "#f0fdf4", color: "#15803d", border: "1px solid #86efac" }}>{p}</span>
                  ))}
                </div>
              </div>
            )}
          </div>
        )}
      </div>
    );
  })}

  <div style={{ marginTop: 20, padding: 16, borderRadius: 10, background: "var(--bg-secondary, #f8fafc)", border: "1px solid var(--border-color, #e2e8f0)" }}>
    <div style={{ fontSize: 14, fontWeight: 700, marginBottom: 6 }}>Overall Assessment</div>
    <div style={{ fontSize: 13, lineHeight: 1.7, color: "var(--text-color, #334155)" }}>
      <strong>Average grade: B+.</strong> The code is well-structured, follows the existing codebase patterns, and addresses the right problems. There are <strong>6 real bugs</strong> that need fixing before deployment — the most critical being the toUTC inversion (File 19), the SQL injection in the migration runner (File 14), and the route ordering in surveys (File 22). The design issues are legitimate but non-blocking. One file (File 17 — Correlation ID) is completely clean. File 16 (API Versioning) needs the most rework — the URL rewriting approach has recursion risk and dead code.
    </div>
  </div>
</div>
```

);
}