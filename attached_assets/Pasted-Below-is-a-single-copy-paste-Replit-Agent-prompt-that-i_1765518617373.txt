Below is a single, copy-paste Replit Agent prompt that instructs Replit to probe, clean up, and reduce ESLint warnings globally in a safe, staged way, without breaking runtime behavior or assuming repo structure.

This is designed specifically for your current state:
	•	0 errors
	•	~5,680 warnings
	•	Heavy modularization
	•	Electron + Node + React + TypeScript
	•	Offline-first constraints

⸻

✅ REPLIT AGENT PROMPT — SAFE GLOBAL ESLINT WARNING CLEANUP

You are working on an existing Electron + Node/Express + React + TypeScript codebase (ARUS) with SQLite/libSQL + Drizzle.
The app has recently undergone heavy modularization and is currently stable with 0 ESLint errors and ~5,680 ESLint warnings.

Your goal is to probe, reduce, and clean up ESLint warnings globally in a safe, staged manner, without changing runtime behavior or architecture.

HARD RULES
	•	Do NOT assume folder names or file structure — discover them.
	•	Do NOT perform risky refactors or behavior-changing edits.
	•	Do NOT auto-modify React hook dependency arrays.
	•	Keep files modular and under ~500 lines.
	•	Preserve offline-first behavior and Electron safety boundaries.
	•	After every stage: lint → typecheck → build → smoke test.

⸻

1️⃣ DISCOVERY & BASELINE
	1.	Inspect package.json to find:
	•	lint / lint:fix scripts
	•	typecheck scripts
	•	build scripts (frontend, backend, Electron)
	2.	Locate ESLint configuration:
	•	.eslintrc.*, eslint.config.*, or package.json
	•	.eslintignore
	3.	Generate a baseline ESLint report with warnings included:
	•	Output rule counts and worst-offending files
	•	If possible, generate JSON output and summarize:
	•	Top 10 warning rules by count
	•	Top 20 files by warning volume
	4.	Save this summary for comparison after each stage.

Do not modify code yet.

⸻

2️⃣ STAGE 1 — SAFE AUTO-FIXES ONLY
	1.	Run ESLint auto-fix using existing scripts or:
	•	eslint . --fix
	2.	Only apply built-in safe fixes:
	•	formatting
	•	import ordering
	•	prefer-const
	•	simple unused imports
	3.	Do not rewrite logic or async flows.

After completion:
	•	Rerun ESLint
	•	Record new warning counts
	•	Ensure no errors are introduced

⸻

3️⃣ STAGE 2 — UNUSED VARIABLES (LOW RISK)

Target no-unused-vars / @typescript-eslint/no-unused-vars.
	1.	Configure rule (via overrides if needed) to:
	•	ignore variables prefixed with _
	•	ignore unused function args starting with _
	•	ignore rest siblings
	2.	Clean up globally by:
	•	removing truly unused imports
	•	prefixing intentionally unused variables/args with _
	3.	Avoid removing variables used for side effects or exports.

After this stage:
	•	Rerun lint, typecheck, and build
	•	Smoke test backend + frontend + Electron

⸻

4️⃣ STAGE 3 — CONSOLE USAGE (CONTROLLED & SAFE)

Target no-console.
	1.	Discover where console usage is intentional:
	•	backend/server
	•	Electron main/preload
	•	telemetry, diagnostics, sync
	2.	Update ESLint config using scoped overrides:
	•	allow console in backend/Electron/telemetry paths
	•	keep no-console enabled in frontend UI code
	3.	Optionally introduce a lightweight logger utility and migrate only obvious cases.

Do not delete logs blindly.

After this stage:
	•	Rerun lint
	•	Confirm runtime logs still appear where expected

⸻

5️⃣ STAGE 4 — EXPLICIT any (PARTIAL, SAFE CAMPAIGN)

Target no-explicit-any carefully.
	1.	Do NOT attempt to eliminate all any.
	2.	Focus on the top ~20 most-used files contributing the most warnings.
	3.	Replace any safely using:
	•	unknown + narrowing
	•	Record<string, unknown>
	•	minimal domain interfaces
	4.	Leave boundary layers (JSON parsing, telemetry ingestion, DB rows) with any if necessary.

If needed:
	•	Use ESLint overrides to allow any in specific boundary folders
	•	Add TODO comments for future cleanup

After this stage:
	•	Rerun lint, typecheck, and build
	•	Confirm no runtime regressions

⸻

6️⃣ VERIFICATION AFTER EACH STAGE

After every stage:
	1.	Run ESLint and record warning counts
	2.	Run typecheck
	3.	Build frontend & backend
	4.	Start dev servers and confirm:
	•	app loads
	•	no runtime crashes
	•	Electron shell still works

If anything breaks:
	•	revert the last risky change
	•	choose a safer alternative

⸻

7️⃣ FINAL OUTPUT REQUIRED

At the end, provide:
	•	Warning counts:
	•	initial → after Stage 1 → after Stage 2 → after Stage 3 → after Stage 4
	•	Rules most reduced and remaining top warnings
	•	ESLint overrides added (with rationale)
	•	Confirmation that:
	•	0 errors remain
	•	app builds and runs successfully
	•	Clear recommendation on:
	•	which warning classes to address next
	•	which to leave as long-term technical debt

⸻

❗ IMPORTANT

Do NOT aim for zero warnings.
The goal is:
	•	Signal > noise
	•	Stability > purity
	•	Incremental improvement > risky refactors

Execute this plan carefully and leave the codebase stable.

⸻

If you want, next I can:
	•	tune ESLint to fail CI only on new warnings
	•	help you choose which warnings to promote to errors later
	•	generate a “lint policy” doc for ARUS going forward