You are a senior full-stack engineer working inside this exact repo (ARUS vessel PdM / operations app with Electron + Node backend + React/Vite frontend + hybrid SQLite/libSQL/Postgres storage).



I want to add a proper, automated Engine Room Logbook feature that uses the data this app already collects (telemetry, work orders, alerts, etc.) and exports to a structured logbook format.

🔍 Phase 0 – Architecture & overlap check (DO THIS FIRST)

Inspect the repo and summarize the current architecture in your own words:

Backend: main server entrypoint(s), framework (likely Express), ORM (e.g. Drizzle/Prisma/Knex), database clients (SQLite/libSQL/Postgres).

Frontend: React/Vite modules, routing, UI library, where “logbook” / “operations” / “engine” views live.

Desktop shell: Electron bootstrap (main process, preload, renderer loading http://localhost:5000).

Data & sync: how telemetry, equipment, work orders, and alerts are modeled and stored.

Search the codebase for any existing “engine room logbook” or generic “logbook” logic:

Tables or models: engine_log, engine_room_log, logbook, er_log, etc.

API routes or services: /api/logbook, /api/engine-log, etc.

Frontend components/pages related to engine or deck logbooks or manual logs.

Based on this inspection:

Tell me clearly whether an Engine Room Logbook feature already exists in some partial/legacy form.

If something similar exists, propose whether to:

(a) extend it,

(b) refactor it,

or (c) deprecate it and replace it.

Do NOT duplicate or conflict with any existing feature: explain how your plan aligns with current architecture and avoids overlap.



❗ Before changing any code, produce a short written plan for the Engine Room Logbook implementation that:

References the concrete files / modules you will touch.

Confirms compatibility with the current database and sync design.

Notes any migrations and how you’ll avoid breaking existing data.



Only after that plan is written and consistent with the repo should you proceed.

🧱 Phase 1 – Data model & migrations



Design or refine a schema for engine room log entries, reusing existing patterns in this repo (naming, ORM style, migrations). The goal:

One row per log line (typically hourly or per watch).

Per vessel, per org, tied to existing entities where appropriate.



Example structure (adapt to actual ORM / types in this project):

engine_room_log_entries

id (uuid)

org_id (respect existing tenant isolation)

vessel_id

timestamp_utc

watch_period (string, e.g. "00–04")

created_source ('auto' | 'manual' | 'auto-edited')

Main engine snapshot:

main_engine_1_rpm

main_engine_1_load_pct

main_engine_2_rpm

main_engine_2_load_pct

any other key parameters your telemetry already stores (temps, pressures).

Aux machinery snapshot:

dg1_running, dg1_load_kw

dg2_running, dg2_load_kw

shaft_generator_running, etc. (map to what’s actually in telemetry).

Tanks/consumables (if data exists or is input manually):

fuel_oil_total_tons

do_total_tons

fw_total_tons

Events & remarks:

remarks (text)

events_json (JSON) – auto-attached events (maintenance, alarms, etc.)

Running hours snapshot:

Optional fields or JSON capturing key equipment running hours at that time.

Sign-off:

watch_engineer_id

chief_engineer_id

engineer_signed_at

chief_signed_at

Versioning & metadata:

version (for future schema-compatible exports)

created_at

updated_at



Tasks:

Implement the new table/migration using the same mechanism this project uses for schema changes.

Ensure:

Multi-tenant safety: always include org_id (or equivalent) if the rest of the schema uses it.

Foreign keys: link vessel_id to the existing vessel table; any user IDs should match the users/crew table.

Add indexes where needed:

(org_id, vessel_id, timestamp_utc)

Any additional indexes needed for listing by vessel/date efficiently.

⚙️ Phase 2 – Backend: auto-generation from telemetry & events



The goal is: the system automatically proposes ER log entries based on existing telemetry and data, and the duty engineer just reviews/edits and signs.

Locate existing telemetry data structures:

Tables like telemetry_readings or similar.

Services or modules like VesselSimulator, MQTT Ingestion, “Digital Twin”, etc.

Understand how signals are identified (equipment IDs, signal kinds, tags, topics).

Implement a service/module (in the existing backend style) to:

For a given org_id and vessel_id, and a given timestamp (e.g. top of the hour):

Query recent telemetry (e.g. last 10–15 min):

Main engines RPM and load %

DG loads, running status

Any key pressures/temps that are consistently available.

Compute robust stats:

Use average (or median) values for that period to avoid spikes.

Fetch running hours for critical equipment (if you already maintain them).

Collect events for that period:

Work orders started/completed in that time window.

Critical/major alerts from the alerts/analytics subsystem.

Build a new engine_room_log_entries row with:

created_source = 'auto'

pending_engineer_review = true (if you want such a flag – add the column if needed).

If an entry already exists for that vessel + timestamp:

Update it with telemetry data only if not manually overridden, or

Skip creation to avoid duplicate entries, depending on project conventions.

Scheduling:

Integrate this with whatever job/scheduler system this repo uses (cron-like, Node schedule, custom scheduler), being mindful of embedded/standalone mode where long-running jobs may be limited.

Aim for at least “on demand” generation first (API endpoint) and later scheduled generation when embedded mode rules allow.

API endpoints:

Add backend routes like:

GET /api/engine-room-log?orgId=...&vesselId=...&from=...&to=...

POST /api/engine-room-log/generate – generates entries for a time range (or for “now”) from telemetry.

PATCH /api/engine-room-log/:id – update fields, add remarks, etc.

POST /api/engine-room-log/:id/sign-engineer

POST /api/engine-room-log/:id/sign-chief

Respect the existing tenant isolation middleware (e.g. x-org-id) and auth patterns.

Ensure all new code uses existing logging/observability patterns and error handling.

🖥️ Phase 3 – Frontend UI for Engine Room Logbook



Build a frontend experience in line with the existing UI (React + Vite + whatever component library you’re using):

Main Engine Logbook page (likely per vessel):

Filter by:

Vessel

Date range

Watch period

List entries in a table / timeline:

Time, watch period

Key summaries (ME RPM/load, DG status, FO/DO levels).

Status:

Pending review

Signed by engineer

Signed by chief

Source badge:

Auto (telemetry)

Auto-edited

Manual.

Entry detail/editor view:

Show all auto-filled fields with labels and units.

Allow editing any field manually.

Show event summary (from work orders & alerts) and a free-text “remarks” field.

Give a clear indication of which values came from telemetry vs manual override.

Include:

“Save” (keep as draft/pending)

“Confirm & sign (Engineer)” – sets engineer_signed_at & watch_engineer_id.

Optionally “Chief review & sign”.

UX & usability:

Make it easy to navigate by watch (00–04, 04–08, etc.).

Provide a per-day overview of all entries.

Hook this UI to the new API endpoints in a way that matches existing frontend data fetching patterns (React Query, SWR, custom hooks, etc.).

📤 Phase 4 – Export to official Engine Room Logbook formats



Use the existing ER LOG BOOK.xlsx (or similar) templates in the repo if they exist; otherwise, design a basic export and leave hooks for plugging in the company template.

Implement a backend export route, such as:

GET /api/engine-room-log/export?orgId=...&vesselId=...&from=...&to=...&format=xlsx|pdf

Implementation details:

For XLSX:

Use the existing Excel utilities in this repo (if any).

Or add a small, focused Excel export utility, consistent with current dependencies.

Map engine_room_log_entries to the correct rows/columns in the template.

For PDF:

Reuse existing PDF/report generation infrastructure (I saw jspdf and html2canvas in the build list).

Either render via a server-side template or client-side print-friendly view.

Ensure exports are per vessel and respect org_id.

🧪 Phase 5 – Tests, performance, and safety

Add or update tests (unit/integration/end-to-end) consistent with current testing practices:

Telemetry → log entry generation.

CRUD operations on engine log entries.

Export correctness for a small dataset.

Performance:

Make sure telemetry aggregation queries are efficient:

Use proper indexing.

Use bounded time windows.

Ensure that generating logs for long periods (e.g. 30 days) doesn’t hammer the DB unnecessarily.

Embedded/standalone mode:

Be cautious with background schedulers in Electron/desktop mode.

Ensure the app doesn’t crash if telemetry is sparse or missing – log entries should still be creatable manually.

📚 Phase 6 – Developer documentation



Finally, add a short developer-oriented document (e.g. in docs/ or README section) explaining:

What the Engine Room Logbook feature does.

High-level data flow:

telemetry → engine_room_log_entries → exports.

Key modules and files (backend & frontend).

How to:

Run migrations.

Generate logs for testing.

Export sample ER logs.

Important constraints

Do not break existing behaviour related to telemetry, work orders, alerts, or sync.

Do not duplicate any existing logbook functionality; extend or refactor it instead.

Follow the existing code style, conventions, and module organisation.

Before each major change (schema, API, UI), briefly explain:

What file(s) you are changing.

Why it’s consistent with the existing architecture.



Start now by:

Describing the current architecture relevant to this task.

Confirming whether any engine-room logbook functionality already exists.

Presenting your implementation plan.

Then implement the feature end-to-end as described above.