You are Replit Agent working INSIDE an EXISTING codebase. The Schedule planner already supports:
- tapping empty cells to create assignments (bottom sheet)
- tapping blocks to edit assignments (bottom sheet)
- assignments render as spanning blocks in the grid
- rank→role validation exists (crew rank controls allowed roles)
- offline-first writes + sync queue exist

YOUR TASK: Add SAFE, constraint-aware DRAG & DROP for assignment blocks (desktop-first, mobile optional), without breaking offline sync or rank/role enforcement.

ABSOLUTE RULES
- Do NOT start a new project or replace the scheduling UI.
- Reuse existing stack, UI components, styling, and state management.
- Do NOT bypass existing SchedulingService/Repository. All updates must go through existing upsert/update functions and must remain offline-first.
- Do NOT create a second assignment model for dragging. Drag uses the same Assignment entity.
- Must be constraint-aware: HARD violations block drop; SOFT allow with warning.
- Do not implement freeform role changes via drag. Drag only changes vesselId and/or dates.
- Keep it behind a feature flag if your repo supports flags (preferred).

PHASE 0 — DISCOVERY (MANDATORY)
1) Identify where assignment blocks are rendered (component file paths).
2) Identify current pointer/touch handlers on blocks/cells and confirm selection/edit behavior still works.
3) Identify existing constraints API/function:
   - checkConstraints(draftAssignment) or equivalent
4) Identify how pending ops/sync queue is created on update.
Output “Drag & Drop Integration Plan” with file paths and approach.

IMPLEMENTATION REQUIREMENTS

A) SUPPORTED DRAG OPERATIONS
1) Move block horizontally (shift dates):
   - dragging left/right changes startDate/endDate by delta days
   - duration stays constant
2) Move block vertically (change vessel):
   - dragging to another vessel row changes vesselId
   - dates unchanged unless user also dragged horizontally
3) OPTIONAL (phase 2b): resize start/end via handles
   - If you implement resize, do it after basic drag works and passes tests.

B) DESKTOP FIRST, MOBILE OPTIONAL
- Desktop:
  - click-and-drag works
- Mobile:
  - do NOT break tap-to-edit
  - if adding mobile drag, require long-press to enter “drag mode”
  - otherwise skip mobile drag for now

C) DRAG VISUALS (MUST FEEL “CRISP”)
- While dragging:
  - show a ghost preview block in the target position
  - keep original block visible or dimmed (choose one consistent style)
  - show snap-to-day grid (no partial-day placements)
- On hover target:
  - highlight target vessel row
  - highlight target date columns

D) VALIDATION ON DROP (NON-NEGOTIABLE)
On drop:
1) Construct a proposed assignment draft:
   - assignmentId
   - new vesselId
   - new startDate/endDate
2) Run constraints check for the proposed assignment:
   - If HARD violations: reject drop, animate snap-back, show toast with first HARD reason
   - If SOFT violations: allow drop, show warning toast and badge on block
3) Enforce rank→role mapping:
   - role must remain unchanged during drag
   - if role becomes invalid for some reason (edge case), reject drop with clear message

E) OFFLINE-FIRST WRITE PATH
If drop is accepted:
- Call SchedulingService.update/upsertAssignment with the new vesselId/dates
- Update local store optimistically
- Enqueue sync op via existing queue
- Update sync status pill/pending count
Drag must never require network.

F) CONFLICT SAFETY
- If your sync can return conflicts, ensure:
  - dragged updates produce a deterministic op payload (old->new)
  - conflicts are surfaced in the existing “sync issues” UI (or minimal modal)

G) KEEP TAP-TO-EDIT WORKING
- A short click/tap selects a block and opens Edit sheet (existing behavior).
- Drag is only when pointer moves beyond a small threshold.
- Implement a movement threshold (e.g., 6–10px) before starting drag.

TECHNICAL APPROACH (USE WHAT’S ALREADY IN REPO)
- If a drag library already exists in the repo, reuse it.
- If not:
  - prefer a lightweight approach using pointer events + absolute positioning
  - do NOT add heavy dependencies unless necessary
- Timeline grid is day-based: compute indices from dates.

ALGORITHM: DATE/VESSEL COMPUTATION
- Convert pointer X to day index:
  dayIndex = clamp(round((x - gridLeft) / columnWidth), 0, numDays-1)
- Convert pointer Y to vessel row:
  rowIndex = clamp(floor((y - gridTop) / rowHeight), 0, vessels.length-1)
- Convert dayIndex to date:
  date = addDays(rangeStartDate, dayIndex)
- Keep duration constant:
  durationDays = diffDays(endDate, startDate)
  newStart = date
  newEnd = addDays(newStart, durationDays)

TESTING (MANDATORY)
Add/extend tests using existing framework:

1) Drag horizontally shifts assignment dates by correct number of days.
2) Drag vertically moves assignment to another vesselId.
3) HARD constraint violation blocks drop and preserves original assignment.
4) SOFT constraint allows drop and shows warning indicator state.
5) Offline mode: drag drop still updates local store and adds pending op.
6) Tap still opens Edit sheet (drag threshold prevents accidental drag).

Run test suite and provide a TEST REPORT:
- commands run
- pass/fail status

DELIVERABLE
- Assignment blocks can be dragged to new dates/vessels on desktop without breaking selection/edit.
- Drops are constraint-aware (HARD blocks, SOFT warns).
- Updates persist offline-first and sync later.
- Tests passing.

NOW START
1) Output Drag & Drop Integration Plan (file paths + approach)
2) Implement desktop drag
3) Validate on drop + write via SchedulingService
4) Run tests + output TEST REPORT