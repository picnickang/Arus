Here’s a single, self-contained Replit repair prompt you can paste in to fix and harden your storage code:

You are a senior TypeScript + Node.js + Drizzle ORM engineer.



I have a storage layer file (for example: server/storage.ts) that implements two storage backends:

DatabaseStorage (Postgres via Drizzle)

MemStorage (in-memory mock)



Your job is to fully repair, de-duplicate, and harden this storage code while keeping the public API stable.



Please open server/storage.ts and do all of the following:

1. Fix Drizzle imports and helpers



Ensure the file imports all helpers it uses from Drizzle, and only once:

From "drizzle-orm" import:

and, or, eq, isNull, inArray, desc

From "drizzle-orm/pg-core" import:

arrayContains (for filtering text[] columns)



Remove any unused Drizzle imports and fix all missing ones so the file compiles cleanly with strict TypeScript.

2. Sensor template methods – single, correct implementation



The storage file currently contains logic for sensor templates (backed by a sensor_templates table), but it may have:

duplicated method implementations

inconsistent behavior

missing org-safety



In DatabaseStorage, ensure there is exactly one implementation of each of these methods:

getSensorTemplates(orgId: string, equipmentType?: string): Promise<SensorTemplate[]>

getSensorTemplateById(id: string, orgId: string): Promise<SensorTemplate | null>

createSensorTemplate(template: InsertSensorTemplate): Promise<SensorTemplate>

updateSensorTemplate(id: string, orgId: string, data: Partial<InsertSensorTemplate>): Promise<SensorTemplate>

deleteSensorTemplate(id: string, orgId: string): Promise<void>



Implement them with the following behavior:



2.1 getSensorTemplates

Returns both:

org-scoped templates: sensor_templates.org_id = orgId

system templates: sensor_templates.org_id IS NULL

If equipmentType is provided, further filter so that:

either equipment_types IS NULL (global templates), or

equipment_types contains the given equipmentType using arrayContains.

Order results with:

system defaults first (is_system_default DESC)

latest updated first (updated_at DESC)



So the WHERE condition should be roughly:

and(
  or(
    eq(sensorTemplates.orgId, orgId),
    isNull(sensorTemplates.orgId)
  ),
  equipmentType ? or(
    isNull(sensorTemplates.equipmentTypes),
    arrayContains(sensorTemplates.equipmentTypes, [equipmentType])
  ) : undefined
)
(Handle this cleanly in TypeScript without as any if possible; otherwise confine casting to the smallest necessary expression.)



2.2 getSensorTemplateById

Fetch a template by id, but only if:

sensor_templates.id = id AND

(sensor_templates.org_id = orgId OR sensor_templates.org_id IS NULL)

Return null if not found.



2.3 createSensorTemplate

Require a non-empty orgId for user-created templates.

Enforce:

user-created templates must be org-scoped (org_id = orgId)

user-created templates cannot be system defaults (is_system_default = false), regardless of what the caller sends.

Set created_at and updated_at to new Date() if not automatically handled.

Return the newly inserted row (.returning()[0]).



2.4 updateSensorTemplate

Validate that:

orgId is present and non-empty.

Lookup the existing template by id, with the same security condition as getSensorTemplateById:

only allow updating if the template belongs to that org or is system default and org is allowed to see it.

Enforce these safety rules:

Do not allow modifications to system default templates (is_system_default = true). If such a template is found, throw an error like "Cannot modify system default templates".

Strip any isSystemDefault / is_system_default field from the incoming data so a caller cannot change it.

Update updated_at to new Date() and return the updated row.



2.5 deleteSensorTemplate

Lookup the template by id and orgId (same rules as above).

If the template is is_system_default = true, throw an error like "Cannot delete system default templates".

Otherwise, delete it.

3. Multi-tenancy / org-safety helper



Add a private helper in DatabaseStorage:

private validateOrgId(orgId: string | undefined, operation: string): asserts orgId is string {
  if (!orgId || orgId.trim() === "") {
    throw new Error(`[Security] orgId is required for ${operation}`);
  }
}
Use this in all relevant methods, including:

getSensorTemplates

getSensorTemplateById

createSensorTemplate

updateSensorTemplate

deleteSensorTemplate



Any method that requires tenant scoping should call this.validateOrgId(orgId, "methodName") at the top.

4. MemStorage – safe mock implementations



MemStorage should implement the same interface (IStorage or equivalent), but can be a simple in-memory/mock.



Make sure that:

MemStorage also has one implementation of:

getSensorTemplates

getSensorTemplateById

createSensorTemplate

updateSensorTemplate

deleteSensorTemplate

These implementations must be type-safe and should NOT throw in normal flows. They can:

keep an internal Map<string, SensorTemplate> or array to store templates, OR

return deterministic mock data.

If keeping the logic very simple, it’s acceptable to:

return empty arrays for getSensorTemplates,

return null for getSensorTemplateById,

and return a stub object for create / update (but still in the correct shape).



The key goal is: no unexpected runtime exceptions from MemStorage in development or tests, and the file should compile without any leaks.

5. General cleanup

Remove any duplicate method definitions that exist due to previous merges (especially for sensor template methods).

Ensure all imports at the top are used; remove dead imports.

Ensure all exported types and classes are still exported as before (no breaking changes to the public API).

Make sure the file passes TypeScript strict mode: no implicit any, no as any except where absolutely unavoidable, and no type errors.

Return the fully corrected server/storage.ts file as a single, complete code block, ready for me to paste back into my project.