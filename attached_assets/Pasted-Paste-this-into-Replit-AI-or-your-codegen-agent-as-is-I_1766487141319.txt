Paste this into Replit AI (or your codegen agent) as-is. It’s written to force modularity, maintainability, zero duplication, and production readiness across the C# agent + Node bridge + decoder.

⸻

Replit Prompt

You are a staff-level engineer responsible for refactoring and implementing an offline-first telemetry ingestion system. The repo already has:
	•	TelemetryBatchWriter at server/telemetry-batch-writer.ts (batching, backpressure, retries, metrics)
	•	pool in server/db-config.ts
	•	TelemetryReading interface in server/telemetry-batch-writer.ts

We are adding:
	•	A thin C# .NET Worker Service (“driver shim”) that only does hardware I/O + writes raw frames to a local SQLite raw_frames table + emits heartbeat + gRPC health/stats.
	•	A Node SQLite → Postgres bridge that reads raw_frames by cursor, decodes frames into TelemetryReading[], and sends them through the existing TelemetryBatchWriter.

Hard constraints (must follow)
	1.	No duplication:
	•	Do NOT re-implement batching/backpressure/retries/metrics already in TelemetryBatchWriter.
	•	Do NOT create a second “writer” to Postgres; reuse the existing one.
	2.	Clear module boundaries:
	•	C# agent: hardware I/O + SQLite writes + heartbeat + gRPC only. No decoding. No ML. No cloud sync.
	•	Node bridge: reads SQLite + decoding + calls existing writer. Nothing else.
	3.	Production readiness:
	•	Crash-safe cursor advancement: cursor updates ONLY after confirmed Postgres commit success.
	•	SQLite safety: WAL + busy_timeout=5000 on every connection.
	•	Deterministic idempotency keys for Postgres inserts (use ON CONFLICT DO NOTHING or upsert strategy already present).
	•	Structured logging, error classification (transient vs permanent), and metrics (reuse existing metrics if present).
	4.	Modularity:
	•	Implement decoding as a plugin registry: protocol -> decoder, PGN/MID -> handler.
	•	Add unit tests for at least: cursor behavior, J1939 envelope parsing, one PGN handler, and “offline Postgres” retry behavior.
	5.	Maintainability:
	•	Strong typing (TypeScript types + C# records).
	•	Single source of truth for shared “frame envelope” format and table schema.
	•	Configuration via env vars + config file, with safe defaults and validation.

Required deliverables

A) Create/modify files to implement the following structure:

Node
	•	server/services/sqlite-bridge/
	•	index.ts (loop runner)
	•	cursorStore.ts (SQLite cursor table)
	•	sqliteRawFrameSource.ts (batched reads)
	•	bridgeProcessor.ts (decode + enqueue into TelemetryBatchWriter)
	•	config.ts (env validation + defaults)
	•	server/telemetry/decode/
	•	index.ts (dispatch by protocol)
	•	types.ts
	•	registry.ts (PGN/MID registry)
	•	j1939.ts (envelope parse + PGN extraction)
	•	j1587.ts (stub + structure)
	•	util.ts

C#
	•	apps/agent/Arus.TelemetryAgent/
	•	Program.cs (Windows Service + gRPC)
	•	Worker.cs (heartbeat + source manager skeleton)
	•	Storage/SqliteFrameWriter.cs (WAL + busy_timeout + batch flush + retention)
	•	Services/TelemetryAgentGrpcService.cs (status + stats stream)

B) Implement a single canonical raw frame payload envelope (document it in both Node & C#):
	•	payload = canId(u32le) + dlc(u8) + data(0..8 bytes) for J1939
	•	For non-J1939 protocols, define a consistent minimal envelope and document it.

C) Implement the bridge so it reuses:
	•	TelemetryBatchWriter for all Postgres writes
	•	pool from server/db-config.ts

D) Add a commit acknowledgement hook if needed:
	•	If TelemetryBatchWriter doesn’t expose a way to know when a batch is committed, add a minimal non-breaking API (e.g., flushOnce(): Promise<void> or drain(): Promise<void> or an event emitter) so the bridge can safely advance the cursor. Do NOT redesign TelemetryBatchWriter.

E) Add retention:
	•	C# agent deletes raw_frames older than 30 days (configurable), runs every 6 hours, uses incremental vacuum.

F) Add tests (minimum set):
	•	Cursor only advances after successful commit (mock writer commit failure).
	•	J1939 envelope parsing extracts PGN and SA correctly.
	•	One PGN handler produces a valid TelemetryReading.
	•	Bridge does not crash when Postgres offline; retries without advancing cursor.

Output requirements
	1.	Start by scanning the repo and listing any overlapping existing modules so we don’t duplicate them.
	2.	Then produce an implementation plan with file-by-file changes.
	3.	Then generate the code with comments focused on why boundaries exist and how to extend decoders.
	4.	Finally, add a short “maintenance guide”:
	•	How to add a new PGN decoder
	•	How to add a new source in the C# agent
	•	How to validate bridge health (gRPC + heartbeat + metrics)

Guardrails
	•	No large rewrites.
	•	Minimal invasive changes.
	•	Keep things boring and robust.
	•	Prefer composition over inheritance.
	•	Any new config must be validated (fail fast on invalid values).

Proceed.

⸻

If you want, I can also tailor this prompt to your exact conventions (Zod config, folder naming, your existing logging/metrics libs) if you paste the relevant snippets from server/telemetry-batch-writer.ts and server/db-config.ts.