You are a senior systems architect.

Your task is to refactor the ARUS application into a fully modular, domain-clean architecture.

Before doing anything, perform a full architectural assessment.

PHASE 1 — ARCHITECTURE ANALYSIS (DO NOT CHANGE ANY CODE YET)



Analyze the entire repository and produce the following:

Dependency Map

Identify modules, services, views, routes, and their cross-dependencies.

Highlight circular dependencies.

Detect duplicated logic (alerts, telemetry, equipment health, status badges, etc.).

Identify UI pages that overlap heavily (Fleet Overview, Bridge View, Equipment Health).

Monolith Hotspots

Find parts of the app where multiple responsibilities are mixed.

Identify any module that handles unrelated domains.

Detect files with too many responsibilities (backend or frontend).

Domain Separation Assessment

Categorize all existing logic into these domains:

Telemetry ingestion

Telemetry processing & alerts

Inventory & parts

Work orders & maintenance

Crew management & hours-of-rest

Vessel operations (logs, deck logbook, engine logbook)

Sync layer (local + cloud)

Data storage (SQLite + Postgres hybrid)

UI/UX view system

Background schedulers and jobs

AI/LLM functionality (optional or currently disabled)

Improvement Report

Provide a detailed report on:

What should be modularized

What should be moved into separate “domains”

What should become shared components

What should be decoupled

What should be deleted or merged



Wait for approval before making changes.

PHASE 2 — PROPOSE A NEW MODULAR ARCHITECTURE (NO CODE YET)



Based on the audit, propose a new architecture using a Domain-Modular Structure, such as:

/core
  /db
  /auth
  /sync
  /config
  /common-utils

/domains
  /telemetry
  /inventory
  /work-orders
  /crew
  /vessels
  /logs
  /compliance
  /alerts
  /reports
  /llm

/frontend
  /components
  /ui
  /hooks
  /pages (modular & consolidated)

/electron
  /ipc
  /preload
  /updater

/tests
For each domain, specify:

Role

Responsibilities

API routes

Data models

UI screens

Background tasks

Sync requirements



Also propose:

What redundant pages can be merged

How to standardize UI patterns

What shared components should be created

How to reduce file count and complexity

How telemetry pipelines should be isolated



Wait for approval before refactoring.

PHASE 3 — IMPLEMENT MODULARIZATION (CODE CHANGES ALLOWED)



Once approved, begin code transformation in stages:

Create a /domains structure and move logic into domain folders.

Extract common code (e.g., API error handlers, data validators, MQTT utilities).

Standardize DTOs and models using TypeScript and Zod.

Refactor telemetry ingestion into a standalone module with:

Dedicated types

Processing pipeline

Alert rules engine

Throttling buffers

Refactor inventory, logs, work orders, and crew so each becomes self-contained.

Create shared UI + component system for desktop/mobile layouts:

standardized tables

standardized cards

standardized forms

shared modals

Implement code splitting and lazy-loading for performance.

Remove dead code, old routes, and duplicated React pages.

Stabilize and re-test sync layer after modularization.

Run automated tests and generate a report on:

Breakages

Missing interfaces

API mismatches

Schema inconsistencies

PHASE 4 — FINAL DELIVERY



Produce:

A migration guide

A new architecture diagram

A README explaining the modules

Updated Typescript types and interfaces

A list of newly created reusable components

A dev onboarding guide

IMPORTANT RULES

Do NOT introduce breaking API changes unless necessary.

Always check for compatibility with existing database schemas.

Ensure no feature duplication.

Ensure mobile and desktop views remain functional.

Ensure Telemetry and Sync modules remain stable at all times.

Ask for approval before major structural moves.