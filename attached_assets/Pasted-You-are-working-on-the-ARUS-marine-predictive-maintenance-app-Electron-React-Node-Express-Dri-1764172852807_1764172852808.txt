You are working on the ARUS marine predictive-maintenance app (Electron + React + Node/Express + Drizzle + SQLite/PostgreSQL hybrid).



Step 1 – Architecture & Schema Audit (NO CHANGES YET)

Find the inventory / parts modules:

Backend: routes, services, repositories (inventory, parts, stock, etc.).

DB tables (Drizzle schema or SQL migrations) for:

inventory_items or parts

any inventory_movements / stock_transactions.

Find the work orders modules:

Backend: routes/controllers/services for work orders.

DB tables for:

work_orders

any work_order_items, work_order_parts, or similar link tables.

Answer these questions in a short summary:

Is there already a link table between work orders and parts?

If yes, what is it called and how is it used?

If no, where is the best place (file + schema) to define it to keep consistency with the rest of the app?

Does the app already adjust inventory when a WO is completed?



Only after confirming this, proceed to implementation.

Step 2 – Database design (SQLite + PostgreSQL compatible)



If a proper link table does not exist, create or fix one, e.g.:

work_order_parts:

id (PK)

work_order_id → FK → work_orders.id

part_id → FK → inventory_items.id (or existing parts table)

quantity_planned (number)

quantity_used (number)

unit_cost_snapshot (number/decimal)

line_total_cost (computed or stored)



Make sure:

Migrations are defined for both SQLite and PostgreSQL modes.

Foreign keys are aligned with the existing work_orders and inventory tables.

Add useful indexes:

(work_order_id)

(part_id)

Optional: (work_order_id, part_id) unique constraint to avoid duplicates.



If a link table already exists but is incomplete, extend it safely via migrations rather than breaking existing data.

Step 3 – Backend API: Link WOs ↔ Parts



Update or add endpoints that:

Attach parts to a work order

POST /api/work-orders/:id/parts

Body: array of { partId, quantityPlanned, quantityUsed? }

Update parts on a work order

PUT /api/work-orders/:id/parts

Get parts for a work order

GET /api/work-orders/:id/parts



Enforce:

Validation for part existence and positive quantities.

Tenant / org isolation and x-org-id behavior already used elsewhere.

No duplicate rows for the same work_order_id + part_id.

Step 4 – Inventory Impact on Work Order Close



Check existing WO lifecycle:

Find the “close work order” path, e.g.:

POST /api/work-orders/:id/close

or status updates that set status = 'closed'.



Extend this logic so that when a work order is closed:

For each work_order_parts row:

Determine quantity_used (fallback to quantity_planned if not explicitly set).

Decrement stock in inventory_items (or the existing inventory table).

Optionally log stock movements:

In inventory_movements or equivalent:

movement_type = 'ISSUE_TO_WO'

work_order_id

part_id

qty_change = -quantity_used

Recalculate WO cost:

Sum quantity_used * unit_cost_snapshot for all parts.

Store total_parts_cost on work_orders.



Ensure logic works in:

Embedded SQLite mode.

Cloud/PostgreSQL mode.

Handles missing inventory gracefully (log and warn, don’t crash the app).

Step 5 – Frontend UX: Connect Work Orders to Inventory



Find the Work Orders UI and implement or refine:

Parts tab / section in WO detail view

Add a “Parts” section with:

Search + select part (using inventory API).

Show current stock and location.

Quantity input.

Display a table for attached parts:

Part number / name

Quantity (planned, used)

Current stock

Cost (if available)

Validation and feedback

Warn if requested quantity > available stock.

Allow override with clear warning (in case of stock discrepancies).

Show the estimated total parts cost on the WO.

Performance

Use debounced search for parts.

Avoid flooding the API with calls while typing.

Step 6 – Testing & Guardrails



Add tests for:

Creating a WO and attaching parts.

Closing a WO and ensuring:

stock levels decrease correctly,

parts cost is calculated correctly,

no negative stock unless explicitly allowed.

Behavior in offline/embedded mode (if applicable in the repo).

Migration compatibility for existing databases (no data loss).

Step 7 – Final Developer Notes



When you’re done, produce:

A short summary of:

existing vs. new behavior,

any intentional breaking changes (if unavoidable),

how to use the new endpoints + UI.

File paths changed for:

schema

migrations

backend routes

frontend components



Make sure everything is consistent with the existing architecture and does not duplicate similar functionality elsewhere in the codebase.

