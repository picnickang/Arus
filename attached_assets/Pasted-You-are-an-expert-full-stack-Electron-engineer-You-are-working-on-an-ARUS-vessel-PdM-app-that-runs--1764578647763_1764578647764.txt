You are an expert full-stack/Electron engineer. You are working on an ARUS vessel PdM app that runs in Electron (desktop) and in a browser environment. I want to refactor the software update system to use the standard Electron + GitHub Releases flow instead of the current custom DB-based scheduler that’s throwing no such table: update_settings errors.



GOALS

Electron desktop app (macOS) should:

Use electron-updater + GitHub Releases for auto-updates.

Have a Software Update tab/page in the UI that:

Shows current version and status.

Allows manual “Check for updates”.

Allows “Download update” and then “Install & restart”.

Shows basic status messages (no update / update available / downloading / ready to install / error).

Non-Electron / web deployments:

Must not break.

Should not try to run Electron update code.

Software Update tab should degrade gracefully (e.g. “Updates are managed by server / not available in this environment”).

Remove or disable the broken custom update system:

There is currently an UpdateScheduler which logs:

Error in scheduled update check: LibsqlError: SQLITE_ERROR: no such table: update_settings.

I want this cleaned up:

No more hard failures.

No hard dependency on update_settings or a libSQL connection for updates.

STEP 1 – DISCOVER CURRENT UPDATE LOGIC

Search the codebase for any update-related logic, including but not limited to:

UpdateScheduler

update_settings

updateSettings

any scheduler or cron-like code dealing with app updates (NOT telemetry pruning, etc.)

For each place you find:

Summarize briefly (in comments) what it currently does.

Identify if it’s Electron-specific, server-side, or shared.

Identify any DB tables or drivers it depends on (like libSQL, drizzle, sqlite).

Based on this, propose (in comments and code structure) which parts:

Should be removed,

Should be soft-disabled (no-op / warnings only),

Or can be repurposed for a different role (e.g. only tracking last update check timestamp in local DB, but not required for updates).



Do NOT change any code yet, just annotate and plan.

STEP 2 – ADD ELECTRON + GITHUB UPDATER PLUMBING



Now implement standard Electron auto-update for macOS Electron builds.

Dependencies & build config:

Add (or confirm) dependencies:

electron-builder

electron-updater

In package.json or electron-builder.yml, add/adjust build config with a GitHub publish target. Example structure (adapt to existing config, do NOT blindly overwrite):

"build": {
  "appId": "com.arus.pdm",
  "mac": {
    "category": "public.app-category.business",
    "target": "dmg"
  },
  "publish": [
    {
      "provider": "github",
      "owner": "YOUR_GITHUB_OWNER",
      "repo": "YOUR_REPO_NAME"
    }
  ]
}


Add or update NPM scripts:

"electron:build:mac": "electron-builder --mac"

Keep existing electron/electron:dev scripts intact.

Main process integration (Electron only):

In the main process file (where BrowserWindow is created), integrate autoUpdater from electron-updater.

Ensure all update code is guarded so it only runs:

When process.versions.electron exists, and

When app.isPackaged === true (no auto-updates in dev mode).

Implement IPC channels for the renderer:

update:check → calls autoUpdater.checkForUpdates()

update:download → calls autoUpdater.downloadUpdate()

update:install → calls autoUpdater.quitAndInstall()

Listen for update events and forward to renderer (via webContents.send):

update-available

update-not-available

download-progress

update-downloaded

error

Example shape (adapt to actual file structure and TypeScript/JS style):

import { autoUpdater } from "electron-updater";
import { ipcMain, app } from "electron";

function setupAutoUpdater(mainWindow: BrowserWindow) {
  if (!process.versions.electron || !app.isPackaged) {
    return; // disable in dev or non-electron env
  }

  autoUpdater.autoDownload = false;

  autoUpdater.on("update-available", (info) => {
    mainWindow.webContents.send("update:available", info);
  });

  autoUpdater.on("update-not-available", (info) => {
    mainWindow.webContents.send("update:not-available", info);
  });

  autoUpdater.on("download-progress", (progressObj) => {
    mainWindow.webContents.send("update:download-progress", progressObj);
  });

  autoUpdater.on("update-downloaded", (info) => {
    mainWindow.webContents.send("update:downloaded", info);
  });

  autoUpdater.on("error", (err) => {
    mainWindow.webContents.send("update:error", err.message || String(err));
  });

  ipcMain.handle("update:check", async () => {
    await autoUpdater.checkForUpdates();
  });

  ipcMain.handle("update:download", async () => {
    await autoUpdater.downloadUpdate();
  });

  ipcMain.handle("update:install", () => {
    autoUpdater.quitAndInstall();
  });
}


Insert setupAutoUpdater(mainWindow) after the main window is created.

Expose current version via IPC:

Implement ipcMain.handle("update:getVersion", () => app.getVersion()).

STEP 3 – WIRE THE EXISTING “SOFTWARE UPDATE” / “UPDATE” TAB



The app already has a settings/software update area. Instead of creating a brand-new page, find the existing update/settings UI and wire it to the new Electron update IPC.

Search the React frontend for:

“Software Update”

“UpdateScheduler”

anywhere “update” is mentioned in settings.

In that React component/page:

Create a simple state machine for update status:

idle, checking, available, downloading, downloaded, error, not-available.

On mount:

Call an API or IPC to get currentVersion from update:getVersion (if running in Electron).

Wire buttons:

“Check for updates” → ipcRenderer.invoke("update:check")

“Download update” → ipcRenderer.invoke("update:download")

“Install and restart” → ipcRenderer.invoke("update:install")

Listen to IPC events:

update:available, update:not-available, update:download-progress, update:downloaded, update:error

Update status and progress accordingly.

Environment-aware behavior (critical):

Add a small helper in the frontend to detect Electron vs browser:

export function isElectron() {
  // use a safe runtime check
  return !!(window && (window as any).process && (window as any).process.versions && (window as any).process.versions.electron);
}


In the Software Update component:

If !isElectron(), show a message like:

“You are running in a browser / vessel web deployment. Software updates are managed by the server or deployment platform and cannot be triggered here.”
Hide or disable the Electron update buttons in that case.

Keep the styling consistent with existing shadcn/Tailwind UI components (Buttons, Cards, Badges, etc.). Do NOT introduce a completely different design system.

STEP 4 – CLEAN UP / SOFT-DISABLE THE OLD UPDATE SCHEDULER

Go back to the server side (Node/Express/Turso/SQLite/libSQL) update scheduler you found in Step 1.

Refactor it so that:

It no longer attempts to query or depend on the update_settings table.

It no longer throws hard errors like:

SQLITE_ERROR: no such table: update_settings

At most, it can:

Log a single warning on startup if that table is missing, OR

Be completely removed / turned into a no-op if it is no longer needed.

Make sure:

All cron jobs or intervals related to updates are either:

removed, or

clearly documented as no-ops with TODO comments.

This does not touch other schedulers (telemetry pruning, sync manager, ML threshold calibrator, etc.).

If there is a DB schema for update_settings:

Either:

Keep it but make its usage optional and guarded, OR

Remove it entirely if it served only the outdated custom updater.

Ensure that migrations do not fail if this table is missing.

STEP 5 – SAFETY CHECKS & COMPATIBILITY



Before finishing, perform the following checks and adjustments:

TypeScript / lint / build:

Run the project’s TypeScript build / lint commands and fix any type errors introduced by the refactor.

Ensure Electron main process compiles cleanly.

Ensure frontend builds (both Replit/web and Electron) are not broken.

Environment checks:

Verify there is no reference to autoUpdater or ipcRenderer in server-only code.

Ensure isElectron() guards are correctly placed so that web deployments don’t try to use Electron APIs.

Unit/integration tests (if present):

If there are tests around the settings/update page, update them to reflect the new IPC-driven behavior.

If there are no tests, add at least:

A small test validating that the Software Update tab behaves gracefully in non-Electron environment (shows the fallback message).

A test for the state machine transitions based on mock update events.

Update documentation:

Add a short Markdown doc (e.g. docs/updates.md or a section in existing docs) explaining:

How to configure build.publish for GitHub.

How to produce macOS builds: npm run electron:build:mac.

That updates are handled via GitHub Releases + electron-updater now.

Any environment variables needed for GitHub tokens (if CI is in use).

CONSTRAINTS

Do not remove or break any other ARUS features:

Telemetry ingestion, MQTT reliable sync, SQLite/Turso dual storage, PdM models, crew management, logbooks, etc.

Keep all changes backwards compatible with current local/vessel deployments.

Keep existing logging style/format intact for non-update systems.



When you’re done, summarize (in comments at the top of the main Electron file and the Software Update React component) the new update architecture and how a developer should configure GitHub + builds to make it work.