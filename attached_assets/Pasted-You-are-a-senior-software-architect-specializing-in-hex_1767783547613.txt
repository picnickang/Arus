You are a senior software architect specializing in hexagonal (ports & adapters) architecture, high-volume telemetry ingestion, and time-series systems. You are working inside an existing Node.js + TypeScript application for vessel/equipment telemetry and predictive maintenance.

OBJECTIVE
Review the current telemetry ingestion implementation and refactor or recommend changes so it follows a **hexagonal architecture** with clear ports/adapters, strong idempotency, and scalable database standards — WITHOUT rewriting the system from scratch.

PRIMARY QUESTIONS YOU MUST ANSWER
1) Can the current telemetry ingestion be cleanly expressed as hexagonal architecture?
2) Which parts are domain, application, inbound adapters, and outbound adapters?
3) Which improvements will meaningfully improve reliability, replayability, and scale?

---

### STEP 1 — DISCOVERY (DO NOT SKIP)
Scan the repository and explicitly list:
- All telemetry entry points (HTTP, MQTT, WebSocket, file replay, cron jobs)
- Parsing/validation logic (schemas, DTOs)
- Deduplication / idempotency logic (if any)
- Database tables related to telemetry (raw, decoded, alerts)
- Background jobs/workers/queues
- Observability (logs, metrics, error handling)

Summarize the **current ingestion flow** in a bullet list and a simple text diagram.

---

### STEP 2 — HEXAGONAL MAPPING
Map the current code into hexagonal layers:

#### Domain (pure, no I/O)
- Telemetry envelope
- Event identity rules
- Validation and normalization rules

#### Application (use cases)
- IngestTelemetryBatch
- DecodeTelemetry
- ReplayTelemetry

#### Inbound Adapters
- HTTP routes
- MQTT handlers
- CLI / file replay

#### Outbound Adapters
- Raw telemetry persistence
- Decoded signal persistence
- DLQ storage
- Metrics / logging

Explicitly identify **violations** (e.g., DB calls inside controllers).

---

### STEP 3 — CODING STANDARD RECOMMENDATIONS
Propose a clean coding standard:
- Folder structure (domain / application / adapters / infra)
- Naming conventions
- Schema versioning strategy
- Idempotency rules (event_id, seq, hashes)
- Error handling patterns
- Feature flags for ingestion changes

ONLY recommend changes that are incremental and compatible with the existing codebase.

---

### STEP 4 — DATABASE STANDARD RECOMMENDATIONS
Evaluate current tables and propose improvements aligned with:
- Append-only raw telemetry
- Canonical decoded signals
- Dead-letter queue
- Time-series indexing and retention
- Idempotent inserts
- UTC timestamps

Provide **idempotent SQL** (`CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`) consistent with the repo’s startup index-provisioning approach.

---

### STEP 5 — IMPROVEMENT RANKING
Rank all suggested changes by:
- Reliability impact
- Operational safety
- Effort (S / M / L)
- Risk

Classify them as:
- MUST DO
- SHOULD DO
- NICE TO HAVE

---

### STEP 6 — PATCH-READY OUTPUT
After analysis, generate:
- Interface definitions for ports (TypeScript)
- Example adapter implementations (DB, HTTP)
- A minimal ingestion use-case implementation
- Test plan:
  - telemetry ingestion integration test
  - idempotency test
  - persistence verification test

Do NOT generate full rewrites. Provide focused, patch-ready snippets only.

---

### DESIGN RULES (MANDATORY)
- Ingestion must be fast and non-blocking
- Raw telemetry must be stored before decoding
- Decoding must be async and replayable
- Ingestion must be idempotent
- Failures must go to a DLQ, not be dropped
- All timestamps stored in UTC
- No infrastructure imports inside domain code

---

### EXECUTION
Start by listing the telemetry-related files you find. Then follow the steps in order and produce the deliverables.

Be explicit, concise, and pragmatic.