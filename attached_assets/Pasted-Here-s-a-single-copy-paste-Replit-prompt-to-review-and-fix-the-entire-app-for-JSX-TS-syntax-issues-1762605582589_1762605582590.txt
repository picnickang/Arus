Here’s a single copy-paste Replit prompt to review and fix the entire app for JSX / TS syntax issues and similar structural problems.

Replit prompt (for the whole app):



You are Replit’s AI pair programmer working on a full-stack TypeScript monorepo for a marine PdM app.



The project structure (approximate):

client/ – React + TypeScript + Vite, shadcn/ui, TanStack Query

server/ – Node/Express, Drizzle ORM, PostgreSQL, MQTT, schedulers, ML helpers

shared/ – shared schema.ts and Zod types

Various config & scripts: seeding, sync, schedulers, etc.



Recently, the app has had recurring syntax/JSX issues like:

Unexpected token, expected ","

Ternary branches returning multiple JSX siblings instead of a single root element

JSX inside conditionals without a wrapper

Similar structural problems in many pages (not just equipment-registry.tsx)



I want you to do a full-app structural and syntax repair pass, not just fix one file.

1. Global scan for JSX and TS syntax issues



Go through all *.tsx and *.ts files, especially under:

client/src/pages/**

client/src/components/**

Any other TSX files (dialogs, analytics, dashboards, etc.)



For JSX:

Fix all conditional rendering patterns that can break JSX parsing, for example:

condition ? <A /> : <B /><C />            // ❌
{condition && (
  <A />
  <B />                                   // ❌ multiple siblings
)}


Make sure every conditional branch returns a single root element:

condition ? (
  <>
    <Table>...</Table>
    <Pagination />
  </>
) : (
  <>
    <EmptyState />
    <HelperText />
  </>
);
or

{condition && (
  <>
    <A />
    <B />
  </>
)}


Also check:

Return statements in components (return (...)) always have one root node.

No stray JSX just hanging outside expressions or return blocks.

No extra closing </Table> / </div> / </Card> tags or missing openings.



Fix all such issues across the app, not just where errors have already been reported.

2. Fix other common structural mistakes



While scanning, also repair these patterns:

Dangling commas or incorrect tokens after JSX blocks:

return (
  <Card>...</Card>,
  <div>...</div>   // ❌ invalid
);


Mis-nested JSX:

Opening/closing tags that don’t match.

Fragment <> opened but closed as </div> (or vice-versa).

JSX used directly where a string/number is expected (e.g., in object literals by mistake).



Aim for code that would pass a TypeScript + ESLint + JSX parse.

3. TypeScript & import sanity pass



Do a quick TypeScript sanity sweep, especially in:

client/src/pages/**

client/src/components/**

server/**

shared/schema.ts & related files



Fix things like:

Missing imports for types or components (e.g., Equipment, SensorConfiguration, hooks, shadcn components).

Incorrect import paths (@/hooks/useOrganization vs @/contexts/OrganizationContext, etc.).

Imported but unused symbols that cause confusion — it’s ok to remove unused imports if they’re truly unused.

Basic type mismatches where props clearly expect a certain shape and the data is static/obvious.



Do not do big refactors — just fix enough so the code compiles and behaves as intended.

4. Keep behavior & layout the same



When you add fragments or wrappers:

Prefer <>...</> (React fragments) if you only need structure.

Use div/Card/section only when it makes semantic or layout sense.

Don’t change existing logic, props, or handlers.

Keep the UI behavior unchanged: same tables, filters, dialogs, charts, and buttons.

5. Verify with build & dev commands



After you’ve applied all fixes:

Run the TypeScript / build / lint steps used by this repo (for example):

pnpm build or npm run build

pnpm lint or npm run lint

pnpm typecheck or similar if defined

Fix any remaining syntax or JSX errors they report.

Ensure the dev server (Vite) starts cleanly, with:

No Unexpected token, expected ","

No JSX parse errors

No obvious TypeScript syntax errors

Manually verify that key pages render without crashing, especially:

Equipment Registry page

Inventory / parts / stock pages

Vessels page

Work orders and maintenance pages

PdM / analytics dashboards



All table + pagination + empty/filtered states should compile and render without JSX errors.

6. Deliverables

Update all affected files directly in the repo.

Show me:

A list of files you modified (paths only).

The final, fixed version of at least one previously problematic file (e.g. client/src/pages/equipment-registry.tsx) as a reference.

Confirm that:

The project builds successfully.

Dev server starts without JSX syntax errors.

The main React pages render correctly.

Use the entire repo context and ensure no JSX/TS syntax issues remain anywhere, not just in spots that already threw errors.