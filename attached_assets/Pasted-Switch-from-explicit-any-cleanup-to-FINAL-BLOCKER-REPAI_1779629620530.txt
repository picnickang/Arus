Switch from explicit-any cleanup to FINAL BLOCKER REPAIR MODE.

Current baseline: 23.

Do not simply replace any with unknown. Each remaining any is masking a real runtime or schema drift issue.

Goal:
Repair the blockers safely, then reduce explicit-any baseline to 0.

Work in this order:

1. server/services/ml/ml-training-job-queue.ts
- Fix pg-boss v10 handler mismatch.
- Confirm whether WorkHandler receives Job<T>[] or single Job<T>.
- Update worker logic to correctly handle batch jobs if required.
- Type TrainingJobData explicitly.
- Preserve existing job processing behavior.
- Remove the remaining anys only after the runtime contract is correct.

2. server/services/ml/prediction-outcome-tracker.ts
3. server/services/ml/model-evaluation-gate.ts
4. server/services/ml/prediction-calibration.ts
- Resolve schema drift against modelPerformanceValidations.
- Do not write nonexistent columns.
- Either:
  a) update code to match current schema, or
  b) add a proper schema migration if those fields are genuinely required.
- Prefer minimal safe code alignment unless product logic clearly requires schema expansion.

5. server/ml-prediction/model-loader.ts
- Fix CircuitBreaker API drift.
- Either use the current consolidated CircuitBreaker API correctly, or add a compatible adapter wrapper.
- Do not fake methods with unsafe casts.

6. server/shared/base-repository.ts
- Treat as highest-risk.
- Do not broadly rewrite all repository generics unless necessary.
- Prefer a local typed adapter/wrapper approach that preserves concrete repository extensions.
- Keep public repository behavior unchanged.

Validation after each blocker group:
- npx tsc --noEmit
- scoped eslint for touched files
- node scripts/check-explicit-any-burndown.mjs
- architect/code review

Rules:
- No explicit any.
- No as any.
- No eslint-disable.
- No ts-ignore.
- No hiding schema drift with casts.
- Fix the underlying mismatch first, then remove the any.

Stop only when:
1. baseline = 0, or
2. a schema migration/product decision is genuinely required.

Final report:
- 23 → final baseline
- blocker fixed per file
- files changed
- migrations added, if any
- tsc result
- lint result
- baseline result
- remaining manual decisions, if any