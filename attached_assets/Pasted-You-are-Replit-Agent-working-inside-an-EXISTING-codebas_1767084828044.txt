You are Replit Agent working inside an EXISTING codebase. This prompt is ONLY about FRONTEND QUALITY and UI/UX POLISH for the Crew & Compliance module after the scheduling refactor. Do NOT change backend logic, DB schema, sync engine, or email system unless a UI bug strictly requires a minimal API fix.

PRIMARY GOAL
Make the scheduling UI “crisp”:
- fast
- clean
- consistent
- visually aligned with existing design system
- highly usable for daily ops (small fleet, frequent changes)
Keep the existing navigation/tabs: Crew Roster / Schedule / Hours of Rest / Compliance. Improve them; do not reorganize the product.

NON-NEGOTIABLES
- Reuse existing UI kit/components, styling conventions, spacing scale, typography, icons, theming.
- Do not introduce a new component library unless the repo already uses it.
- Maintain accessibility (keyboard nav, contrast, focus states).
- Optimize performance: timeline virtualization, memoization, minimal re-renders.
- All UI changes must be covered by UI tests where the repo supports them.
- Provide a “UI POLISH CHECKLIST” and validate every item before declaring done.

PHASE 0 — FRONTEND DISCOVERY (DO THIS FIRST)
1) Identify:
- frontend framework (React/Next/etc.)
- styling system (Tailwind/CSS Modules/styled-components/etc.)
- component library + tokens (buttons, cards, typography, spacing)
- state management (Redux/Zustand/React Query/etc.)
- existing patterns for tables, drawers, modals, toasts, empty states
2) Locate and list exact files for:
- Crew Roster tab
- Schedule tab (planner)
- Hours of Rest tab
- Compliance tab
- shared components used in these tabs
Output a “Frontend UI Architecture Snapshot” and then begin implementation.

CRISP UI DELIVERABLES (MUST IMPLEMENT)
A) SCHEDULE TAB — PLANNING BOARD UI POLISH
- Timeline grid must feel “enterprise crisp”:
  - stable row heights
  - aligned column headers
  - sticky vessel names (left) and sticky date header (top)
  - smooth scroll (virtualized)
  - hover + selection states that are subtle and consistent
- Assignment blocks:
  - clear color/label hierarchy using existing tokens
  - readable role + crew name at a glance
  - overflow handling (truncate + tooltip)
  - warning badges for constraint issues (error/warn icons)
  - drag/drop preview + snapping (if current dnd supports)
  - resize handles that are visible but not ugly
- Side panel crew pool:
  - fast filter UX (rank, cert, availability, vessel)
  - multi-select filters with clear “reset”
  - skeleton loading, empty states, and no jank
- Drawer (assignment details):
  - consistent spacing and typography
  - tabs: Details / Constraints / Suggestions / Audit
  - “Apply Suggestion” primary action with confirm/toast
  - show “Why” explanations in scannable bullets
- Sync status:
  - subtle pill “Offline / Syncing / Up to date”
  - pending ops count
  - conflicts link to “Sync Issues” panel

B) CREW ROSTER TAB — READABILITY + ACTIONABILITY
- Make roster list/table:
  - sortable columns
  - quick search
  - show “Current vessel”, “Next hitch”, “Cert expiry soon” chips
  - consistent row density and spacing
  - fast rendering (virtualize if large list)

C) HOURS OF REST TAB — CLARITY
- Rest Hours grid:
  - clear legend (compliant / warning / violation)
  - hover details
  - export button styling consistent
  - show violations summary card above grid (optional but recommended)

D) CONSISTENT MICRO-UX
- Loading states: skeletons (not spinners everywhere)
- Empty states: friendly and instructive
- Error states: actionable messages with retry
- Toasts/notifications: consistent placement and wording
- Confirm dialogs for destructive actions (unassign/cancel)
- Keyboard shortcuts (optional but great): e.g., “/” focus search, ESC close drawer

PERFORMANCE REQUIREMENTS (MUST PASS)
- The Schedule planner must render and scroll smoothly:
  - use virtualization for rows and date columns if needed
  - memoize heavy components
  - avoid rerender storms during drag/drop
  - debounce expensive filters
- Add basic profiling notes (what was optimized and how).

DESIGN CONSISTENCY REQUIREMENTS
- Enforce a single spacing and typography scale.
- Unify icons and status colors using existing theme tokens.
- Ensure alignment: headers, paddings, gutters, column widths.
- No mixed button styles. Use existing button variants only.

ACCESSIBILITY REQUIREMENTS
- Keyboard navigation:
  - tab through filters, assignments, drawer actions
  - ESC closes drawer/modals
  - focus visible
- ARIA labels for drag handles and buttons
- Color contrast meets existing standards.

TESTING (MUST RUN)
1) UI tests (use existing tooling):
- Schedule tab renders with sample data
- Drawer opens, tabs switch
- Suggestions list renders and apply action triggers mutation call
- Offline pill state renders correctly
2) Visual regression (if repo has it): update baselines carefully
3) Run full frontend test suite and include a TEST REPORT listing commands run.

OUTPUTS REQUIRED
- “Frontend UI Architecture Snapshot”
- “UI POLISH CHECKLIST” (bulleted, with boxes) and mark completed items
- “Performance Notes”
- “TEST REPORT” with commands and pass status

NOW DO THE WORK
1) Start with discovery and output the architecture snapshot.
2) Implement UI polish changes in small, clean commits.
3) Run tests and deliver the checklist + performance notes + test report.
4) Do not touch backend except for minimal contract fixes required for UI correctness.