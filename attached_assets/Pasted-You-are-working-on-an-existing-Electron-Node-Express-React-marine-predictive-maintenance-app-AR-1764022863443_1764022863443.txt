You are working on an existing Electron + Node/Express + React marine predictive maintenance app (ARUS) with a hybrid PostgreSQL + SQLite/libSQL storage architecture, MQTT ingestion, a digital twin, and an embedded telemetry simulator.



Your job is to do a focused, non-destructive review and improvement pass on telemetry monitoring, visualization, and actionable insights, but ONLY after verifying that proposed changes do not duplicate or regress existing features.



Please follow these steps in order:

0. Project + architecture scan (MUST DO FIRST)

Inspect the project root and infer the current architecture:

Electron main process / bootstrapping

Backend: Express server, telemetry ingestion, digital twin, ML/RUL logic

Databases: embedded SQLite/libSQL (vessel-local) + optional cloud/PostgreSQL/Turso

Frontend: React/Vite, charts, dashboards, AI/insights views

Identify existing telemetry-related modules and routes, such as (names may vary):

Telemetry simulator / ingestion: MQTT, VesselTelemetrySimulator, ingestion workers

APIs: /api/equipment, /api/equipment/health, /api/alerts/*, /api/sync/*, etc.

Digital twin & ML: health models, RUL estimators, threshold calibrators

UI: Fleet / Vessel / Equipment dashboards, HealthMonitor, Diagnostics, AIInsights, AnalyticsHub, charts (LineChart, ChartWrapper, etc.), alerts/notifications views

Produce a short summary in a comment or log:

What telemetry data exists now (schema + APIs)?

How telemetry is currently visualized (screens/components)?

How AI / actionable insights are surfaced today (if any)?

What real-time mechanisms are in place (polling/websockets/MQTT → digital twin → UI)?



Do NOT change any code yet. This step is purely descriptive.

1. Overlap & value check



Based on that scan:

List concretely:

Existing telemetry visualizations

Existing AI/ML insights (RUL, health scoring, alerts, recommendations)

Existing alerts/notifications mechanisms (including rate limits)

For improvements you might suggest (e.g., new dashboards, new charts, new insight panels, new tables), explicitly answer:

Does this feature already exist under a different name or screen?

Is this a genuine improvement in clarity / usability / maintainability / marine-specific value, or just a visual re-skin?

Does it conflict with any current architectural decisions (e.g., offline-first vessel mode, multi-tenant org isolation, SQLite/local vs PostgreSQL/cloud)?



If you identify potential changes that are pure duplication or would cause regressions, explicitly reject them and explain why in a comment, then do not implement them.



Only proceed to implementation for changes that pass this “adds real value, no overlap” test.

2. Telemetry monitoring & visualization review



Now review the telemetry monitoring and display end-to-end:

Confirm:

How live telemetry flows (simulator/MQTT → ingestion → DB → digital twin → API → React)

Which React components/screens consume:

Equipment status

Equipment health

Telemetry time series

Alerts & events

Evaluate and improve (only where missing or weak):

Real-time dashboards

Single-vessel “bridge” style view with key KPIs (RPM, load, temp, vibration, fuel, alarms)

Fleet overview (per-vessel status, health, alerts summary)

Time-series charts

Per sensor & per equipment line charts with proper units, aggregation, and time selection

Clear labels & legends oriented to marine users (main engine, aux genset, thrusters, pumps)

Health / RUL UI

Traffic-light status indicators (GOOD / WARNING / CRITICAL)

Remaining Useful Life (RUL) display if ML models exist

Clear indication of why something is at risk (e.g., high vibration vs temperature trend vs hours run)

Alerting UI

Sorted & filterable alert timeline

Severity coloring

Acknowledge/resolve flow (if backend supports it)



Make changes to React components and API usage only where they clearly tighten up telemetry presentation or fix obvious UX/logic issues. Avoid refactoring large parts of the app unless absolutely necessary.

3. Actionable insights evaluation (marine PdM-specific)



For each part of the app that is intended to provide actionable insights (not just raw data):

Identify:

Where AI/ML or rule-based logic generates:

Degradation/health scores

RUL estimates

Threshold breaches

Maintenance recommendations

Where those insights are exposed in the UI (e.g., dashboards, reports, AI/insights pages)

Evaluate marine PdM value:

Are we clearly answering questions like:

“Which equipment needs attention this week and why?”

“What is the risk of failure before the next planned port call/dry-dock?”

“What is the impact of ignoring this alert (e.g., cost, downtime, safety)?”

Do insights connect to existing elements:

Work orders / maintenance schedules

Inventory / spare parts

Hours of run / mission profiles?

Propose and (if non-overlapping) implement incremental improvements, such as:

Better wording/structure of insight panels

Aggregated “Top 5 risks” view per vessel or per fleet

Clear linking from an alert/insight → equipment detail → existing maintenance plan/work orders

Risk levels that combine multiple signals (trend + threshold + RUL) into a single explanation



Again, do not duplicate existing insights screens or flows; only refine, connect, and clarify.

4. Database + schema sanity for telemetry & updates



Within the constraints of the current hybrid storage architecture:

Verify that telemetry-related tables and views exist and are used consistently for:

Live/near-real-time telemetry

Aggregated metrics for dashboards

Equipment health & RUL data

Alerts / notifications

Sync/pending-conflicts tracking (vessel ↔ cloud)

Check for known issues visible in logs (do NOT fabricate them; use actual code/logs), such as:

Rate limiting / 429 errors on telemetry/alerts endpoints

Unauthorized / missing x-org-id issues

Health check functions using a wrong DB client shape (e.g., calling db.execute on a client that doesn’t support it)

Missing tables like update_settings vs schema definitions

For any genuine issues you find:

Fix schema vs code mismatches in the least invasive way

Guard optional/remote features in embedded desktop mode (e.g., skip cloud-only checks when in vessel/local mode)

Ensure that telemetry & health queries use the correct DB client (SQLite/libSQL when offline, cloud when configured)



If a fix would require a large migration or architectural shift, do not implement it; instead, add a clear TODO comment and a short design note explaining the issue and options.

5. Multi-device & offline-first considerations (check only, minimal changes)



This app is designed for desktop Electron and potentially other devices:

Confirm:

That offline-first behavior (vessel/local mode) is respected in telemetry & insights flows

That APIs used in the frontend degrade gracefully when cloud sync / MQTT broker is not available

That any layout or component assumptions don’t hard-break on smaller viewports (you don’t have to fully redesign for mobile, just ensure no catastrophic breakage if the app is later wrapped)

Only make small, targeted changes:

Defensive checks around remote-only services

Light responsive fixes (e.g., avoid impossible fixed widths on critical dashboards)

6. Reporting back



At the end, provide:

A short architecture-aware summary:

What existed already (telemetry, dashboards, insights, DB)

What you chose not to touch (and why, e.g. duplication, risk, architecture mismatch)

What you actually changed, grouped by:

Telemetry UI

Actionable insights UX

DB/schema or runtime fixes

Any high-priority follow-up items that require product/architecture decisions rather than code tweaks.



Please now scan the repo and start with step 0 (architecture + overlap analysis), and only then implement incremental changes that clearly add value without overlapping existing features.