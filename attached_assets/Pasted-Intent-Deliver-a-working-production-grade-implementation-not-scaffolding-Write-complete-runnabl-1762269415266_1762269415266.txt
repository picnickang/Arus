Intent: Deliver a working, production-grade implementation, not scaffolding. Write complete, runnable code with wiring, schemas, tests, seed data, scripts, and docs. If something is unknown, make a reasonable, documented decision and implement it. Do not output TODOs, placeholders, or pseudocode.



Context (must be honored)

Backend: Node.js + Express (TypeScript), JWT RBAC, zod validation, rate limiting, Prometheus metrics, worker threads for inference.

Frontend: React 18 + Wouter + TanStack Query + Tailwind + Recharts + Leaflet, PWA offline.

ML: LSTM/XGBoost/RandomForest ensemble w/ SHAP explainability, profile-driven dataset mixer, drift monitors, adaptive retrain suggestions.

Governance: Model Lineage (JSONL/DB) + Event Provenance (SHA-256 chain), Governance UI.

Observability: 50+ metrics, perf target p95 ≤ 1.5s for /score & /vessels/:id/performance.



Non-negotiable build rules

No skeletons. Every route, page, component, and job must be wired and runnable.

Strong typing end-to-end (TS on server, typed API clients/hooks on UI).

Validation everywhere (zod schemas for every POST/PUT/PATCH).

Fixtures included for all pages and tests (if remote datasets gated by creds, fallback to fixtures).

Tests must pass: unit + API + E2E. Add a performance harness and make it part of CI.

Docs generated with reproducible commands.

Tasks (do all)



0) Workspace & Env

Detect monorepo structure (/apps/server, /apps/client, /scripts, /docs, /checkpoints, /data).

Verify .env (DATA_DIR, CHECKPOINT_DIR, JWT_SECRET, ADMIN_TOKEN; optional KAGGLE_*). If missing, create .env.example and load sane defaults.

Install deps and build TypeScript.



1) API ↔ UI contract enforcement

Parse all client fetchers (TanStack Query) and verify actual server routes exist with matching schemas.

Where mismatched/missing, implement the server route and update the client hook. Produce /docs/audit/api_contract_matrix.md with ✅/❌ and file links.



2) Features (must be working in UI)



Implement/verify the following end-to-end, backed by real responses or fixtures:

Fleet Overview: RAG health, filters, mini map, freshness bars.

Vessel Performance: 4 VPS charts (Load↔SFOC, Fuel↔Time, Power↔STW, Load Histogram) with baseline line, fleet percentile band, anomaly shading & tooltip (contributors, confidence), toggleable overlays saved to user prefs.

Alerts → Work Orders: ack/assign/close, timeline view.

Context Timeline: operating modes (DP/Transit/Harbor/etc.), event chips.

Governance Dashboard: Model Lineage browser, Prediction Audit log (provenance chain), “Verify chain” button, CSV/JSON/PDF exports.

CII: tile + trends + export.

Co-Pilot: working panel that calls backend for summaries.

PWA Offline: Fleet/Vessel/Alerts pages load offline; deferred mutations replay on reconnect.



3) Governance & ML checks

On train/finetune, append a lineage record with artifact hashes and metrics; on /score, append provenance (prediction event with telemetry slice hash) and return modelId.

Add /model/lineage?filters…, /governance/events?filters…, /governance/verify-provenance (runs verifier, returns {ok, brokenAt?}).

Ensure drift monitors and retrain suggestions are visible in capabilities or governance pages.



4) Performance & Ops

Implement worker-thread pool for scoring with backpressure; batch adjacent windows.

Create /scripts/perf_harness.js to load-test /score and /vessels/:id/performance with realistic windows; print p50/p95/p99, error rate, throughput; exit non-zero if p95 > PERF_P95_MS (default 1500).

Expand Prometheus metrics: inference_latency_seconds, inference_batch_size, inference_queue_depth, data_freshness_seconds{vesselId}, drift_events_total, retrain_suggestions_total.



5) Security

RBAC: Operator/Engineer/Manager/Admin. Manager/Admin required for model promotion, provenance verify, retrain approvals.

Add helmet, CORS allowlist (prod), express-rate-limit; never log secrets.

zod validation for all request bodies and query params; return structured 400 errors.



6) Tests (must run & pass)

Unit (Vitest): dataset mixer, thresholds, attribution, provenance chain util, model lineage util.

API (Supertest): auth errors, schema errors, success cases for the routes above.

E2E (Playwright): Fleet → Vessel (overlays & anomalies), Alerts → WO flow, Governance chain verify, Offline replay.

Emit coverage report and include pass/fail in terminal summary.



7) Docs & Dashboards

Generate:

/docs/audit/architecture_map.md (module graph, entry points, public routes)

/docs/audit/feature_parity.md (✅ list with deep links)

/docs/audit/ml_governance.md (sample lineage/provenance, chain verification result)

/docs/audit/performance_benchmarks.md (perf harness numbers)

/docs/audit/security_checklist.md (OWASP-style table + fixes)

/docs/dashboards/*.json (Grafana dashboards for RED/SRE)

Update README quick-start with exact commands below.



8) CI

Add .github/workflows/ci.yml that runs: lint → test → perf → build; fail PR if perf p95 exceeds threshold.

Required outputs (scripts & commands)



package.json (top-level) must include:

{
  "scripts": {
    "dev": "concurrently -n server,client \"pnpm --filter @marine/server dev\" \"pnpm --filter @marine/client dev\"",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "e2e": "playwright test",
    "perf": "node scripts/perf_harness.js",
    "verify:prov": "ts-node apps/server/scripts/verify_provenance.ts",
    "docs:audit": "node scripts/emit_audit_index.js"
  }
}
Smoke test (must succeed)

# seed & datasets (use fixtures if external creds missing)
npm run build
npm run dev &  # start server+client

# health & caps
curl -s http://localhost:8000/health
curl -s http://localhost:8000/capabilities

# score path
curl -s -X POST http://localhost:8000/score \
 -H 'Content-Type: application/json' \
 -d '{"vessel_id":"V001","profile":"tug","rpm":[1200,1210,1185,1220],"shaft_torque":[300,305,295,310],
      "vibration_x":[0.12,0.13,0.11,0.12],"vibration_y":[0.09,0.08,0.10,0.09],"vibration_z":[0.11,0.12,0.13,0.12],
      "oil_temp":[85.2,85.1,85.4,85.0],"coolant_temp":[80.1,80.2,80.1,80.3],"fuel_rate":[35.1,35.6,34.9,36.0]}'

# perf harness (must pass, p95 ≤ 1500ms unless PERF_P95_MS overrides)
npm run perf

# tests
npm run test && npm run e2e
Definition of Done (fail the job if any unmet)

100% of client requests hit real, validated server routes (see api_contract_matrix.md).

All pages listed in Features render with live data or fixtures; overlays and anomaly shading work with tooltips.

/score returns { anomaly_score, modelId, contributors, confidence } and appends a provenance entry; lineage is appended on train/finetune.

perf p95 ≤ threshold; Prometheus shows non-zero counters/histograms.

RBAC + validation + rate limits + helmet + CORS in place.

Unit, API, and E2E tests pass and coverage file emitted.

Docs present in /docs/audit/*, dashboards in /docs/dashboards/*.

CI workflow exists and fails on perf regressions.

Final terminal summary (print this at the end)

PASS/FAIL per category: Features, API/UI Contract, ML/Governance, Security, Performance, Observability, PWA/Edge, Tests

p50/p95/p99 for /score and /vessels/:id/performance

Chain verification: OK or first broken entry

Test counts & coverage %

Paths to generated docs

Top 5 follow-ups (each ≤ 2 hours) with file links



Build everything. No placeholders. If an external source is unavailable, use fixtures and clearly document assumptions, but still deliver working routes, UI, and tests.



End of prompt.