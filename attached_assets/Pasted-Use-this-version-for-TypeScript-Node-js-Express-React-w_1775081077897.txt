Use this version for TypeScript + Node.js + Express + React with SQLite now and PostgreSQL compatibility/migration in mind:
You are a senior staff engineer and systems architect specializing in TypeScript, Node.js, Express, SQLite, PostgreSQL, and industrial SaaS platforms.
I have an existing application called ARUS with the following stack:
•	Backend: Node.js (Express) in TypeScript
•	Database: SQLite and PostgreSQL considerations
•	Frontend: React
•	Current capabilities:
•	LLM-generated reports (OpenAI API)
•	Some ML-based predictive maintenance features (basic)
Important context:
•	I need the system to work well with SQLite for simplicity and fast iteration
•	I do not want to paint myself into a corner if I later standardize on PostgreSQL
•	I want architectural decisions, schema choices, and code structure that are portable across both SQLite and PostgreSQL as much as practical
I want to evolve ARUS into a full intelligent operations platform that combines:
1.	Predictive maintenance (failure prediction + root cause + recommendations)
2.	Inventory management (parts, tools, assets, locations)
3.	Crew scheduling / workforce management
4.	An AI agent layer that can autonomously:
•	detect issues
•	diagnose causes
•	recommend actions
•	create work orders
•	assign crews
•	reserve inventory
Use these systems as inspiration:
•	microsoft/agentic-factory-hack (agent orchestration)
•	Atlas CMMS (work orders, maintenance workflows)
•	Shelf.nu (inventory / asset tracking)
•	ERPNext (enterprise inventory + procurement patterns)
Your job is to DESIGN and PARTIALLY IMPLEMENT a system for ARUS.
Step 1: System Architecture
Design a clean architecture for ARUS that includes:
•	service boundaries (maintenance, inventory, scheduling, agent)
•	request-driven and event-driven coordination where appropriate
•	database schema strategy that works with SQLite now and PostgreSQL later
•	where the AI agent fits (orchestrator vs embedded service)
•	how to keep this as a modular monolith first
Output:
•	a text-based architecture diagram
•	a clear module breakdown
•	recommended libraries for a TypeScript + Express stack that support both SQLite and PostgreSQL cleanly
Step 2: Data Models and Database Schema
Define TypeScript types and SQL schemas for:
Core entities:
•	Asset
•	SensorData / Telemetry
•	MaintenanceEvent
•	WorkOrder
•	InventoryItem
•	InventoryLocation
•	InventoryReservation
•	Technician
•	Crew
•	Schedule / Assignment
Include:
•	relationships
•	indexes
•	audit/history tables if useful
•	fields needed for predictive and operational workflows
Important:
•	avoid designs that depend heavily on PostgreSQL-only features unless clearly marked as optional enhancements
•	call out which schema/features are fully portable and which are database-specific
•	suggest how to handle migrations cleanly across SQLite and PostgreSQL
•	recommend ID strategy, timestamp strategy, enum strategy, and JSON field strategy that works across both
Step 3: Backend Structure (Node.js / TypeScript)
Design the backend folder structure.
Example:
•	/modules/maintenance
•	/modules/inventory
•	/modules/scheduling
•	/modules/agent
•	/shared
•	/db
For each module, define:
•	routes/controllers
•	services
•	repositories
•	domain logic
•	validators
•	events
Then generate example TypeScript code for:
•	creating a work order
•	reserving inventory for a work order
•	assigning a technician
•	updating asset status after maintenance
Prefer code that is realistic for Express + TypeScript and portable across SQLite/PostgreSQL.
Step 4: Agent Layer (CRITICAL)
Design an agent system inspired by agentic-factory-hack, but implemented in Node.js and suitable for a modular monolith.
The agent should:
•	take in signals (example: anomaly detected)
•	query internal data (assets, history, manuals, work orders, inventory)
•	generate a diagnosis
•	produce structured actions:
•	create work order
•	reserve parts
•	assign crew
•	escalate if confidence is low
Output:
•	agent architecture
•	tools/functions the agent can call
•	structured action schema
•	example orchestration loop in TypeScript
•	safety/approval points for autonomous actions
Do not assume a giant framework. Prefer a practical custom implementation unless a small library clearly helps.
Step 5: Predictive Maintenance Integration
Explain how to integrate ML models into this stack.
Compare these options:
1.	Python microservice for inference
2.	Python batch jobs called from Node
3.	JavaScript/TypeScript inference in-process
For each option, explain:
•	pros/cons
•	implementation difficulty
•	best fit for ARUS
Then recommend one.
Include:
•	example API contract
•	how predictions trigger workflows
•	how predictions feed the agent
•	how to store model outputs in a way compatible with SQLite and PostgreSQL
Step 6: Scheduling Logic
Design a practical scheduling engine for:
•	technician assignment
•	crew assignment
•	shift awareness
•	skill matching
•	availability
•	optional location proximity
Include:
•	algorithm choice
•	why it fits an MVP
•	how to evolve later into more advanced optimization
Generate example TypeScript code for a first-pass scheduling service.
Step 7: Cross-Database Engineering Decisions
Give concrete guidance on:
•	which Node.js database library or ORM/query builder to use
•	whether to use Prisma, Drizzle, Kysely, or raw SQL
•	migration strategy
•	transaction handling differences between SQLite and PostgreSQL
•	concurrency limitations in SQLite
•	write throughput considerations
•	how to structure repository code so the business logic is database-agnostic
•	when ARUS should stay on SQLite
•	when ARUS should move to PostgreSQL
•	how to make that transition low-risk
Be opinionated and practical.
Step 8: Prioritized Build Plan
Give me a phased roadmap:
Phase 1: MVP
Phase 2: Operational system
Phase 3: Autonomous agent system
For each phase include:
•	exact features to build
•	key database tables
•	API endpoints
•	background jobs
•	what to ignore for now
•	main risks
Step 9: Portability Rules
Create a short set of engineering rules for the ARUS codebase to preserve portability between SQLite and PostgreSQL.
Examples:
•	preferred column types
•	how to handle enums
•	how to store structured metadata
•	SQL features to avoid
•	migration discipline
•	testing strategy across both databases
Constraints
•	Everything must be TypeScript-first
•	Backend is Node.js + Express
•	Frontend is React
•	Database needs to support SQLite now and PostgreSQL later, or both
•	Prefer simple, shippable architecture
•	Prefer modular monolith over microservices unless clearly justified
•	Avoid overengineering
•	Assume this is an existing codebase that needs incremental evolution, not a rewrite from scratch
Output style
•	Be concrete, not theoretical
•	Include real TypeScript code where useful
•	Make this directly implementable
•	Make reasonable assumptions and proceed without asking clarifying questions
At the end, provide:
1.	the recommended target architecture
2.	the recommended database access approach
3.	the first 10 backend files I should create
4.	the first 5 database tables I should implement
5.	the key portability decisions that will keep SQLite and PostgreSQL aligned
6.	the single best migration path from SQLite-first to PostgreSQL-readyMy own tweak: add one more sentence near the top if you already know your DB layer, like:
Current data layer: Prisma
or
Current data layer: Drizzle ORM
That makes Claude’s output much more usable.