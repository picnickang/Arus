UI Align Correction — Match the Reference Image More Closely

The current implementation is NOT visually close enough to the attached reference images.

Do not continue adding features.
Do not add new backend logic.
Do not redesign architecture.

Focus only on visual/layout alignment.

Goal:
Make the admin command center and portal pages visibly match the attached mockups much more closely.

Reference-image requirements:
- dark navy / charcoal full-screen background
- left vertical sidebar with compact icon/navigation stack
- top header with page title, status pill, and compact actions
- large command-center hero/status area at top
- 3–4 KPI cards directly under the header
- main content in rounded dark cards with subtle borders
- accent colors: cyan/teal/blue, amber for warnings, red for critical
- dense but readable operational cards
- clear card hierarchy
- no plain white page sections
- no generic default shadcn look
- no oversized empty whitespace
- no old dashboard/module-grid feel

Admin command center layout should be:

1. Shell
- dark full-screen app shell
- left sidebar
- top header
- main scrollable content area

2. Top operational strip
- Fleet status
- Critical alerts
- Active work orders
- At-risk assets
- AI recommendations

3. Main grid
Left/center:
- Critical attention cards
- Predictive maintenance / AI recommendation cards
- Work orders needing action

Right rail:
- Crew issues
- Logistics blockers
- System health
- Recent activity

4. Bottom/module row
- Maintenance
- Crew Management
- Logistics
- AI Analytics
- System Admin

Do not show this as a simple list of modules.

User portal layout should match the mobile/user preview:
- compact cards
- current vessel card
- shift card
- tasks card
- alert/safety card
- feedback CTA
- mobile-first spacing

Implementation constraints:
- Preserve hexagonal navigation policy.
- Keep role logic out of React components.
- Use existing data sources only.
- No fake analytics.
- Static visual placeholders are allowed only if clearly derived from existing empty states.
- No backend changes.
- No explicit any / as any / ts-ignore / eslint-disable.

Testing:
- source-scan or component tests should assert the presence of:
  - dark shell classes
  - sidebar shell
  - command-center KPI strip
  - right rail
  - module row
  - user portal compact card layout
- existing tests must continue passing.

Validation:
- npx tsc --noEmit
- strict checks
- scoped eslint
- relevant UI tests
- architect review

Final report:
- screenshots/previews if possible
- what changed visually
- which reference-image elements are now matched
- remaining visual gaps