You are working on the ARUS / RecipeRealm marine PdM platform.

Tech stack:

Electron + React/Vite SPA frontend (TypeScript)

Node/Express backend (TypeScript compiled with esbuild)

Hybrid database: PostgreSQL (cloud) + SQLite/libSQL (vessel / embedded) using Drizzle ORM

Deployment modes: VESSEL (offline-first desktop) and CLOUD (multi-tenant)

Strong STCW/crew features already exist.



I want you to do an end-to-end architecture-aware upgrade of Crew Management, Crew Scheduler, and Hours of Rest, but ONLY in ways that are compatible with the current design and don’t duplicate existing features.

0. First: understand the current implementation

Locate all modules, APIs, and DB schemas related to:

Crew management

Crew scheduler

Hours of Rest / STCW

Typical places:

client/src/features/crew/**

client/src/features/scheduling/**

client/src/features/hours-of-rest/**

any shared STCW/compliance utilities

backend routes and services under server/** for crew, schedules, and HoR

Drizzle schema files for crew, schedules, HoR, and compliance.

Briefly summarize in comments at the TOP of a new file docs/crew-stack-notes.md:

What screens and UX already exist for:

Crew list & details

Scheduler views

Hours of Rest grid / reports

What validation and STCW compliance logic already exists.

What APIs and DB tables are currently used.

Any existing notions of compliance warnings, document tracking, or dashboards.

Before changing anything, verify:

How deployment mode is determined (local SQLite vs Postgres).

How tenant/org isolation is enforced (x-org-id, default-org-id, etc.).

That any schema changes you will make can work in both SQLite and Postgres and through the unified schema export (schema-runtime or similar).



Do NOT implement new features until you have written that short architectural summary.

1. Target improvements (only if they are NOT already implemented)



After your analysis, upgrade these three domains only where missing:



A. Crew Management

Add / ensure:

Certification Expiry Warnings

Track key STCW certificates per crew member (CoC, medical, safety, etc.).

Store: certificate type, number, issuing authority, issue date, expiry date, and file reference (if documents module already exists, reuse it).

Add backend logic to raise 30/60/90-day upcoming expiry alerts.

Surface this in frontend as:

Badges in the crew list (e.g., “Expiring in 30 days”).

A filter for “expiring soon” and “expired”.

Crew Document Tracking

If there is already a generic document table, extend/associate it with crew.

Otherwise, create a minimal crew_documents table that works for SQLite+Postgres, following existing schema conventions.

Allow upload/attachment metadata from the crew detail view (no need for full file storage in this pass; just reuse whatever object storage / path system exists, or mark TODO if storage is disabled in embedded mode).

Org/Vessel-scoped queries

Ensure all crew endpoints and DB queries enforce:

orgId scoping.

Per-vessel association where appropriate.



B. Crew Scheduler

Add / ensure:

Auto-populate Hours of Rest from Schedules

Connect the schedule output to the HoR grid so that once a schedule is published, the corresponding HoR entries are generated or updated.

This must work in an idempotent way (running twice does not duplicate entries).

Respect deployment mode and tenant isolation.

Schedule-to-HoR Preview

On the scheduler UI, add a “STCW/HoR Preview” action that:

Runs the same STCW rules as the HoR module on the proposed schedule.

Highlights violations (per crew member, per day) before publishing.

Backend: add a dedicated API endpoint if needed to compute compliance from a schedule draft without persisting it.

Fairness & Night-shift Logic

Review existing fairness distribution and night-shift limits.

If already implemented, just document them in docs/crew-stack-notes.md.

If partially implemented, finish them in a minimal, clearly commented way.



C. Hours of Rest (HoR)

Add / ensure:

Auto-population integration

Implement the server & DB support for the auto-populate function described above, using existing HoR tables.

Maintain the ability for manual edits after auto-fill.

Fatigue Risk Indicator (if not present)

Define a simple fatigue risk score per crew member, based on:

Consecutive days with minimized rest.

Number of STCW violations or near-violations in a rolling window.

Store the latest score and expose it through an API.

Show a simple color-coded indicator in HoR and/or crew views (e.g., low/medium/high with tooltips).

Keep the computation light so it works offline in vessel mode.

Fleet Compliance Dashboard (if not present elsewhere)

Implement a small backend aggregator that:

Summarizes STCW compliance status across vessels and crews (e.g., % compliant days, number of open violations, # expiring certs).

Add a simple frontend widget or page section that surfaces this overview.

Respect org/vessel scoping.

2. Architecture & compatibility requirements



Before implementing each major change, do a quick check and align with existing architecture:

DB/ORM

Use the existing Drizzle schemas and the unified schema export module (e.g. schema-runtime.ts) so that changes are consistent between SQLite and Postgres.

If you need new tables or columns, add them in the existing schema files and ensure:

Types work in both SQLite and Postgres.

Default values and NOT NULL constraints won’t break existing data.

Provide a backwards-compatible migration strategy or, if the project uses auto-sync schema, confirm that the new schemas do not conflict.

API design

Reuse route patterns and middleware (tenant isolation, auth, requireOrgId, etc.).

Keep URLs consistent with current style: /api/crew/..., /api/scheduler/..., /api/hours-of-rest/... etc.

Maintain separation between “preview” endpoints and “mutating” endpoints.

Offline / vessel mode

Make sure new features degrade gracefully when:

No cloud sync is configured.

MQTT broker is offline.

Avoid introducing mandatory external calls for core HoR/scheduling/crew functions.

Permissions

Check if a role/permissions system already exists; if so, enforce that only appropriate roles can edit schedules, HoR, or compliance settings.

3. Implementation steps



Follow this workflow:

Planning

Based on your initial analysis, add a short implementation plan to docs/crew-stack-notes.md under a heading “Planned Upgrades”.

Explicitly mark any items that the app already supports so you don’t reimplement them.

Backend

Implement schema changes (crew docs, certificate tracking, fatigue scores, any new join tables).

Add or extend services and routes for:

Certificate & document management.

Schedule→HoR preview & auto-populate.

Fatigue risk calculation.

Fleet compliance dashboard aggregation.

Add unit tests or integration tests where the project already has a testing pattern (at least for the new calculations and auto-populate logic).

Frontend

Upgrade Crew Management screens with:

Certificate expiry badges, filters, and document links.

Upgrade Scheduler with:

Buttons/controls for HoR preview and publish.

Visual indicator for violations in the preview result.

Upgrade Hours of Rest:

Auto-populate from a selected schedule (with confirmation dialog).

Fatigue risk indicators in the HoR grid and/or crew panels.

Add a basic Fleet Compliance dashboard view or widget using existing layout components (e.g. TabbedPageLayout, ResponsiveTable, ContextHelp).

UX/Validation

Ensure forms have client-side validation that matches backend constraints (dates, required fields, etc.).

Keep UX simple and consistent with existing design.

Multi-device + performance

Make sure new queries are filterable by vessel and org and are paginated or reasonably constrained.

Confirm that WebSocket and polling behaviour remains stable and does not introduce excessive API calls (no 429 rate-limit loops).

4. Verification and documentation



When you finish:

Run the full build/test pipeline used by this project (e.g. npm test, npm run build:server, npm run build:renderer, etc.) and fix any errors.

In docs/crew-stack-notes.md, add a short “Verification” section:

List tests run.

Note any known limitations or TODOs.

Add a brief “How to use” note for each new feature so future developers and users know where to find:

Certificate expiry warnings & docs.

Schedule→HoR preview and auto-populate.

Fatigue risk indicator.

Fleet compliance dashboard.



Make all changes in a clean, incremental way, respecting the existing architecture and avoiding duplication of features that already exist.