
You are an expert in Electron + Node/Express + TypeScript + SQLite.

You’re working on a desktop predictive-maintenance app (ARUS / RecipeRealm) that uses:

Electron main process in electron/main.ts (built to dist-electron/main.cjs)

Backend server bundled from server/index.ts (and/or server/index-wrapper.js)

SQLite local DB



The Electron app starts, spawns the server, but fails the health check and shows a fatal startup error even though the server seems to initialize the DB correctly and exits with code 0.



Here is the relevant console output from my Mac:

npx electron .

🎬 ARUS Electron app starting...
🔧 Mode: Development
🖥️  Platform: darwin
🚀 Starting ARUS embedded server...
🔌 Allocated port: 5000
📂 Project root: /Users/homeimac/Downloads/RecipeRealm
📂 Dev server path: /Users/homeimac/Downloads/RecipeRealm/server/index-wrapper.js
🔧 Dev command: node /Users/homeimac/Downloads/RecipeRealm/server/index-wrapper.js
ℹ️  Using wrapper to handle async IIFE initialization
✅ Server process spawned with PID 66110
⏳ Waiting for server... (3/30)
⏳ Waiting for server... (6/30)
[Server:66110:Error] objc[66110]: Class GNotificationCenterDelegate is implemented in both ...sharp...libvips... One of the two will be used. Which one is undefined.
⏳ Waiting for server... (9/30)
⏳ Waiting for server... (12/30)
[Server:66110:Error] 2025-11-19 18:53:25.439995: I tensorflow/core/platform/cpu_feature_guard.cc:193] This TensorFlow binary is optimized...
[Server:66110] === Database Configuration ===
[Server:66110] Deployment Mode: VESSEL (Offline-First)
[Server:66110:Error] [WARN ] 2025-11-19T10:53:25.802Z [Turso Sync] Cloud sync not configured - running offline-only...
[Server:66110] → Applying SQLite performance optimizations...
[Server:66110] ✓ SQLite performance optimizations applied
[Server:66110] • Foreign keys: ON
[Server:66110] ✓ Local SQLite: /Users/homeimac/Downloads/RecipeRealm/data/vessel-local.db
[Server:66110] → Initializing SQLite database tables...
[Server:66110] [SQLite Init] Initializing vessel mode database...
[Server:66110] [SQLite Init] Database initialized successfully with 131 tables (100% feature parity) at: /Users/homeimac/Downloads/RecipeRealm/data/vessel-local.db
✓ SQLite tables initialized
==============================
📴 Server exited (code: 0, signal: null)
⏳ Waiting for server... (15/30)
...
⏳ Waiting for server... (30/30)
❌ Fatal startup error: Error: Server failed health check after 30 seconds
    at /Users/homeimac/Downloads/RecipeRealm/dist-electron/main.cjs:37:490
The Electron main process is doing an HTTP health check like:

http.get(`${SERVER_URL}/api/dashboard`, {
  headers: { 'x-org-id': 'default-org-id' }
}, ...)
The server exits cleanly right after “SQLite tables initialized”. After that, the health-check loop never gets a 200, so Electron throws Server failed health check after 30 seconds.



I need you to diagnose and fix this end-to-end, once and for all, and then update the code so this workflow is robust.



What I want you to do

Understand the current startup flow

Open electron/main.ts (or whatever file builds to dist-electron/main.cjs) and locate:

startEmbeddedServer

The health-check logic (loop calling /api/dashboard)

Confirm exactly which URL is used for health checking and how many attempts / timeouts.

Inspect the dev server entrypoint

Open server/index-wrapper.js and server/index.ts.

Find how the Express / HTTP server is started (e.g. app.listen(port, ...)).

Check whether index-wrapper.js is wrapping an async IIFE or calling a function that returns and calls process.exit(0) once initialization is finished.

Look for any logic that intentionally exits the process after DB init (e.g. migration scripts, one-shot jobs).

Confirm the server actually listens and stays alive

In the Replit shell, run:

node server/index-wrapper.js
or, if dev uses TypeScript directly:

npx tsx server/index.ts


Verify:

The process keeps running (does not exit immediately).

Use curl (or Node fetch) to hit:

curl -i -H "x-org-id: default-org-id" http://localhost:5000/api/dashboard
and see if:

The route exists and returns 200.

Or it returns 404/500, or never responds.

Fix the premature exit

If index-wrapper.js (or some init code) ends with process.exit(0) or similar after DB initialization, remove that exit and make sure the HTTP server is started and kept alive.

Ensure the Express app is actually calling app.listen(PORT, ...) using process.env.PORT provided by Electron, and not defaulting to another port.

If the server is implemented as an async function that starts a server and never returns, make sure the wrapper doesn’t await it and then exit.

Align health endpoint & route

If /api/dashboard is not a fast, guaranteed-to-exist route, create a dedicated health endpoint, for example in your server routes:

app.get('/health', (_req, res) => {
  res.status(200).json({ ok: true });
});


Update the Electron main process to use /health instead of /api/dashboard for the health check.

Make sure this endpoint:

Doesn’t require auth beyond x-org-id or, if it does, that you honour default-org-id correctly.

Returns 200 quickly even if DB migrations or background jobs are still running.

Add better logging for health failures

In Electron’s startEmbeddedServer (or equivalent), log the exact HTTP status code and any response body when the health check fails.

Also log when the server process exits (you already log code and signal, keep that).

Handle the sharp / libvips duplication warning

We’re seeing:

Class GNotificationCenterDelegate is implemented in both ...sharp...libvips...
One of the two will be used. Which one is undefined.


Treat this as a warning, not the root cause, but:

If we have both sharp and @img/sharp-* installed, refactor dependencies so only one libvips provider is used.

Update package.json and node_modules imports accordingly so the server uses a single sharp build.

Make the startup robust in both dev and prod

Ensure the logic that determines dev vs production server command is correct:

Dev: node server/index-wrapper.js (or npx tsx server/index.ts)

Prod: process.execPath with ELECTRON_RUN_AS_NODE, pointing at the bundled server/index.js under process.resourcesPath.

Confirm in both cases that:

The process stays alive.

The health endpoint returns 200.

Electron only shows the fatal error if the server process truly fails (non-zero exit or repeated 5xx).

Run and validate

After applying fixes, run:

npm run build:electron-main
npm run build:server
npx electron .


Confirm in logs that:

The server starts and does not exit.

The health check succeeds (log a “Server is healthy at …” line).

The Electron window opens and the app loads normally.

Provide concrete code changes

Show me the final patched versions (or minimal diffs) for:

electron/main.ts (or whichever feeds dist-electron/main.cjs)

server/index-wrapper.js

server/index.ts (or main server entry)

Any new /health route file you add

Any package.json sharp/libvips cleanup you do

Make sure your changes are consistent with TypeScript config, build scripts and my existing project structure.



Please now:

Inspect the repo,

Apply the changes described,

And show me the updated files/diffs so I can copy them back into my project.