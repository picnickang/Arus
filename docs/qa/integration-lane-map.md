# Integration Lane Map

Generated by `node scripts/run-integration-lane.mjs --write-report`.

## Policy

- `npm run test:integration` runs only the deterministic embedded lane.
- `npm run test:integration:embedded` is the default release-safe integration gate.
- `npm run test:integration:postgres` is explicit and requires `DATABASE_URL`.
- `npm run test:integration:server` is explicit and owns server-style HTTP/browser checks.
- `npm run test:integration:legacy` remains visible but is excluded from the default gate until migrated.
- The lane runner uses serial Jest execution with background-worker environment flags and `--forceExit`; open-handle cleanup remains tracked as stabilization debt rather than blocking completed assertions.

## Current Counts

| Lane | Count |
| --- | ---: |
| embedded-deterministic | 20 |
| postgres-contract | 7 |
| server-e2e | 5 |
| legacy-quarantine | 64 |

## Embedded Lane Evidence

`npm run test:integration:embedded` passed as of the 2026-06 sweep with 17 suites and 144 tests.

## Server Lane Evidence

`npm run test:integration:server` passed during this stabilization pass with 4 suites and 48 tests.

## Lane Assignments

| Test file | Lane | Reason | Current status | Required migration/fix |
| --- | --- | --- | --- | --- |
| `tests/integration/activity.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/asset-anomaly-coolant-overheating.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/asset-anomaly-detection-deep.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/asset-anomaly-postgres.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/attention-inbox-role-gate.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/audit-chain-mixed-hash-versions.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/briefing.test.ts` | server | Exercises browser/server-style HTTP behavior; must run through the explicit server lane. | Passing in this stabilization pass: 4 suites, 48 tests. | Keep self-starting with embedded SQLite; add future server-style HTTP/browser checks here before promoting them into the default gate. |
| `tests/integration/compliance-exports.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/crew-photo-object-serving.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/crew-scheduling.test.ts` | legacy | Swept 2026-06 under the embedded env (8/12 green): /api/crew reads 403 on unseeded permission grants and assignments 500 on a sqlite crew_assignment column gap; envelope unwraps verified correct. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/crew-suite/crew-access-readiness.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/crew-crud-roster.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/former-crew-access-risks.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/offboarding-reinstatement.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/permission-registry.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/rbac-negative-matrix.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/role-dashboards.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/crew-suite/safety-alarms.test.ts` | legacy | Uses crew-suite/form helper conventions that still depend on live-server or legacy seeded data assumptions. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/cross-tenant-domains.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/email-notifications-api.test.ts` | server | Exercises browser/server-style HTTP behavior; must run through the explicit server lane. | Passing in this stabilization pass: 4 suites, 48 tests. | Keep self-starting with embedded SQLite; add future server-style HTTP/browser checks here before promoting them into the default gate. |
| `tests/integration/equipment-dependencies-graph-projection.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/equipment-dependencies-notes-patch.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/equipment-hub-acknowledge.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/equipment-telemetry-partitioning.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/feature-flag-tenant-isolation.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/findings.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/forms/alerts.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/audit-propagation.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/certificates.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/cost-savings.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/crew-alerts.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/crew.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/cross-org-isolation.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/deck-logbook.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/equipment.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/inventory.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/logbook.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/maintenance.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/ml-training.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/scheduled-reports.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/service-orders.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/settings.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/shifts.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/vessel-schematic.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/forms/work-orders.test.ts` | legacy | Uses forms helper with direct pg Pool cleanup and live-server assumptions; confirmed in the 2026-06 sweep — helpers dial 127.0.0.1:5432 (ECONNREFUSED) under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Replace direct pg helper cleanup with embedded fixtures or move to postgres-contract with explicit DATABASE_URL. |
| `tests/integration/hub-admin-grant-route.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/import-amos-golden.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/import-shipmate-golden.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/crew-cert-expiry.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/equipment-decommission.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/inventory-supplier-link.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/template-to-wo.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/wo-cost-rollup.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/journeys/wo-lifecycle.test.ts` | legacy | Legacy journey flow not yet proven under the embedded deterministic bootstrap; swept 2026-06 with the forms lane — direct pg Pool connections ECONNREFUSED under the embedded env. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/kb-upload-reliability.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/lr35-aud-cluster-task208.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/lr35-maintenance-orgid-gate.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/lr35-pdm-promote-rollback-gate.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/lr35-rag-security-admin-gate.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/ml-promote-two-person.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/ml-train-idempotency.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/object-storage-client-concurrency.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/operator-experience-routes.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/operator-information-needs-routes.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/outcome-tracking.test.ts` | server | Exercises browser/server-style HTTP behavior; must run through the explicit server lane. | Passing in this stabilization pass: 4 suites, 48 tests. | Keep self-starting with embedded SQLite; add future server-style HTTP/browser checks here before promoting them into the default gate. |
| `tests/integration/pdm-decision-support-registry.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/pdm-decision-support-routes.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/pdm-score-logs-vessel.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/pdm-scoring-producer.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/permission-audit-read.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/permission-grants-lockout.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/permissions-hub-resolution.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/permissions-me-primary-role.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/rag-conversation-ownership.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/rag-conversations.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/rls-cross-tenant-api.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/rls-cross-tenant.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/role-403-matrix.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/role-crud-workflow.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/role-hub-access-audit.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/safety-bulletins-feed.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/safety-bulletins.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/schematic-layout-crud.test.ts` | legacy | Swept 2026-06 under the embedded env (0/11 green): every route 500s because vessels.schematic_layout is missing from the sqlite schema; the deterministic :5000 bootstrap itself works. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/task-235-domains.test.ts` | legacy | Not yet proven in the embedded lane; retained as visible legacy debt until migrated or reclassified. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/telemetry.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/tenant-quota-throttle.test.ts` | postgres | Exercises PostgreSQL-only behavior such as RLS, migrations, immutable audit, quota, or tenant DB contracts. | Not part of default gate; fail-fast when DATABASE_URL is absent. | Run with a seeded PostgreSQL contract database and document RLS/migration prerequisites. |
| `tests/integration/validate-response.test.ts` | legacy | Swept 2026-06 under the embedded env (9/13 green): pdm/dashboard and permissions/me 500 on sqlite schema gaps (failure_predictions join, users.hub_admin/hub_access), permissions/audit 403 on an unseeded grant, risk-queue 400; envelope assertions pass. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/vessel-diagram-registry-routes.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/vessel-performance-auth.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/websocket-strict-mode.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/work-order-assignment-route-gate.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/work-order-assignment-service.test.ts` | embedded | Proven deterministic in embedded SQLite/local mode; no DATABASE_URL or manual localhost required. | Passing as of the 2026-06 sweep: 17 suites, 144 tests. | Keep in release gate; add similar suites only after they pass embedded lane locally. |
| `tests/integration/work-orders.test.ts` | legacy | Swept 2026-06 under the embedded env (7/13 green): list routes 500 on a sqlite work_orders column gap and create/auth status expectations are stale (400 vs 201/401); envelope unwraps verified correct. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/integration/workflow-gap-closure.test.ts` | legacy | Mixes direct pg Pool access, live HTTP calls, and stale list-response expectations. | Quarantined from default gate; known to fail or risk OOM in aggregate runs. | Audit dependencies, remove live-service assumptions, add deterministic fixtures, then promote to embedded or postgres. |
| `tests/e2e/activity.e2e.ts` | server | Exercises browser/server-style HTTP behavior; must run through the explicit server lane. | Passing in this stabilization pass: 4 suites, 48 tests. | Keep self-starting with embedded SQLite; add future server-style HTTP/browser checks here before promoting them into the default gate. |
| `tests/e2e/briefing.e2e.ts` | server | Exercises browser/server-style HTTP behavior; must run through the explicit server lane. | Passing in this stabilization pass: 4 suites, 48 tests. | Keep self-starting with embedded SQLite; add future server-style HTTP/browser checks here before promoting them into the default gate. |
