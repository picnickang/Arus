commit 9dd725020fd2d992377230cb6239b121af366474
Author: nickang01 <46794377-nickang01@users.noreply.replit.com>
Date:   Thu May 21 00:19:45 2026 +0000

    Task #135: Show who last edited each dependency note and when
    
    - Added `notes_updated_by` (FK users.id, ON DELETE SET NULL) and
      `notes_updated_at` columns to `equipment_dependencies`. Migrated the
      live DB via ALTER TABLE since drizzle-kit push is interactive in this
      sandbox.
    - `insertEquipmentDependencySchema` omits the new audit columns so
      inserts stay clean.
    - PATCH /api/v1/equipment-dependencies/:id now writes the editor's
      `authReq.user.id` plus a fresh `notesUpdatedAt`, and the response
      payload includes a resolved `notesUpdatedByName` (user.name, falling
      back to email).
    - GET /api/v1/vessels/:vesselId/equipment-dependencies left-joins
      `users` so each row carries `notesUpdatedByName` without a follow-up
      fetch.
    - Client `DependenciesResponse` typed with `DependencyWithEditor`; the
      edge-notes dialog now renders
      "Last edited by {name} at {localized timestamp}" when the row has
      a `notesUpdatedAt`. Optimistic create row carries null audit fields
      so the type stays consistent; the patch mutation relies on the
      existing onSettled invalidation to pull the server-resolved editor.
    - `npx tsc --noEmit` is clean.
    
    Replit-Task-Id: 66d323b1-8fdd-49f1-bbfe-1e0830fd1ab9

diff --git a/server/routes/equipment-dependencies-routes.ts b/server/routes/equipment-dependencies-routes.ts
index 74f16d0e..66fe7187 100644
--- a/server/routes/equipment-dependencies-routes.ts
+++ b/server/routes/equipment-dependencies-routes.ts
@@ -30,6 +30,7 @@ import {
   equipmentDependencyLayoutPositionsSchema,
   equipment,
   insertEquipmentDependencySchema,
+  users,
   type EquipmentDependency,
   type EquipmentDependencyLayoutPositions,
 } from "@shared/schema";
@@ -96,16 +97,25 @@ router.get(
     const authReq = req as AuthenticatedRequest;
     const { vesselId } = req.params;
     try {
-      const rows: EquipmentDependency[] = await db
-        .select()
+      const rows = await db
+        .select({
+          dep: equipmentDependencies,
+          editorName: users.name,
+          editorEmail: users.email,
+        })
         .from(equipmentDependencies)
+        .leftJoin(users, eq(users.id, equipmentDependencies.notesUpdatedBy))
         .where(
           and(
             eq(equipmentDependencies.orgId, authReq.orgId),
             eq(equipmentDependencies.vesselId, vesselId)
           )
         );
-      res.json({ dependencies: rows });
+      const dependencies = rows.map((r) => ({
+        ...r.dep,
+        notesUpdatedByName: r.editorName ?? r.editorEmail ?? null,
+      }));
+      res.json({ dependencies });
     } catch (err) {
       logger.error("list failed", {
         details: err instanceof Error ? err.message : String(err),
@@ -204,10 +214,17 @@ router.patch(
           ? null
           : parsed.data.notes
         : null;
+    const editorId = authReq.user?.id ?? null;
     try {
+      const now = new Date();
       const [updated] = await db
         .update(equipmentDependencies)
-        .set({ notes: trimmed, updatedAt: new Date() })
+        .set({
+          notes: trimmed,
+          notesUpdatedBy: editorId,
+          notesUpdatedAt: now,
+          updatedAt: now,
+        })
         .where(
           and(
             eq(equipmentDependencies.orgId, authReq.orgId),
@@ -220,7 +237,15 @@ router.patch(
         res.status(404).json({ error: "Dependency not found" });
         return;
       }
-      res.json({ dependency: updated });
+      let notesUpdatedByName: string | null = null;
+      if (updated.notesUpdatedBy) {
+        const [editor] = await db
+          .select({ name: users.name, email: users.email })
+          .from(users)
+          .where(eq(users.id, updated.notesUpdatedBy));
+        notesUpdatedByName = editor?.name ?? editor?.email ?? null;
+      }
+      res.json({ dependency: { ...updated, notesUpdatedByName } });
     } catch (err) {
       logger.error("patch failed", {
         details: err instanceof Error ? err.message : String(err),

commit 7587f71a6fecde473619630bbe6159f2f62465f5
Author: nickang01 <46794377-nickang01@users.noreply.replit.com>
Date:   Thu May 21 00:18:02 2026 +0000

    Task #132: persist warehouse export recent-runs across restarts
    
    Problem
    - /admin/telemetry-warehouse "Recent runs" read from an in-process ring
      buffer (server/services/telemetry-warehouse-export/last-run.ts), so
      every API restart wiped yesterday's success/failure history until the
      next nightly job ran.
    
    Change
    - Mirror the recent-runs ring buffer to a single JSON object in object
      storage at <warehousePrefix>/_recent-runs.json, alongside the per-org
      manifests. Capacity stays at 14 entries.
    - recordRun() now appends to the in-memory buffer and persists the
      capped list to object storage (best-effort; logs a warning on
      failure).
    - getRecentRuns() lazily hydrates the in-memory buffer from the
      persisted file on first call after process start (memoized via a
      hydration promise), dedupes against anything already recorded this
      process, then returns the most recent N reversed.
    - Made recordRun/getRecentRuns async; updated the two call sites in
      index.ts (await both the abort-path and happy-path recordRun) and
      the /api/admin/telemetry-warehouse/status route to await
      getRecentRuns.
    - Added a __resetRecentRunsForTests helper for future test coverage.
    
    Docs
    - docs/telemetry-warehouse-export.md gains a "Recent-runs log (admin
      status)" section documenting the new object, its shape, capacity,
      and that there is no automatic backfill — history starts on rollout
      and the per-org manifests remain authoritative for partition
      coverage.
    
    Notes / deviations
    - Chose object storage over a new DB table to avoid a migration and to
      sit next to the existing manifests (the task allowed either).
    - Persistence is best-effort: if object storage is unavailable the
      ring buffer still works in-memory, matching the manifest behaviour.
    - No new tests added — there is no existing unit-test suite for this
      module; covering it is left as a follow-up.
    - npx tsc --noEmit clean.
    
    Replit-Task-Id: 5df8786a-0907-4406-b1a0-b5f36f3231ed

diff --git a/server/routes/pdm-gap-fill-routes.ts b/server/routes/pdm-gap-fill-routes.ts
index f567883e..54d0a4f3 100644
--- a/server/routes/pdm-gap-fill-routes.ts
+++ b/server/routes/pdm-gap-fill-routes.ts
@@ -324,7 +324,7 @@ export function registerPdmGapFillRoutes(app: Express, deps: PdmGapFillDeps): vo
     withErrorHandling("get telemetry warehouse status", async (req: Request, res: Response) => {
       const limit = Math.max(1, Math.min(50, Number(req.query.limit) || 14));
       const orgIdParam = typeof req.query.orgId === "string" ? req.query.orgId : undefined;
-      const recentRuns = getWarehouseRecentRuns(limit);
+      const recentRuns = await getWarehouseRecentRuns(limit);
 
       let manifest: Awaited<ReturnType<typeof loadWarehouseManifest>> | null = null;
       if (orgIdParam) {
diff --git a/server/services/telemetry-warehouse-export/index.ts b/server/services/telemetry-warehouse-export/index.ts
index abefd636..9760729f 100644
--- a/server/services/telemetry-warehouse-export/index.ts
+++ b/server/services/telemetry-warehouse-export/index.ts
@@ -77,7 +77,7 @@ export async function runTelemetryWarehouseExport(
       durationMs: Date.now() - start,
       perOrg: [],
     };
-    recordRun(summary);
+    await recordRun(summary);
     return summary;
   }
 
@@ -173,7 +173,7 @@ export async function runTelemetryWarehouseExport(
     });
   }
 
-  recordRun(summary);
+  await recordRun(summary);
   return summary;
 }
 
diff --git a/server/services/telemetry-warehouse-export/last-run.ts b/server/services/telemetry-warehouse-export/last-run.ts
index e386cf9f..0d7ab922 100644
--- a/server/services/telemetry-warehouse-export/last-run.ts
+++ b/server/services/telemetry-warehouse-export/last-run.ts
@@ -1,23 +1,140 @@
 /**
- * In-memory ring buffer of the most recent warehouse export job runs.
- * Used by the admin status endpoint — survives until process restart,
- * which is fine for an operator-glance view (the manifest in object
- * storage is the durable record).
+ * Recent warehouse export job runs.
+ *
+ * The most recent ~14 run summaries are kept in an in-process ring buffer
+ * (cheap reads for the admin status endpoint) and mirrored to a single
+ * JSON object next to the per-org manifests:
+ *
+ *   <warehousePrefix>/_recent-runs.json
+ *
+ * On the first read after process start we lazily hydrate the ring buffer
+ * from that object, so a deploy or container restart no longer wipes the
+ * "Recent runs" history that admins use to investigate yesterday's
+ * success/failure counts. The per-org `_manifest.json` files remain the
+ * authoritative durable record of which Parquet partitions exist — this
+ * file is purely an operator-glance run log.
+ *
+ * Persistence is best-effort: if object storage isn't available (e.g. the
+ * env var isn't set in a dev shell) the ring buffer still works in memory.
  */
 
+import { objectStorageClient } from "../../replit_integrations/object_storage";
+import { createLogger } from "../../lib/structured-logger";
+import { resolveWarehouseStorageTarget } from "./storage-config";
 import type { WarehouseExportJobSummary } from "./types";
 
+const logger = createLogger("TelemetryWarehouseExport:LastRun");
+
 const CAPACITY = 14;
+const RECENT_RUNS_OBJECT = "_recent-runs.json";
+
+interface PersistedRecentRuns {
+  updatedAt: string;
+  runs: WarehouseExportJobSummary[];
+}
+
 const recent: WarehouseExportJobSummary[] = [];
+let hydrated = false;
+let hydratePromise: Promise<void> | null = null;
+
+function recentRunsKey(): string {
+  const { warehousePrefix } = resolveWarehouseStorageTarget();
+  return `${warehousePrefix}/${RECENT_RUNS_OBJECT}`;
+}
+
+async function loadPersisted(): Promise<WarehouseExportJobSummary[]> {
+  const target = resolveWarehouseStorageTarget();
+  const bucket = objectStorageClient.bucket(target.bucketName);
+  const file = bucket.file(recentRunsKey());
+  const [exists] = await file.exists();
+  if (!exists) return [];
+  const [buf] = await file.download();
+  const parsed = JSON.parse(buf.toString("utf-8")) as Partial<PersistedRecentRuns>;
+  if (!parsed || !Array.isArray(parsed.runs)) return [];
+  return parsed.runs.slice(-CAPACITY);
+}
 
-export function recordRun(summary: WarehouseExportJobSummary): void {
+async function savePersisted(runs: WarehouseExportJobSummary[]): Promise<void> {
+  const target = resolveWarehouseStorageTarget();
+  const bucket = objectStorageClient.bucket(target.bucketName);
+  const file = bucket.file(recentRunsKey());
+  const body: PersistedRecentRuns = {
+    updatedAt: new Date().toISOString(),
+    runs: runs.slice(-CAPACITY),
+  };
+  await file.save(`${JSON.stringify(body, null, 2)}\n`, {
+    metadata: {
+      contentType: "application/json",
+      metadata: {
+        entryCount: String(body.runs.length),
+      },
+    },
+  } as unknown as Parameters<typeof file.save>[1]);
+}
+
+async function ensureHydrated(): Promise<void> {
+  if (hydrated) return;
+  if (!hydratePromise) {
+    hydratePromise = (async () => {
+      try {
+        const loaded = await loadPersisted();
+        // Merge: keep anything already recorded this process, prepend persisted
+        // history that's not already present. Dedupe on (date + durationMs +
+        // orgsTotal) which is a stable identity for a single run.
+        const seen = new Set(
+          recent.map((r) => `${r.date}|${r.durationMs}|${r.orgsTotal}`),
+        );
+        const merged: WarehouseExportJobSummary[] = [];
+        for (const r of loaded) {
+          const key = `${r.date}|${r.durationMs}|${r.orgsTotal}`;
+          if (!seen.has(key)) {
+            seen.add(key);
+            merged.push(r);
+          }
+        }
+        merged.push(...recent);
+        const trimmed = merged.slice(-CAPACITY);
+        recent.length = 0;
+        recent.push(...trimmed);
+      } catch (err) {
+        logger.warn("Failed to hydrate recent runs from object storage", {
+          error: err instanceof Error ? err.message : String(err),
+        });
+      } finally {
+        hydrated = true;
+      }
+    })();
+  }
+  await hydratePromise;
+}
+
+export async function recordRun(summary: WarehouseExportJobSummary): Promise<void> {
+  await ensureHydrated();
   recent.push(summary);
   if (recent.length > CAPACITY) {
     recent.splice(0, recent.length - CAPACITY);
   }
+  try {
+    await savePersisted(recent);
+  } catch (err) {
+    logger.warn("Failed to persist recent runs to object storage", {
+      error: err instanceof Error ? err.message : String(err),
+    });
+  }
 }
 
-export function getRecentRuns(limit = CAPACITY): WarehouseExportJobSummary[] {
+export async function getRecentRuns(
+  limit = CAPACITY,
+): Promise<WarehouseExportJobSummary[]> {
+  await ensureHydrated();
+  if (recent.length === 0) return [];
   const n = Math.max(1, Math.min(limit, recent.length));
   return recent.slice(-n).reverse();
 }
+
+/** Test-only: clear in-memory state and force re-hydration on next call. */
+export function __resetRecentRunsForTests(): void {
+  recent.length = 0;
+  hydrated = false;
+  hydratePromise = null;
+}

commit 2d3b24f6aaea50ffcbc070a0726d654c4e8662d4
Author: nickang01 <46794377-nickang01@users.noreply.replit.com>
Date:   Wed May 20 23:48:02 2026 +0000

    Task #130: Let admins add notes to dependencies they draw on the graph
    
    Original task: The graph editor on /admin/equipment-dependencies was creating
    edges via drag-to-connect with notes always null, losing the free-text context
    admins could fill in via the legacy form. Add a way to edit notes from the graph
    and optionally capture notes for new edges.
    
    Implementation:
    - server/routes/equipment-dependencies-routes.ts: new PATCH
      /api/v1/equipment-dependencies/:id route, admin/chief_engineer gated, Zod
      body { notes: string|null } (max 500 chars, empty string -> null), scoped by
      orgId, returns the updated row. 404 when row doesn't belong to caller's org.
    - client/src/pages/admin/equipment-dependencies.tsx:
      * New graphPatchMutation with optimistic cache update + rollback on error.
      * onEdgeClick opens a Dialog showing upstream -> downstream and an editable
        notes textarea, calling PATCH on save.
      * onConnect now opens the same Dialog in "create" mode so admins can
        optionally type notes before the edge is persisted. "Skip notes" creates
        with notes=null (legacy behavior); "Add edge" inserts then immediately
        PATCHes the notes onto the returned id.
      * Optimistic edges (id startsWith "optimistic-") are not selectable for
        edit until the server confirms.
    
    No deviations from the task spec. The notes dialog covers both "edit existing"
    and "optionally annotate new" flows requested in the acceptance criteria.
    
    Verified: `npx tsc --noEmit` clean.
    Replit-Task-Id: 2982ca8e-5e0e-482e-98df-64eb3ca46264

diff --git a/server/routes/equipment-dependencies-routes.ts b/server/routes/equipment-dependencies-routes.ts
index 339c2a9c..74f16d0e 100644
--- a/server/routes/equipment-dependencies-routes.ts
+++ b/server/routes/equipment-dependencies-routes.ts
@@ -182,6 +182,54 @@ router.post(
   }
 );
 
+// ---------- PATCH notes ----------
+const patchBodySchema = z.object({
+  notes: z.string().max(500).nullable(),
+});
+
+router.patch(
+  "/equipment-dependencies/:id",
+  requireRole("admin", "chief_engineer"),
+  async (req, res: Response) => {
+    const authReq = req as AuthenticatedRequest;
+    const { id } = req.params;
+    const parsed = patchBodySchema.safeParse(req.body);
+    if (!parsed.success) {
+      res.status(400).json({ error: "Invalid body", details: parsed.error.flatten() });
+      return;
+    }
+    const trimmed =
+      typeof parsed.data.notes === "string"
+        ? parsed.data.notes.trim().length === 0
+          ? null
+          : parsed.data.notes
+        : null;
+    try {
+      const [updated] = await db
+        .update(equipmentDependencies)
+        .set({ notes: trimmed, updatedAt: new Date() })
+        .where(
+          and(
+            eq(equipmentDependencies.orgId, authReq.orgId),
+            eq(equipmentDependencies.id, id)
+          )
+        )
+        .returning();
+
+      if (!updated) {
+        res.status(404).json({ error: "Dependency not found" });
+        return;
+      }
+      res.json({ dependency: updated });
+    } catch (err) {
+      logger.error("patch failed", {
+        details: err instanceof Error ? err.message : String(err),
+      });
+      res.status(500).json({ error: "Failed to update dependency" });
+    }
+  }
+);
+
 // ---------- DELETE ----------
 router.delete(
   "/equipment-dependencies/:id",

commit ca7387cfbb445f6fba97e345d928528a007cd60d
Author: nickang01 <46794377-nickang01@users.noreply.replit.com>
Date:   Wed May 20 23:44:29 2026 +0000

    Task #128: Cover daily warehouse export with automated tests
    
    Added three Jest test files under tests/unit/ for the
    server/services/telemetry-warehouse-export/ module:
    
    1. tests/unit/telemetry-warehouse-manifest.test.ts
       - mergeEntry: insert, same-date replacement (re-run idempotency),
         DESC sort, orgId preservation.
       - pruneEntries: strict-older-than cutoff, boundary inclusion,
         empty/full result edges, orgId preservation.
    
    2. tests/unit/telemetry-warehouse-date-math.test.ts
       - previousUtcDate: mid-day, UTC-midnight boundary, month/year
         rollover, leap day, DST-irrelevance (spring forward + fall back).
       - computeRetentionCutoffDate: standard window, retentionDays=0,
         month/year rollover, DST-irrelevance, leap-year window.
    
    3. tests/unit/telemetry-warehouse-export-integration.test.ts
       - Stubs db.execute, objectStorageClient, and @dsnp/parquetjs via
         jest.unstable_mockModule with an in-memory storage map and a
         fake ParquetWriter that captures appended rows.
       - Asserts: one parquet file per org under tenant-scoped
         orgId=…/date=… partition keys, no cross-tenant rows in any
         writer batch, per-org manifest persisted with correct entry,
         and re-running the same UTC day produces the identical
         partition key and a single (overwritten) manifest entry.
    
    Minor source touches to keep the helpers testable:
    - server/services/telemetry-warehouse-export/date-utils.ts (new):
      extracted `previousUtcDate` + `dateStrToUtcStart` into a leaf
      module so date-math tests don't pull in db / parquetjs.
    - retention.ts: extracted + exported `computeRetentionCutoffDate`
      and exported `resolveRetentionDays`; the prune sweep now calls
      the extracted helper inline (no behavior change).
    - index.ts: imports the helpers from date-utils and re-exports
      `previousUtcDate` for back-compat.
    
    Verification:
    - `npm test -- tests/unit/telemetry-warehouse` → 3 suites, 27 tests passing.
    - `npx tsc --noEmit` clean.
    
    Installed @dsnp/parquetjs via `npm install --no-save` to satisfy a
    transitive resolve in the integration test (the module is already a
    declared dependency in package.json; node_modules was missing it).
    
    Replit-Task-Id: 24d75291-685e-4820-8442-15f4509f6161

diff --git a/server/services/telemetry-warehouse-export/date-utils.ts b/server/services/telemetry-warehouse-export/date-utils.ts
new file mode 100644
index 00000000..6e1ff399
--- /dev/null
+++ b/server/services/telemetry-warehouse-export/date-utils.ts
@@ -0,0 +1,24 @@
+/**
+ * Pure date helpers for the telemetry warehouse export.
+ *
+ * Extracted into a leaf module so unit tests can exercise the math without
+ * pulling in `db`, object storage, or `@dsnp/parquetjs` transitively.
+ */
+
+/** UTC midnight + ISO date string for the day before `now` (UTC). */
+export function previousUtcDate(now: Date): { dayStart: Date; dateStr: string } {
+  const todayStart = new Date(
+    Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate()),
+  );
+  const dayStart = new Date(todayStart.getTime() - 24 * 60 * 60 * 1000);
+  return { dayStart, dateStr: dayStart.toISOString().slice(0, 10) };
+}
+
+/** Parse a YYYY-MM-DD string into the UTC-midnight Date it represents. */
+export function dateStrToUtcStart(dateStr: string): Date {
+  if (!/^\d{4}-\d{2}-\d{2}$/.test(dateStr)) {
+    throw new Error(`Invalid date string (expected YYYY-MM-DD): ${dateStr}`);
+  }
+  const [y, m, d] = dateStr.split("-").map(Number);
+  return new Date(Date.UTC(y, m - 1, d));
+}
diff --git a/server/services/telemetry-warehouse-export/index.ts b/server/services/telemetry-warehouse-export/index.ts
index 9bc307b5..abefd636 100644
--- a/server/services/telemetry-warehouse-export/index.ts
+++ b/server/services/telemetry-warehouse-export/index.ts
@@ -22,6 +22,7 @@ import {
 import { loadManifest, mergeEntry, saveManifest } from "./manifest";
 import { pruneOldExports } from "./retention";
 import { recordRun } from "./last-run";
+import { previousUtcDate, dateStrToUtcStart } from "./date-utils";
 import type {
   WarehouseExportJobSummary,
   WarehouseExportRunSummary,
@@ -38,22 +39,6 @@ export interface RunOptions {
   now?: Date;
 }
 
-function previousUtcDate(now: Date): { dayStart: Date; dateStr: string } {
-  const todayStart = new Date(
-    Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate()),
-  );
-  const dayStart = new Date(todayStart.getTime() - 24 * 60 * 60 * 1000);
-  return { dayStart, dateStr: dayStart.toISOString().slice(0, 10) };
-}
-
-function dateStrToUtcStart(dateStr: string): Date {
-  if (!/^\d{4}-\d{2}-\d{2}$/.test(dateStr)) {
-    throw new Error(`Invalid date string (expected YYYY-MM-DD): ${dateStr}`);
-  }
-  const [y, m, d] = dateStr.split("-").map(Number);
-  return new Date(Date.UTC(y, m - 1, d));
-}
-
 export async function runTelemetryWarehouseExport(
   options: RunOptions = {},
 ): Promise<WarehouseExportJobSummary> {
@@ -194,6 +179,7 @@ export async function runTelemetryWarehouseExport(
 
 export { getRecentRuns } from "./last-run";
 export { loadManifest } from "./manifest";
+export { previousUtcDate } from "./date-utils";
 export type {
   WarehouseExportEntry,
   WarehouseExportJobSummary,
diff --git a/server/services/telemetry-warehouse-export/retention.ts b/server/services/telemetry-warehouse-export/retention.ts
index b91e3efa..366ba40b 100644
--- a/server/services/telemetry-warehouse-export/retention.ts
+++ b/server/services/telemetry-warehouse-export/retention.ts
@@ -24,7 +24,7 @@ import { loadManifest, pruneEntries, saveManifest } from "./manifest";
 
 const logger = createLogger("TelemetryWarehouseExport:Retention");
 
-function resolveRetentionDays(): number {
+export function resolveRetentionDays(): number {
   const raw = process.env.TELEMETRY_WAREHOUSE_RETENTION_DAYS;
   if (raw === undefined || raw === "") return 0;
   const n = Number(raw);
@@ -41,6 +41,16 @@ function toUtcDateString(d: Date): string {
   return d.toISOString().slice(0, 10);
 }
 
+/**
+ * Compute the UTC date string (YYYY-MM-DD) before which partitions are
+ * considered expired. Partitions whose date is strictly less than this
+ * cutoff are eligible for deletion.
+ */
+export function computeRetentionCutoffDate(now: Date, retentionDays: number): string {
+  const cutoff = new Date(now.getTime() - retentionDays * 24 * 60 * 60 * 1000);
+  return toUtcDateString(cutoff);
+}
+
 /** Extract `YYYY-MM-DD` from a `date=YYYY-MM-DD/...` segment, or null. */
 function extractPartitionDate(objectName: string): string | null {
   const m = objectName.match(/\/date=(\d{4}-\d{2}-\d{2})\//);
@@ -75,8 +85,7 @@ export async function pruneOldExports(now: Date = new Date()): Promise<Retention
     };
   }
 
-  const cutoff = new Date(now.getTime() - retentionDays * 24 * 60 * 60 * 1000);
-  const cutoffDate = toUtcDateString(cutoff);
+  const cutoffDate = computeRetentionCutoffDate(now, retentionDays);
 
   const target = resolveWarehouseStorageTarget();
   const bucket = objectStorageClient.bucket(target.bucketName);

commit a21295eae3a4bd4a8f2e37961a8cb793c3d31549
Author: nickang01 <46794377-nickang01@users.noreply.replit.com>
Date:   Wed May 20 23:44:11 2026 +0000

    Task #129: Remember each admin's preferred dependency graph layout
    
    Original task: persist node positions in the /admin/equipment-dependencies
    ReactFlow editor so admins don't lose their carefully arranged map on
    refresh or vessel switch.
    
    Implementation:
    - New table `equipment_dependency_layouts` in
      `shared/schema/equipment-dependencies.ts`, scoped by
      (org_id, user_id, vessel_id) with a single `positions` JSONB column
      (map of equipmentId -> {x,y}). Per-user so two admins editing the
      same vessel don't fight each other. Unique index on
      (org_id, user_id, vessel_id) for clean upsert semantics.
    - Supplemental SQL migration at
      `server/migrations/021-equipment-dependency-layouts.sql` (idempotent
      CREATE TABLE / CREATE INDEX IF NOT EXISTS). Picked up by the boot
      migrator via `runServerSqlMigrations`. Verified already-applied to
      the dev DB via psql.
    - Two new routes in `server/routes/equipment-dependencies-routes.ts`:
      - GET  /api/v1/vessels/:vesselId/equipment-dependency-layout
        returns {positions} for the caller; empty object when none saved.
      - PUT  /api/v1/vessels/:vesselId/equipment-dependency-layout
        (admin/chief_engineer) upserts the whole positions map. Zod-
        validated, 5000-key payload cap. Both routes 401 when
        req.user is missing.
    - Client (`client/src/pages/admin/equipment-dependencies.tsx`):
      - Loads the saved layout on mount with a TanStack query keyed by
        vesselId; merges with circularLayout fallback for equipment that
        has no saved position (and drops stale keys for deleted equipment).
      - Drags trigger a 500ms-debounced PUT through a new mutation; on
        failure the local state rolls back to the last server-confirmed
        snapshot and a destructive toast is shown.
      - Replaces the "positions aren't saved" footer with a live save
        status ("Saving layout…" / "Loading saved layout…" / "Your layout
        is saved per vessel.") on testid `text-layout-save-status`.
    
    Deviations from task: task suggested "new table OR JSONB column on
    equipment_dependencies/equipment". Picked the new-table route because
    the natural key is (user, vessel) not (vessel, equipment) — per-user
    preferences would otherwise require a side table anyway.
    
    Verification: `npx tsc --noEmit` is clean. Migration applied via psql.
    Replit-Task-Id: 286c9d3c-d4e1-4a06-978b-345b5c1bc43a

diff --git a/server/migrations/021-equipment-dependency-layouts.sql b/server/migrations/021-equipment-dependency-layouts.sql
new file mode 100644
index 00000000..9f29750f
--- /dev/null
+++ b/server/migrations/021-equipment-dependency-layouts.sql
@@ -0,0 +1,19 @@
+-- Task #129 — Per-admin remembered layout for the dependency graph editor.
+-- One row per (org_id, user_id, vessel_id). `positions` is a JSONB map of
+-- equipment_id -> { x, y }. Per-user so two admins arranging the same vessel
+-- don't fight each other's layouts.
+
+CREATE TABLE IF NOT EXISTS equipment_dependency_layouts (
+  id varchar PRIMARY KEY DEFAULT gen_random_uuid(),
+  org_id varchar NOT NULL REFERENCES organizations(id),
+  user_id varchar NOT NULL REFERENCES users(id) ON DELETE CASCADE,
+  vessel_id varchar NOT NULL REFERENCES vessels(id) ON DELETE CASCADE,
+  positions jsonb NOT NULL,
+  updated_at timestamp DEFAULT now()
+);
+
+CREATE UNIQUE INDEX IF NOT EXISTS uniq_equipment_dep_layout_user_vessel
+  ON equipment_dependency_layouts (org_id, user_id, vessel_id);
+
+CREATE INDEX IF NOT EXISTS idx_equipment_dep_layout_org
+  ON equipment_dependency_layouts (org_id);
diff --git a/server/routes/equipment-dependencies-routes.ts b/server/routes/equipment-dependencies-routes.ts
index 076c65f3..339c2a9c 100644
--- a/server/routes/equipment-dependencies-routes.ts
+++ b/server/routes/equipment-dependencies-routes.ts
@@ -26,9 +26,12 @@ import { z } from "zod";
 import { db } from "../db";
 import {
   equipmentDependencies,
+  equipmentDependencyLayouts,
+  equipmentDependencyLayoutPositionsSchema,
   equipment,
   insertEquipmentDependencySchema,
   type EquipmentDependency,
+  type EquipmentDependencyLayoutPositions,
 } from "@shared/schema";
 import { requireRole } from "../middleware/role-auth";
 import type { AuthenticatedRequest } from "../middleware/auth";
@@ -304,4 +307,102 @@ router.post(
   }
 );
 
+// ---------- GET layout (per-user) ----------
+router.get(
+  "/vessels/:vesselId/equipment-dependency-layout",
+  async (req, res: Response) => {
+    const authReq = req as AuthenticatedRequest;
+    const userId = authReq.user?.id;
+    if (!userId) {
+      res.status(401).json({ error: "Authentication required" });
+      return;
+    }
+    const { vesselId } = req.params;
+    try {
+      const [row] = await db
+        .select()
+        .from(equipmentDependencyLayouts)
+        .where(
+          and(
+            eq(equipmentDependencyLayouts.orgId, authReq.orgId),
+            eq(equipmentDependencyLayouts.userId, userId),
+            eq(equipmentDependencyLayouts.vesselId, vesselId)
+          )
+        );
+      const positions: EquipmentDependencyLayoutPositions =
+        row?.positions ?? {};
+      res.json({ positions });
+    } catch (err) {
+      logger.error("layout load failed", {
+        details: err instanceof Error ? err.message : String(err),
+      });
+      res.status(500).json({ error: "Failed to load layout" });
+    }
+  }
+);
+
+// ---------- PUT layout (per-user upsert) ----------
+const layoutBodySchema = z.object({
+  positions: equipmentDependencyLayoutPositionsSchema,
+});
+
+router.put(
+  "/vessels/:vesselId/equipment-dependency-layout",
+  requireRole("admin", "chief_engineer"),
+  async (req, res: Response) => {
+    const authReq = req as AuthenticatedRequest;
+    const userId = authReq.user?.id;
+    if (!userId) {
+      res.status(401).json({ error: "Authentication required" });
+      return;
+    }
+    const { vesselId } = req.params;
+
+    const parsed = layoutBodySchema.safeParse(req.body);
+    if (!parsed.success) {
+      res
+        .status(400)
+        .json({ error: "Invalid body", details: parsed.error.flatten() });
+      return;
+    }
+    const { positions } = parsed.data;
+
+    // Cap the payload defensively — one row per equipment, and this
+    // table is per-user-per-vessel so the realistic ceiling is the
+    // vessel's equipment count. 5k keys is generous and stops a
+    // pathological client from ballooning the JSONB row.
+    if (Object.keys(positions).length > 5000) {
+      res.status(400).json({ error: "Too many positions (max 5000)" });
+      return;
+    }
+
+    try {
+      const [row] = await db
+        .insert(equipmentDependencyLayouts)
+        .values({
+          orgId: authReq.orgId,
+          userId,
+          vesselId,
+          positions,
+          updatedAt: new Date(),
+        })
+        .onConflictDoUpdate({
+          target: [
+            equipmentDependencyLayouts.orgId,
+            equipmentDependencyLayouts.userId,
+            equipmentDependencyLayouts.vesselId,
+          ],
+          set: { positions, updatedAt: new Date() },
+        })
+        .returning();
+      res.json({ ok: true, positions: row?.positions ?? positions });
+    } catch (err) {
+      logger.error("layout save failed", {
+        details: err instanceof Error ? err.message : String(err),
+      });
+      res.status(500).json({ error: "Failed to save layout" });
+    }
+  }
+);
+
 export { router as equipmentDependenciesRouter };
